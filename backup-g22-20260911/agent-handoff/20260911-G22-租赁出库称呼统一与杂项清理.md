# 20260911-G22-租赁出库称呼统一与杂项清理

> **状态**：⏳ 待执行（收尾时改 ✅ 并回填 commit 哈希）
> **commit**：（待回填）
> **日期**：2026-09-11
> **项目根（全部相对路径以此拼接）**：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\`
> **失败清单（绝对路径）**：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\goal-failures-g22.md`
> **形态**：精简命令路由（道远 09-11 拍板·G21 先例）——/goal 命令只含双层路由+前置校验+任务概述+完成判定+约束；全量锚文本/写死字符串/计数预期/默认决策以本文档为准

## 背景（2026-09-11 道远拍板，G20 支线核验 5 条候选遗留的处置决策）

G20 收尾时登记了 5 条候选遗漏，道远逐条拍板如下（原话要点）：

1. **期初入库**：不做。期初存量走实施数据迁移直接导库，界面不加「期初」类型 → 本 goal 仅登记决策，零原型改动
2. **赔偿核销**：道远要求用完整中文讲清楚再处理（不许中英夹杂、不许省略）→ 本 goal 先全量调查赔偿核销链路，按默认决策执行，收尾报告必须完整中文解释
3. **操作日志旧模块名**：采纳，改
4. **来源字段混写供应商名**：「不要在来源直接写路凯这种——租入-路凯、租入，不应该存在一个字段中」→ 全量检查「租入-XX」形态，来源字段只留来源类型
5. **租赁出库需要审核；「没有组合出库了，只有租赁出库或销售出库」**——原型中的「组合出库」全量改，并全量检查「原型已改、其他位置沿用老称呼」的残留

## 前置状态校验（防重复/并发执行，先于一切）

1. **单写者互斥**：python 扫 `P3-R01-包装租赁管理后台原型/` 与 `agent-handoff/` 最近文件 mtime，距今 ≥30 分钟无变化才动手；不符 → 停止，写失败清单「疑似并行会话在写，30 分钟后可重跑」
2. **防重复**（任一不符 → 停止并写失败清单「疑似已执行」）：
   - `P3-R01-包装租赁管理后台原型/租赁管理/组合出库列表.html` 仍存在且 `租赁管理/租赁出库列表.html` 不存在
   - demo-data.js 含实体键 `comboOutbounds:`（键保留，仅作防重复哨兵）且含「组合出库」字样
   - `agent-handoff/_索引.md` 无 G22 行
3. `git status` 有未提交改动 → 先 `git add -A && git commit -m "存档：G22 开工前快照（不 push）"`
4. **口径基数**：原型目录 HTML 总数=**115**（G20 后）；不符 → 记偏差并以实际数为验证门基数
5. **备份**：改动前建 `backup-g22-20260911/`（项目根），按原相对路径镜像全部将改文件（至少含：4 个组合出库文件、demo-data.js、我的待办.html、操作日志.html、F01、A03/A04 JSON、A05、A02、基础数据/BOM维护.html、基础数据/BOM.html、仓储作业/其他出库列表.html、仓储作业/弹窗/其他出库新建.html、仓储作业/库存查询.html、租赁管理/租赁单列表.html、租赁管理/退租入库列表.html、系统管理/用户权限.html、系统管理/角色管理.html、财务协同/应收账单.html、verify_listfull.py、v32_menu_verify.py——以 dry-run 实际命中清单为准补齐）

## 第 0 步·上下文获取（动手前读完）

1. `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\_AGENT基线.md` 全文（第四节铁律+第七节上手清单逐条适用）
2. 本任务文档全文
3. `agent-handoff\20260911-G20-F01支线口径与原型必修.md` 执行记录节（最近一次的验证门口径与失败先例：B3 弹窗 markup 勿插 script 内、B9 范本死代码、B10 断网豁免）
4. 工具（均在 `_scan_tmpdir\`）：audit_interaction.py / g17_audit_diff.py（基线 audit_results_g17baseline.json）/ v32_menu_verify.py / verify_listfull.py / g18 重注入=用户目录技能 annotate.py
5. 普查留档（本会话已跑，可参考但数字必须自己重核）：`_scan_tmpdir/g22_survey_a/b/c/d.txt`

## 任务一：组合出库→租赁出库 全量称呼统一

道远拍板：「没有组合出库了，只有租赁出库或销售出库」。菜单名早已是「租赁出库」（v3.1），文件名/实体/文案/单据类型名仍用老称呼。

### 1a 文件改名（4 个，git mv 保留历史）

| 旧 | 新 |
|---|---|
| `租赁管理/组合出库列表.html` | `租赁管理/租赁出库列表.html` |
| `租赁管理/组合出库录单.html` | `租赁管理/租赁出库录单.html` |
| `租赁管理/弹窗/组合出库单详情.html` | `租赁管理/弹窗/租赁出库单详情.html` |
| `租赁管理/弹窗/组合出库确认.html` | `租赁管理/弹窗/租赁出库确认.html` |

改名后全量引用替换（go()/href/注入标记注释/菜单项 onclick，三形态 `组合出库列表.html` 相对路径按所在目录层级各异——参考 G09 文件移动三形态替换先例）。普查基线（G22 开工时点）：demo-data 内「组合出库列表.html」49 处；「组合出库确认.html」引用方=组合出库列表页；「组合出库单详情.html」无引用（孤儿模板，仍改名）。

### 1b 中文称呼替换

「组合出库」→「租赁出库」全站精确替换，范围=原型目录全部 html/js/json/md，**先 dry-run 计数后 apply**。普查基线：demo-data 151 处（含 49 处 go 链接随 1a 已处理）、46 个 HTML/MD/JSON 文件。要点：

- 单据类型名：dictItems CK 行 `"name": "组合出库单"`→「租赁出库单」（**单号前缀 CK 保留不动**）
- 应收账单 feeType「租赁费（按组合出库自动汇总）」→「租赁费（按租赁出库自动汇总）」
- 页面 title 已是「租赁出库」的（列表/录单页）不动
- 文件头注释/注入标记里的老文件名同步改
- **白名单（不改）**：F01 内 SRC_DATA 版本沿革历史原话；A03/A04 中「原 XX」释义历史说明（如有）；`agent-handoff/`、会议文件夹、P1-R01 历史段、`99-归档/`、`backup-*/`、`_scan_tmpdir/` 全部不动
- 改完 A03/A04 JSON 后**双轮重注入**（A03 先注→A04 后注，零告警）

### 1c 英文实体键保留

`comboOutbounds` 实体键、字段缩写（zl/combo 等）**不改**——英文键非用户可见称呼，改名联动 renderListPage/listfull/audit 风险大。记入执行记录偏差说明。

## 任务二：租赁出库审核落地（待办 15→16 类）

道远拍板：租赁出库需要审核。现状：comboOutbounds 8 行无「待审核」态（ops=详情/打印出货单/出库确认），确认弹窗内含审核结论/审核意见/审核人元素但入口叫「出库确认」；待办 15 类无租赁出库；权限配置审核矩阵已有「租赁出库」项（无需动）。

1. **demo-data comboOutbounds 追加 1 行**（默认决策表①写死值），状态=待审核
2. **todoItems 追加 1 行**（默认决策表②写死值）→15 键变 16 键；node --check 必跑
3. **我的待办.html** 类型筛选 option 加「租赁出库」（插在「租赁单」后）
4. **A04** 我的待办 pin「15 类」→「16 类」，双轮重注入；页面 pin 同步
5. **出库确认弹窗升级审核口径**：弹窗标题改「租赁出库审核」（文件已随 1a 改名）；列表页 ops 文案「出库确认」→「审核」（先查 verify_listfull.py 有无断言「出库确认」，有则同步断言脚本——G17 断言同步先例）
6. **todoItems 行 link**=`租赁管理/租赁出库列表.html?audit=1`——复用既有 auto-open 审核弹窗机制（list-generic/页内 script 既有先例：回款登记 auto-open）；若该页无 auditModal/auto-open 机制，按其他列表页同款补接线；补不了 → 记失败清单，link 退化为裸列表页

## 任务三：老称呼全量清理（操作日志模块名+普查）

「原型已改、其他位置沿用老称呼」的全量收口。语境判定铁律：**现行功能引用（菜单/标签/列头/按钮/筛选项/枚举值/数据记录）→改；「原 XX」「已并入/已移除」释义句→保留；历史文档不动**。

1. **操作日志**（`系统管理/操作日志.html` option + demo-data opLogs 实体 module 字段同步）映射表：
   - 包装管理 → 租赁管理（G09 改名未同步）
   - 财务应收 → 财务管理（v3.2 组合并未同步）
   - 基础资料 → 基础数据（现行目录/组名）
   - 订单管理 → 销售管理（现行目录/组名）
   - 仓储作业 → 仓储管理（v3.2 组名；注意 demo-data 其他实体的 module 值同改）
2. **用户权限/角色管理** 页内「财务应收」残留（普查 13 处跨 5 文件）→同映射改
3. **F01** 内「包装管理」「财务应收」现行引用 →同映射改（SRC_DATA 沿革保留）
4. **全量普查其余老称呼**（普查基线计数，逐一过语境）：在租台账 12、租出台账 12、退租申请 4、丢损赔偿单 26、组装管理 13、拆卸管理 2、器具档案 4、零部件档案 4——**只改现行功能引用语境**（如筛选项/按钮/链接文案/模块枚举），释义句（「原在租台账已并入库存查询」类）保留；每词处理结果记入执行记录（改 N 处/留 N 处释义）

## 任务四：来源字段供应商名混写清理（「租入-路凯」38 处）

道远原话：「不要在来源直接写路凯这种。租入-路凯、租入，不应该存在一个字段中。」

1. 全站「租入-路凯」及同形态（「自购-XX」「自有-XX」，若有）清理，普查基线：demo-data 22、A04 3、库存查询 1、BOM 3、产品档案 2、租赁单列表 5、退租入库列表 2
2. **规则**：来源类字段值只留来源类型（「租入-路凯」→「租入」）；若同表/同弹窗已有供应商字段且该处供应商名缺失 → 把供应商名补到供应商字段；无供应商字段 → 只留「租入」（供应商维度缺失=G16 已登记 stockFlows 数据缺口，正式版补字段，原型不硬塞）
3. BOM 组合构成文本（mix）「围板箱×1（租入-路凯）」→「围板箱×1（租入）」
4. A04 改动后双轮重注入

## 任务五：赔偿核销链路调查与补口子

道远要求完整中文讲清楚。分两步：

1. **调查**（结果写执行记录，用完整中文、不省略）：其他出库列表筛选有「赔偿核销」选项但新建弹窗没有的原因；BS 前缀单号 32 处（应收 8/应付 6/回款 5/核销 4/退租入库 4/其他出库 5）在各实体中扮演什么角色
2. **处理**（默认决策表③）：
   - 其他出库新建弹窗（`仓储作业/弹窗/其他出库新建.html`）出库类型 radio 补「赔偿核销」（插「盘亏」后「其他」前；页内 createModal 如存在同款 radio 组同步——双层同步铁律）
   - BS 前缀单号**默认保留**：G09 拍板「丢损赔偿按对象直接转应收/应付」后，BS 就是赔偿类单据的编号前缀（模块没了、编号口径还在）。仅当调查发现 BS 单号仍**链接到已删除的丢损赔偿单页面**（死链形态）才处理该链接；纯文本引用保留

## 任务六：期初决策登记

`_AGENT基线.md` 第六节「已拍板不复议」追加一条：「其他入库不设『期初』类型——期初存量走实施数据迁移直接导库，界面不做期初入口（2026-09-11 道远拍板，G22）」。

## 默认决策表

1. **comboOutbounds 新增行**（字段按实体既有结构，单号=CK-20260910-022）：project=PRJ-2603、customer=上汽大众宁波分公司、zl=ZL-20260910-040、combo=「ZH-2603-B × 30 套」、so=「—（租赁出库）」、addr=「宁波杭州湾基地」、status=「待审核」、date=「2026-09-10」、ops=详情/审核
2. **todoItems 新增行**（插末条后，键='CK-20260910-022'）：auditor=李国栋、type=租赁出库、docNo=CK-20260910-022、summary=「上汽大众宁波 · 围板箱套件 30 套」、project=PRJ-2603、submitter=赵磊、time='09-10 15:20'、action=待审核、link='租赁管理/租赁出库列表.html?audit=1'
3. **BS 单号**：默认保留（见任务五）；仅死链形态才修
4. **实体键/单号前缀**：comboOutbounds 键、CK 前缀、字段缩写一律不改
5. **语境判定**：现行功能引用改、释义句保留、历史文档不动；拿不准的语境 → 保留并记偏差
6. **菜单名不变**：菜单早就是「租赁出库」，本 goal 只改其 href 指向新文件名；v32_menu_verify.py 若断言旧文件名 → 同步断言（G17 先例）
7. **verify_listfull.py 同步**：组合出库批次断言里的文件名/实体名/ops 文案随本 goal 同步更新（先跑看红再改，改后三批全绿）
8. **「组合」残留词**：「租赁组合」「组合件」等含「组合」但非「组合出库」的词不动
9. **页数口径**：4 文件改名不增不减，HTML 总数=115 不变
10. 新增行一律遵循 demo-data 末条逗号坑：末位插入先补逗号，改后 node --check

## 失败处理策略（无人值守铁律）

任何 assert 失败/锚点不匹配/预期外结构：跳过该项，记入 `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\goal-failures-g22.md`（字段：任务号/文件/原因），继续下一项。禁止卡住等待、禁止猜测性改写、禁止静默吞掉。收尾报告必须含失败清单结论行（空清单给「无失败项」）。

## 纪律（违反=返工）

- `_AGENT基线.md` 第四节+第七节全部逐条适用
- 原型手工维护：读取-精确替换+assert 计数+幂等；禁裸 open('w') 整页写
- **URL 防污染**：替换「组合出库」时区分 go()/href 文件名言（任务一 1a 处理）与中文称呼言（1b 处理）——文件名替换先行且只动文件名串，称呼替换随后只动中文词；两阶段各自 dry-run 计数
- **弹窗 markup 严禁插入 script 块内**（G20 B3 事故）；新增 onclick 自带 DOM 副作用（铁律⑦）
- demo-data.js 末条逗号坑；改后必跑 node --check
- HTML 块改动后标签配平自检（与备份差值对比，固有 JS 模板假象豁免）
- 备份按原相对路径；中文路径一律 python 内嵌处理
- 多股改动入库一律 git add -A；禁 push
- 验证数字一律自己重跑，不引用本任务书普查基线读数（基线仅供 dry-run 对账）

## 验证门（全部满足才算完成；证据=命令输出贴执行记录）

1. **断言脚本** `_scan_tmpdir/g22_verify.py`（新建留档）逐行 PASS：
   - 4 个旧文件名不存在、4 个新文件名存在；「组合出库列表.html」全站 0 命中；菜单项 `go('租赁管理/租赁出库列表.html')` 存在
   - 原型目录可见「组合出库」字样=0（白名单豁免逐条注记）；dictItems `"name": "租赁出库单"` 存在
   - todoItems 键数=16 且含 type「租赁出库」；我的待办 option 含「租赁出库」；A04「15 类」=0、「16 类」≥1
   - 操作日志 option 与 demo-data module 值无「包装管理/财务应收/基础资料/订单管理/仓储作业」
   - 「租入-路凯」全站=0（含 demo-data/页面/A04）
   - 其他出库新建弹窗含「赔偿核销」radio
   - 原型 HTML 总数=115
2. `node --check _data/demo-data.js` 退出码 0
3. `audit_interaction.py` 全量：死链 0/JS 0（F01 ERR_CONNECTION_ 断网豁免沿 G20 扩写）/g17_audit_diff.py 既有键新增 0；键改名 4 个（旧键消失 4=新键出现 4，各三项 0 或既有水平）
4. `v32_menu_verify.py` 14/14（断言同步后）
5. `verify_listfull.py` 三批全绿（断言同步后，期望值以 G20 后口径为基）
6. **PW 抽验** `_scan_tmpdir/g22_pw.py`（留档）+截图 `_scan_tmpdir/g22-*.png`：
   ①我的待办类型选「租赁出库」命中行≥1、pin 含「16 类」不含「15 类」
   ②租赁出库列表直达：渲染 9 行（8+新增 1）、点待审核行「审核」弹窗可见、标题含「租赁出库审核」
   ③操作日志模块筛选选「租赁管理」命中行≥1
   ④BOM维护 组合构成文本不含「租入-」
   ⑤其他出库新建弹窗打开含「赔偿核销」
   ⑥mobile/待办审批.html 待办行数=16、JS 0
   全程各页 JS 0
7. A03→A04 双轮重注入输出「零告警」

## 收尾（回写三件+git 批次为硬性）

1. 本任务文档标 ✅ + 执行记录（逐段结果/验证门证据摘要/默认决策命中/偏差闭环/**赔偿核销调查完整中文结论**）+ commit 哈希回填头部
2. `agent-handoff/_索引.md` 追加 G22 行（日期/G 号/任务名/状态/commit 哈希/文件名）
3. `_AGENT基线.md` 快照滚动：4 文件改名+待办 16 类+老称呼清理+来源字段口径+期初拍板登记（第六节）+当前任务块更新；基线快照内「组合出库列表.html」等历史表述不动（当前任务块之前的快照段只追加新口径，历史段不回改——除「当前任务」整块替换）
4. `P1-R01-需求梳理与功能框架.md` 附录 10.2 台账：4 文件改名登记
5. 失败清单结论行贴出；`backup-g22-20260911/` 保留不清理
6. 基线第六节挂起决策更新：5 条候选遗留标记处置结果（①期初=已拍板不做·②赔偿核销=G22 已处理·③操作日志=G22 已处理·④BOM 来源=证伪撤项+G22 来源混写清理·⑤矩阵差异=租赁出库已入待办 16 类）
7. `git add -A && git commit`（信息前缀 G22，**禁 push**）；哈希回填本文档头部与索引行

## 执行记录

（执行后填写：任务逐段结果 / 验证门证据 / 失败清单结论 / 默认决策命中 / 偏差清单 / 赔偿核销调查结论）
