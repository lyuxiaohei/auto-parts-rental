# G03 · 复验收与总收口（菜单重组 v3.2 三连跑 3/3）

> **元信息**：G03 ｜ 日期 2026-09-09 ｜ 状态 ✅ 已完成 ｜ commit：3b7092f（段1·spotcheck）+ 6f920c2（段2·收口文档）+ 本提交（段3·终锚）
> **双层路由**：公共背景读 `agent-handoff/_AGENT基线.md`；本文件=本 goal 任务全量细则。收尾回写三件：本文件标 ✅+执行记录+commit 回填头部 ｜ `_索引.md` G03 行挂哈希 ｜ 基线「当前基线快照」滚动至 v3.2 终态（goal-creator v7）。
> **需求原文（2026-09-09 道远验收反馈②）**：对于上次执行的效果再做一个验收。
> **口径**：G01（角色管理数据驱动化）与 G02（菜单 v3.2 侧边栏重写）各自已过批末门并提交；本 goal=对 v3.1+v3.2 全部资产做终验抽验（复验收），并完成 P1-R01 追加句、失败清单总结论、三文档与索引终态、基线终态滚动、记忆库同步。

## 前置校验（不过即停）

1. `git log --oneline | grep -c "G03-复验收·执行"` =0 → 可执行；≥1 → 失败清单追加「G03 已执行，跳过」停止（全史锚）
2. `git log --oneline | grep -c "G01-角色管理·执行"` ≥1 且 `grep -c "G02-菜单v3.2·执行"` ≥1，缺任一 → 失败清单追加「G03 前置缺失」停止（前置链）
3. `_data/` mtime ≥30 分钟无变化（资源互斥单写者）；工作区非 clean 先 `git add -A` 留档；备份 `_scan_tmpdir/backup-menuregroup32-20260909/` 存在

## B. 任务组

1. **写并跑 `_scan_tmpdir/v32_spotcheck.py`**：v31_spotcheck.py 全量继承（组名与损益口径更新至 v3.2）+角色页数据驱动实测，**≥16 项写死**——含 v3.1 资产复验收断言（项目管理组居首/三处小标签归属/菜单含「租赁出库」且跳组合出库列表.html/菜单无「盘点录入」项/盘点录入页 selected=盘点/我的待办一级直达）+ v3.2 新断言（仓储管理/财务管理新组名/损益在财务管理组尾跳盈亏报表）+ 角色页实测（renderListPage 行数=9/openRolePerm 权限配置弹窗标题与勾选矩阵抽查/新增角色 createModal 三字段）。
2. **P1-R01 附录 10.2 P3-R01 行追加 v3.2 记录句**（不抹历史）：要点=G01 角色管理数据驱动化（roles 实体 9 条+renderListPage+权限配置/新增角色弹窗+双层新模板 弹窗/权限配置.html、新增角色.html）+G02 菜单 v3.2（损益移财务管理组·菜单名「项目损益」/财务协同→财务管理/仓储作业→仓储管理·40 页重写）+audit 111 页/verify 243/抽验数字。
3. **失败清单写总结论行**：三连跑共享文件 `goal-failures-menuregroup32-20260909.html`——G01/G02/G03 三行终态+总结论（失败 0 项或明细）。
4. **收尾三件套+基线终态+记忆库**：本文件 ✅+哈希回填；`_索引.md` G01/G02/G03 三行终态核对（G01/G02 应已 ✅，补 G03）；`_AGENT基线.md`「当前基线快照」滚动至 v3.2 终态（111 页/菜单 v3.2/roles 实体 31 实体·demo-data 30+roles/G01-G03 ✅·当前任务行清空）+agent 记忆库 `p3r01-prototype-baseline.md` 同步 → git 一提交（信息含「G03-复验收·执行」）。

## C. 默认决策表

- v32_spotcheck.py 继承口径：A1 组序=v3.2 八组（含仓储管理/财务管理）；B 组损益断言改财务管理组口径；其余 v31 断言全量保留（即 v3.1 复验收）；总数执行后回填但 **≥16 写死下限**
- P1-R01 追加句形态=在 P3-R01 行尾追加一句（同 v3.1 先例），不另起行、不改既有句
- 基线终态句要点：109→**111 页**（+弹窗/权限配置+新增角色 两模板）；菜单 v3.2 终版（G02 A 表）；demo-data **31 实体**（30+roles）；三连跑 G01-G03 ✅
- 记忆库同步=p3r01-prototype-baseline.md 的菜单现状句与实体数/页数句更新+MEMORY.md 索引行同步
- 原型文件本 goal **零改动**（纯验证+文档收口）；若抽验暴露缺陷：修复一次→复跑该断言→仍败记失败清单并继续，不回改已 ✅ 的 G01/G02 文档（缺陷留档由 G03 执行记录注明）

## D. 验证门（全部满足，证据贴对话）

- `==== 菜单重组 v3.2 抽验：N 过 / 0 败 ====`（N≥16，含 v3.1 复验收断言）
- grep `P1-R01-需求梳理与功能框架.md` 命中含「菜单重组 v3.2」的追加句（1 处）
- grep `_索引.md`：G01/G02/G03 三行均 ✅ 且 commit 列非空
- grep `_AGENT基线.md` 现状含「菜单 v3.2」与「111 页」；记忆库文件含 v3.2 现状句
- 失败清单总结论行（失败 0 项或三批明细）
- git log 新提交号一行（含「G03-复验收·执行」）

## 执行记录（收尾回填）

- **执行**：2026-09-09 无人值守三连跑 3/3 完成。前置校验 4 条全过（G03 锚 0／G01 dccaa23+G02 df4bda7 在案／_data 最近写入距今 312 分钟／备份在位／工作区 clean）。
- **B1**：`_scan_tmpdir/v32_spotcheck.py` 38 项断言（v3.1 复验收 14 项+v3.2 新断言 9 项+角色页数据驱动实测 9 项+其余结构与跳转）——**38 过 / 0 败**；提交①=3b7092f。首跑 2 败（A4/B3）经定位为断言脚本口径 bug（A4 项目管理组切片误跨组、B3 应付小标签归并误计组尾「项目损益」），按 C 表「修复一次→复跑该断言」修复后 38/38 全绿，**非原型缺陷，原型本 goal 零改动**。
- **B2**：P1-R01 附录 10.2 P3-R01 行尾追加「09-09·菜单重组 v3.2（G01-G03 三连跑）」记录句（同 v3.1 先例，不另起行不抹历史）。
- **B3**：失败清单 G03 行终态=失败 0 项+总结论行（G01-G03 全绿；G04 行保持「—」待下轮）；提交②=6f920c2。
- **B4**：三件套+基线终态+记忆库（p3r01-prototype-baseline.md 新建+MEMORY.md 索引行）——本提交（段3·终锚）。
- **默认决策命中**：记忆库 `p3r01-prototype-baseline.md` 实查不存在（基线声明的双写载体此前未落盘）→按 C 表「记忆库同步」语义新建该文件并补 MEMORY.md 索引行，与基线快照双写同步；抽验总数 38（≥16 下限达标）。
- **验证门证据**：①`==== 菜单重组 v3.2 抽验：38 过 / 0 败 ====` ②P1-R01 命中「菜单重组 v3.2」追加句 1 处 ③_索引 G01/G02/G03 三行 ✅ 且 commit 非空 ④基线含「菜单 v3.2」「111 页」、记忆库含 v3.2 现状句 ⑤失败清单总结论行=失败 0 项 ⑥git log 含「G03-复验收·执行」。
