# 20260920-G56 · F01 业务流程导航图 v3.9 与 F02 绘图信息全量对齐（任务书）

| 项 | 内容 |
|---|---|
| G 号 / 日期 | G56 / 2026-09-20 · **✅ 已执行（七门全过＋独立验收 6 PASS/0 FAIL·执行记录见文末）** |
| 任务一句话 | F01 导航图自 v3.8（09-16）后原型已落地四批流程变更（背靠背直发＋虚拟仓 D-167／采购退货两守卫 D-157／销售退货与退款 G33·D-123／页面口径数字过期），本任务将 F01 升 v3.9 对齐原型现状，并把停在 v3.3 的 P1-R03-F02 绘图信息手工全量重写对齐 v3.9 |
| 上游依据 | 道远 2026-09-20 对话指示：核对 F01 与 P1-R03-F02 同现有原型的出入→生成 goal 更新这两个文件，**「尽量不要使用脚本批量跑，以免出错」**；改图内容全部取自已拍板已落地事项（见下），不自创新流程 |
| 已拍板口径（内容真值源） | ①直发链：类型加在**租入单**（袁工 09-18 亲述·直发单/自发单命名道远 09-19 确认）——直发单审核后**系统自动**生成租入入库（入直发虚拟仓 XNC-ZF·字典 KW-05·系统内置不可删改·不占实体库存）并自动生成租赁出库（CK-023 链·终态链 RZD-010→RZRK-024→CK-024）；②退租链：退租入库=人工录入（客户报数触发）→系统识别直发类型→库位自动带出直发虚拟仓（TZRK-009）→**系统自动生成归还出库**（GHCK-001·09-18 转写以录音为准）；③报数维护：客户报数增减经其他入库「报数调整」单人工调整虚拟仓账面（QTRK-004 演示单）；④采购退货两守卫（D-157）：入库前已付款→生成退款单（退款额≤应付已付额·退款登记提交时校验）；入库前未付款→不立退款单（拒收未入库不挂账自终结／入库后退货冲减在途应付）；应付生成口径=验收通过的实际入库数量；⑤销售退货→应收冲减（G33·D-123 退货退款闭环）；⑥术语现状：归还出库（D-159）／BOM组合（D-163）／银行回单核销（G40） |
| 待追认口径 | 以下预设值入默认决策表（文案级可逆），收尾报告列「待追认清单」：B1 回程行画法／页数与弹窗句新文案／不进图清单／F02 改为手工对照维护（提取脚本退役） |
| 改动面 | **三件主文件**：`P3\F01 HTML`＋`P1-R03\F02 md`＋`P3\F01 开发规则 md`；收尾文档：本任务书／`_索引.md`／`_AGENT基线.md`／P2-R01（D-172）／拍板登记／失败清单；验证产物：`_scan_tmpdir\g56_*` 脚本与截图 |
| 红线零改动 | `_data\` 全部共享件与 demo-data.js（数据层无涉）；全部业务页面 HTML；mobile；A02/A03/A04/A05/A06（已由 D-157/D-159/D-167 各自批次同步·本任务仅验证 F01 引用一致，不改其内容）；禁 push |

## 【第 0 步·上下文获取与前置校验】

1. 项目根＝`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁`（下称 ROOT）。原型根＝`ROOT\P3-R01-包装租赁管理后台原型`（下称 P3）；F01＝`P3\P3-R01-F01-业务流程导航图.html`；F02＝`ROOT\P1-R03\P1-R03-F02-业务流程导航图绘图信息.md`；开发规则＝`P3\P3-R01-F01-业务流程导航图-开发规则.md`。
2. 必读：`ROOT\agent-handoff\_AGENT基线.md`；本任务书全文；**开发规则全文（§11 修改流程＝改图铁律·§4 节点六型·§5 双线制·§12 口径）**。
3. 现状锚点（2026-09-20 实测·行号会漂移，按锚文本定位）：
   - F01＝v3.8（09-16）：双 `<svg>` 主线 viewBox `0 0 880 2184`／支线 `0 0 880 944`；`.html` href 唯一目标 **46 个·缺失 0**；关键词命中：直发 0／背靠背 0／XNC 0／退货 0／退款 0／报数 0／虚拟仓 4 处（均为「客户虚拟仓不维护」D-145 口径）；`.sub` 行含「**38** 个页面」；页脚含「38 页」与「67 个弹窗可独立演示」；版本出现四处＝`<head><title>`／eyebrow／`.sub`／footer 沿革。
   - 原型实测：**PC 126＋mobile 5＝131 个 HTML**；弹窗仅余 **3 个**（`基础数据\弹窗\停用确认.html`＋`系统管理\弹窗\停用确认.html`＋`系统管理\弹窗\重置密码确认.html`）。
   - 新流程页面（均已存在·href 直接指列表页）：`采购管理\采购退货单列表.html`（另审核/新建/详情 3 页）／`销售管理\销售退货单列表.html`（另 3 页）／`财务协同\退款登记.html`（另新建/详情 2 页）／`租入管理\` 11 页（租入单 4·租入入库 3·归还出库 4）。
   - F01 仓储作业已有承载（S1/S5：盘点差异→盘盈转其他入库·盘亏转其他出库；盘点/调拨/其他出入库节点在位）——本任务不动。
   - 开发规则 §12.3 已含直发虚拟仓与客户虚拟仓划界句（G54 写入·零动）。
4. 前置校验（任一不过→停止并写失败清单）：
   - 并行写者：`git log --since=2.hours --oneline`＋`.git` 目录 mtime；有他人新提交或未知变动→停止留档。
   - 幂等（按缺项断点续做）：F01 版本串＝v3.9 且 grep 门全过→直接进收尾；＝v3.8 全缺→开工；部分新节点已在→从缺项续。
   - 改前基线：`git status -- F01路径 F02路径 开发规则路径` 三文件须干净（改前状态＝HEAD，出错 `git checkout -- <文件>` 回退）。**不建 backup-\* 目录（0920 起备份目录机制退役·改前快照＝HEAD 基线）**。
   - 路径自检：本任务书全部绝对路径 `os.path.exists` 实测输出贴对话。

## 【T1】F01 · L3 背靠背直发分叉（D-167 对齐）

1. L3 泳道租入线改「自发／直发」双路径：现行链（租入单→租入入库→库存〔租入在库〕→租赁单→租赁出库）标注「自发」；**新增直发路径**＝租入单（直发·审核后系统自动）→租入入库（直发虚拟仓·系统自动）→租赁出库（系统自动·供应商直发客户）→⇢ T1/T2；**不经「库存」实体节点**（账面入虚拟仓·不占实体库存），租赁线（租赁单→租赁出库）两路径共用。
2. 画法约束：节点只用开发规则 §4 既有六型（直发路径三节点＝白底实体＋副标前缀「系统自动·」区分，**不得发明第七种颜色/盒型**）；连线用既有 1px `#4b5563` 主线规格；S 系列短标题惯例同样适用于新注记。
3. 直发注记框（虚线决策框规格·三行）：
   - 「类型＝直发单／自发单（租入单 radio·D-167）」
   - 「直发链系统自动：租入入库入直发虚拟仓 XNC-ZF（字典 KW-05·系统内置不可删改·不占实体库存）→自动生成租赁出库」
   - 「人工单据不可手选虚拟仓（下拉 filterV 排除）」
4. 报数调整小注记（直发分叉下方·单行）：「客户报数增减经其他入库『报数调整』单人工调整虚拟仓账面（QTRK-004）」。
5. `SRC_DATA.l3` 键补点（head 加一句现口径＋points 追加·**历史原话只加不改**——铁律）。
6. L3 空间不足→其下泳道整体下移＋主线 viewBox 扩高；**块内坐标位移必须覆盖 `y=`／`y1=`／`y2=`／path `d` 全部形式并跑 line-vs-rect 对齐断言**（改图铁律）。

## 【T2】F01 · T1/T2 退租归还链虚拟仓口径（D-167 对齐）

1. T1「退租入库」节点副标或邻注补：「直发件退租＝人工录入（客户报数触发）→系统识别直发类型→库位自动带出直发虚拟仓」。
2. T2「归还出库」注记补：「系统识别背靠背·自动生成（GHCK）；退租入库=人工·归还出库=系统（09-18 口径）」。
3. `SRC_DATA.t1`／`t2` 键各补点（只加不改）。

## 【T3】F01 · B1 退货回程＋财务通道退款（D-157／G33 对齐）

1. B1 泳道加回程行（第三行·虚线回程框群·低于销售行）：
   - 「采购退货单」节点（href `采购管理/采购退货单列表.html`）＋两守卫注记：「入库前已付→生成退款单（退款额≤已付额）；入库前未付→不立退款（拒收自终结／冲在途应付）·应付按实际入库数量」。
   - 「退款单」节点（href `财务协同/退款登记.html`）→ 指向 F2 标签行。
   - 「销售退货单」节点（href `销售管理/销售退货单列表.html`）→ 冲减应收（指向 F1 标签行）。
2. 财务通道标签行副标：F2 补「＋退款（已付退货）」；F1 补「销售退货冲减」。
3. `SRC_DATA.b1`／`fin` 键补点（只加不改）。
4. B1 区空间不足→同 T1.6 铁律（回程行插在 B1 与 L1 之间时 L1 起全部下移）。

## 【T4】F01 · 口径行刷新＋版本四处＋Mermaid（结构改动放最后执行）

1. `.sub` 行：「**38** 个页面」→「**131** 个页面（PC 126＋mobile 5）· 本图 46 个可点页面目标」（执行时实测复核三数，变则以实测为准）。
2. 页脚：「38 页」同步改；「67 个弹窗可独立演示」→「弹窗页面化后仅余 3 个确认类弹窗」（实测口径）；沿革链末追加 v3.9 条：「v3.9（09-20 直发与退货对齐）：L3 补背靠背直发分叉（系统自动链·直发虚拟仓 XNC-ZF·D-167）＋T1/T2 退租归还虚拟仓口径＋B1 补采购/销售退货回程（两守卫·D-157）＋F2 补退款＋页数/弹窗口径刷新·Mermaid 源同步」。
3. 版本四处刷新：`<head><title>`／eyebrow（「v3.9 · 2026-09-20 直发分叉与退货回程对齐」）／`.sub`／footer——**版本刷新面含 head title**（G40 假绿坑）。
4. 页尾 Mermaid 源数据同步：L3 补直发边、B1 补回程、F2/F1 标签补退款/冲减。
5. 渲染截图 ≥4 张（L3 全貌／B1 全貌／页脚沿革／支线 S7 不动证）存 `_scan_tmpdir\g56_shots\`。

## 【T5】F02 绘图信息全量手工重写（对齐 v3.9·**不跑提取脚本**）

1. 头部：对象版本 v3.9；生成方式注记改「extract_f01_spec.py／gen_f02_md.py 于 2026-09-20 退役，此后**手工对照 F01 维护**（道远 0920：尽量不要使用脚本批量跑）」。
2. §1 画布与文档结构：按 F01 实测重写（主线 `0 0 880 2184`／支线 `0 0 880 944`·页面骨架自上而下逐段）。
3. §3 主线区节点清单＋§4 支线区节点清单：**全量重列**——逐节点从 SVG 属性直读主标/副标/盒样式/跳转/x,y/w×h（含 T1-T3 新增全部节点；可点节点总数、主线/支线分账数以实测为准写入 §1）。
4. §5 样式规格：字族按 D-165 后实测（体系栈/等宽栈·Geist 已撤）；颜色/字号出现次数按实测重数。
5. §6 SRC_DATA：8 键确认＋本轮 l3/t1/t2/b1/fin 补点说明。
6. §7 视觉问题清单：复核逐条标注「已修／仍在」（字体外链项＝已修 3fd908a）。
7. §8 演进史：补 v3.4～v3.9 六条（素材＝F01 页脚沿革原文·逐版一句）。
8. 一致性硬门：F02 内旧口径串＝0（`组合出库列表`／`租赁管理/租入`／`租赁管理/归还`／`银行水单`／`盈亏报表`／`1280`／`v3.3`（除演进史引述外））。

## 【T6】开发规则小同步

1. 头部版本号→v3.9；§5 主线结构规则补一句：「L3 租入线自发/直发双路径（v3.9）——直发三节点系统自动·不经库存实体节点·注记框三行见 F01」。
2. §12.3（G54 划界句）零动；§13 当前版本遗留刷新（画法遗留按实况）。

## 【T7】验证门（全部满足才算完成·证据贴对话）

1. **结构门**：python 标签配平断言（`<svg>/<a>/<rect>/<text>/<g>` 开闭配对·F01 整文件）；双 viewBox 内容底不裁切（主线/支线内容 maxY≤viewBox 高−16 余量）。
2. **href 门**：提取 F01 全部 `.html` href 实测存在·缺失＝0（新增目标 ≥5：采购退货单列表/销售退货单列表/退款登记 等）。
3. **grep 门**：`v3.9`≥4 处；`v3.8 原型业务流程导航` 残留＝0；`直发`≥5；`背靠背`≥2；`XNC-ZF`≥1；`采购退货`≥2；`销售退货`≥2；`退款`≥3；`67 个弹窗`＝0；`38 个页面`＝0；`Geist`＝0（维持 D-165）。
4. **渲染门**（Playwright·`_scan_tmpdir\g56_render.py`·逐项 PASS 清单贴对话）：F01 打开 JS 错＝0；L3 直发分叉三节点＋注记框可见；B1 回程行三节点＋两守卫注记可见；T1/T2 新注记可见；S7 支线与本改动前同构（对照 git HEAD 版截图）；截图 ≥4 张。
5. **F02 一致性门**：F02 记载的可点节点总数/泳道数/支线数/viewBox/SRC_DATA 键数与 F01 实测**全等**（逐项对账表贴对话）；§8 旧口径串 grep＝0。
6. **开发规则门**：`v3.9` 断言＋§5 新句在位＋§12.3 原文未动（diff 证据）。
7. **独立验收**（不得自证）：自检全过后派**只读子 agent**（general-purpose·除 `_scan_tmpdir\g56_accept\` 外不得写任何文件）复验，逐项 `[PASS/FAIL] 检查项——证据摘要`＋末行 `总判定：N PASS / M FAIL` **原样贴进对话**；FAIL→修复重派同项最多 2 轮，仍不过记失败清单保留原状。
   - 子 agent 提示词四要素：**角色**＝只读独立验收员；**对象**＝F01 HTML＋F02 md＋开发规则 md（v3.9 批次·只读）；**检查清单**＝①结构门双 viewBox＋配平自跑 ②href 门自跑全存在 ③grep 门关键串复测（v3.9≥4／v3.8 残留 0／直发·退货·退款·XNC-ZF 命中／67 弹窗 0／38 页 0）④自开浏览器渲染 F01（JS 0·直发分叉与 B1 回程可见）⑤F02↔F01 节点数/泳道/viewBox/SRC_DATA 键对账 ⑥`git status` 改动面仅本任务文件（三件＋收尾文档＋_scan_tmpdir）；**输出格式**＝逐项 PASS/FAIL＋证据＋总判定行。

## 【T8】收尾

1. 失败清单 `ROOT\agent-handoff\goal-failures-g56.md`（无失败也写结论行）。
2. 本任务书头部标 ✅＋执行记录节（各门输出摘要＋**待追认清单**单列）。
3. `_索引.md` 挂 G56 行；`_AGENT基线.md` 滚动（F01 v3.9·F02 手工维护制）。
4. P2-R01：头部最后更新行追加 **D-172**＋附录 A 台账加行（F01 v3.9 与 F02 全量对齐·道远 0920 指示·引 D-167/D-157/G33）。
5. 拍板登记.md 补登上游指示条目（2026-09-20 道远：F01/F02 与原型出入对齐·goal 生成·「尽量不要使用脚本批量跑」）。
6. git 两笔提交（**只 add 本任务文件·禁 add -A·禁 push**——工作区有 mobile/zip 并行残留勿卷入）：①F01＋F02＋开发规则；②收尾文档。哈希回填任务书头部与索引行。

## 【默认决策表】（拿不准按此执行·不提问·收尾报告注明命中项；标【追认】者列入待追认清单）

| 决策点 | 预定值 |
|---|---|
| 新版本号与日期 | v3.9 / 2026-09-20 |
| 直发分叉位置 | 画进 L3 泳道内（沿 G54 待追认「F01 不画新支线」口径·不新增 S8）【追认】 |
| 系统自动节点样式 | 白底实体＋副标「系统自动·」前缀（§4 六型内·不新增盒型） |
| 页数口径文案 | 「131 个页面（PC 126＋mobile 5）· 本图 46 个可点页面目标」·执行时实测复核【追认】 |
| 弹窗句文案 | 「弹窗页面化后仅余 3 个确认类弹窗」【追认】 |
| B1 回程画法 | B1 泳道第三行虚线回程框群·两守卫注记随采购退货节点【追认】 |
| 不进图清单 | 库存流水／器具出租履历／出货单打印／应收生成／编码规则／mobile 5 页（支撑页·非业务流程）【追认】 |
| F02 维护方式 | 手工对照 F01 逐节重写·提取脚本退役注记【追认】 |
| SRC_DATA 补点 | 只加不改·历史拍板原话逐字保留（铁律） |
| 新注记文案 | 以本任务书 T1-T3 所列字句为准·改字须算单行容量（容器宽÷字号·固定容器字符预算教训） |
| 回退方式 | `git checkout -- <三文件>`（改前基线＝HEAD·不建 backup 目录） |
| 退货节点 href | 一律指列表页（采购退货单列表/销售退货单列表/退款登记） |

## 【失败处理策略】

- 任一任务组 assert 失败／结构不符→跳过该项、写入失败清单、继续下一项；禁止卡住等待、禁止对不确定结构猜测性改写。
- 改坏结构→立即 `git checkout --` 对应文件回到 HEAD 重来该步骤；失败清单记录经过。
- 全部门未过但主体完成→失败清单如实记载未过项与原因，任务书标 ⚠️ 部分完成，不谎报。

## 【纪律】

1. **一律 Edit/Write 手动改·禁脚本批量刷**（道远 0920 原话「尽量不要使用脚本批量跑，以免出错」·0918 三轮强调同口径）；验证用渲染实测/截图/审计/断言脚本不算刷。
2. 改图铁律（开发规则 §11＋历史教训）：**结构改动放最后**（先注记文案后动坐标）；块内坐标位移覆盖 `y=`/`y1=`/`y2=`/path `d` 全部形式＋line-vs-rect 对齐断言；改完必须渲染截图；S 系列标题短式；版本刷新含 `<head><title>`；SRC_DATA 历史原话只加不改；HTML 标签配平自检。
3. F02 手工重写·不运行不新写提取脚本。
4. 中文路径写文件瞬报 Errno 22→3 次重试＋临时文件 os.replace。
5. 长文本插改走 Write/Edit 工具·禁 bash 内嵌 python 长文本。
6. 禁 push；git add 按文件逐一列出。
7. 完成判定不得自证（T7.7 独立验收子 agent）。

---

## 【执行记录】（2026-09-20 收尾补记）

**前置校验**：并行写者放行（任务书落盘 18:16:41 晚于最后一笔提交 cfcef3b 18:15:54·其后无新写入）；幂等断点＝F01 v3.8 全缺→开工；三主文件 git status 干净；任务书绝对路径 11 条 os.path.exists 全 EXISTS（输出已贴对话）。

**T1~T6 全部按任务书执行**（全程手动 Edit·块级精确替换位移）：
- **T1** L3 直发分叉：第三行直发线三节点（白底实体＋副标前缀「系统自动·」·§4 六型内）＋⇢T1/T2 chip＋直发注记框三行（800×56 决策框规格）＋报数调整单行注记；自发线=现行链标注（租入线（自发）·租入单/租入入库副标补「自发」·租赁线标注「自发/直发共用」）；`SRC_DATA.l3` head 追加＋2 条。
- **T2** T1 入口行右注第三行（直发件退租链）＋T2 主行下注记（背靠背 GHCK）；`t1`/`t2` 各＋1 条。
- **T3** B1 第三行退货回程线（三节点白底+ink 虚线边〔待追认〕＋D-157 两守卫注记框三行）；F1 标签行＋销售退货冲减、F2 标签行＋退款（已付退货）；`b1`＋2 条、`fin`＋1 条。
- **T4** 口径行：**131 个页面（PC 126＋mobile 5）· 本图 49 个可点页面目标**（三数实测：HTML 131=126+5；唯一 href 46+3=49）；页脚弹窗句改「弹窗页面化后仅余 3 个确认类弹窗」；版本四处（head title/eyebrow/.sub/footer）＋v3.9 沿革条；Mermaid 源同步（L3 直发边/B1 回程/F1F2 标签）。
- **T5** F02 全量手工重写（对象 v3.9·脚本退役注记·§1~§8 全节重列·样式计数实测重数·旧口径串 7 项清零）。
- **T6** 开发规则 v3.9：头部版本＋§5 L3 双路径句＋§13 回程画法待追认条＋§1 viewBox 2484（独立验收发现旧值 2184 残留后修齐）；§12.3 零动（git diff 证据已贴）。
- **坐标位移**：主线 viewBox 2184→**2484**（B1 回程行 +92／L3 直发行与注记 +208／L4/T1/T2/F/图例 +300）；位移覆盖 y=/y1=/y2=/path d 全形式；line-vs-rect 对齐断言全过（21 个 56h 行位＋箭头全部对齐行中心 y+28·七条泳道分隔线 388/616/844/1280/1604/1958/2164）。

**T7 七门证据摘要**（明细已贴对话）：
1. 结构门 ALL PASS（9 标签配平/双 viewBox 2484·944/maxY 2464≤2468·926≤928/21 行位/箭头对齐/6 path 精确坐标）
2. href 门：唯一目标 **49**（主线 25＋支线 30·复用 6）·缺失 **0**·新增三目标在位
3. grep 门：v3.9=8≥4·v3.8 残留 0·直发 35·背靠背 6·XNC-ZF 5·采购退货 6·销售退货 11·退款 22·67 弹窗 0·38 页 0·Geist 0
4. 渲染门：**37 PASS/0 FAIL**（JS 0·17 组新文本可见·15 条新增文案 getComputedTextLength 全部在容器预算内·S7 三节点不动证）＋截图 4 张（`_scan_tmpdir/g56_shots/`：L3 直发分叉/B1 退货回程/页脚沿革/S7 不动证）
5. F02 一致性门：节点框 89=58+31·`<a>`102·唯一目标 49·viewBox·SRC_DATA 8 键·主线标题 8/支线 7 **全等**；演进史外旧口径串 7 项=0
6. 开发规则门：v3.9 头部＋§5 新句＋§13 新条＋§12.3 原文在位（diff 仅 4 处预期改动）
7. **独立验收（只读子 agent·原样输出）**：

> [PASS] ① 结构门——7 标签全配平（svg 2/2、a 102/102、g 12/12、text 310/310、rect 125 全自闭、line 63 全自闭、path 8 全自闭）；双 viewBox 实测序列＝`0 0 880 2484`／`0 0 880 944`；主线 maxY＝2466≤2468（rect 底边 2466/text 2464/line 2460/path 2012），支线 maxY＝926≤928（rect 底边 926/text 924/line 920），子项 3/3。
> [PASS] ② href 门——href 出现 102 处、去重唯一目标 49（=预期），逐一 os.path.exists 缺失 0；新增三目标在位 3/3：`采购管理/采购退货单列表.html`、`销售管理/销售退货单列表.html`、`财务协同/退款登记.html`，子项 2/2。
> [PASS] ③ grep 门（F01）——11/11 子项达标：v3.9＝8（≥4）；`v3.8 原型业务流程导航`＝0；直发＝35（≥5）；背靠背＝6（≥2）；XNC-ZF＝5（≥1）；采购退货＝6（≥2）；销售退货＝11（≥2）；退款＝22（≥3）；`67 个弹窗`＝0；`38 个页面`＝0；Geist＝0。
> [PASS] ④ 渲染门——chromium 打开 file:/// F01（中文路径编码正常，title＝v3.9 直发分叉与退货回程对齐）；pageerror 0＋console error 0；10/10 文本有 bbox（租入单（直发）y≈1314／系统自动·直发虚拟仓 XNC-ZF／系统自动·供应商直发客户／类型＝直发单／自发单 y≈1385／采购退货单×2／退款单×3／销售退货单×2／两守卫（D-157）／直发件退租＝人工录入 y≈1845／背靠背（GHCK））；支线 S7 三节点 exact 匹配 3/3 有 bbox（转移出库列表/新建/单详情），子项 3/3。
> [PASS] ⑤ F02↔F01 对账——7 组数字全等：节点框 58＋31＝89；`<a>` 102（行内链 13）；唯一目标主线 25＋支线 30、交集恰为 F02 所列 6 页（库存查询/租赁单列表/退租入库列表/应收账单/应付账单/销售订单列表）、并集 49；双 viewBox 同；SRC_DATA 8 键逐键全等（b1/l1/l2/l3/l4/t1/t2/fin）；16px 标题主线 8＋支线 7＝15；F02 `## 8.` 之前禁词 7/7＝0（组合出库列表/租赁管理/租入/租赁管理/归还/银行水单/盈亏报表/1280/v3.3；v3.3 仅演进史节内 2 次，允许）。
> [PASS] ⑥ 改动面门——git status 41 条全分类：M×3 恰为三件主文件（F01.html 18:41／开发规则.md 18:48／F02.md）；预登记残留如约在册不计败（mobile/ 6 条 M·17:29，zip D×1＋??×2）；?? 中 G56 产物＝_scan_tmpdir/g56_accept、g56_render.py、g56_shots、g56_verify.py，另有 `agent-handoff/20260920-G56-F01导航图v3.9与F02全量对齐.md`（18:16，G56 本批交接文档，在预登记 `_scan_tmpdir/g56_*` 口径之外，已按批次自身产物注明）；其余 `_scan_tmpdir` 22 条非 g56 草稿 mtime 14:07–17:42 均早于 G56 时间窗（18:16–18:48），归既有并行任务残留。三件主文件之外无任何 tracked 文件被改/删。
> 残留观察（不属六项门、未计 FAIL）：开发规则.md 第 11 行主线 viewBox 仍写旧值 `0 0 880 2184`，与 F01 实际 2484 不一致（该文件其余 v3.9 章节口径一致），建议下批改齐。〔验收后已当场修齐 2484〕
> 总判定：6 PASS / 0 FAIL

**失败清单**：`goal-failures-g56.md`——**无失败项**（登记 4 条过程记录）。

**待追认清单（单列·8 项）**：①直发分叉画在 L3 泳道内（不新增 S8）②B1 回程画法＝虚线回程框群（六型外变体）③页数口径文案「131 页〔PC126+mobile5〕·49 目标」④弹窗句「仅余 3 个确认类弹窗」⑤不进图清单（库存流水/器具出租履历/出货单打印/应收生成/编码规则/mobile 5 页）⑥F02 手工对照维护制（提取脚本退役）⑦T1/T2/L3 注记摆位 ⑧开发规则 §11.1 备份条款与 0920 备份退役政策同步（留待下轮）。

**git 提交**：①F01＋F02＋开发规则＝`77b6bf0`；②收尾文档＝本笔（哈希见 `git log` 最新一条·含本任务书/索引/基线/P2-R01 V1.15·D-172/拍板登记/失败清单）。禁 push 已遵守。
