# G04 · 三连跑独立验收（菜单重组 v3.2 合并命令任务段 T2）

> **元信息**：G04 ｜ 日期 2026-09-09 ｜ 状态 ✅ 已完成 ｜ commit：〈执行提交=本段提交，哈希见下〉
> **双层路由**：公共背景读 `agent-handoff/_AGENT基线.md`；本文件=本 goal 任务全量细则。收尾回写三件：本文件标 ✅+执行记录+commit 回填头部 ｜ `_索引.md` G04 行挂哈希 ｜ 基线「当前任务」行标四连跑+独立验收完成（goal-creator v7）。
> **需求（2026-09-09 道远）**：生成目标提示词，验收以上三条提示词——对 G01/G02/G03 三连跑做**独立验收**（先例=78f0aa 任务一独立验收：静态断言+实点+台账核对全一致）。
> **铁律**：**纯只读验收**——原型目录/`_data`/既有验证脚本一律不修改；复跑即权威，不引用执行会话贴过的数字（第四节⑦）；发现问题只记报告与失败清单，不擅自修复。

## 前置校验（不过即停）

1. `git log --oneline | grep -c "G04-独立验收·执行"` =0 → 可执行；≥1 → 失败清单追加「G04 已执行，跳过」停止（全史锚）
2. `git log --oneline | grep -c "G01-角色管理·执行"` ≥1 且 `"G02-菜单v3.2·执行"` ≥1 且 `"G03-复验收·执行"` ≥1，缺任一 → 失败清单追加「G04 前置缺失」停止（前置链）
3. `P3-R01-包装租赁管理后台原型/_data/` mtime ≥30 分钟无变化（资源互斥单写者）；工作区非 clean 先 `git add -A` 留档

## B. 验收组（20 项写死，全部只读）

1. **B0 提示词文本复验（5 项）**——验收对象=三条命令存档 `_scan_tmpdir/goal-v32-G01-roledriven.txt`、`goal-v32-G02-menurewrite.txt`、`goal-v32-G03-accept.txt`：
   ① 三文件存在且各 ≤4000 字符；② 提问措辞（是否/要不要/请确认/确认后/商量）各 0 处；③ 块标记（前置校验/任务/完成判定/约束）各 ≥4；④ 双层路由绝对路径 os.path.exists 全过且无腐蚀形态（`租赁gent-handoff`/`handoff0909` 0 处）；⑤ 锚词/前置链/完成判定数字与 G01-G03 三文档一致（抽 G01 的 12 项、G02 的 ≥12 与 100/88/55、G03 的 ≥16 与三行索引）。
2. **B1 执行链与回写完整性（6 项·静态 grep）**：
   ① git log 全史三执行提交（G01-角色管理·执行/G02-菜单v3.2·执行/G03-复验收·执行）各 ≥1；② `_索引.md` G01/G02/G03 三行均 ✅ 且 commit 列非空；③ 三份 G 文档头部状态=✅、commit 已回填、执行记录节非空；④ `P1-R01-需求梳理与功能框架.md` 含「菜单重组 v3.2」追加句恰 1 处；⑤ `_AGENT基线.md` 快照含「菜单 v3.2」「111 页」「31 实体」；⑥ 失败清单含 G01/G02/G03 三行+总结论行。
3. **B2 验证门复跑（5 道·全部自跑取数）**：
   ① audit 全量自跑：111 页/死链 0/JS 错仅 F01 外链噪音 1 条/与 **G06 执行提交**（无则 G03）时的 `audit_results.json` 逐页 diff 新增 0（旧版取法：`git log --grep "G06-F01修正·执行" -1 --format=%h`，回退 `G03-复验收·执行` 后 `git show <hash>:_scan_tmpdir/audit_results.json`；若该提交未含此文件则取全史最后一个含它的提交，口径=problems/dead_links/js_errors 逐页集合不扩）；② verify 三批 100/88/55 失败 0；③ `verify_roles_v32.py` 12 项失败 0；④ `v32_menu_verify.py` ≥12 项失败 0；⑤ `v32_spotcheck.py` ≥16 项失败 0。
4. **B3 交付物实地抽查（4 项·只读）**：
   ① demo-data.js `roles` 实体恰 9 键（RL-01~RL-09）；② 角色管理.html 含 `renderListPage` 与 `openRolePerm` 命中；③ 侧边栏组名形态 `</span>财务协同<span` 与 `</span>仓储作业<span` 0 处（40 含侧边栏页）；④ `系统管理/弹窗/权限配置.html` 与 `新增角色.html` 存在。
5. **B4 验收报告与收尾**：写 `_scan_tmpdir/acceptance-v32-20260909.md`（B0-B3 逐项结论表+失败明细+总结论行）→ G04 回写三件套（本文件 ✅+哈希；`_索引.md` G04 行；基线「当前任务」行标四连跑+独立验收完成）→ git 一提交（信息含「G04-独立验收·执行」）。

## C. 默认决策表

- 验收断言总数 **20 项写死**（B0 5+B1 6+B2 5+B3 4），输出行 `==== G04 独立验收：20 项全过，失败 0 项 ====`；任一项失败→如实输出 `失败 X 项`+报告明细+失败清单补 G04 行后正常收尾提交（不修原型、不阻塞）
- 验收脚本新建仅限 `_scan_tmpdir/`（如 `accept_v32_check.py`），不改动既有 `v32_*.py`/`verify_roles_v32.py`（复跑用原脚本）
- audit 基线对比默认口径见 B2①（逐页集合不扩=新增 0）；若历史中无任何提交含 audit_results.json，则退化为绝对口径：111 页/死链 0/JS 仅 F01 1 条/问题数 ≤40（报告注明退化原因）
- 验收报告格式：Markdown 表（编号/验收点/证据/结论），结论行模板「G04 独立验收：20 项全过，失败 0 项（或 失败 X 项：明细…）」
- 记忆库同步：基线「当前任务」行更新即可；p3r01-prototype-baseline.md 若 G03 已同步 v3.2 终态则 G04 不再改写（避免双写漂移），仅在执行记录注明核对结果

## D. 验证门（全部满足，证据贴对话）

- `==== G04 独立验收：20 项全过，失败 0 项 ====`（或如实 X 失败）
- B2 五道门输出行原样贴对话（audit 行/三行 batchN/12 项角色门/N≥12 菜单门/N≥16 抽验门）
- `ls _scan_tmpdir\acceptance-v32-20260909.md` 存在且 grep 结论行命中
- git log 新提交号一行（含「G04-独立验收·执行」）+ 本文件头部状态=✅ 且 commit 已回填 + `_索引.md` G04 行=✅
- 失败清单 G04 行（全过或失败明细）

## 执行记录（收尾回填）

- **2026-09-10 副机 macOS 会话执行（合并命令 T2）**：`==== G04 独立验收：20 项全过，失败 0 项 ====`；验收报告=`_scan_tmpdir/acceptance-v32-20260909.md`（B0-B3 逐项结论表+口径注记+总结论行）。
- **B0 提示词复验 5/5**：三存档 2149/2000/2229 字符（≤4000）；提问措辞 5 词×3 存档全 0；四类块标记齐备（合计 10/10/9 处）；D:\ 路径换算后 os.path.exists 全过、腐蚀形态 0；数字锚与三任务文档一致（12 项/≥12+100/88/55/≥16+三行索引）。
- **B1 执行链与回写 6/6**：三 ·执行 提交各 1；索引三行 ✅ 挂哈希；三文档头部 ✅+执行记录非空；P1-R01「菜单重组 v3.2」恰 1 处；基线 111 页/31 实体/菜单 v3.2（=**v3.2** 形态）；失败清单三行+总结论行。
- **B2 五道门自跑 5/5**：①audit 全量自跑 111 页/死链 0/JS 0/problems 36，与 **G06 执行提交（66ebd90）** 版 audit_results.json 逐页 diff 新增 0（页路径反斜杠归一化）；②`==== batch1 验证门：100 项断言，失败 0 项 ====`／`==== batch2 验证门：88 项断言，失败 0 项 ====`／`==== batch3 验证门：55 项断言，失败 0 项 ====`；③`==== 角色管理数据驱动验证门：12 项断言，失败 0 项 ====`；④`==== 菜单 v3.2 验证门：14 项断言，失败 0 项 ====`；⑤`==== 菜单重组 v3.2 抽验：38 过 / 0 败 ====`。
- **B3 实地抽查 4/4**：roles 恰 9 键 RL-01~RL-09；角色管理.html renderListPage×1+openRolePerm×10；侧边栏旧组名形态 0 处（108 含导航结构页扫描）；权限配置/新增角色两模板存在。
- **口径注记**（详见验收报告第二节）：①B0③「各 ≥4」按四类块标记齐备+合计 ≥4 判定（完成判定/约束为节标题各 1 处属命令正常结构）；②B1⑤ 基线以「菜单=**v3.2**」形态记载，语义等价；③副机新装 Playwright/Chromium，既有脚本零改动（run_audit_mac.py/run_script_mac.py 包装器换算加载）；④验收脚本新建仅 _scan_tmpdir/accept_v32_check.py（B0+B1+B3 静态 15 项）。
- **记忆库核对**：p3r01-prototype-baseline.md 副机不存在（跨机说明——路径存在时才回写），注记跳过；主机侧 G03 已同步 v3.2 终态，按 C 表「避免双写漂移」不再改写。
