# G11 · 标注污染清查：元注释退出页面本体、回归标注层（/原型标注）

> **元信息**：G11 ｜ 日期 2026-09-10 ｜ 状态 ⬜ 待执行 ｜ commit：执行后回填 ｜ 并入合并命令 `_scan_tmpdir/goal-g10g11-combined-20260910.txt`（T2 段）执行，末段 T3=git push origin main（道远授权）
> **双层路由**：公共背景读 `agent-handoff/_AGENT基线.md`；本文件=本任务全量细则。收尾回写三件：本文件标 ✅+执行记录+commit 回填头部 ｜ `_索引.md` G11 行挂哈希 ｜ 基线「当前任务/快照」行更新（goal-creator v7）。
> **失败清单**：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\_scan_tmpdir\goal-failures-g11-20260910.md`（新建，空清单=全过）
> **需求原文（2026-09-10 道远）**：「/原型标注 使用该技能，全量检查页面中的哪些内容应该是标注，但是直接写进了页面？如，库存查询页面中的：客户虚拟仓＝在客户处的租赁资产按客户归集（on-hire）；客户转租为其子状态（2026-09-08 会议 T1 方向）。客户在租明细（原在租台账·2026-09-10 并入）＝行内『客户在租』按客户/项目下钻；租出与退回进度在租赁单列表『退回进度』列跟踪。」
> **口径（原型标注技能核心原则——原型与标注分离）**：页面本体不带任何元注释（会议日期/拍板/纪要引用、「原XX·并入/改挂」溯源、FP/REQ 编号、★ 标记、标题括号说明尾巴），全部由标注层承载；默认打开=干净业务原型（可演示），开标注=需求规格。

## B. 任务组

1. **B1 加载技能与全量扫描（只读）**：读 `skills/原型标注/SKILL.md` 与 `skills/原型标注/references/annotation-format.md`（数据格式唯一口径）；脚本扫描 38 业务页（含我的待办）页面本体文本，命中模式=见 C 表「污染模式」，产出 `_scan_tmpdir/g11-meta-scan.json`（逐页逐条：文件/元素/原文/初判类别）；确认库存查询示例行在列。
2. **B2 分类定案**：按 C 表规则把扫描清单分为 A 直删 / B 迁入 A03 后删 / C 白名单保留 三类，定案清单写 `_scan_tmpdir/g11-disposition.md`；疑似项一律按 B（迁入，保守不丢信息）并在清单注「默认决策命中」。
3. **B3 页面清除（38 业务页）**：脚本精确替换+逐条 assert+改前备份 `_scan_tmpdir/backup-g11-20260910/`（原相对路径）：删 A 类整行/整段；B 类同位置清除（页面只留纯业务交互句，若有）；标题括号尾巴（`主名（说明）`→`主名`）与 ★ 标记同步清除。
4. **B4 迁入标注层并重注**：B 类条目迁入 `P3-R01-A03-标注数据.json`（格式=annotation-format.md；页已有键→加 pin，无键→新建键；note=业务口径全文，fp/req 可留空，出处会议日期保留在标注便签内——标注层本就承载出处）；跑 `python skills/原型标注/scripts/annotate.py --pages P3-R01-包装租赁管理后台原型 --data P3-R01-包装租赁管理后台原型/P3-R01-A03-标注数据.json` 与 A04 数据各全量重注一次（幂等）；输出「未命中/多处匹配/替换元素」三类告警必须逐条处理至 0（消歧写法=开标签带 onclick/唯一属性，见技能文档）。
5. **B5 验证与文档**：D 验证门全跑；A03 _meta date 注记；P1-R01 变更记录⑲；git 一提交（消息含「G11-标注污染清查·执行」）+回写三件。

## C. 默认决策表（执行中按此处理，不提问）

- **范围**=38 业务页（含我的待办）；**F01 豁免**（SRC_DATA 会议出处弹层是其设计功能）；**弹窗模板 69 页仅扫描登记** `_scan_tmpdir/g11-modal-scan.md`，本任务不改动
- **污染模式**（命中即候选）：①`（20\d{2}-\d{2}-\d{2}` 日期与 会议/拍板/纪要/决策/确认 共现 ②`G0\d` 任务号 ③`原.{0,12}(并入|改挂|随.{0,8}删除|移除)` 溯源 ④`FP\d|REQ[-\d]` 编号 ⑤标题括号说明尾巴/★ 标记 ⑥pn-hint 元素全文审查
- **保留白名单（C 类，不动）**：纯交互/状态提示（「自动生成不可编辑」「已盘 N/M 项」类）；业务状态词（待确认/待审核/待发货等 tag——是状态不是元引用，勿误删）；税率换算/校验提示等业务交互说明
- **处置**：含业务口径→迁 A03（便签承载全文含出处）；纯元注释无业务信息→直删；混合句→整段迁标注，页面只留纯业务交互句；拿不准→按迁入处理+决策清单注明
- A04 流程链标注不动（无元注释污染；若扫描命中按同则处理并注明）
- 改动后页面计数/菜单/文件零变更；不重打 zip

## D. 验证门（证据全部贴对话）

1. annotate.py A03+A04 两次重注输出：`未命中 0 / 多处匹配 0 / 替换元素 0`（原样贴出，告警逐条处理记录）
2. 残留扫描（B1 同脚本复跑）：非白名单命中=0（输出计数行+白名单命中清单）
3. Playwright 抽 3 页（库存查询/租赁单列表/销售订单列表）：默认态无便签裸露 + `?notes=1` 角标渲染计数>0 + 右下角「标注 N」入口在（逐页一行 PASS）
4. audit 108 页：死链 0/JS 错 0/相对执行时 git HEAD 版 audit_results.json 逐键 diff 新增 0（路径分隔符归一；前置校验先记录 HEAD 并自跑基线快照）
5. 示例闭环：库存查询页面本体 grep「原在租台账」=0；A03 库存查询键含「客户在租」pin（grep 证据行）
6. 失败清单结论行（失败 0 项或明细）

## 执行记录（收尾回填）

（执行后填写：扫描命中总数/三类分布/各 B 组结果/偏差注记/默认决策命中情况/遗留登记）
