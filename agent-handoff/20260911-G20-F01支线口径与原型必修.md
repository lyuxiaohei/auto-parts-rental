# 20260911-G20-F01支线口径与原型必修

> **状态**：✅ 已完成（验证门 7 条全过）
> **commit**：6f38c32（工作）——收尾回写见下一提交
> **日期**：2026-09-11
> **项目根（全部相对路径以此拼接）**：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\`
> **失败清单（绝对路径）**：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\goal-failures-g20.md`
> **形态**：B（长任务书整段粘贴）——本命令全量自包含，执行者零上下文可跑；本文档=该 goal 的完整凭据与执行记录处

## 背景（2026-09-11 支线核验结论，证据锚点已核）

F01 导航图 v3.5（`P3-R01-包装租赁管理后台原型/P3-R01-F01-业务流程导航图.html`）6 条支线主干与原型一致，但有 4 处图错 + 3 项原型必修，经道远拍板本 goal 修复：

- 图错①：S5「租赁出库录单」节点重复（F01 行 626-627，两处均链 `租赁管理/组合出库录单.html`），且漏画退租入库录入、租入归还新建两入口
- 图错②：S1 库存查询副标「在库/在途/客户/退租/租入」与原型五卡「在库/在客户(租出)/客户转租出/退租在途/租入在库」（`仓储作业/库存查询.html:349-366`）不符
- 图错③：S3 角色管理链接指向 `系统管理/弹窗/角色管理.html`——G01 前旧 5 角色孤儿模板；现行为 `系统管理/角色管理.html` 独立页（9 角色）
- 图错④：应收/应付「四类来源」与原型实际各 5 类不符（`财务协同/应收账单.html:360` 筛选 5 类含预付款(保证金)；`财务协同/应付账单.html:356` 筛选 5 类）
- 原型必修①：`租赁管理/退租入库列表.html` 自称「客户退回后直接录入」但全页无新建/录入按钮（:372 仅批量导出）——T1 统一入口承诺的落地缺口，拍板做**弹窗**形态（页数/菜单不变）
- 原型必修②：我的待办三口径并存——静态筛选 15 类（`我的待办.html:358/374`）、A04 pin-1 写「18 类」（`P3-R01-A04-流程链标注数据.json:285`）、运行时实体仅 12 类（`_data/demo-data.js:15775-15788`，缺其他出库/库存调拨/租入单）；且 RZ-20260905-004 的 type「租入库」（:15783）与筛选项「租入入库」失配致速滤零命中
- 原型必修③：`系统管理/弹窗/角色管理.html` 孤儿模板（旧 5 角色）下线清理

## 前置校验（先于一切，任一不符的处理写死）

1. **单写者互斥**：python 扫原型目录与 `agent-handoff/` 最近文件 mtime，距今 ≥30 分钟无变化才动手；不符 → 停止，失败清单记「疑似并行会话在写，30 分钟后可重跑」
2. **防重复执行**：F01 不含「v3.6」；退租入库列表.html 不含「新建退租入库单」；`租赁管理/弹窗/退租入库新建.html` 不存在；`_索引.md` 无 G20 行——任一不符 → 停止，失败清单记「疑似已执行」；若仅 G20 号被占 → 取下一空号并将本文档改名同步
3. **工作区存档**：`git status` 有未提交改动 → 先 `git add -A && git commit -m "存档：G20 开工前快照（不 push）"`
4. **口径基数**：原型目录 HTML 总数=115（G19b 后口径）；不符 → 记偏差并以实际数为验证门基数
5. **备份**：改动前建 `backup-g20-20260911/`（项目根），按原相对路径镜像全部将改文件：F01、退租入库列表.html、demo-data.js、我的待办.html、A03/A04 json、用户权限.html、g12-whitelist.md、A05、A02、系统管理/弹窗/角色管理.html（删除前留存）

## 任务一：F01 导航图修正（v3.5→v3.6）

文件：`P3-R01-包装租赁管理后台原型/P3-R01-F01-业务流程导航图.html`。全部精确替换+assert 计数。

1. **S5 重复节点修正**：第二个「租赁出库录单」chip（行 627，x=472 y=544）改文案「退租入库新建」、副标「⟷ 退租入库列表 · 弹窗」、href 改 `租赁管理/弹窗/退租入库新建.html`；S5 第二行（y=588）新增 chip：x=658 w=120 h=36，文案「租入归还新建」、副标「弹窗 · ⟷ 租入归还列表」、href `租赁管理/弹窗/租入归还新建.html`。assert：改前「租赁出库录单」恰 2 次、改后恰 1 次；新增 chip 样式照搬同区既有 chip
2. **S1 五态命名对齐**：库存查询 chip 副标「在库/在途/客户/退租/租入」→「在库/客户/转租/退租/租入」；S1 口径注记行（行 591，x=336 y=130）替换为「库存状态五态：在库/在客户（租出）/客户转租出/退租在途/租入在库」，并在 y=144 新增一行（同样式 8px mono #6b7280，x=336）「在途＝应入库未入库（退租待入库/采购到货/租入到货）· 租入＝在库未转租，转租后计入客户态」。assert：旧副标串 0 命中；严禁移动其他区元素
3. **S3 角色管理改链**：行 607 href `系统管理/弹窗/角色管理.html`→`系统管理/角色管理.html`，链接文案「弹窗」→「独立页」
4. **应收应付 5 类口径**（枚举写死，禁自创）：
   - 应收副标（行 509）→「五类应收来源 · 含供应商应收/预收」
   - 应收注记（行 526-527）两行→「来源 5 类：B1 销售费 · L1~L4 租赁费 · 丢损赔偿（对客户）·」「供应商应收 · 预收款（保证金）」
   - 应付副标（行 533）→「五类应付来源 · 含对客户应付/预付」
   - 应付注记（行 540）→「来源 5 类：采购应付 · 租金应付 · 赔付应付 · 对客户应付 · 预付款」
   - Mermaid 源（行 704/707）两处「4 来源」→「5 来源」，应收枚举补「预收款（保证金）」、应付「对客户应付/预付款」拆为两类
   - assert：改后「四类应收来源」「四类应付来源」「4 来源」均 0 命中
5. **版本号 v3.6**：`<title>`、eyebrow 行（行 67，改「v3.6 原型业务流程导航 · 2026-09-11 支线核验修正」）、.sub 行（行 69 内 v3.5 字样）、footer（行 662 头部 v3.5·2026-09-10 → v3.6·2026-09-11）；footer 版本沿革末尾追加一行：「v3.6（09-11 支线核验修正）：S5 重复节点修正+补退租入库新建/租入归还新建两入口 · S1 五态命名对齐原型 · S3 角色管理改链独立页 · 应收应付来源改 5 类」
6. **不动项**：7 条主线泳道与 T1/T2 结构、其余节点坐标、SRC_DATA 8 键、双 viewBox、图例、S2/S4/S6 内容

## 任务二：原型必修 3 项

### T2a 退租入库录入入口（弹窗形态）

1. 新建 `租赁管理/弹窗/退租入库新建.html`：结构/类名/骨架照搬范本 `租赁管理/弹窗/租入归还新建.html`；文件头自注「弹窗模板：退租入库新建（createModal）· 注入标记：租赁管理/退租入库列表.html」
2. `租赁管理/退租入库列表.html`：行 372 head-btns 的批量导出按钮后追加 `<button class="btn btn-sm" onclick="openModal('createModal')">新建退租入库单</button>`；detailModal 块（行 642 起）结束后内嵌与模板逐字一致的 createModal markup；openModal/closeModal 页内已有（行 695-696），严禁重复定义
3. 弹窗字段（写死）：标题「新建退租入库单」；关联租赁单 select 静态 3 项（取 demo-data.js leaseOrders 在租行前 3 单号，不足按实际）；退回日期 date 默认 2026-09-11；明细静态 3 行（WBX-1210L 围板箱 退回 86 缺损 3 / GB-800 隔板 退回 40 缺损 0 / 锁扣组件 退回 80 缺损 0）；验收备注 textarea 占位「缺损情况、照片张数等」；按钮「取消」「提交审核」均 closeModal('createModal')，提交版另加 showToast('已提交审核（原型演示）')——页内无 showToast 则仅 closeModal 并记偏差
4. 断言：标签配平自检通过；createModal 恰 1 处；按钮 onclick 自带 DOM 副作用

### T2b 我的待办 15 类口径

1. `_data/demo-data.js` todoItems（行 15775 块）：行 15783 `'租入库'`→`'租入入库'`（恰 1 处 assert）；末条 SK 行后追加 3 行（字段值见默认决策表①，逐字写死）；追加后实体键数=15；**改后必跑 `node --check`**（末条逗号坑：新增行尾逗号风格与现有行一致）
2. A04 json 我的待办 pin-1（行 285）「18 类单据统一进待办」→「15 类单据统一进待办」（恰 1 处 assert）
3. 顺带项：A03 json 库存查询 pin-2 在途定义删「调拨在途」（与调拨列表「第一期不做调拨在途」拍板对齐；grep 定位，恰 1 处 assert）
4. 按 G18 文档（`agent-handoff/20260911-G18-说明文字清理转标注.md`）所载流程双轮重注入（A03 先注→A04 后注，脚本 `_scan_tmpdir/g18_t1_apply.py`/`g18_t2_apply.py` 或该文档所载现行脚本），要求零告警；注入后 `我的待办.html` 页内 proto-pin-1 文本=「15 类…」（注入产物，禁止手改页面 pin 块绕过流程）

### T2c 角色管理旧弹窗模板下线

1. 删除 `系统管理/弹窗/角色管理.html`（删除前已入备份）
2. 清理活引用（全部精确替换/删行+assert）：
   - `系统管理/用户权限.html` 行 640 注释行删除
   - `agent-handoff/g12-whitelist.md` 行 41 条目删除
   - `P3-R01-A05-字段字典.md` 行 620 条目删除（台账以现存文件为准）
   - `P3-R01-A02-页面类型与入口对照表.md` grep「角色管理」相关格改独立页口径
   - A03/A04 json grep「弹窗/角色管理」：有该页标注条目则一并删条目
3. 回退链：若发现除上述外的活引用（排除 `99-归档/`、`backup-*/`、`_scan_tmpdir/` 日志与备份、`.git`）无法精确移除 → 放弃删除，文件顶部加废弃 banner「本模板已下线，现行口径见 系统管理/角色管理.html（独立页）」，记偏差
4. 页数口径：+1 退租入库新建模板 −1 角色管理模板 = 总数 115 不变（验证门断言）

## 默认决策表（可预见小决策全部预置；表外情形→最简合理默认+收尾注明，禁止提问）

1. **todoItems 新增 3 行**（字段顺序对齐现有行 auditor/type/docNo/summary/project/submitter/time/action，docNo=键，插入位置=SK 行后）：
   - `'QTCK-20260904-003'`：type 其他出库 · summary「华东中心仓 · 报废隔板 12 块」· project 华东中心仓 · auditor 李国栋 · submitter 赵磊 · time '09-04 10:05' · action 待审核 · link `仓储作业/其他出库列表.html?audit=1`
   - `'DB-20260906-008'`：type 库存调拨 · summary「华东中心仓→华南中心仓 · 围板箱 50 只」· project 华东中心仓 · auditor 李国栋 · submitter 赵磊 · time '09-06 11:40' · action 待审核 · link `仓储作业/库存调拨列表.html?audit=1`
   - `'RZD-20260909-010'`：type 租入单 · summary「路凯 · 大箱租入 40 只（月租）」· project PRJ-2603 · auditor 王强 · submitter 王强 · time '09-09 09:50' · action 待审核 · link `租赁管理/租入单列表.html?audit=1`
2. F01 全部替换文案以任务一逐条写死值为准；新增 chip/注记行样式照搬同区既有同类元素
3. 新弹窗模板不注入 pc-auth.js（先抽查 2 个既有弹窗模板确认无引用，比照执行；若发现模板普遍已注入则同步注入并记偏差）
4. 租赁单列表行内「退租入库」操作跳转不改（仍跳列表页；录入入口在列表页头部）——本 goal 不改租赁单列表
5. mobile 五页消费 todoItems 增至 15 行属预期同步，不裁剪
6. 复核发现已合规的项 → 记录「无改动」继续，不算失败
7. 新术语先查 `P1-R04-术语表.md`；本 goal 不新增术语（「退租入库新建」为既有词组合）

## 失败处理策略（无人值守铁律）

任何 assert 失败/锚点不匹配/预期外结构：跳过该项，记入失败清单 `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\goal-failures-g20.md`（字段：任务号/文件/原因），继续下一项。禁止卡住等待、禁止对不确定结构猜测性改写、禁止静默吞掉。收尾报告必须含失败清单结论行（空清单给「无失败项」）。

## 纪律（违反=返工）

- `agent-handoff/_AGENT基线.md` 第四节（历史事故与真相）全节 + 第七节（上手清单）逐条适用，不复述
- 原型手工维护：读取-精确替换+assert 计数+幂等；禁止裸 open('w') 整页写入
- HTML 块改动后标签配平自检；改内嵌 JS 字符串后必跑该页 audit
- 备份按原相对路径还原语义；中文路径一律 python 内嵌处理（禁依赖 shell 中文参数）
- 多股改动入库一律 `git add -A`（`_data/` untracked 坑）；禁 push
- 验证数字一律自己重跑，不引用历史报告读数

## 验证门（全部满足才算完成；证据=命令输出贴收尾报告）

1. **断言脚本**：`_scan_tmpdir/g20_verify.py`（新建留档）输出逐行 PASS——F01：「租赁出库录单」=1、「退租入库新建」chip=1 且 href 正确、「租入归还新建」chip=1、「弹窗/角色管理.html」=0、「五类应收来源」=1、「五类应付来源」=1、「4 来源」=0、「v3.6」≥3 处、旧五态副标=0；退租入库列表：「新建退租入库单」=1、id="createModal"=1、标签配平；demo-data：todoItems 键数=15、「租入库」=0；A04「18 类」=0；我的待办页「18 类」=0；原型 HTML 总数=115；`系统管理/弹窗/角色管理.html` 不存在
2. **语法**：`node --check P3-R01-包装租赁管理后台原型/_data/demo-data.js` 退出码 0
3. **全量 audit**：`python _scan_tmpdir/audit_interaction.py` 重跑 → 死链 0 / JS 错 0（115 页）；`python _scan_tmpdir/g17_audit_diff.py` → 新增问题 0；新增键仅允许 `租赁管理/弹窗/退租入库新建.html`（且其三项全 0）；键消失仅允许 `系统管理/弹窗/角色管理.html`
4. **菜单门**：`python _scan_tmpdir/v32_menu_verify.py` → 14/14（菜单零改动回归）
5. **列表断言**：`python _scan_tmpdir/verify_listfull.py` 三批 → 期望值沿用 G18/G19b 文档所载现行口径（G14 记 103/88/43）；若口径漂移以文档实际值为准并记偏差
6. **PW 抽验**：`_scan_tmpdir/g20_pw.py`（留档）+ 截图 `_scan_tmpdir/g20-*.png`——①退租入库列表点「新建退租入库单」→ createModal 可见 → 提交 → 弹窗关闭；②我的待办 chip「租入入库」点击后命中行 ≥1、st-foot 文本含「覆盖 15 类」、pin-1 含「15 类」不含「18 类」；③`系统管理/角色管理.html` 直达渲染 9 角色行；④`mobile/待办审批.html` 渲染无 JS 错、待办行数=15。输出 PASS/FAIL 清单
7. **标注重注入**：A03→A04 双轮脚本输出「零告警」行贴出

## 收尾（回写三件+git 批次为硬性）

1. 本文档标 ✅ + 执行记录（任务逐段结果/验证门证据摘要/默认决策命中/偏差闭环）+ commit 哈希回填头部
2. `_索引.md` 追加 G20 行：日期/G 号/任务名/状态/commit 哈希/文件名
3. `_AGENT基线.md` 快照滚动：115 页口径（模板 69 净不变：+退租入库新建 −弹窗/角色管理）、todoItems 15 行、F01 v3.6、当前任务块更新
4. `P1-R01-需求梳理与功能框架.md` 附录 10.2 台账：+退租入库新建.html −弹窗/角色管理.html
5. 失败清单结论行贴出；`backup-g20-20260911/` 保留不清理
6. 基线「挂起决策」节登记全量档候选遗留：其他入库「期初」类型缺口、其他出库「赔偿核销」新建侧缺口与 BS 单号残留、操作日志旧模块名、BOM 维护无租入来源样例、待办/审核 14/15 口径矩阵差异
7. `git add -A && git commit`（信息前缀 G20，**禁 push**）；哈希回填本文档头部与索引行

## 执行记录

（执行后填写：任务逐段结果 / 验证门证据 / 失败清单结论 / 默认决策命中 / 偏差清单）

> **执行**：2026-09-11 无人值守（16:40 限额 429 中断 → 同日额度恢复后续跑收口；中断点=T5 脚本已写未生效，磁盘核查恢复现场）

**前置**：单写者 6 文件 mtime<30min 全部=本 agent 自身 T5 探测/脚本产物（无他会话写者，放行记 B0）；防重复 4 形态全过；存量存档 bc973dc；备份 backup-g20-20260911/（15 文件按原相对路径镜像）。

**T1 F01 v3.6**：S5 重复 chip 改「退租入库新建」（href=弹窗/退租入库新建.html）+S5 第二行补「租入归还新建」chip（x=658·样式照搬同区）；S1 副标五态改「在库/客户/转租/退租/租入」+注记行改全称+新增「在途＝应入库未入库…」行（x=200 y=130 实锚，任务书写 x=336 为文本元素内偏移）；S3 角色管理改链独立页；应收/应付副标+注记+Mermaid 源 4→5 类；版本 5 处 v3.5→v3.6+沿革追加。偏差 B1（Mermaid 源同步含新节点致「租赁出库录单」=2）/B2（SRC_DATA 历史沿革「各 4 来源」原话保留）。

**T2a 退租入库录入**：新建 租赁管理/弹窗/退租入库新建.html（结构照搬租入归还新建·字段写死：关联租赁单 select 静态 3 项/退回日期 2026-09-11/明细 3 行 WBX 退回86缺损3+GB-800 退回40+锁扣 退回80/验收备注占位/提交 toast）+列表页头部「新建退租入库单」按钮+createModal 内嵌。**失败 B3**：markup 首插误落 script 块2 内（锚为 JS 注释）→整 script 失效→listfull 崩 closeModal 未定义；修复=整块移至 demo-data.js 引用行后，PW 复验新建/详情开关 JS 0。**失败 B9**：范本尾部 G13⑨ 租入联动 script 为退租场景死代码（riSelect 不存在）→audit JS 错×1；整删复验 JS 0。

**T2b 待办 15 类**：todoItems「租入库→租入入库」+SK 行后追加 3 行（QTCK 其他出库/DB 库存调拨/RZD 租入单·默认决策表①写死值）=15 键 node 0；A04 pin-1「18 类→15 类」；A03 库存查询 pin-2 删「调拨在途」；A03→A04 双轮全站重注入（30 页+18 页全 OK 零告警），页面 pin 同步刷新。

**T2c 角色旧模板下线**：删 系统管理/弹窗/角色管理.html；清活引用 3 处（用户权限注释行/g12-whitelist 条目/A05 条目）+A02 挂靠列改独立页口径；全站 grep 无其余活引用；页数 115 不变（+退租入库新建 −角色模板，弹窗模板 69 净不变）。

**T5（道远合并项·租入单域日租金口径）**：demo-data rentInOrders 5 条 info「日租金 ×× 元/日」→月租（对齐档案参考价 45/12/10 元口径换算）+dictItems BF-01「按天/按天计租→按月/按月计租」4 处+释义「在租天数×日租金→月租金/按套数单价」；租入单列表列头「日租金(元)→租金」+副注、审核弹窗、我的待办摘要、数据字典 3 处同步；A04 pin#1「日租金 400→月租 45.00 元/只」重注入刷新；否定式「无日租金」5 处保留。纪要依据：09-08 L72「无日租金（本期不设）」+L70-81 月租金/按套数单价。

**验证门 7 条全过**：①g20_verify.py **49/49**（含 T5 追加断言组）②node --check 0 ③audit **115 页死链 0/JS 0**（F01 断网 ERR_CONNECTION_RESET ×3 豁免·B10 豁免串扩写）**g17baseline diff 新增 0**（新增键退租入库新建三项全 0·键消失角色旧模板）④菜单门 **14/14** ⑤listfull 三批 **103/88/43 失败 0** ⑥PW **13/13**（退租新建开/关·待办 select 选「租入入库」命中≥1+pin 15 类·角色页 9 行·mobile 待办 15 行）+截图 g20-*.png×4 ⑦双轮重注入零告警（日志 g20_t3_a03/a04.log）。

**默认决策命中**：D1（3 行字段写死值逐字采用）D2（F01 写死值）D3（新模板不注入 pc-auth·抽查范本确认）D5（mobile 待办 15 行预期同步·PW 实证）D6（无改动项记录）D7（无新术语）。

**偏差闭环**：B0-B2/B4-B8/B10 共 9 条+B3/B9 失败修复 2 项，全部闭环于 `agent-handoff/goal-failures-g20.md`（结论行：**失败 2 项已修复·偏差 9 条闭环·无未决项**）。

**备份**：backup-g20-20260911/ 保留不清理。脚本留档：g20_t1_f01/g20_t2a/g20_t2b/g20_t5/g20_verify/g20_pw.py。
