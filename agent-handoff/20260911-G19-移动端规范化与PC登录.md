# G19 任务书：移动端样式规范化（prototype-design 规范·H5 口径）+ PC 登录页与默认登录态

> **状态**：待执行 ｜ **日期**：2026-09-11 ｜ **commit**：（收尾回填）
> **执行环境**：Claude Code /goal 无人值守
> **上游**：G16/G17/G18 全部 ✅（索引查证）｜ 全站基线：113 HTML = PC 108 + mobile 5

> **⚠️ 移动端定位（道远 2026-09-11 拍板）：企微内嵌 H5，不是小程序。** prototype-design 的小程序规范中，**骨架件不适用**——状态栏（09:41）、胶囊按钮（capsule-btn）、手机外框（phone-frame）、小程序导航栏均**不引入**（顶部导航由企微 App 原生承载，页面内只保留 `m-header` 轻标题条现状形态）；**内容区组件规范适用**——卡片/列表行/表单/按钮/空状态/底部 Tab 的结构、间距、圆角、阴影规范照抄（配色按 D1 用项目色板）。

## 【第 0 步·上下文获取】

1. 读 `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\_AGENT基线.md` 全文（第四节铁律；mobile 现状=G14 块）
2. 读样式规范三件（**内容区组件照抄、骨架件跳过（H5 定位）、配色不照抄**，见 D1/D2）：
   - `C:\Users\Administrator\.claude\skills\prototype-design\templates\miniprogram-template.md`（取内容区/底部 Tab 形态；**跳过** phone-frame/状态栏/capsule-btn/小程序导航栏）
   - `C:\Users\Administrator\.claude\skills\prototype-design\components\miniprogram-components.md`（组件：卡片/按钮/列表/表单/空状态结构与间距规范——本任务主要施工依据）
   - `C:\Users\Administrator\.claude\skills\prototype-design\templates\backend-template.md`（PC 登录页布局参照）
3. 读项目现行移动端：`P3-R01-包装租赁管理后台原型\mobile\_m.css` + `_m.js` + 五页（登录/待办审批/审批详情/库存查询/我的）
4. 项目根：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\`

## 【任务总览】

T1 差距清单 → T2 移动端规范化重写（_m.css+五页结构对齐）→ T3 PC 登录页新建 → T4 默认登录态与退出机制（38 业务页注入）→ T5 验证门 → T6 收尾回写。

**核心口径**：首次打开原型=默认已登录（零守卫直开）；仅当用户点「退出登录」后才进登录页；登录成功回到默认态。**演示零障碍**。

## 【T0 前置校验与清场】

1. `agent-handoff\_索引.md` 中 G16/G17/G18 三行状态均=✅，任一不满足→写失败清单停止（顺序执行防写冲突）
2. 原型 `_data\` mtime ≥30 分钟（铁律①）
3. 存量>0 文件则 `git add -A && git commit -m "G19前置：存量存档"`，不 push
4. 备份到 `_scan_tmpdir\backup-g19-20260911\`（按原相对路径）：`mobile\` 全部 7 文件、38 业务页清单（T4 注入前 dry-run 产出后备份）、`_索引.md`、`_AGENT基线.md`、`P3-R01-A02-页面类型与入口对照表.md`

## 【T1 差距清单（执行记录留档，不改文件）】

逐项对照 miniprogram-components 与 _m.css 现状，输出差距表（组件/规范要求/现状/动作），覆盖：卡片（圆角/阴影/内边距/通栏分组）、按钮（主/次/禁用）、列表行（标题/摘要/元信息层级）、表单（标签/输入/校验态）、空状态、底部 Tab、页面标题条（m-header 现状保留，仅对齐字号/间距）。**骨架件单列一节标「不适用·跳过」**：状态栏/胶囊按钮/手机外框/小程序导航栏（H5 定位，企微原生承载）。落 `_scan_tmpdir\g19-gap-list.md`。此表是 T2 的施工图。

## 【T2 移动端规范化重写】

1. **重写 `_m.css`**：按 T1 差距表对齐规范的组件结构与间距体系；**配色沿用项目口径**：主色 `#1677ff`（与 PC 一致）+ 企微绿 `#07c160` + 背景灰/卡片白/文字三级（照抄规范变量命名 `--primary`/`--card-bg` 等，值用本项目色板；**不引入电商红渐变**——D1）。类名保持 `m-` 前缀防 PC 冲突（G14 铁律）；max-width 480 居中/375 视口基准保留
2. **五页结构对齐**：按 T1 差距表补齐**内容区组件**（卡片结构/空状态/表单态），**不新增任何骨架件**（无状态栏/胶囊/手机外框；m-header 轻标题条保留）；`mobile\登录.html` 同步规范化。逐页「读取-精确替换+assert」；页面业务逻辑（_m.js 的 go/mGuard/m-auth/m-done/toast）**零改动**
3. 不改：五页业务功能、demo-data 消费、底部 Tab 三项构成、文件名

## 【T3 PC 登录页新建】

1. 新建 `P3-R01-包装租赁管理后台原型\登录.html`（根级独立页，与 我的待办.html 同级）：
   - 布局参照 backend-template 居中卡片式：左侧品牌区（项目名+一句话定位）/右侧登录表单（账号/密码**预填演示值**：liu.dy / ······、记住登录勾选默认勾、登录主按钮、「企微扫码登录」次按钮占位）
   - 样式沿用 PC 端 admin-ui-spec 口径（顶栏/配色/按钮与 108 页一致：#1677ff、卡片圆角 8px、btn 高 30px 体系）
   - 交互：点登录→`localStorage.removeItem('pc-logout')`→跳 `首页\项目看板.html`；账号密码留空点登录→行内红字提示「请输入账号/密码」（不弹窗）
2. `P3-R01-A02-页面类型与入口对照表.md` 增订一行（独立页·G19）

## 【T4 默认登录态与退出机制】

**键设计**：`localStorage['pc-logout']`——无键或 ≠'1' = 已登录（首次打开/清缓存后都直达，演示零障碍）；='1' = 已退出。

1. 新建 `P3-R01-包装租赁管理后台原型\_data\pc-auth.js`（共享脚本，两件事）：
   - 守卫：`pc-logout=='1'` 且当前页非登录页→`location.href` 跳根级 `登录.html`（相对路径按页深计算——脚本内用 `location.pathname` 解析层级，不硬编码每页路径）
   - 顶栏注入：头像（.avatar）变为可点下拉（纯 CSS hover 或 JS click 均可，D4）含「退出登录」项；点击→`localStorage.setItem('pc-logout','1')`→跳登录页
2. **38 业务页注入**（批量脚本 `_scan_tmpdir\g19_t4_inject.py`：读取-在 `</body>` 前插一行 `<script src="../_data/pc-auth.js"></script>`（根级页为 `_data/`）+assert 注入数=38）：A 类数据驱动页与 B 类静态页全含；**不注入**：69 弹窗预览页、F01、mobile 五页、登录页自身（防死循环）
3. mobile 的 `m-auth` 键域独立保留，PC 不读不写（D5）

## 【T5 验证门（全部机器判定，输出贴执行记录）】

1. `_m.css` 断言：含 `--primary: #1677ff`（或等价写法）与 `m-` 前缀类 ≥20 个；grep `#ff6034|#ee0a24` = 0（电商红零混入）
2. 五页结构断言（Python/rg）：每页含内容区组件（卡片容器类）+ `m-header`；骨架件 grep `status-bar|capsule-btn|phone-frame`=0（五页合计）；`_m.js` 与改前逐字节一致（git diff 验证）
3. PC 登录页：`登录.html` 存在；A02 含其登记行
4. 注入断言：38 业务页 grep `pc-auth.js` 恰好 38 处；弹窗/F01/mobile/登录页 = 0
5. Playwright（`_scan_tmpdir\g19_t5_verify.py` 留档）：
   - **默认登录态**：清 localStorage 后直接开 我的待办.html → 不跳登录页（URL 不变）✓
   - **退出流**：待办页点头像下拉→退出登录→URL=登录.html；再开库存查询→被守卫跳登录页 ✓
   - **登录流**：登录页点登录→落 项目看板.html；再开待办页→不再跳登录 ✓
   - **mobile 五页**：加载 0 JS 错误；截图前后对比留档（g19-mobile-{页名}.png×5）
   - 登录页空账号点登录→行内提示出现 ✓；全程 JS 0
6. audit 回归：`_scan_tmpdir\audit_interaction.py` 全量（114 页口径=113+登录.html）→ 死链 0/JS 0/相对 g14baseline diff **新增 0**（pc-auth.js 注入若致探测变化逐条核对，文案/结构误报记注记）
7. 菜单门 `v32_menu_verify.py` 14 项全过（侧边栏零改动）
8. 失败清单 `_scan_tmpdir\g19-failures.md` 结论行

## 【T6 收尾三件回写（硬性）】

1. 本文档 ✅+commit 哈希+「## 执行记录」（T0-T5 逐组+T1 差距表指针+验证门输出摘要+偏差清单）
2. `_索引.md` G19 行；`_AGENT基线.md` 快照滚动（mobile 规范化+PC 登录页+pc-auth 机制+113→114 页口径+audit 新基线注记）
3. `git add -A && git commit -m "G19 移动端规范化+PC登录页与默认登录态：..."`（要点+证据+偏差）——**不 push**

## 【默认决策表（无需向任何人提问）】

| # | 决策点 | 预定值 |
|---|---|---|
| D1 | 规范配色与项目冲突（苏银豆红 vs 企微蓝绿） | **结构取规范、配色留项目**：#1677ff+#07c160；若道远后续要全套苏银豆风另起任务 |
| D2 | 状态栏/胶囊按钮/手机外框/小程序导航栏（骨架件） | **全部不引入**（企微 H5 原生承载，道远 2026-09-11 拍板）；页面只保留 m-header 轻标题条 |
| D3 | mobile 底部 Tab 三项构成 | 不动（G14 定版：待办/库存/我的） |
| D4 | 顶栏下拉实现 | JS click 切换（hover 在触屏不可靠）；下拉项=当前用户名展示+退出登录 |
| D5 | PC 与 mobile 登录态打通 | 不打通（pc-logout 与 m-auth 独立键域） |
| D6 | 「记住登录」勾选 | 视觉件（静态勾选），不接逻辑 |
| D7 | 企微扫码登录按钮 | 占位按钮（点击 toast「演示原型·暂未接入」） |
| D8 | 登录页放根级还是系统管理 | 根级 `登录.html`（外层入口，对照 我的待办.html 先例） |
| D9 | 38 页注入遇页无 .avatar 头像 | 跳过注入下拉、仍注入守卫 script；差异记偏差清单 |
| D10 | 「美感」判定 | 不可机器判定→以结构断言+截图留档替代，人审在收尾报告标注 |
| D11 | _m.css 重写方式 | 手工重写整文件（CSS 允许整写——非 HTML 原型页；改前已备份，改后五页 PW 全验） |
| D12 | pc-auth.js 守卫对 file:// 协议 | 直接支持（pathname 解析对 file:// 成立）；审计 harness 接管 go() 的页面照常工作 |

## 【失败处理策略】

- 任一组 assert 失败→跳过该组+记 `_scan_tmpdir\g19-failures.md`（时间/组号/断言/现场）+继续下一组
- T2 单页失败→该页从备份还原，其余页继续；T4 注入数≠38→列出差异页逐个核对（页面无 </body> 等结构异常单独记）
- 守卫脚本导致 audit 上下文销毁类异常→参考 G15 f01-fab 事故：守卫跳转仅对 pc-logout='1' 生效，audit 默认态无跳转；若仍冲突则 audit 前 `localStorage.removeItem('pc-logout')` 预置（写进验证脚本）
- 全部组跑完才结束；结论行必写

## 【纪律】

1. 单写者（T0.2 mtime 检查）；用完即关
2. HTML 批量改动=读取-精确替换+assert 计数，禁裸 open('w') 整页写（_m.css 除外，D11）
3. URL/onclick 语境不碰（G15 事故）；新增 onclick 自带 DOM 副作用（铁律⑦）
4. 多股改动入库 `git add -A`；全程不 push
5. demo-data.js 本任务零改动（登录态纯 localStorage，不动数据层）；若必须动→末条逗号坑+node --check
6. 证据先于断言：每道验证门输出贴执行记录后才算过
