# 20260923 会话交接 · G61~G63f（主会话直改批次）

> 用途：本批为「道远点单式」直接施工（无任务书），内容散在多轮对话里——本档=冷启动交接。新会话先读：`_AGENT基线.md`（现状+纪律）→ 本档（未闭环）→ `拍板登记.md` 尾部（逐条拍板原文）→ 第5次沟通 `[待办事项].md`（开发底座）。
> 施工纪律照旧：原型页面**手动 Edit**（禁脚本刷改；验证脚本可用）·改前备份到 `_scan_tmpdir/backup-gXX-日期/`·改后 node --check＋PW 实测＋截图·**拍板即落账**（拍板登记＋A02/A05/待办同步）。

## 一、本批做了什么（速查）

| 批次 | 内容 | 关键文件 |
|---|---|---|
| **G61** | ①库存查询行操作收敛「详情」（+转移出库/终止转移灰位），原「库位明细/客户在租」两弹窗并入 `仓储作业/库存流水.html` 升格**「物料详情」**（物料信息/库位分布/客户在租/进出流水/流转信息 五卡）；②资产轨迹（器具出租履历）记录升级新建版式＋未命中空态（修兜底第一条串数据）；③**收发存→定名「库存台账」**（收敛为期间对账口径·行内轨迹并入物料详情·菜单 120 页改名） | `_data/demo-data.js`（stockFlows 18 条新建版式·assetTracks/rentTracks 20 条）·`_data/detail-generic.js`（+itemSections 段）·`仓储作业/库存查询.html`·`库存流水.html`·`器具出租履历.html`·`收发存.html` |
| **G62** | ①采购入库详情/审核「基本信息」去入库库位（明细行级已有）；列表「入库库位」改**多值胶囊**（样式=盘点「关联单据」+N 胶囊）；②盘点记录表头/值错位修复（09-11 批次表头插中、数据格追加末尾）——`stocktakes` 5 条＋静态行归位 | `采购管理/采购入库录单-列表/详情`·`仓储作业/盘点列表.html`·demo-data |
| **G63** | ①**23 个列表页补「创建日期」列＋筛选**（列插操作列前·筛选=折叠 range＋cfg `{label:'创建日期', field:'createdAt', range:true}`）；demo-data **24 实体补 `fields.createdAt`＋cells 末格**（单号内嵌日期推导）；②客商管理「合作状态」筛选项修复（全部/正常/停用） | 23 列表页＋demo-data；跳过：采购订单/销售订单/租赁单（已有）、项目档案（立项日期）、应收账单（生成时间）、银行回单核销（到账日期） |
| **G63b** | 采购入库物料名称统一：录单「名称规格」→「物料名称」（详情/审核 itemCols 本就一致；列表为多物料单不展示；无独立编辑页）＋物料详情 formRows 同改 ×18 | `采购入库录单.html`·demo-data |
| **G63c** | 采购退货「退库库位」→「**退货库位**」（销售退货「退回库位」保持不变） | `采购退货新建.html` |
| **G63d** | 租赁单新建基础信息去「计费方式」radio（明细行级已有·G39 口径）；租赁单详情 formRows 同步移除（明细列保留）；编辑复用新建页 | `租赁单新建.html`·demo-data（leaseOrders 9 条 formRow）·A03/notes-data 锚点改挂「含税单价(元)」 |
| **G63e** | ①销售出库新建库存不足红条**仅「生成采购订单」**；租赁出库录单保持双入口（生成采购订单＋生成租入单）；②租赁出库列表第 2/3 行错位修复（G54 直发链两条记录键行带注释·补日期正则漏采→补 5 条含 RZRK/QTRK/RZD） | `销售出库新建.html`·demo-data（comboOutbounds/rentInbounds/rentInOrders/otherInbounds） |
| **G63f** | 退租菜单名**回改「退租入库」**（原「退租登记」0923 拍板已 ⊘ 作废）；120 页侧栏＋新建页页签/注记文案 | 120 页侧栏·`退租入库新建.html`·A02/A05/F01/P2-R01/待办 同步 |

## 二、未闭环清单（新会话接手点）

1. **退租发起入口【待定】**（第5次沟通待办 §5-1）：候选一＝租赁出库列表已出库行加「退租」→带单进两步页；候选二＝退租入库列表头补入口按钮；原型暂未布入口——**等道远拍板后补链**。
2. **销售订单新建/租赁单新建库存不足红条【待追认】**：目前均为双入口（生成采购订单＋生成租入单）；销售出库已按拍板收敛为仅采购订单（G63e），订单页是否同改待道远定。
3. **标注 dev 锚点隐患清单（未出）**：全站扫描发现若干历史 dev 标注挂在大容器（td/tbody/按钮）上，抽屉捕获层会吞掉容器内点击——需逐条手动改挂（建议先出清单再改）。
4. **P2-R01 未补 G61~G63f 批次条目**：G58 式「拍板→D 号台账行＋正文段」尚未落（现有 D-186 最后）；下批可补 D-187~ 并同步 A02 v6.15 A05 沿革。
5. **A02 页数口径**：G57 实测 137 页（新页收发存已计入）；若后续再增页需同步 A02/基线计数行（基线 L12 已更新为 137）。

## 三、关键路径与纪律（防踩坑）

- **demo-data 补字段必查"行内注释格式"记录**：键行形如 `'CX-…': {  /* 注释 */` 的记录会躲过常规正则（本批踩过两次：库位首条、直发链两条）——补齐后必须**逐行**核对（表头宽 colspan 感知=行格数）。
- **colspan 组表头页**（产品档案）表格计数要按 colspan 求和，不能数 th 个数。
- **dev 标注不要挂大容器**（table/td/tbody/button）：会吞容器内所有点击（本批已修一处）。
- 改菜单/列名先想**沿革注记**（⊘ 作废标注 + 旧名保留说明），文档同步清单＝A02·A05·P2-R01·F01（含 spec.json）·待办·拍板登记。

## 四、新会话开场白（粘贴即用）

> 读 `agent-handoff/_AGENT基线.md` ＋ `agent-handoff/20260923-会话交接-G61~G63f.md` ＋ `agent-handoff/拍板登记.md` 尾部 ＋ 第5次沟通待办文档；优先处理「未闭环清单」，逐条拍板后施工；纪律照旧（手动 Edit＋锚点断言＋PW 验证＋落账）。
