# G08 · 退租专项拆分 T1/T2 两条编号流程（F01 v3.3）

> **元信息**：G08 ｜ 日期 2026-09-10 ｜ 状态 ✅ 已完成 ｜ commit：待入库（改动未提交，道远拍板后统一 commit）
> **双层路由**：公共背景读 `agent-handoff/_AGENT基线.md`；本文件=本任务全量细则。收尾回写三件：本文件标 ✅+执行记录 ｜ `_索引.md` G08 行 ｜ 基线「当前任务/快照」行更新（goal-creator v7）。
> **需求原文（2026-09-10 道远，主机 Kimi Code 会话）**：「P3-R01-F01-业务流程导航图.html 中的退租需要拆分成 2 个编号流程」——G07 两事件模型已定方向（客户退租→退租入库 ｜ 归还供应商→租入归还），本任务把单条 T1 拆成与其对齐的两条编号流程。

## B. 任务组（已全部执行）

1. **B1 拆分方案拍板**：按事件方向拆（与 09-10 拍板原文对齐），切在「租入在库」节点——**T1 客户退租**（租赁单「退租」→退租入库统一入口→①缺损转应收/应付→②完好：自有→在库五态→循环再出租；租入→租入在库，止于回库）；**T2 租入归还**（双入口=T1 退租回库租入行＋租入未转租直还→租入归还[关联租入单·同步明细·分批]→租金应付）。不按资产归属拆（退租入库是不分归属的统一入口，违背 09-10 拍板）。
2. **B2 F01 改图（v3.2→v3.3，脚本 `_scan_tmpdir/g08_t1t2_split.py` 精确替换+逐条 assert）**：
   - T1 收窄：标题「退租专项」→「客户退租（退租入库·统一入口）」；租入行末端 租入归还/租金应付 两节点删除，改「⇢ T2 · 租入归还」交棒 chip（灰底入口，href 租入归还列表）；「↩ 租入（L3/L4）」注记保留 T1；②完好标签尾注改「租入行回库后交棒 T2」
   - 新增 T2 泳道（y 1348-1452）：标题+src-tag(t2)+方向注记（我方仓库→供应商，与 T1 相反）+双入口注记行（「未转租直还」由底部文字注记升格为正式入口口径）+三节点链 租入在库（灰底入口）→租入归还→租金应付（蓝底终点）；侧注「租金应付进 F2 应付账单（L3/L4 已实现·租入单按月生成）」
   - 底部三行注记改写：L4=T1+T2 两事件按明细行归属组合 / T2 双入口 / 入库仅更新库存与财务解耦（缺损 T1 入口一次完成，T2 只挂租金与归还侧赔付）
   - 财务通道区 y≥1400 全部坐标（y/y1/y2 共 45 处）整体 +144，viewBox 1618→1762（位移铁律：覆盖全部坐标形式）
   - 编号规则行：「T 退租专项」→「T 退租/归还（T1 客户退租 / T2 租入归还）」；L3/L4 跨泳道指引 2 处→「退租见 T1 · 租入归还见 T2」（L1/L2 两处保留指 T1）；头部 eyebrow/sub/title/footer v3.3+泳道 6→7+changelog 句
   - SRC_DATA：t1 head 改「T1 · 客户退租（退租入库）」；新增 t2 键（09-04 L3 独立成单 + 09-10 两事件拍板 + v3.3 拆分三条），SRC 7→8 键
3. **B3 F02 规格重提取 v3.3**：extract_f01_spec.py + gen_f02_md.py 重跑（节点 74→76/泳道 12→13/SRC 7→8 键）；**顺手根治 G07 踩过的坑**——gen_f02_md.py 硬编码 6 处（泳道分段 band/viewBox/泳道数/编号体系/各域职责/演进史）改为 v3.3 口径，重生成不再覆盖手补叙事，演进史 v2.7→v3.3 全链入生成器
4. **B4 台账对齐四件**：A02（F01 行 v3.3 描述+注记⑦增列）、A04 _meta（source/date/desc 三串，页键零变更）、P3-R04（节名「退租 T1」→「退租 T1/T2」+更新记录，12 条场景判定不变）、P1-R01（F01 行 v3.0→v3.3 叙事+变更记录⑯）

## C. 默认决策（执行中已按此处理）

- 拆分线=「租入在库」节点：T1 止于回库（自有→在库五态、租入→租入在库），T2 自租入在库起；「↩ 自有（L1/L2）」「↩ 租入（L3/L4）」两注记均留 T1（标记来源泳道），T2 不重复挂
- T2 入口=单节点「租入在库」（灰底入口，遵循入口节点原则），双来源以注记行承载，不铺两个入口节点
- 页面/菜单/页键零变更：38 页、菜单 v3.3、A04 页键、P3-R04 场景计数全部不动；不重打 zip（无页面增减，20260910 包仍现行；下轮有页面变更时再打）
- 支线 S1「⟵ T1 退租拆散（L2/L4 分支）」注记不动（按零件入库属 T1 退租入库）

## D. 验证门（证据）

- **几何**：rect-vs-rect 重叠 diff vs HEAD——20 对（既有跨泳道装饰性）→ 20 对，**新增 0**
- **渲染目检**：Playwright 截图 2 张（`_scan_tmpdir/g08-t1t2-a.png`/`-b.png`）——T1/T2 区块、交棒 chip、财务通道移位后对齐，无遮挡无压字
- **弹层实点**：8 个 src-tag 逐一点击，#srcPop 全部弹出且 head 正确（t1=客户退租 / t2=租入归还），JS 错 0
- **audit**：F01 单页 `audit_interaction.py F01`——问题 0/死链 0/JS 错 0（本任务零页面变更，单页门即可）
- **管线**：F02 重提取 节点 76/泳道 13/SRC 8 键；A04 JSON 复验合法
- 改前备份：`_scan_tmpdir/backup-g08-20260910/`（F01 html + F02 md）

## 执行记录（收尾回填）

- **执行（2026-09-10 · 主机 Win · Kimi Code 会话）**：失败 0 项；B1-B4 全过。两次 assert 拦截均在写盘前零污染：①位移计数预期 17 实为 45（漏数 F1 行 5 节点 4 连线，按实际口径修正）；②bump 函数丢 `="` 拼接符致锚点失配（修正后重跑）
- **续改（同日·道远目检拍板「T1 占位置太多、高度太高且太长、节点间距应一致」）**：T1 紧凑重排——①节点间距统一 20px（原入口箭头 190px、分支 20px 不一）；②四行等行距 20px（入口/①缺损/②自有/②租入，原行距 16/30/88 不等）；③①② 标签行内化（挪行左，不再独占整行）；④客户转租/客户虚拟仓两注记下移 T2 下口径注记区（3→5 行）；⑤T2 块 −128、财务通道区 −96，T1 高 432→304、viewBox 1762→1666。脚本 `_scan_tmpdir/g08b_t1_compact.py`（锚点整块替换+两段位移 assert 22/45）；gen_f02_md.py band/viewBox/演进史/主干 path 注记同步，F02 重提取；验证=重叠新增 0、行内间隙断言 [20]/[20,20]、截图 `_scan_tmpdir/g08b-t1-compact.png` 目检、audit F01 0/0/0。改前备份 `backup-g08-20260910/P3-R01-F01-业务流程导航图-precompact.html`
- **坑位注记（下 agent 注意）**：①gen_f02_md.py 硬编码已按 v3.3 口径修正（泳道 band/viewBox/编号体系/演进史），后续改图重跑管线即可，不必再手补叙事——但**改泳道结构时务必同步 MAIN_BANDS 分段**；②T2 块在 SVG 文档序末尾（分隔线元素之前插入），坐标位于 T1 与财务通道之间——SVG 按坐标渲染无影响，提取类脚本若按文档序假设泳道顺序需注意；③paseo browser 后台 webview 不 paint，截图验证走本地 http.server + Playwright
- **遗留登记（待下轮）**：①G07 遗留继续有效（F01 旧术语清理[零部件买卖/器具/L3 坏句「向供应商/供应商租入」/第2次标题]、即将到期待客户确认、工期表重估追加本次范围）；②A04 pin 步骤分母仍为 v2.8 口径（既定不变）；③F01 完整截图 PNG（v2.9 同名配套）未更新——如需对外发图按现 v3.3 重截
