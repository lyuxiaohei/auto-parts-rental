# 20260918-G53 · 单据三态三卡版式全站铺开（任务书）✅ 已完成（2026-09-18·主提交哈希见下行回填）

> 执行哈希：主提交 `<G53HASH>`（回填于索引/任务档哈希回填提交）；前置批 `370c81a`。T3 五门全过·独立验收 5 PASS / 0 FAIL·失败清单无失败项（登记 15 条＋待办 3 条）。

| 项 | 内容 |
|---|---|
| G 号 / 日期 | G53 / 2026-09-18 |
| 任务一句话 | 把 21 张单据的详情/审核页全部切到「三卡新建版式」：只改 `_data/demo-data.js` 实体数据（info→formRows＋补 itemCols/items＋命名向新建对齐），**页面与渲染器零改动** |
| 上游依据 | ①道远 09-18 三连拍板：以新建页为基准调详情/审核＋合理多出保留＋拆成几个卡片（样板=调拨已验收「可以」）②方案文档 `P3-R06-单据三态字段与板块统一梳理（讨论稿）.md`（23 单据定稿字段表=**字段真值源**）③已完成样板=transfers 实体 3 条记录（demo-data.js 内可直接参照） |
| 改动面 | **仅** `_data/demo-data.js`（20 个实体约 110 条记录）＋收尾文档（本任务书/索引/基线/P3-R06 状态）＋备份目录。**禁改任何 HTML**（新建/录单/录入/生成/确认页是道远确认过的基准；详情/审核页由渲染器自动切换）与 `_data/detail-generic.js`（已就位） |
| 前置批次提交 | 本任务开工前，先把当日已确认的前置改动单独提交一笔：调拨/其他入库/其他出库/转移出库四张新建页明细改造＋detail-generic.js formRows 三卡模式＋transfers 样板 3 条＋P3-R06 讨论稿（git add 指定路径，**禁 git add -A**——工作树有并行会话残留） |

## 【第 0 步·上下文获取】

1. 项目根=`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁`（下称 ROOT；本任务书其余相对路径均以 ROOT 拼接）。
2. 必读：`agent-handoff/_AGENT基线.md`（现状与铁律）；`P3-R06-单据三态字段与板块统一梳理（讨论稿）.md` **第四节**（每单据定稿字段表——formRows 组装直接照抄）；`P3-R01-包装租赁管理后台原型/_data/detail-generic.js` 的 `renderFormModeHTML`（读懂数据契约：formTitle/formRows[{label,text?,tag?,url?,full?}]/itemTitle/itemCols/items）。
3. 样板参照：`_data/demo-data.js` 内 `transfers` 实体 3 条记录（DB-20260901-003 / DB-20260826-002 / DB-20260812-001）——formRows 字段序、items 行形态、chain/timeline 保留方式照此。

## 【任务】

### T0 前置校验（任一不过→停止并写失败清单，不执行 T1+）

1. 并行写者检测：`git -C ROOT log --since=2.hours --oneline` 与 `.git` 目录 mtime；若 2 小时内有他人新提交或 `git status --porcelain` 出现非本任务已知文件的新变动→停止留档。
2. 渲染器就位：`grep -c renderFormModeHTML "P3-R01-包装租赁管理后台原型/_data/detail-generic.js"` ≥2。
3. 幂等检查：transfers 实体已含 `'formTitle': '调拨信息'`（已完成样板，跳过不改）。
4. 备份：`P3-R01-包装租赁管理后台原型/_data/demo-data.js` 复制到 `ROOT/backup-g53-20260918/demo-data.js`（按原相对路径结构）。
5. 路径自检：对本任务书出现的全部绝对路径跑 `python -c "import os;print(os.path.exists(r'...'))"` 输出贴对话。

### T1 逐实体改造（主体工作·20 个实体）

对下表每个实体：把每条记录的 `'info': [...]` 整块替换为 `'formTitle'/'formRows'/'itemTitle'/'itemCols'/'items'`；与明细重复的 `feeSecTitle/feeCols/fees` 一并移除（其内容转入 itemCols/items）；`row/cells/ops/title/titleNo/chain/timeline` **原样保留不动**。**每改完一个实体**：`node --check demo-data.js`＋快照 `copy demo-data.js → ROOT/_scan_tmpdir/g53_snapshots/<实体名>.js`，再进下一实体。

| # | 实体 | 记录数 | formTitle（=新建页信息卡标题） | itemTitle | itemCols（=新建页明细列去操作列） |
|---|---|---|---|---|---|
| 1 | purchaseOrders | 7 | 订单信息 | 采购明细 | 序号/物料编码/物料名称/规格/单位/数量/未税单价(元)/税率/含税单价(元)/含税金额(元) |
| 2 | purchaseInbounds | 8 | 基本信息 | 到货明细 | 序号/物料编码/物料名称/规格/单位/数量/未税单价(元)/税率/含税单价(元)/含税金额(元)/批次号/入库库位 |
| 3 | purchaseReturns | 3 | 退货信息 | 退货明细 | 序号/物料编码/物料名称/规格/单位/可退上限/退货数量/备注 |
| 4 | rentInOrders | 5 | 租入信息 | 租入明细 | 序号/物料编码/物料名称/规格/单位/数量/计费方式/日租金(元/天)/税率/含税单价(元)/含税金额(元) |
| 5 | rentInbounds | 3 | 入库信息 | 入库明细 | 序号/物料编码/物料名称/规格/单位/入库数量 |
| 6 | rentInReturns | 3 | 归还信息 | 归还明细 | 序号/物料编码/物料名称/规格/单位/租入数量/已归还/本次归还数量 |
| 7 | leaseOrders | 9 | 租赁信息 | 租赁明细 | 序号/物料编码/物料名称/规格/单位/数量/计费方式/日租金(元/天)/税率/含税单价(元)/含税金额(元) |
| 8 | comboOutbounds | 10 | 基本信息 | 出库明细 | 序号/组合件编码/组合件名称/计费方式/出库数量/日租金(元/天)/税率/含税单价(元)/含税金额(元)/出库库位 |
| 9 | returnInbounds | 10 | 退租入库信息 | 退回明细 | 序号/物料编码/物料名称/规格/单位/退回数量/缺损数量/去向 |
| 10 | transferOutbounds | 5 | 转移信息 | 转移明细 | 序号/物料编码/物料名称/规格/单位/转移数量/备注 |
| 11 | stocktakes | 5 | 基本信息 | 盘点明细 | 序号/物料编码/物料名称/规格/单位/库位/账面数量/实盘数量/差异/差异说明 |
| 12 | otherInbounds | 3 | 入库信息 | 入库明细 | 序号/物料编码/物料名称/规格/单位/入库数量/备注 |
| 13 | otherOutbounds | 5 | 出库信息 | 出库明细 | 序号/物料编码/物料名称/规格/单位/出库数量/备注 |
| 14 | salesOrders | 8 | 订单信息 | 订单明细 | 序号/物料编码/物料名称/规格/单位/数量/未税单价(元)/税率/含税单价(元)/含税金额(元) |
| 15 | salesOutbounds | 7 | 出库信息 | 出库明细 | 序号/物料编码/物料名称/规格/单位/出库数量/未税单价(元)/税率/含税单价(元)/含税金额(元)/备注 |
| 16 | salesReturns | 3 | 退货信息 | 退货明细 | 序号/物料编码/物料名称/规格/单位/可退上限/退货数量/备注 |
| 17 | receipts | 5 | 收款信息 | 收款明细 | 原实体 feeCols 原样转 itemCols（关联账单/费用项/本次收款(元)/核销去向） |
| 18 | payments | 5 | 付款信息 | 分期付款计划 | 原实体 feeCols 原样转 itemCols（笔次/比例(%)/分期金额(元)/计划日期/状态） |
| 19 | invoices | 6 | 开票信息 | 开票明细 | 原实体 feeCols 有则原样转，无则不给 items |
| 20 | refunds | 4 | 退款信息 | （原 feeCols 有则转）退款明细 | 同上 |

执行顺序：先 #10/#12/#13（今日新改四页对应实体，口径最新鲜）→ 再 1-9/11/14-16 业务单据 → 最后 17-20 财务件。receivableBills/payableBills 两实体**本轮不做**（专用渲染器 receivable-bill-detail.js / payable-bill-detail.js 不走 detail-generic，改造需另行拍板——写进失败清单待办节）。

**formRows 组装规则**（每条记录）：
- 字段序＝`单号、状态`（开头两项，状态用 `{label:'状态',tag:'原状态值'}`）→ 该单据新建页 form-label 序（P3-R06 第四节定稿表照抄；执行时用 `grep form-label 新建页.html` 现场核对一次）→ 系统字段收尾（制单人/审核人/经办人/业务员/登记人/创建时间/制单时间/入库时间，取原 info 已有者）。
- 原信息单值汇总字段（物料/器具/数量/采购内容/租赁内容/出库内容/退租内容等）**删除**，由明细卡承载（#5/#17-20 等无物料明细或明细即费用行的除外，按定稿表）。
- 流程/汇总类字段（计租天数/退回进度/租金标准/货款支付状态/已生成租金应付/核销状态/数量合计/订单金额/月租等）按 P3-R06 定稿表归位。
- 命名统一（详情向新建对齐，22 组全集见 P3-R06 第四节逐单据行）：关联采购入库（原「关联原单」）、关联销售出库（原「关联原单」）、关联采购订单号（原「关联采购订单」）、关联销售订单号（原「参考关联销售订单」）、关联应收账单（原「关联应收」）、关联应付账单（原「关联应付」）、收货地点（原「送达地点」）、出库类型（原「出库方式」）、退回日期（原「入库日期」）、质检要求（原「验收方式」）、费用分类（原「费用类型」）、开票金额（原「价税合计」）、购方名称（客户）（原「购方」）、退款日期（原「登记日期」）、盘点日期（原「创建日期」）、合同起止（原「租期」）、押金（去「（元）」后缀差异，统一无后缀「押金」）、复盘人（去空格「复 盘 人」）。
- 条件字段（报废原因/处置方式/关联赔偿单/关联盘点单/退租方式/验收情况/关联退款单/库存状态/终止日期/来源/关联租入库等）保留在该出现的位置。
- comboOutbounds 两形态记录（直转形态含 来源/关联租入入库/关联租入单）按各自字段组装，不强求一致。
- **不改 row.fields/cells 键名与值**（列表页依赖）；不改 chain/timeline 内容。

### T2 明细行数据构造（并入 T1 逐实体做，单列以醒目）

- items 每行=字符串数组（与 itemCols 等长）；序号列='1','2'…；空值='—'。
- 物料行优先取该记录原 fees/原 info 单值拆解；不足一行的，按该单据业务语境补 1-2 行演示数据，物料编码/名称/规格/单位一律查 `products` 实体（fields.name/spec＋详情 info 计量单位：块/只/件/张/套/个/台/托——**托盘=块，不是张**）。
- 价格/金额：千分位两位小数字符串；计费方式/税率照业务惯例（按月/按天/13%）。
- 数量列纯数字（不带单位）——单位在单位列。

### T3 验证门（全部满足才算完成；证据输出贴对话）

1. **语法门**：`node --check demo-data.js` 退出码 0；20 个快照文件 exists。
2. **渲染门**（写 Playwright 脚本 `_scan_tmpdir/g53_render_verify.py`，验证用脚本允许）：对 20 实体的详情页逐页打开（每实体默认记录）断言——a) 无 pageerror/console.error；b) `#detailBody > .fm-card` ≥2 张且第一张卡标题=T1 表 formTitle；c) 业务单据明细卡存在、首三列=序号/物料编码/物料名称（comboOutbounds 为序号/组合件编码/组合件名称；财务件按 T1 表）；d) 原 chain/timeline 的记录链与时间线仍渲染；e) `.dgrid`（旧四段式）零残留。20/20 PASS。
3. **审计门**：复用 `_scan_tmpdir/_run_filter_audit3.py` 的 `audit_page` 跑 20 详情页＋对应审核页 ≥20 页，problems/dead_links 全 0。
4. **改动面门**：`git status --porcelain` 中 **HTML 文件改动=0**（今日前置四页已在前置提交里收口）；唯一改动=`_data/demo-data.js`＋backup-g53-20260918/＋agent-handoff/＋P3-R06＋_scan_tmpdir/g53_*。
5. **独立验收**（完成判定不得自证）：执行者全部自检通过后，派一个**只读子 agent**（general-purpose，无写权限指令）按下列提示词复验，逐项 `[PASS/FAIL] 检查项——证据摘要`输出，末尾 `总判定：N PASS / M FAIL`，执行者把子 agent 结论**原样贴进对话**；有 FAIL→修复后重派（同一项最多 2 轮），仍不过记失败清单保留原状。
   - 子 agent 提示词四要素：**角色**＝只读独立验收员，除 `_scan_tmpdir/g53_accept/` 外不得写任何文件；**对象**＝ROOT 下 `_data/demo-data.js`＋20 个详情页 HTML（只读打开）；**检查清单**＝①node --check 退出码 ②自抽 8 个实体（含 transferOutbounds/otherOutbounds/purchaseOrders/leaseOrders/receipts）各打开默认详情页：三卡结构/无 JS 错误/明细首三列/链时间线 ③grep 抽验命名统一 6 组（'关联原单'在 20 实体 formRows 中 0 命中、'送达地点'/'价税合计'/'验收方式' 0 命中）④git status 无 HTML 改动 ⑤抽样 3 条记录的 items 行长=itemCols 长；**输出格式**＝逐项 PASS/FAIL+证据+总判定行。

### T4 收尾三件套＋回写

1. 失败清单 `agent-handoff/goal-failures-g53.md`（无失败项也要写结论行＋待办：receivableBills/payableBills 专用渲染器三卡化待拍板）。
2. 本任务书头部标 ✅＋执行记录节（实体清单完成表/验证输出摘要/命名统一落位数）。
3. `_索引.md` 挂 G53 行（日期/任务名/状态/commit 哈希/文件名）；`_AGENT基线.md` 滚动（demo-data 实体三卡化完成、21/23、渲染器 formRows 契约）。
4. git：前置提交（T0 前）＋G53 主提交各一笔（只 add 本任务文件），哈希回填任务书头部与索引行；**不 push**。

## 【默认决策表】（拿不准按此执行，不提问；收尾报告注明命中项）

| 决策点 | 预定值 |
|---|---|
| 某记录明细值无从推导 | 按 T2 构造演示数据（products 口径），不停 |
| 原记录缺 info（仅 row） | 依据 row.fields 组装最小 formRows（单号/状态/可用字段），items 按语境 1 行 |
| Windows 写文件 Errno 22 | 同路径重试 3 次＋临时文件 os.replace |
| Edit 锚失配（唯一性不满足） | 扩大上下文重试 2 次；仍失配→该记录记失败清单跳过（**禁止**对不确定结构猜测改写） |
| 中途 node --check 失败 | 用最近快照恢复该实体段（对照 `_scan_tmpdir/g53_snapshots/`），修完再续 |
| items 里出现逗号数字 | 保持千分位（渲染器数字检测兼容 `[\d,]+`） |
| 原信息字段无对应值 | text:'—'，不删字段（P3-R06 定稿表有者必留） |
| 名称规格合写 vs 分列 | 一律分列（物料名称=products.name，规格=products.spec） |
| comboOutbounds 直转形态 | 保留其独有字段（来源/关联租入入库/关联租入单/出库日期），不强套组合件模板 |
| 审核页/详情页 HTML | 零改动（渲染器自动切换；发现渲染缺陷记失败清单，不改 detail-generic.js） |
| 执行中断续跑 | 同一 goal 重跑：已完成实体（快照存在且 node --check 过）不重做，从断点实体续 |

## 【失败处理策略】

跳过→留档→继续；失败清单=`agent-handoff/goal-failures-g53.md`（实体/记录/失败原因/现场摘录）；任何一步无死路；**禁对不确定结构猜测改写**；轮次上限 150（约 110 条记录＋验证），到顶写明剩余清单收口。

## 【纪律】

1. **禁脚本批量刷页面/数据**：demo-data 一律 Edit 工具逐处手动改（道远两轮明示）；脚本仅限验证（T3）与只读提取。
2. 每处 Edit 前：定位锚点→确认唯一（grep -c）→改后即验（node --check 按实体粒度）。
3. 备份先行（T0-4）；快照按实体滚动（T1）。
4. 禁 git add -A；禁 push；禁改 HTML 与 detail-generic.js；禁动 mobile。
5. demo-data 实体键引号两种风格并存（`'info':` 与 `info:`）——锚点匹配时按该记录实际风格写。
6. 中文路径写入遇瞬时报错按默认决策表重试；不改路径写法。

---

## 【执行记录】（2026-09-18 收尾回填）

### T0 前置校验（全过）

1. 并行写者：最后提交 296c45e=22:34:29（G52 前置会话）·`.git` mtime 22:34·全库 23:03 后零写入（任务书定格 23:00:08）·原型目录最后写入 22:55:35（detail-generic 前置改造）→ 前置会话已收手，无活跃写者（判定依据详见失败清单登记 13）。工作树 153 项变动全部归入：前置批次文件＋基线/g51_failures 已登记残留＋skills 维护，无未知新变动。
2. 渲染器：`grep -c renderFormModeHTML detail-generic.js` = **2** ≥2 ✅。
3. 幂等：transfers 三条含 `'formTitle': '调拨信息'`（10672/10729/10780）✅ 跳过不改。
4. 备份：`backup-g53-20260918/P3-R01-包装租赁管理后台原型/_data/demo-data.js`＋平铺副本双落位 ✅。
5. 路径自检 12/12 True（任务书/基线/P3-R06/两 _data 件/四新建页/审计三脚本/索引）。
6. **前置批提交 `370c81a`**：detail-generic.js（+69 formRows 三卡）＋demo-data.js（transfers 样板 3 条＋当日交互批次数据清扫）＋P3-R06 讨论稿；四新建页明细改造已含于 355a31b 不重复提交。

### T1/T2 实体完成表（20 实体 115 条·全部 node --check 0＋快照）

执行顺序按任务书：ZY/QTRK/QTCK → PO/PI/PR/RZD/RZI/RZR/LZ/CK/TZRK/PD/SO/XSCK/XSTH → HK/PAY/INV/TKD。

| # | 实体 | 记录 | 形态 | # | 实体 | 记录 | 形态 |
|---|---|---|---|---|---|---|---|
| 1 | purchaseOrders | 7 | 引号展开式 | 11 | stocktakes | 5 | 引号展开式 |
| 2 | purchaseInbounds | 8 | 引号展开式 | 12 | otherInbounds | 3 | 引号展开式 |
| 3 | purchaseReturns | 3 | 紧凑式 | 13 | otherOutbounds | 5 | 引号展开式 |
| 4 | rentInOrders | **6**（任务书预估 5·实为 6 含草稿行） | 引号展开式 | 14 | salesOrders | 8 | 引号展开式 |
| 5 | rentInbounds | 3 | 引号展开式 | 15 | salesOutbounds | 7 | 引号展开式 |
| 6 | rentInReturns | 3 | 引号展开式 | 16 | salesReturns | 3 | 紧凑式 |
| 7 | leaseOrders | 9 | 引号展开式 | 17 | receipts | 5 | 紧凑式 |
| 8 | comboOutbounds | 10（含 1 直转形态） | 引号展开式 | 18 | payments | 5 | 紧凑式 |
| 9 | returnInbounds | 10 | 引号展开式 | 19 | invoices | 6 | 紧凑式 |
| 10 | transferOutbounds | 5 | 紧凑式 | 20 | refunds | 4 | 紧凑式 |

合计 **115/115 转换，残留 info/fees/feeCols/feeSecTitle = 0，items 行长 ≠ itemCols = 0**（node 运行时全量断言）。receivableBills/payableBills 未做（待办）。

### 命名统一落位（22 组全集按定稿表逐单据执行，抽验 10 组）

详情侧改名全部落地：关联采购入库（PR 3 条）/关联销售出库（XSTH 3）/关联采购订单号（PI 8）/关联销售订单号（PO 7·原「参考关联销售订单」）/关联应收账单（XSCK 6＋HK 5＋INV 6）/关联应付账单（PAY 5）/收货地点（CK 9·原「送达地点」）/出库类型（CK 9·原「出库方式」）/退回日期（TZRK 10·原「入库日期」）/质检要求（PI 8·原「验收方式」并入）/费用分类—/开票金额（INV 6·原「价税合计」）/购方名称（客户）（INV 6）/退款日期（TKD 4·原「登记日期」）/盘点日期（PD 5·原「创建日期」）/合同起止—（RZD 起止日期承载）/押金（RZD 6·无「（元）」后缀）/复盘人（PD 5·去空格）。旧标签在 20 实体 formRows/items 内 **0 命中**（子 agent 复核：关联原单/送达地点/价税合计/验收方式均 0）。

### T3 验证门（五门全过·证据见失败清单「复验产物」节）

1. **语法门**：node --check 退出码 0；`_scan_tmpdir/g53_snapshots/` 20 实体快照齐。
2. **渲染门**：`g53_render_verify.py` 20 详情页（每实体默认记录）→ **RENDER GATE: 20 PASS / 0 FAIL**（无 JS 错误·fm-card≥2·首卡标题=formTitle·明细首三列·流转卡 chain/timeline 在·.dgrid 残留 0）。
3. **审计门**：`g53_audit_targeted.py`（复用 `_run_filter_audit3.audit_page`）20 详情＋15 审核/确认页共 **35 页全 PASS**（problems/dead_links/js_errors 全 0）。
4. **改动面门**：M 类 HTML 9 件全部为开工前已在案并行残留（与 T0 快照比对一致）；G53 归属 HTML 改动 **0**；原型目录唯一改动=_data/demo-data.js。
5. **独立验收**：只读子 agent 五项 → **总判定：5 PASS / 0 FAIL**（①语法 ②渲染抽验 8 实体四断言全绿 ③命名 6 组新名全中·4 旧标签 0 命中 ④改动面 ⑤行长抽样 3 条＋全量 129 行明细 0 错位）；结论原文已贴执行对话。

### T4 收尾

失败清单 `goal-failures-g53.md`（无失败项·登记 15 条·待办 3 条：应收/应付专用渲染器三卡化待拍板、info2 表单模式渲染缺口、writeoffs 未入本轮）；本任务书头部标 ✅＋本执行记录；`_索引.md` 挂 G53 行；`_AGENT基线.md` 滚动（demo-data 20 实体三卡化·21/23 单据·formRows 契约现状）；git 前置批 370c81a＋G53 主提交两笔（只 add 任务文件）·哈希回填——**不 push**。
