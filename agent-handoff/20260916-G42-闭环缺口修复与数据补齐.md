# G42 任务书 · 闭环缺口修复与数据补齐

> **状态**：⏳ 待执行
> **日期**：2026-09-16 ｜ **来源**：09-16 闭环核查报告剩余项·道远 9 条拍板（拍板登记 2026-09-16 条）
> **前置**：G41 ✅（D-150）·09-16 会话转移出库加审 ✅（D-151·133 页口径·待办/矩阵/待办单据类型 20 类）
> **范围声明**：原型 + demo-data + 支撑文档；需求真值源 P2-R01；**不动** 枚举接线批次（G30 遗留·另行立项）、移动端（G50 域）、退租入库新建关联字段（**道远拍板不处理**：无法追踪到是哪笔租赁单·与 D-106 同向）

## 第 0 步 · 上下文获取与前置校验

1. **项目根**（全部相对路径以此拼接）：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁`
2. 必读（绝对路径）：
   - `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\_AGENT基线.md`
   - 本任务书（T1~T11 细节唯一执行真值源）
   - `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\拍板登记.md`（2026-09-16 条）
3. 前置校验（任一不过→写失败清单后停止）：
   - `git log --oneline | head -30` 无 G42 提交；本任务书状态行仍为「⏳ 待执行」
   - `git status --short` 快照存档 `_scan_tmpdir/g42_gitstatus_pre.txt`；若 `采购入库列表.html`／`demo-data.js` 存在他人未提交改动（G41 会话删打印批）→ **允许执行**，执行记录与提交信息注明「携带并行未提交改动」
   - `_data/demo-data.js` 当前态 `node --check` 通过

## 任务组

### T1 · 删 BOM 维护「提交审核」按钮（一.2 拍板：删）

- 文件：`P3-R01-包装租赁管理后台原型/基础数据/BOM维护.html` 约 506 行
- 改动：submit-bar 三按钮 `取 消｜暂 存｜提交审核` → 只删 `<button class="btn">提交审核</button>`，其余不动
- 断言：改后该页 `提交审核` 0 次；`暂 存`、`取 消` 仍在；div 配平计数不变

### T2 · 付款死引用改实有单号（一.3）

- 文件：`_data/demo-data.js` payments `PAY-20260818-001`（约 1080 行区）
- 现状：关联应付 `AP-20260810-002` 在 payableBills 13 键中不存在（4 处引用：row.cells、info 关联应付、related bills cells+links）
- 改动：全部替换为实有单号——**选择规则**：payableBills 中 supplier 含「环通」且租金应付、日期 ≤ 2026-08-18 者优先；无则取金额 58,000.00 者；再无取 `AP-20260815-003`
- 断言：node 断言——`AP-20260810-002` 全文件 0 次；新单号 ∈ `Object.keys(DEMO_DATA.payableBills)`

### T3 · 租赁单新建所属项目下拉修正（一.4）

- 文件：`P3-R01-包装租赁管理后台原型/租赁管理/租赁单新建.html` 约 360 行
- 现状缺陷：`PRJ-2601` 两个 option（「华骏重卡·长春基地」/「华骏重卡」）；2602=「东海商用」2603=「星途」2604=「长风汽制」与项目档案不符；缺 2605/2606
- 改动：下拉重写为在执行六项目，文案=`{编码} {项目名称全称}`（与 projects `fields.name` 逐字一致）：PRJ-2601 华骏重卡·长春基地 驾驶室围板箱租赁｜PRJ-2602 华骏重卡·青岛基地 保险杠料架租赁｜PRJ-2603 长丰锂电·电池包周转箱租赁｜PRJ-2604 南方汽造·座椅周转箱租赁（试点）｜PRJ-2605 华骏重卡·蔚山基地 围板箱租赁扩建｜PRJ-2606 长丰锂电·二期 电池包周转箱扩容
- 同步：`租赁管理/租赁单列表.html` 约 359 行所属项目筛选补 `PRJ-2606`（裸编码格式照旧）
- 断言：新建页 `PRJ-2601` option 恰 1；六编码各 1 次；`东海商用`/`星途`/`长风汽制` 在该 select 内 0 次

### T4 · 应收账单补「预收」类型（一.5）

- 文件：`P3-R01-包装租赁管理后台原型/财务协同/应收账单.html` 约 364 行
- 改动：账单类型筛选 `<option>预付款（保证金）</option>` 后插 `<option>预收</option>`
- 同步（先核再动）：dictItems `应收账单类型` 组（ARB-01~06）未含预收 → 追加 `ARB-07 预收`（格式照抄组内行）；`系统管理/数据字典.html` 静态卡「应收账单类型」cnt 6→7
- 断言：筛选含预收；node 运行时 ARB 组含预收；演示行 `AR-2026-09-PRJ2601-YS` 不动

### T5 · 用户权限头部按钮文案（一.6）

- 文件：`P3-R01-包装租赁管理后台原型/系统管理/用户权限.html` 约 374 行
- 改动：头部按钮文案 `角色管理`（实际跳 `权限配置.html`）→ `权限配置`，onclick 不动
- 断言：`onclick="go('../系统管理/权限配置.html')">权限配置</button>` 存在且头部按钮无「角色管理」字样残留

### T6 · 应收生成页残留 pin 清除（一.7 · 防误操作三道）

- 文件：`P3-R01-包装租赁管理后台原型/财务协同/应收生成.html` 约 649-650 行
- 现状：`proto-pin-1`/`proto-pin-2` 两枚整 div 内容为「到货明细/提交验收（FP3-01 采购入库单）」，与应收生成业务无关（A03 无此页键·属历史复制残留孤儿）
- 步骤：
  1. 先 `grep -o 'id="proto-pin-[0-9]*"'` 列全量，确认除这两枚外是否还有其他 pin
  2. 只删内容同时含 `FP3-01` 与 `采购入库单` 的整 div（python 锚点断言替换·禁手抹）；div 边界以 `<div class="proto-pin" id="proto-pin-N">` 起、对应闭合 `</div>` 止（逐层计数取边界·不越界）
  3. 删后若 `id="proto-pins"` 容器内无任何 pin：容器、右下角 fab 行（`f01-fab`/`pn-fab`）、proto-notes 专用样式块与脚本**一并整体移除**（防死「标注」按钮）；若容器内还有其他 pin 则容器保留
- 断言：该页 `FP3-01` 0 次；Playwright 打开无 JS 错；`应收生成` 业务关键词（账单类型/计费模式）不受影响；div 配平

### T7 · 客商详情补税号 ＋ 全站「详情 vs 新建」字段匹配排查（漏掉新增拍板）

- **T7a 税号进客商详情**：
  - 数据源：partners 8 家已含开票资料/税号字段则直取；无 → 先补 `invoiceTaxNo`（**编造规范写死**：18 位统一社会信用代码格式、`9` 开头、大写字母+数字、第 3~8 位 `131015`、第 9~17 位 `MA1F`+序号大写字母数字、末位数字；不作真校验位验证；脚本内可复算）
  - 页面：`基础数据/客商详情.html` 补「开票资料」信息段（发票类型/纳税人识别号/开户行/账号/结算周期），与 `客商开票资料.html` 同源
- **T7b 全量排查**：
  - 范围：PC 功能页全部「新建表单页 × 详情页」对（单据+档案实体；不含 mobile、不含弹窗模板）
  - 判定口径：新建表单出现的业务字段（排除按钮/附件假上传/审核结论区），详情页 info 应有对应行；缺 → 补静态 info 行（值取该记录 fields，无值 `—` 占位）；**只补详情不删新建字段**
  - 产出：脚本与差集清单留档 `_scan_tmpdir/g42_detail_newfield_diff.md`（排查前差集数 → 修复 → 复查差集数）
- 断言：客商详情页含 `纳税人识别号`；node 运行时 partners 8/8 有 invoiceTaxNo；复查报告末行 `修复后差集：0`（或全部登记失败清单）

### T8 · 客户转租退租演示行（二.1）

- 文件：`_data/demo-data.js` returnInbounds（现 9 行）
- 新增 1 行（默认值写死）：单号 `TZRK-20260915-012`｜客户 安吉智行物流｜物料 `XNC-ZZ-WBX` 围板箱 1200×1000×970｜100 只（**部分退回**·余 140 只继续在租）｜status 已入库；info 注明 `转租物经直接客户退回（XNC-ZZ-WBX·客户转租出→退租入库）`；chain：终端用户 → 安吉智行（直接客户）→ 退租入库（本单）
- 同步：stockEvents 追加 `EV-20260915-023` 退租事件行（side 客户在租·note 转租物部分退回）；**不改** stockFlows 快照行
- 断言：returnInbounds=10 行、stockEvents=23 行（node 计数）；新单号在键中；node --check 通过

### T9 · 租入押金退还载体（二.2）

- 文件：`_data/demo-data.js` rentInReturns 3 行
- 改动：3 行 info 各补一条押金退还表达——已归还 2 行=`押金退还：¥{金额} 原路退回`；待审核 1 行=`押金待退：¥{金额}（归还审核通过后原路退回）`；金额取关联租入单（rentInOrders）押金字段值，取不到按 12,000/9,000/15,000 体量写死
- 同步：`租入管理/租入归还审核.html` pn-hint 增半句 `押金随归还审核原路退还`；MSG-03 既有字典不动；「押金标准待客户」注记**保留**
- 断言：rentInReturns 3 行 info 均含「押金」；审核页含新 hint

### T10 · 损益/看板与财务单号互通（二.3）

- 文件：`_data/demo-data.js` projectDocs、boardRows、profitRows
- 改动：
  1. projectDocs 中 `AR-20260905-0029`/`AR-20260831-0028` 等非实有应收单号 → 按「同项目+同类型就近」映射为 receivableBills 实有键（先 dump 实有键清单再替换）
  2. boardRows/profitRows 每行补收入/成本侧单号引用（`lk` 样式跳对应列表页），取值须为实有键
- 断言（node 逐单号 Counter·不用 set 去重）：替换后三实体出现的 `AR-`/`AP-` 单号 100% ∈ receivableBills/payableBills 键集；旧假单号 0 残留

### T11 · 退款类型扩值＋演示行（二.4）

- 类型枚举 2→4（方向映射沿用 D-123 框架）：采购退货退款（应付退款·我方收款）｜销售退货退款（应收退款·我方付款）｜**预收退回**（应收退款·我方付款）｜**多付退回**（应付退款·我方收款）
- 改动面：
  1. `财务协同/退款新建.html` refundTypeSel 与 `退款登记.html` 筛选 → 4 值（form-tip 同步四向映射）
  2. dictItems `退货类型` 组 2→4（执行时核实组名与行格式）
  3. refunds +2 行：`TKD-20260916-004` 预收退回·待审核·关联 `AR-2026-09-PRJ2601-YS`·20,000.00（预收 50,000 部分退回）；`TKD-20260916-005` 多付退回·已确认·关联 `AP-20260905-012`·10,000.00（该应付 timeline 增退款单号引用·与「供应商退回 ¥10,000 原路冲减预付」呼应成真）
- 说明：待办 20 类**不扩**（退款登记已在矩阵）；D-123「退货退款专用」定位由本拍板扩展，P2-R01 正文随收尾同步
- 断言：两页各含 4 值；refunds=5 行（node）；TKD 新单号在键中

## 默认决策表

1. 未尽文案沿用页面既有语体（简体·「·」分隔·全角括号）；新术语先查 `P1-R04-术语表.md`
2. 编造数据：日期 ≤ 2026-09-16；单号沿用各实体前缀流水规则末位顺延；金额与既有行体量协调
3. T3 已完结项目（PRJ-2599/2598）不进新建下拉
4. T7b 只补详情不删新建字段；附件/假上传/审核区不算业务字段
5. T8 部分退回 100 只，不改 stockFlows 快照数量（快照口径）
6. T10 行内单号用既有 `lk` 样式跳列表页，不做参数化定位
7. T11 新增两退款行一待审核一已确认；待办/审核矩阵不扩
8. 文档回灌写法照 D-151 先例（台账行＋头部版本行＋A02 版本增订行）
9. 收尾数字（页数/实体数/行数）一律 node 运行时实算，禁止沿用记忆值
10. 提交信息：`G42 闭环缺口修复与数据补齐（道远 09-16 九拍板）：…——不 push`

## 失败处理策略

- assert 失败/结构不符：跳过该项 → 记 `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\goal-failures-g42.md`（不存在则创建·含项号/现象/证据/原状）→ 继续下一项
- 禁止卡住等待；禁止对不确定结构猜测性改写
- demo-data 每轮改动后必 `node --check`；失败即回滚本轮该实体块（改前快照留 `_scan_tmpdir/g42_dd_backup/`）

## 纪律

1. 精确锚点替换＋改后断言；HTML 锚必须带标签上下文（裸类名 find 会命中 CSS 定义吃段）
2. HTML 删块/切块后跑标签配平自检（div 开闭计数相等）；注入块边界＝成块 `<style id="proto-notes…">` 及配套 `<script>`，剥离不得越界到业务节点
3. demo-data 锚定以实体键行（`'ZY-…': {`）为准，键前行内注释不参与匹配；只动目标实体块，不碰 `'row'` 内层结构；**追加记录遵守末条无逗号坑（插新条先给前条补逗号）**
4. 断言防假绿：数量/单号断言逐项 Counter，禁 set 去重漏检；先算期望值再执行；「与基线对比」期望增量以实测为准不预设
5. 长文本/脚本注入走 Write 工具或临时文件＋锚点 assert，禁 bash 内嵌 python 写大段（反引号/三层引号坑）；Windows 中文路径写文件＝临时文件 + `os.replace` + 3 次重试
6. Playwright 校验：截图断言用 file:// 路径不带 query（as_uri 不认 query）；点击验证优先 evaluate 编程点击；导航竞争用 expect_navigation
7. 同一工作区同一时刻只允许本 agent 一个写者：执行中若共享文件 mtime 突变 → 停手重读再继续；**禁 `git add -A`**（只 add 本任务书 T 组涉及文件）
8. 全量排查类产出（T7b）脚本与报告落 `_scan_tmpdir/`，不进原型目录
9. 收尾文档写完全库 grep 旧 token 残留（`AP-20260810-002`／待转移／确认转移／旧头部按钮文案等）＝0
10. 轮次上限 150；`?audit=1`／「打印」等既有机制不顺手扩展

## 验证门（证据形态逐条写死）

1. **命令级**：`node --check _data/demo-data.js` 退出码 0、输出为空——输出贴对话
2. **断言级**：python 脚本一次性跑 T1~T11 全部断言（各任务组「断言」行）＋ T9 全库 grep 旧 token=0——逐行 `[PASS/FAIL]` 输出贴对话
3. **渲染级**：定向审计（复用/改造 `_scan_tmpdir/zy_audit_targeted.py`）覆盖 T1~T11 全部触碰页，problems/死链/JS 错=0——输出贴对话；截图 ≥6 张存 `_scan_tmpdir/g42_shots/`（客商详情税号段／租赁单新建下拉／应收账单筛选／退款登记 4 值／损益或看板单号／BOM 维护提交条）
4. **独立验收**：自检全过后派**只读子 agent** 复验，执行者不自证——提示词四要素：
   - 角色：独立验收员，只读禁改（只许往 `_scan_tmpdir/` 写取证输出）
   - 对象：项目根＋本任务书绝对路径＋验证门 1~3 清单
   - 检查清单：逐条重跑断言级命令（自查取证·不引用执行者自述）＋抽 3 页 Playwright 渲染＋对照任务书断言原文
   - 输出格式：逐项 `[PASS/FAIL] 项号 —— 证据摘要`；末行 `总判定：N PASS / M FAIL`
   - 执行者将子 agent 结论**原样贴进对话**；FAIL 修复重派（同一项最多 2 轮），仍不过记失败清单保留原状

## 收尾（回写交接文档·三件缺一即补）

1. 本任务书头部状态改「✅ 已完成（日期·commit 哈希）」＋文末追加执行记录节（T 组完成情况／默认决策命中／失败清单结论——可为空但必须有结论行）
2. `_索引.md` 挂 G42 行（日期/任务名/状态 ✅/commit 哈希/文件名）
3. `_AGENT基线.md` 快照区追加 G42 追记行
4. P2-R01 附录 A 立 D-152 起（按断点修复／详情-新建匹配／数据补齐三块立行或合并）＋头部版本行；A02 v6.6（页数增减如实写·程序实算）；A05 补 refunds/partners 新字段；拍板登记 2026-09-16 条销账
5. 提交：一 goal 至少一提交（不 push·禁 add -A·携带并行改动须注明）
