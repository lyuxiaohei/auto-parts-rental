# 20260915-G34-表单组件与字典驱动整改

> **状态**：✅ 已执行（2026-09-15·ZCode 主机 Win）｜执行提交 **792fb8a**（demo-data SL/JSQ 15 项先被并行会话 f8ccd3e 完成态卷入并注明归属·792fb8a 含其 CRLF 归一 delta）
> **来源**：道远 2026-09-15 会话指示（物料档案／客商管理／库位档案三处表单调整·10 项）
> **执行命令**：`_scan_tmpdir/g34_prompt.md`（形态 A·/goal 条件路由——本档＝全量凭据、默认决策表与执行记录处）
> **日期**：2026-09-15（立项）
> **决策落账**：本批为**新拍板**，T0 先落 P2-R01 附录 A 台账 **D-127**（拍板即落账·上手清单第 7 条）
> **执行前提**：单写者互斥／git clean；原型改动须备份先行

## 一、需求清单（道远 09-15 指示·10 项）

### 物料档案 · 新建物料弹窗
1. **字段顺序调整为**：编码、名称、类型、供应商内部编码、物料型号、规格、单位
2. **字段改名**：「内部编码」→「**供应商内部编码**」；「型号」→「**物料型号**」
3. **控件占位收窄**：单位、规格、参考未税采购价、参考未税销售价四个控件当前铺满整列（约 430px），改为**短占位**（固定宽 220px 左对齐）
4. **备注改文本域**（`textarea`）·限制输入 **200 字**·带字数计数
5. **修复弹窗整体样式错乱**
6. **供应商「默认税率」由文本框改为字典下拉**（选项来源于数据字典）

### 客商管理 & 物料档案
7. **结算周期统一为同一个数据字典字段驱动**——客商（开票资料弹窗）与物料（供应商税率区）两处共用同一字典组；并**补充几种常见结算周期值**

### 库位档案
8. **仓库名称**改为用户自定义（输入框，非固定下拉）
9. **库位类型**改为来源于数据字典
10. **备注**改为文本域组件

## 二、第 0 步·上下文获取（动手前必做）

1. `agent-handoff/_AGENT基线.md`——第四节（历史事故与真相）＋第七节（新 Agent 上手清单）**逐条适用**（按动作类型反查口径见第六节纪律段）
2. 本批涉及文件（**全部双层维护**：页内嵌 modal 与 `弹窗/` 独立模板同步）：
   - `基础数据/产品档案.html`（页内嵌 createModal）＋ `基础数据/弹窗/新建产品.html`
   - `基础数据/客商管理.html`（页内嵌开票资料弹窗）＋ `基础数据/弹窗/客商开票资料.html`
   - `基础数据/库位档案.html`（页内嵌新建库位弹窗）＋ `基础数据/弹窗/新建库位.html`
   - `_data/demo-data.js`（dictItems 新增两组＋`productTaxes` 税率值）
   - `系统管理/数据字典.html`（字典项渲染）
3. **已诊断的根因（必须按此修，不要瞎调 CSS）**：
   - `基础数据/产品档案.html` 的 createModal 段 **`<div` 39 ／ `</div>` 41 → 多 2 个闭合标签**
   - `基础数据/弹窗/新建产品.html` 的 modal 段 **`<div` 41 ／ `</div>` 42 → 多 1 个闭合标签**
   - 多余的 `</div>` 提前闭合了 `.modal-body`，导致后续 form-row 脱离 CSS grid 容器 → 视觉错乱。**修复方式＝定位并删除多余闭合标签使配平**，禁改样式值掩盖
4. 现状基线：新建物料弹窗为 900px 两列 grid（`.form-row.g2` 跨两列·`.tax-sec` 跨两列）；备注/规格/单位等为 `.input-box` 且 `flex:1`（铺满列宽）
5. 数据字典现状（`_data/demo-data.js`）：**有** `WL`（物料类型 6 值）、`KW`（库位类型 4 值：存储位/拣选位/暂存位/不合格品位）、`ZQ`（周期 3 值：月/年/日）；**无**税率组、**无**结算周期组
6. 结算周期现状（两处**硬编码/静态**）：物料侧＝`基础数据/产品档案.html` 与 `弹窗/新建产品.html` 的 `addTaxRow()` JS 内联数组；客商侧＝`弹窗/客商开票资料.html` 静态 option（月结/发票后 30/60/90/120 天）
7. 库位档案现状：筛选区「库位类型」为硬编码 select；`弹窗/新建库位.html` 的库位类型 select 含**脏占位**（「平面库位／全部／待定选项（演示数据）」）——本包一并修正
8. 工具口径：`_scan_tmpdir/audit_interaction.py`（全量审计）／`g28c_modal_scan.py`（弹窗样式扫描器·G29 建）／`_data/demo-data.js` 改后 `node --check`
9. **勿重做**：G29 弹窗禁滚动条规范（92vh／两列网格）已落地——本包在其基础上调，不回退

## 三、任务分组（T0-T8）

### T0 落账（先于改造）
- P2-R01 附录 A 台账新增 **D-127**（内容＝本批 10 项·注明「2026-09-15 道远指示」·状态 ✅）
- P2-R01 头部「最后更新」刷 2026-09-15（本批）
- 正文对应模块段同步（物料档案/客商管理/库位档案字段口径）

### T1 新建物料弹窗字段顺序与改名（双层）
- 字段序列固定为：**物料编码 → 物料名称 → 物料类型 → 供应商内部编码 → 物料型号 → 规格 → 单位 → 参考未税采购价 → 参考未税销售价 → 参考未税租入价 → 参考未税租赁价 → 备注 → 供应商税率区**
- 改名：`内部编码` → `供应商内部编码`；`型号` → `物料型号`
- **全站标签同步**：列表列头、详情弹窗 label、筛选 label、A03 标注文案——凡出现该两字段处一并改（**数据键名不动**，仅改显示文案）
- 双层同步：`基础数据/产品档案.html` 与 `基础数据/弹窗/新建产品.html`

### T2 修复弹窗样式错乱（标签配平）
- 定位两文件多余闭合标签并删除（页内嵌多 2 个／模板多 1 个·以配平脚本实测为准）
- 修复后**全文件标签配平断言通过**（`<div` 计数 == `</div>` 计数）
- `g28c_modal_scan.py` 扫描无溢出

### T3 控件占位收窄
- 单位、规格、参考未税采购价、参考未税销售价：`.input-box` 由铺满改为**固定宽 220px**（左对齐，右侧留白）
- 参考未税租入价／租赁价（三段式）保持跨列 flex 布局不动
- 复核：若 220px 下 label（120px）＋控件在列内溢出，降到 200px 复测；仍溢出记失败清单

### T4 备注改文本域
- 新建物料弹窗「备注」：`input` → `textarea`，**maxlength=200**，高度 72px，`resize:none`，跨两列
- 右下角字数计数（复用项目既有 `.char-count` / `.char-count.over` 样式），超限标红
- 双层同步

### T5 供应商默认税率改字典下拉
- 供应商税率行的「默认税率」：`input` → `select`，选项来自新增字典组 **SL**
- `addTaxRow()` 的税率参数渲染改为遍历字典取值
- `productTaxes` 演示数据的税率值须落在字典值域内（不一致则按字典值修正并在失败清单注明）
- 双层同步（页内嵌 + 弹窗模板）

### T6 结算周期统一字典驱动 + 扩值
- 新增字典组 **JSQ**（结算周期），物料侧 `addTaxRow()` 与客商侧 `客商开票资料` 弹窗**共用该组**渲染
- 客商侧静态 option 改为按字典渲染；物料侧内联数组删除改为读字典
- 双层同步（客商：页内嵌 + `弹窗/客商开票资料.html`；物料：页内嵌 + `弹窗/新建产品.html`）

### T7 库位档案三项
- **仓库名称**：`select`（正品仓/次品仓/客户虚拟仓…）→ **文本输入框**（用户自定义），placeholder 给示例
- **库位类型**：改为按字典组 **KW** 渲染（含筛选区与新建弹窗两处）；清除「待定选项（演示数据）」等脏占位
- **备注**：`input` → `textarea`（高度 72px·`resize:none`；**不限字数**，与物料备注口径区分）
- 双层同步（`基础数据/库位档案.html` + `基础数据/弹窗/新建库位.html`）

### T8 审计与验证
- 全量审计 `audit_interaction.py` → **128 页**（不新增页面）／死链 0／JS 0／相对 `g33baseline` 逐键 diff 新增 0
- 新基线快照 `_scan_tmpdir/audit_results_g34baseline.json`
- `node --check _data/demo-data.js` 退出 0
- Playwright 抽验三弹窗（物料/客商开票资料/库位）＋截图留档

## 四、默认决策表（执行中不再提问·逐条按此办）

### 值域（自拟内容规范）
- **SL 供应商税率**（6 值）：`0%`／`1%`／`3%`／`6%`／`9%`／`13%`
- **JSQ 结算周期**（9 值·含既有 5 值 + 新增 4 值）：`预付`／`货到付款`／`周结`／`半月结`／`月结`／`发票后 30 天`／`发票后 60 天`／`发票后 90 天`／`发票后 120 天`
- 字典编码：`SL-01…SL-06`、`JSQ-01…JSQ-09`；`category` 分别为「供应商税率」「结算周期」；`status` 统一「启用」
- 字典总量：126 项 26 组 → **141 项 28 组**
- `productTaxes` 税率值若为 `13%` 等已在字典内则不动；出现字典外的值 → 改为最近的字典值并记失败清单

### 样式与组件
- 短占位宽度 **220px**（兜底 200px）；textarea 高 **72px**、`resize:none`
- 备注字数上限：**物料备注 200 字**（带计数）；**库位备注不限**
- 复用既有 `.input-box` / `.char-count` / `.form-row` 体系，**不新造样式类**
- 弹窗宽度与 `max-height` 沿用 G29 规范（`max-height:92vh`·无滚动条）
- 新增 `onclick`／`onchange` 必须有 DOM 副作用，否则审计判死按钮

### 范围边界
- **只改标签文案，不动数据键名**（`fields` 键、实体键、单号前缀一律不改）
- 库区（`原料区 RA` 等）已随 D-102 废弃：新建库位弹窗若残留库区下拉，**一并删除**并在失败清单注明
- 详情弹窗/列表页只在「字段显示名」层面同步，不触碰其它交互

### 兜底自愈
- 标签配平修复后仍有残余错乱（视觉）→ 用 `g28c_modal_scan.py` 逐项判定，仍异常记失败清单并保留原状
- 字典渲染后下拉项数为 0（字典键取不到）→ 回退硬编码并记失败清单（禁静默空下拉）
- 复核发现某文件本就合规 → 记录「无改动」继续，不计失败

## 五、失败处理策略（无人值守铁律）
- 任何 assert 失败／锚点不匹配／预期外结构：跳过该项，记入 `agent-handoff/goal-failures-g34.md`（任务号＋文件＋原因），继续下一项
- 禁止卡住等待、禁止对不确定结构猜测性改写、禁止静默吞掉失败
- 前置校验任一阻塞性不符＝停止执行并写失败清单，禁止带病开工
- 收尾报告必须含失败清单结论（可为空；空则给「无失败项」结论行）

## 六、纪律（违反=返工）

- **基线第四节（历史事故与真相）＋第七节（上手清单）全部逐条适用**——按本包动作类型反查，不得用括号列举法替代：
  - 改 HTML 块 → 带标签锚（类名 find 会吃 CSS 段）＋改后**标签配平自检**（事故 2·本包 T2 即此类）
  - 改 demo-data → 逐实体精确替换＋**必跑 `node --check`**（末位追加补逗号·G13 语法事故）
  - Playwright 校验 → 编程点击＋导航等待＋`unquote()`＋`textContent`（铁律⑤·IAB 管道缺陷）
  - 备份/还原 → 按原相对路径（丢子目录产生游离文件，审计死链当场抓）
  - 改内嵌 JS 字符串 → 必跑该页 audit（引号丢失→整页功能挂）
- 全部「读取-精确替换」脚本＋`assert` 计数＋幂等；**禁止裸 `open('w')` 整页写入**（事故 1）
- 备份先行：`P3-R01-包装租赁管理后台原型/` 整体复制至 `_scan_tmpdir/backup-g34-20260915/`（按原相对路径）
- 多股改动一律 `git add -A`（`_data/` 为 untracked，`commit -a`/`add -u` 会漏——事故 7②）
- 环境：中文路径 python 内嵌（`io.open` encoding='utf-8'）／Windows 读写 `newline=''`／行尾判断用二进制读（事故 3）
- 单写者互斥：原型目录同一时刻只允许本 agent 写（前置校验① mtime ≥30 分钟）
- **弹窗双层同步**：页内嵌 modal 与 `弹窗/` 模板改一处必改另一处（铁律④衍生）

## 七、验证门（全部满足才算完成·证据形态逐条点名）
1. **配平断言**：三个涉及弹窗文件 `<div` 计数 == `</div>` 计数——证据＝配平脚本输出行（逐文件）
2. **字段序列**：新建物料弹窗 form-label 序列打印，顺序与第一节第 1 条一致——证据＝序列输出
3. **改名全站**：`内部编码`／`型号` 旧标签残留计数、新标签 `供应商内部编码`／`物料型号` 出现计数——证据＝计数输出行
4. **字典**：`node --check` 退出 0＋dictItems 组数 28／项数 141＋SL/JSQ 值域打印——证据＝脚本输出
5. **组件改造**：textarea 数（物料备注 1 + 库位备注 1）、税率 select 数、结算周期字典渲染命中——证据＝断言输出
6. **审计**：`audit_interaction.py` 全量 → 128 页／死链 0／JS 0／相对 g33baseline diff 新增 0——证据＝汇总行
7. **弹窗扫描**：`g28c_modal_scan.py` 无溢出——证据＝扫描输出行
8. **抽验**：Playwright 三弹窗（物料／客商开票资料／库位）交互 0 错误＋截图×3——证据＝脚本输出＋截图路径
9. **回写**：P2-R01 D-127 落账 diff｜任务档标完成＋执行记录｜`_索引.md` G34 行挂 commit 哈希｜`_AGENT基线.md` 快照滚动｜A05 字段字典同步改名——证据＝diff 行
10. **失败清单结论行**（空则「无失败项」）
11. **git 批次**：一 goal 至少一提交（提交信息前缀 `G34`·**禁 push**），哈希回填任务书与索引——证据＝`git log --oneline` 输出行

## 八、与其他任务的关系
- **G33**：本包在其终态（菜单 v5·128 页·dictItems 126 项 26 组）之上作业，字典计数以其为基数
- **D-93 枚举字典化**：本包是 D-93 的延续（税率/结算周期/库位类型三组接线）
- **G29 弹窗规范**：本包 T2 修复不改变 92vh／两列网格基线
- **D-99/D-100**：本包第 2、7 条是对其的修订（字段改名·结算周期扩值并字典化）——落账时在 D-127 注明修订关系

## 执行记录（2026-09-15 · ZCode 主机 Win）

**前置校验 5/5 通过**：①原型目录最近 mtime 88 分钟（≥30）②agent-handoff 最新 8 分钟＝G34 立项批次（3ffd161 10:12:37 提交产物·任务书/prompt/索引同批·豁免）③_scan_tmpdir 无 g34 执行产物（g34_prompt.md/g34_selfcheck.py=立项产物豁免）④git clean 无未提交改动 ⑤备份先行 `backup-g34-20260915/`（154 文件）。

| 任务 | 结果 | 证据要点 |
|---|---|---|
| T0 | ✅ | P2-R01 头部最后更新刷 09-15（G34·D-127）＋附录 A D-127 行（注明修订 D-99/D-100）＋正文 UC-BAS-03 与「结算周期」字段行口径同步（值域=字典 JSQ） |
| T1 | ✅ | 字段序列双层＝编码→名称→类型→供应商内部编码→物料型号→规格→单位→采购价→销售价→租入价→租赁价→备注（g34_verify 判定2 双层 PASS）；改名全站：旧「内部编码」/「型号」标签残留=0（唯一残留＝A05 G31 登记块历史行 D-99 原文·变更记录性质保留，G34 登记节已登记新显示名） |
| T2 | ✅ | 根因＝类型行后游离 `</div>` 提前闭合 .modal-body；**实测两文件各多 1**（任务书预估页内嵌多 2·以配平脚本实测为准·失败清单 F-02 注记）；删除后七文件全配平：产品档案 136/136·新建产品 41/41·客商开票资料 21/21（模板另缺 .modal-body 闭合已补）·客商管理 132/132·库位档案 116/116·新建库位 15/15·数据字典 143/143 |
| T3 | ✅ | 四控件（规格/单位/参考未税采购价/参考未税销售价）`width:220px;flex:none`×2 文件×4=8 处；PW 实测弹窗内短占位控件=4；列宽约 420px 未溢出（PW 截图目检） |
| T4 | ✅ | 物料备注 textarea（maxlength=200·72px·resize:none·复用 .input-box/.char-count）双层；g34TaCount 计数函数（满额标红 .over）＝G21 脚本块尾追加；PW 实测计数联动 6/200 |
| T5 | ✅ | addTaxRow 税率 input→select·值源 dictItems SL（字典缺失回退硬编码六值）；productTaxes 5 行税率值全在值域内（13%×5·无需修正）；PW 实测下拉=SL 6 值·预填选中 13% |
| T6 | ✅ | 新组 JSQ 9 值（既有 5+预付/货到付款/周结/半月结）；物料侧内联数组删除改读字典（双层）；客商侧 invoiceInfoModal 静态 option 改字典渲染（页内嵌+模板·模板补引 demo-data.js）；PW 实测两处下拉=JSQ 9 值 |
| T7 | ✅ | 仓库名称→文本输入框（placeholder「自定义输入，如 上海一号仓 / 正品仓」）；库位类型→字典 KW 渲染（新建弹窗+筛选区两处·清「平面库位/全部/待定选项」脏占位·模板静态兜底同步换 KW 4 值）；备注→textarea 不限字数；**模板残留库区下拉行删除**（原料区 RA 等·D-102 废弃口径·失败清单 F-01） |
| T8 | ✅ | audit_interaction 128 页全量：死链 0／JS 错 0／相对 g33baseline 逐键 diff 新增 0 → `audit_results_g34baseline.json` 落盘；g28c_modal_scan 171 弹窗·问题 0·页异常 0；node --check 退出 0；dictItems 141 项 28 组（node 实读）；PW 三弹窗抽验 3/3 PASS＋截图×3（g34_pw_prod/kst/loc.png） |

**执行口径注记**：①HTML 全 CRLF——三文件（新建产品 16 行/客商开票资料 330 行/demo-data 追加块）历史混合行尾随本次改造归一为纯 CRLF（改造脚本 EOL 自适应＋前置归一断言）；②「BOM维护.html 一个型号含多个配件」为 REQ-05 需求原文引用（标注层），非字段标签，不改；③数据字典 createModal select 既有「全部/待定选项（演示数据）/物料分类」历史脏项属挂起 #17（G30 接线差集）范围，本包只增不改。

**失败清单**：`agent-handoff/goal-failures-g34.md` —— 2 条均为任务书预要求的登记性记录（F-01 库区残留行删除注记／F-02 配平数预估与实测差异），全部处置完成，**无跳过项·无未决失败**。

**git**：执行提交＋收尾回填提交（哈希见 _索引.md G34 行）——不 push。

