# 20260915-G40-原型侧变更包（术语三条补做＋F01 导航图同步）

> **状态**：⏳ 已立项待执行
> **来源**：道远 2026-09-15 指示——把「涉及原型的变更」从 G41 拆出**单独执行**（G41 只做文档收口）
> **性质**：**仅改原型**（文本替换＋5 处文件改名＋F01 图与开发规则）；**需求文档与派生文档不在本任务范围**（归 G41）
> **执行命令**：`_scan_tmpdir/g40_prompt.md`（形态 A）
> **红线**：**不改业务口径**（只替换术语与命名）｜不动 demo-data 业务数据（仅改 ops/act/文案里的旧词）｜白名单三类不动（见默认决策表）

## 一、背景（为什么单独做）

台账 **D-142「页面内术语三条统一」标 ✅「并入 G36 执行」，实际从未落地**——原型里仍在：

| 术语 | 残留计数（09-15 实测） | 未改的文件名 |
|---|---|---|
| 水单 | **184**（`demo-data.js` 43＋`应收账单.html` 11＋…） | `银行水单核销.html`、`水单核销详情.html` |
| 盈亏 | **123**（侧边栏菜单项 ~120 处＋各页正文） | `盈亏报表.html` |
| 回款 | **154**（`demo-data.js` 21＋`开票登记.html` 6＋…） | `回款登记.html`、`回款详情.html` |

另：G38 承诺清零项中「**货品**」仍残留 **1 处**（库龄／在库时长／流转次数／零件号／库区均已 0）。

同时 **F01 业务流程导航图**（`P3-R01-F01-业务流程导航图.html`＋其开发规则）自 09-10 起未再同步，缺 G37/G38/G39 三项成果（「转移出库」0 命中、计费文案仍为旧口径）。

## 二、第 0 步·上下文获取（动手前必做）

1. `agent-handoff/_AGENT基线.md`——第四节（历史事故与真相）＋第七节（上手清单）**逐条适用**
2. `agent-handoff/20260915-G41-三任务成果回灌与文档全量收口.md`——**第一节差异实测表**（本任务对应 ①②⑥⑫ 四项）＋**T5 中 F01 五类改法**（已按道远确认定稿）
3. 原型现状：`P3-R01-包装租赁管理后台原型/P3-R01-F01-业务流程导航图.html`（主线泳道 880×2184 ＋ 支线 880×846·273 文本节点）＋`P3-R01-F01-业务流程导航图-开发规则.md`
4. 依据：`_scan_tmpdir/g31_t0_计费梳理.md`（计费口径）｜G37/G38/G39 任务书（转移出库形态、字段口径、按天计租）

## 三、任务分组

> **执行顺序（单会话内完成·不拆批）**：T0 落账 → T1 术语文本替换 → T2 文件改名＋引用同步 → T3 残留清理与复核 → T4 F01 五类改法（**T4-2 结构改动最后做**）→ T5 开发规则同步 → T6 验证 → T7 回写
>
> 本任务为**一个 goal 会话**内完成，**不拆批**。若中途无法完成某项：记失败清单并在收尾汇报，**不要停下等待**；可后续以**同一 goal 续跑**，已完成项不重做。

### T0 落账
- P2-R01 取**执行时当时最大 D 号＋1**（D-142 补做落地·注明"补做"）

### T1 术语三条文本替换（范围＝原型目录）
- **水单→银行回单**｜**盈亏→损益**｜**回款→收款**
- 范围：各页 HTML（含 `mobile/`）、`_data/*.js`、A 类文档（A02／A05／A06）
- **精确字符串＋`assert` 计数**；替换后全文 grep 旧词＝**0**

### T2 文件改名＋引用同步（`git mv`）
- `财务协同/回款登记.html`→`收款登记.html`｜`回款详情.html`→`收款详情.html`｜`盈亏报表.html`→`损益报表.html`｜`银行水单核销.html`→`银行回单核销.html`｜`水单核销详情.html`→`银行回单核销详情.html`
- **引用同步四处**：①全站侧边栏菜单项 ②`demo-data.js` 的 `ops[].act`／说明文案 ③A02／A05 路径行 ④A03／A04 标注层 selector
- 改名前后各跑一次引用 grep，**死链必须 0**

### T3 残留清理与复核
- 清「**货品**」1 处（定位后按 G38 口径改「物料」）
- **复核 G38 清零承诺**：库龄／在库时长／流转次数／零件号／入库库区 应均为 0（非 0 则记失败清单）

### T4 F01 导航图五类改法（按 G41 任务书 T5 定稿）
1. **计费文案 7 处** → 按持有量计租＋期段账单（起租日｜止租日｜天数｜日单价｜小计）
2. **新增「转移出库」节点**（**全图唯一结构改动**）：**只在 L1 与 L3 各放一个节点＋一条跨线注记「L1–L4 通用」**；副标「客户间转移·直接客户→终端客户」；决策注记四点＝不勾稽原单（D-106）／按租出结算＝默认·转移单不进财务链路／按终端结算＝特例·账单主体切终端（历史不回改）／终止转移＝回「在客户（租出）」
3. **状态驱动方 2 处**：「客户转租·拉表改状态」→ 改「转移出库单 · 行内跳转录单页 · 『客户转租出』由转移单审核驱动 · 还回＝终止转移（D-146）」
4. **虚拟仓注记 1 处** → 改「客户在租以库存状态『客户端（租出）』维度表达；**客户虚拟仓不维护**（D-145）」
5. **版本与日期刷新**
- **改动顺序**：第 1／3／4／5 类先做（文本）；**第 2 类（SVG 结构）最后做**
- **风险控制**：第 2 类改完**必须渲染截图核对**（禁只改代码不看图）＋跑全站审计（F01 为 90+ 处引用的枢纽图）；**F01 同时属 T1 替换范围**（含「盈亏」1 处、「回款」2 处）——T4 改图时须核对 T1 结果，避免互相覆盖

### T5 F01 开发规则同步
- `P3-R01-F01-业务流程导航图-开发规则.md`（现停 09-10）：补新增节点规格、计费口径、命名规则、版本与日期

### T6 验证
- 术语残留 0＋改名后审计死链 0＋F01 渲染截图与节点在位＋PW 抽验 F01 关键链接可达

### T7 回写
- 任务档执行记录｜`_索引.md` G40 行挂哈希｜`_AGENT基线.md` 快照滚动

## 四、默认决策表（执行中不再提问·逐条按此办）

### 替换与改名
- 精确字符串＋`assert` 计数＋幂等；**禁裸 `open('w')` 整页写入**
- 改名用 `git mv`（保 git 历史）；新名与旧名的映射逐条打印
- **白名单（不动）**：`P1-R04-术语表.md` 自身「原用词」列｜A05／A06 沿革注记｜`P2-R01-产品需求文档.md` 等需求文档与会议材料

### F01 改图
- 新节点文案固定：主标 `转移出库`、副标 `客户间转移 · 直接客户 → 终端客户`
- 只改 L1／L3 两处＋跨线注记；**L2／L4 不单独画**（注记覆盖）
- 新增 `onclick`／链接须自带 DOM 副作用（去审计判死规则）；F01 链接一律相对路径
- viewBox 高度若因新增节点需调整，**主图与支图分别核对**，不得裁切既有节点

### 范围边界
- **只改原型**：需求文档、派生文档（A02 正文口径、P1-R04 术语表销账、P3-R04、P2-R02 等）**均归 G41**——本任务只在 A02／A05 中同步"路径行"（因改名而产生），**不改其口径文字**
- 不动业务数据；`demo-data.js` 仅改旧词与路径，改后必跑 `node --check`

### 兜底自愈
- 某页结构不符／锚点不匹配 → 跳过、记失败清单、继续
- 复核发现已合规 → 记「无改动」继续，不计失败

## 五、失败处理策略（无人值守铁律）

- 任何 assert 失败／锚点不匹配／结构异常：跳过该项，记入 `agent-handoff/goal-failures-g40.md`（任务号＋文件＋原因），继续下一项
- 禁止卡住等待、禁止猜测改写、禁止静默吞掉失败
- 前置校验任一阻塞性不符＝停止并写失败清单
- 收尾必须给失败清单结论（可为空；空则「无失败项」）

## 六、纪律（违反=返工）

- **基线第四节＋第七节全部逐条适用**（按动作类型反查：改 HTML 块→带标签锚＋配平；改 demo-data→精确替换＋`node --check`；Playwright→编程点击＋`unquote()`；备份还原→按原相对路径）
- 备份先行：`P3-R01-包装租赁管理后台原型/` 整体复制至 `_scan_tmpdir/backup-g40-20260915/`（按原相对路径）
- 中文路径 python 内嵌（`io.open encoding='utf-8'`）／Windows `newline=''`／行尾与宿主一致／计数用 `str.count` 禁 `grep -c`
- 多股改动 `git add -A`；工作区有非本 agent 改动 → **只提交精确路径**
- 单人单写者：本任务与 G41 **串行**（G41 依赖本任务产物）

## 七、验证门（证据全部贴进对话）

> **验收方式**：按 goal-creator v9——执行者自检通过后派**独立子 agent**（只读·自查取证）逐项复验，结论原样贴进对话

1. **术语清零**：水单／盈亏／回款在原型目录残留 **0**（白名单除外）——证据＝逐词 `str.count` 输出
2. **改名到位**：5 个新文件名存在、5 个旧名不存在——证据＝文件清单
3. **引用无死链**：改名后全量审计死链 **0**（侧边栏／demo-data／A02·A05 路径／标注层 selector 四处）——证据＝审计汇总行＋grep 输出
4. **残留复核**：货品 0；库龄／在库时长／流转次数／零件号／入库库区 均 0——证据＝grep 计数
5. **F01 五类改动**：计费文案 7 处、**转移出库节点 L1／L3 各一＋跨线注记**、状态驱动方 2 处、虚拟仓 1 处、版本刷新——证据＝grep＋计数＋**渲染截图**
6. **F01 结构完好**：主图／支图 viewBox 无裁切、既有节点无位移丢失——证据＝截图对比
7. **开发规则同步**：含新增节点规格与计费口径、日期刷新——证据＝diff 行
8. **`node --check`** 退出 0——证据＝输出行
9. **回写**：任务档执行记录｜`_索引.md` G40 行挂哈希｜`_AGENT基线.md` 快照——证据＝diff 行
10. **失败清单结论行**（空则「无失败项」）
11. **独立验收**：子 agent 逐项 `[PASS/FAIL]` 与总判定行原样贴进对话
12. **git 提交**（前缀 `G40`·**禁 push**）——证据＝`git log --oneline` 输出行

## 八、与其他任务的关系

- **G41（文档收口）**：**G41 依赖本任务**——本任务产物（术语落地、改名、F01 改图）是 G41 的 T6（P1-R04 术语销账）与 F01 核对的依据。**两者串行：G40 → G41**
- **G37／G38／G39**：均已 ✅；本任务 T4 的 F01 改动以其成果为口径来源
- **D-142**：本任务为其**补做**；落账（T0）时在台账注明「补做·原标 ✅ 系状态虚报」
- **D-145／D-146／D-147／D-148**：本任务 F01 注记引用的决策依据

## 执行记录（开工时填）
