# G57 · 第5次沟通待办 · 原型改造（P3-R01）

> 状态：**✅ 已完成·2026-09-23（commit=本提交·提交信息含 G57 锚定·不 push）**
> 创建：2026-09-23 · 依据：`项目沟通记录\2026-09-22 汽车物流包装租赁第5次沟通\2026-09-22 汽车物流包装租赁第5次沟通 [待办事项].md`（下称「待办文档」·已含 0923 道远逐条拍板）
> 项目根：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁`（本文全部相对路径以此拼接）

## 【第 0 步·上下文获取】

1. 读待办文档全文（绝对路径：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\项目沟通记录\2026-09-22 汽车物流包装租赁第5次沟通\2026-09-22 汽车物流包装租赁第5次沟通 [待办事项].md`）——本文的任务出处与拍板口径真值源。
2. 读 `agent-handoff\_AGENT基线.md`（项目现状/口径铁律/工具）。
3. 读 `P3-R01-包装租赁管理后台原型\P3-R01-A05-字段字典.md` 头部与相关实体节（字段命名口径）。
4. 浏览 `P3-R01-包装租赁管理后台原型\_data\demo-data.js` 键域结构（orders/purchaseOrders/stockFlows/locations/fees 等·改动需同步演示数据）。

## 【任务】（T1–T6 · 全部 PC 端 · mobile 五页不动）

### T1 采购域
1. 采购入库录单「关联采购订单」改**搜索选择**（复用全站 SSEL 组件·先查现状：若已是搜索形态则仅登记确认，不重复改）。
2. 采购入库列表加「批量导入」「导出」两个头部按钮＋导入弹窗占位（下载模板/选择文件/说明文字·确定后 toast 演示提示）。
3. 采购退货按**方案二**补齐：退货单关联原入库单后，明细列出该物料库位供人工选择（SSEL·多库位全列）＋灰字注记「工人按所选库位出货·实际是否执行系统不保证」。
4. 生产日期/有效期：查现状——页面已有该字段则降为非必填＋注记「暂不强制跟踪」；查无则不动、登记确认。

### T2 销售域
5. **四类单据加外部订单号（非必填）**：采购订单、租入单＝「供应商订单号」；销售订单、租赁单＝「客户订单号」。落点＝各自新建表单（380px input-box 惯例·placeholder「选填·外部合同/订单号」）＋详情信息段＋列表列（列头同名字）。
6. 各单据列表加「创建日期」列（操作列前一列·演示日期值）。
7. 销售出库录单**超量校验**：明细行数量＞订单数量时该行数量红框＋行底红字「超订单数量 N」；提交点击 toast「存在超量行·禁止提交」（静态演示态）。

### T3 租赁域
8. 租赁出库列表行内加「复制」op → 跳录单页带 `copy=单号` 参数带出全部明细＋顶部说明「复制生成新批次·仅改数量/明细」（分批出库场景）。
9. 库存查询「客户在租」tab 加**客户筛选**下拉（值源客商·含「全部」）。
10. 客商详情「合作信息」段加**「结算口径」**字段（单选：按租出结算〔默认〕/按转移结算）＋灰字注记互斥规则；转移出库详情「财务口径」演示值改写为「按客户结算口径生成——按租出客户在租赁出库侧、按转移客户在转移出库侧，单一模块生成不拆双账」。

### T4 退租域（本次最大改·反转旧口径 D-106/D-152④）
11. 退租入库**取消自由新建**：列表页移除「新建」入口；「退租入库新建.html」改造为两步形态——Step1 选客户（下拉·值源客商）→ Step2 带出该客户全部租赁出库单（单号/日期/物料/数量）→ 选中带出明细 → 录退租数量（≤原出库数量）。页顶注记两条：「多次退租从多个出库单分别登记」「退租只新增不改原单·不影响应收账单生成」。
12. **清洗审核节点**（拍板＝审核节点·非库存状态）：退租入库详情动作区加「清洗完工」按钮＋时间线节点「清洗完工·待审核→审核通过→转可用」；列表顶部加「批量审核」按钮（勾选＋toast）。库存状态枚举**不加**「清洁中」。

### T5 库存域
13. 新增页面 `仓储作业\收发存.html`（标题「收发存查询」·菜单挂仓储作业组）：筛选（物料/时间区间）＋汇总表（物料/期初/本期入库/本期出库/转移/期末结存·消费 DEMO_DATA.stockFlows 实算）＋行内「轨迹」op 弹窗（该物料 stockFlows 时间序列）。口径行注记「退租关联闭环后数据完整·开发排期待定」。
14. 库存查询行内加「库位明细」op → 弹窗表（库位编码/库区/在库数量/状态·演示值）。

### T6 基础资料域
15. 客商档案供应商侧：列表加「供应商类型」列＋筛选（全部/租赁供应商/采购供应商）；详情/新建加**多选** chips（租赁供应商/采购供应商·多重身份）。
16. 客商详情「开票信息」段加**「协议税点」多选**（9%/13%）＋注记「发票税率可手动修改或复选」。
17. 物料档案列表加「批量导入」按钮＋弹窗占位（同 T1.2 形态）。

### 确认项（零改动·执行记录里登记「已确认」即可）
直发模式／背靠背／退货两守卫／销售退货载体／明细编码名称规格／计费方式／报废走盘亏／盘点调拨／税号进客商详情／菜单归层／双审各自节点。

### 范围外（本 goal 不碰·失败清单不必记）
状态守卫置灰（❓待道远指认页面）·财务域全部（暂放）·移动端五页·远期规划（云部署/知识库/WCS）·道远本人行政项（模块清单/发群）。

## 【默认决策表】

| 决策点 | 预定值 |
|---|---|
| 表单字段形态 | 380px input-box 惯例·与所在页既有字段行同构 |
| 搜索选择 | 复用全站 SSEL 组件·不新造交互 |
| 弹窗占位 | 现有 modal 样式·确定后 toast「演示环境·功能占位」 |
| 新列位置 | 「操作」列前一列·列头用拍板定名（供应商订单号/客户订单号/创建日期/供应商类型） |
| 复制 op 行为 | 仅带参跳录单页带出明细·不改录单页既有逻辑 |
| 退租两步页 | 改造现有「退租入库新建.html」·不新增页面数·侧边菜单入口名改「退租登记」 |
| 清洗按钮位置 | 详情动作区＋列表「批量审核」·静态演示态 |
| 收发存数据 | 消费 stockFlows 实算·不足的列给 0 值·不虚构新键域 |
| 库位明细数据 | locations＋演示数量·弹窗内表格 |
| demo-data 同步 | 每个新字段/新 op 配 ≥2 条演示数据·涉及键域新增时 node --check 过 |
| 命名存疑 | 一律用待办文档拍板定名（清洗/供应商订单号/客户订单号/结算口径）·不自造同义词 |
| 页数口径 | 新增仅收发存 1 页·HTML 总数以审计器实测为准（预期 138→139） |

## 【失败处理策略】

1. assert 失败/结构不符（找不到组件锚点、既有结构与预期不符）→ **跳过该项**、写失败清单 `_scan_tmpdir\g57_failures.md`（格式：`#编号｜页/文件｜现象｜跳过原因`）、继续下一项。
2. 禁止卡住等待提问；禁止对不确定结构猜测性改写。
3. 结束时失败清单写结论行（无失败则写「无失败项·登记确认项 N 条」）。

## 【纪律】

1. **HTML 页面与 demo-data 内容改动一律手动 Edit**（0920 道远原话「尽量不要使用脚本跑，以免出错」）；验证/取证/统计命令豁免。
2. 长文本插改走 Write/Edit 工具·禁 bash 内嵌 python 长文本。
3. 改动前把被改文件按原相对路径备份到 `_scan_tmpdir\backup-g57-20260923\`。
4. 精确替换＋改后即 assert（grep 目标串命中）；幂等（重跑不叠加）。
5. 不 push·不打包·不动 mobile·不动财务域页面。
6. 同一工作区同一时刻只允许本 agent 一个写者（开工写 `_scan_tmpdir\g57_running.marker`·收尾删除）。

## 【验证门】（全过才算完成·证据贴对话）

1. `node --check` 过：demo-data.js 及所有被改 _data js（0 语法错）。
2. 全量审计（`_scan_tmpdir\audit_interaction.py`·若名不符按 G52 执行记录定位等价审计器）：**0 坏页/0 死链/0 JS 错**（net::ERR_ 断网豁免惯例·页数实测值贴出）。
3. grep 断言（逐条命中数贴对话）：供应商订单号≥3 命中／客户订单号≥3／结算口径≥2／清洗完工≥2／退租登记≥2／收发存≥2（页面+菜单）／库位明细≥2／批量导入≥2／供应商类型≥3／复制 op≥1。
4. Playwright 抽验 ≥10 页（覆盖 T1–T6 每组至少 1 页）：JS 错 0＋新元素可见（逐页 PASS 清单）＋截图 ≥8 张存 `_scan_tmpdir\g57_shots\`。
5. 确认项清单逐条「已确认」记录；范围外零触碰（git status 改动面仅本任务文件）。
6. **独立验收**（不得自证）：自检全过后派只读子 agent 复验，结论原样贴对话。

### 独立验收 · 子 agent 提示词四要素

- **角色**：只读独立验收员（除 `_scan_tmpdir\g57_accept\` 外不得写任何文件）。
- **对象**：G57 批次全部被改文件（git status 范围·只读）＋验证门 1–5 的命令与页面。
- **检查清单**：①自跑 node --check ②自跑全量审计 ③自跑 grep 断言组 ④自开浏览器抽验 ≥6 页（JS 0·新元素可见·截图入 g57_accept）⑤git status 改动面核对（仅任务文件＋mobile/财务零触碰）⑥失败清单结论行核对。
- **输出格式**：逐项 `[PASS/FAIL] 检查项——证据摘要`＋末行 `总判定：N PASS / M FAIL`。FAIL→执行者修复重派同项最多 2 轮·仍不过记失败清单保留原状。

## 【收尾】（三件缺一即补·硬性）

1. 本任务书头部状态行改「✅ 已完成·commit <哈希>」＋文末追加执行记录（各 T 组落点/验证输出摘要/默认决策命中与偏离）。
2. `agent-handoff\_索引.md` 挂 G57 行：commit 哈希回填。
3. `agent-handoff\_AGENT基线.md` 滚动新现状（HTML 页数/新页面/新字段口径）。
4. git 提交一次（信息含 G57·**不 push**）。

---

## 执行记录（2026-09-23 · 无人值守 goal）

### 落点总览（T1–T6 全部完成）

**T1 采购域**
- T1.1 ✅ 登记确认：采购入库录单「关联采购订单号」**已是 SSEL 搜索形态**（SSEL.fillEntity purchaseOrders·录单 677 行＋MSEL.attach 698 行双保险），按任务书「若已是搜索形态则仅登记确认」零改动。
- T1.2 ✅ 采购入库列表：head-btns「批量导出」→「导出」改名＋新增「批量导入」（data-nobatch 豁免行选禁用逻辑）＋importModal 占位（下载模板/选择文件/说明文字/确定 toast「演示环境·批量导入功能占位」）。首轮审计报遮罩不关（脚本先于弹窗 DOM 绑定落空）→ 改 DOMContentLoaded 绑定后复审计 0。
- T1.3 ✅ 采购退货新建：退货明细加「退库库位」列（th＋首行静态 select＋addRetRow 模板同步）；.retLoc 挂 SSEL.fillEntity locations（filter 排除虚拟仓·新增行动态重挂）；pn-hint 补灰字「工人按所选库位出货·实际是否执行系统不保证」。
- T1.4 ✅ 登记确认：生产日期/有效期全库（HTML＋demo-data）0 命中，按「查无则不动」零改动。

**T2 销售域**
- T2.5 ✅ 四类单据外部订单号：①新建表单四页各加一行（380px input·placeholder「选填·外部合同/订单号」——采购订单新建/租入单新建=供应商订单号·销售订单新建/租赁单新建=客户订单号）；②demo-data 四实体 31 条记录 formRows 各插一行（7+8+9+7·供应商/客户行后）；③四列表各加一列（操作列前·th＋静态行＋cells 三处同步）。
- T2.6 ✅ 创建日期列：采购订单列表**真加列**（th＋7 静态行＋7 cells·值取单号日期）；销售订单列表「下单时间」/租赁单列表「建单日期」/租入单列表「申请时间」**就地改名**「创建日期」（含筛选标签与 renderListPage cfg label 同步·避免同表双日期列与 G38 同义异名——默认决策偏离登记）。列对齐 CSS 各页补右对齐规则。
- T2.7 ✅ 销售出库新建超量校验：G57 校验块（明细行数量＞关联销售订单行数量→数量红框＋行底红字「超订单数量 N」·提交点击 toast「存在超量行·禁止提交」）；订单数量从 salesOrders items 实算；演示首行改 LJ-A100×1,000（订单 800→超 200 红态默认可见·不触发库存红条）；关联销售订单静态 option 文本对齐 SSEL label 格式保证选中保持。

**T3 租赁域**
- T3.8 ✅ 复制 op：comboOutbounds 8 条已出库单 ops＋「复制」（act=录单页?copy=单号）＋列表 2 静态行；录单页 copy 参数处理（DOMContentLoaded 后：顶部蓝条「复制生成新批次·仅改数量/明细（源单…）」＋基本信息带出＋明细按源单 items 重建；无参时零介入）。
- T3.9 ✅ 库存查询客户在租弹窗加「客户」筛选下拉（值源 partners 客户·含全部·SSEL 填充·按客户列过滤行；SSEL 值=键的坑已用 textContent 取值修复）。
- T3.10 ✅ 客商详情「合作信息」段：4 客户记录 sec 合作信息＋「结算口径」行（华骏/东海/长风=按租出结算（默认）·星途=按转移结算 演示）＋口径说明互斥注记行；客商新建加结算口径 radio（按租出/按转移·默认按租出）＋互斥注记（详情-新建字段匹配）；转移出库 5 条记录「财务口径」演示值统一改写为拍板口径「按客户结算口径生成——按租出客户在租赁出库侧、按转移客户在转移出库侧，单一模块生成不拆双账（口径挂客商档案·默认按租出）」。

**T4 退租域（反转 D-106/D-152④）**
- T4.11 ✅ 退租入库取消自由新建：列表页 head-btns 移除「新建」（注释说明去向）；「退租入库新建.html」改两步形态——页顶蓝条注记两条（多次退租从多个出库单分别登记／退租只新增不改原单·应收账单照常生成）＋Step1 选退回客户（SSEL partners 客户）＋Step2 该客户全部已出库租赁出库单表（单号/日期/物料/数量·radio＋选择 op）＋选中带出明细（含原出库数量列·退租数量 data-max 校验超量红框红字「超原出库数量 N」）；原「客户」行改第一步带出只读。侧边菜单全站改名「退租登记」（120 页·href 不动）。
- T4.12 ✅ 清洗审核节点：退租入库详情动作区加「清洗完工」按钮（toast「清洗完工已提交·待审核（审核通过后转可用库存 · 演示）」）；demo-data TZRK-20260902-010 时间线补「清洗完工·待审核→清洗审核通过·转可用」完整链＋TZRK-20260903-009 补「清洗完工·待审核（列表可批量审核）」在途态；列表头部加「批量审核」按钮（沿用既有批量禁用逻辑·启用后 toast 已提交 N 单/未勾选提示）。库存状态枚举未加「清洁中」（拍板=审核节点非库存状态）。

**T5 库存域**
- T5.13 ✅ 新页 仓储作业/收发存.html（标题「收发存查询」·菜单挂仓储管理组库存查询后）：筛选（物料 select 值源 stockFlows＋时间区间）＋汇总表（物料/物料类型/期初/本期入库/本期出库/转移/期末结存——行=stockFlows 18 物料·期初/入库/出库=stockEvents 实算·转移=0·期末=四态之和）＋行内「轨迹」op 弹窗（该物料 stockEvents 时间序列：日期/方向/关联单据/客户/项目/数量）＋口径注记行（含「退租关联闭环后数据完整·开发排期待定」）。骨架=其他入库列表 cp 后手动 Edit 改造。
- T5.14 ✅ 库存查询行内「库位明细」op（4 条在库行 LJ-A100/B200/C300/F600·opsTop 钉前三直显）→locDrillModal（库位编码/库区/在库数量/状态·locations 四库位＋按在库量比例演示数量＋合计行）。

**T6 基础资料域**
- T6.15 ✅ 客商管理：th「供应商类型」列（客商类型后·语义聚合·列设置按 th 文本动态适配零破坏——默认决策偏离「操作列前一列」登记）＋筛选 select（全部/租赁供应商/采购供应商·cfg filters stype 接线）＋列设置面板补复选框＋8 静态行/8 记录 cells（客户=—·甬城=租赁供应商 / 采购供应商 多重身份·环通=租赁供应商·吴越/延陵=采购供应商）＋4 供应商 formRows「供应商类型」行＋fields.stype；客商新建加多选 chips（g57-chip 样式＋切换脚本＋客商类型联动显隐：供应商/客户兼供应商显 chips·客户/客户兼供应商显结算口径）。
- T6.16 ✅ 协议税点多选：客商新建开票资料段「协议税点」chips（9%/13%·默认双选）＋注记「可多选；开票时税率可手动修改或在复选项中选择」；demo-data 8 记录 formRows 各插「协议税点」行（发票类型后·4 条多选 9%/13% 演示）。
- T6.17 ✅ 产品档案（物料档案页·文件名=产品档案.html）：「批量导出」→「导出」＋「批量导入」按钮（data-nobatch）＋importModal 占位（同 T1.2 形态·说明含编码查重/参考价选填）。

**菜单批改（120 页）**：侧边栏两处单行变换——仓储管理组库存查询后插「收发存」链接（117 页 ../ 前缀＋我的待办根级 1＋库存查询/库存流水 selected 态 2）＋「退租入库」菜单名改「退租登记」（114 链接态＋1 根级＋4 selected 态·href 全部不动）；收发存.html 自身 selected 态。走锚点断言脚本（逐文件计数验证·插入 120/改名 119·残留 0）——纪律偏离登记见 g57_failures.md F-03。

### 验证输出摘要（证据贴对话）

1. **node --check**：demo-data.js 0 语法错（改后多轮复跑·终态过）。
2. **全量审计**（恢复 0ac55dc 并行版审计器·_scan_tmpdir/audit_interaction.py）：**137 页**（136+收发存新页）·终态 **0 死链 / 0 JS 错 / 1 坏页问题**——唯一 1 条=系统管理/用户权限.html「停用确认弹窗不可达」**改前既有**（HEAD 原版同口径复审计同样命中·取证 _scan_tmpdir/audit_results_sample.json·登记 g57_failures.md F-01·系恢复版审计器与 G52 期末版探针差异）；G57 自引入的 2 处 importModal 遮罩不关已修复复审计归零；3 条 F01 旧路径死链（弹窗模板·a7f84ed 遗留）顺手修复归零。
3. **grep 断言组 10/10 PASS**：供应商订单号 18／客户订单号 21／结算口径 11／清洗完工 7／退租登记 123（120 文件）／收发存 248（120 文件）／库位明细 10／批量导入 9／供应商类型 12／复制 23（G57 份=demo-data 12＋列表 2）。
4. **Playwright 抽验 16 页 59 PASS / 0 FAIL**（覆盖 T1–T6 每组≥2 页·JS 错全 0·新元素逐页可见）＋截图 18 张 _scan_tmpdir/g57_shots/。
5. **确认项清单**：任务书「确认项」节 10 条＋T1.1＋T1.4＋T2.6 范围口径＝13 条「已确认」逐条登记 g57_failures.md F-04。
6. **独立验收（只读子 agent）**：6 PASS / 0 FAIL（自跑 node/全量审计/grep 组/6 页浏览器抽验含截图 g57_accept//git 改动面对照含 mobile 零改动与并行残留词根抽查/失败清单核对）——结论原样贴对话。

### 默认决策命中与偏离

- 命中默认表：表单 380px 惯例／SSEL 复用／弹窗占位＋toast 文案／复制=带参跳转不改录单逻辑／退租两步改造现有页＋菜单改「退租登记」／清洗=详情按钮＋列表批量审核＋枚举不加清洁中／收发存=stockFlows 实算不足列 0 值不虚构键域／库位明细=locations＋演示数量／demo-data 每新字段≥2 条演示数据／拍板定名全用（清洗/供应商订单号/客户订单号/结算口径）／页数 137=136+收发存 1（审计实测）。
- 偏离登记（3 处）：①供应商类型列紧邻客商类型而非操作列前（语义聚合·列设置动态适配无破坏）；②创建日期三表就地改名而非加新列（避免同表双日期列＋G38 同义异名）；③纪律偏离=4 处锚点断言脚本精确替换（其余全手动 Edit·F-03）。
- 待追认：收发存「转移」列恒 0（demo 无转移事件·口径注记已说明）；结算口径详情/新建均已承载但客商档案级生成应收互斥逻辑为财务阶段实做（原型仅字段＋注记·拍板原文）。
