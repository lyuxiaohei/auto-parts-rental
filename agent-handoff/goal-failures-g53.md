# G53 失败清单（单据三态三卡版式全站铺开 · 2026-09-18）

**结论：无失败项。** 20 实体 115 条记录全部完成三卡化改造，T3 五门全过（语法/渲染 20/20/审计 35 页 0·0·0/改动面 HTML 0/独立验收 5 PASS 0 FAIL）。

## 待办（需另行拍板）

1. **receivableBills / payableBills 三卡化**：两实体走专用渲染器 `receivable-bill-detail.js` / `payable-bill-detail.js`，不经 detail-generic 的 formRows 分支；改造需给渲染器加三卡模式或维持专用版式另行设计——**待道远拍板后立项**（任务书 T1 已声明本轮不做）。
2. **info2（执行情况勾稽段）在表单模式不渲染**：purchaseOrders 两笔记录（PO-20260902-018 / PO-20260820-013）的 `info2Title/info2` 数据**原样保留在 demo-data.js**，但 `renderFormModeHTML` 不支持 info2（仅旧四段式渲染）——按默认决策表「渲染缺陷记失败清单，不改 detail-generic.js」处置。两笔记录的勾稽信息仍可从 timeline/chain 读到主干。
3. **writeoffs（银行回单核销）未在三卡范围**：任务书 20 实体清单未含 writeoffs（G36 后走 detail-generic），仍为旧 info/fees 形态；本轮未动（不在 T1 表，无渲染缺陷）。后续如需统一可并入应收/应付三卡化批次。

## 登记项（默认决策表命中与口径说明，非失败）

1. **汇总字段取舍**：transfers 已验收样板（道远「可以」）不含「数量合计」行——据此对 20 实体统一：单值数量/内容汇总（物料/器具/数量/采购内容/租赁内容/出库内容/退租内容/退租方式/验收情况 作条件字段处理者除外）删除由明细卡承载，不新增「数量合计」占位行；有实值的金额/流程汇总（订单金额/月租/租金标准/退回进度/计租天数/货款支付状态/已生成租金应付/到货数量/入库库位汇总等）保留并按 P3-R06 定稿表归位。
2. **F_extra 系统字段**：仅保留原 info 已有者（定稿表有而原记录无的系统字段不造 '—' 占位）；新建表头字段（含 '—' 值）全保留——两口径并行，与 transfers 样板一致。
3. **PI 仓管员值**：原 info 无仓管员，按 P3-R06「详情制单人承载」以该单制单人补入（张帆/林国栋）。
4. **PI 质检要求**：验收方式与质检要求同义（P3-R06 #2），非空值「凭采购订单到货验收」并入质检要求标签。
5. **fees 合计行**：items 不保留「合计」行（金额汇总在 formRows 订单金额/含税金额等字段或明细行直读）。
6. **数据值修正 2 处（T2 构造口径）**：CGRK-20260828-012 与 CGRK-20260827-009 第二行铰链含税金额原值 3.62 系小数错位，按 数量×含税单价 重算为 3,620.00 / 905.00。
7. **RZD-20260902-008 fees 行与单头矛盾**（原 fees 物料=WBX 30 只·单据为塑料托盘 50 块）：按单头口径重构造明细行（PLT-1210P 50 块·12.00 月租），原 fees 不实值弃用；RZD-20260701-001/002 两笔 fees 物料与器具名不符同法处理。
8. **支付件 items 形态**：T1 表 #18 括注「笔次/比例(%)/分期金额(元)/计划日期/状态」按新建页分期付款计划卡构造（单笔全额=1 期 100%）；原 fees（关联账单/账单类型/本次付款）信息已在 formRows（关联应付账单/付款金额）承载，无损失。
9. **盘点 #03 feeSecTitle 语义保留**：PD-202608-03 明细为上期示例（本单待生成），该限定语保留在 itemTitle（「盘点明细（上期 PD-202608-02 示例 · 本单待生成）」）。
10. **退租入库条件字段归位**：011/012 两笔的「退租方式/验收情况」值并入基座标签位（拆散方式=整套退回（不拆散）/缺损情况=验收完好·止租日…），标签向新建页口径对齐。
11. **名称规格分列**：一律分列（物料名称/规格各一列），规格查 products（record 无对应时 '—'：GB-800/GB-2602/金属料箱 800×600/塑料围板箱 800×600/ZH-\* 组合件）；托盘单位一律「块」（RZRK-023/退租入库 006 等原「只/张/件」处按 products 口径改块，row.cells 不动）。
12. **资产来源文本归一**：ZL-033 原文「混合（自购 + 租入  · RZD-20260815-005」（括号不闭合）/双空格，新行归一为「混合（自购 + 租入）· RZD-20260815-005」。
13. **T0-1 判定口径**：开工时最后提交 22:34（G52 前置会话）+ 任务书定格 23:00:08 + 原型目录最后写入 22:55:35（detail-generic 前置改造）+ 全库 23:03 后零写入（goal-creator 技能维护一件非原型）→ 判定前置会话已收手、无活跃并行写者；2 小时内提交均为本任务书上游戏 G52（已知前置），工作树变动全部归入已知清单（g51_failures 登记 22 项残留+前置批次文件）。
14. **XSCK-20260826-012 原 fees 与单头数量矛盾**（fees 铰链 1,300 vs 单头 铰链 900/内衬 400）：按 fees 原值位映射保留（1,300/400），矛盾为改前既有，登记不改。
15. **改动面门口径**：M 类 HTML 9 件（mobile×5＋根目录不合理清单＋skills 模板×3）全部为 T0 开工前已在案的并行会话残留，逐项与开工快照比对一致——**G53 归属 HTML 改动 = 0**；原型目录唯一改动 = _data/demo-data.js（预期内）。

## 复验产物

- 渲染门脚本：`_scan_tmpdir/g53_render_verify.py`（20 详情页断言·20 PASS）
- 审计门脚本：`_scan_tmpdir/g53_audit_targeted.py`（35 页·全 0）
- 快照：`_scan_tmpdir/g53_snapshots/`（20 实体逐个·node --check 后落盘）
- 独立验收：`_scan_tmpdir/g53_accept/verify.md`（只读子 agent·5 PASS / 0 FAIL）
- 备份：`backup-g53-20260918/`（demo-data.js 改前原样·双路径）
