# 20260915-G50-移动端样板风样式优化

> **状态**：✅ 已完成（2026-09-16 · commit 089f8fb）｜**commit**：执行收尾回填
> **项目绝对根目录（本机 Win）**：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\`
> 本文档内全部相对路径均以上述根拼接；路径分隔符 `\` 与 `/` 等价。
> **失败清单（绝对路径）**：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\goal-failures-g50.md`（沿 g38~g41 惯例入 agent-handoff/ 随 git 入库）

## 一、任务背景与终态

- **背景**：移动端 H5 五页（G14 建 / G19a 规范化 / 09-14 v1.1 优化）样式偏朴素。道远 09-15 拍板：以 Axure「移动端典型页面」样板风为基准优化 `mobile/` 下全部页面（我的=「某某APP」变体、审批=审批列表/审批详情页；库存查询等其余页无合适样板，按同风格延展，执行者不得再联网找样板）。
- **终态**：mobile 五页 + 共享件按第四节样式基准完成优化；验证门（第九节）全过；独立子 agent 验收通过；回写三件+commit 哈希回填。
- **范围边界**：只写 `P3-R01-包装租赁管理后台原型/mobile/`（五页 + `_m.css` + `_m.js`）与自有产物（备份/截图/失败清单/本目录文档）。**PC 页面、demo-data、菜单、A02~A06、F01 一律零改动**（沿用 G14/G19a 边界，不跑全站 audit 以外的 PC 校验）。

## 二、前置校验（先于一切，防重复/并发）

1. `agent-handoff/_AGENT基线.md` 不含「G50」完成追记（grep 命中 0 或仅索引待执行行）——否则视为已执行，写失败清单后收尾，禁止重做。
2. `backup-g50-20260915/` 与 `agent-handoff/goal-failures-g50.md` 均不存在——存在即疑似已执行，同上处置（`backup-g37-20260915/` 是真 G37 在档备份，与判定无关不得引用）。
3. 单写者互斥：原型目录内最近文件 mtime ≥30 分钟无变化（尤其 `_data/`）；不满足则写失败清单注明并发冲突并收尾。
4. G42（闭环缺口修复与数据补齐·20260916 任务书）为待执行队列任务且动原型与 demo-data，若发现其正在写（mtime 活跃）→ 同第 3 条处置（G35/G36 已于 09-15 完成，无需检测）。

## 三、第 0 步·上下文获取（动手前必读）

1. `agent-handoff/_AGENT基线.md` 全文——铁律/事故教训/mobile 消费口径（todoItems **20 行** docNo 即键〔D-151 后·含转移出库待审核行〕、stockFlows 有效行、roles 取角色名、m-auth/m-done localStorage 键域、go() 为审计接管点）。
2. 本目录 G14/G19a 相关追记（mobile 边界与企微 H5 定位）。
3. 样式锚点图（第四节清单，已于创建时截好，禁止重新联网抓取）。
4. 工具：`_scan_tmpdir/audit_interaction.py`（全量审计）+ 最新 `baseline-*.json`（diff 基准以执行时最新者为准）。

## 四、样式基准（Axure 样板风提炼·锚点图）

锚点目录 `_scan_tmpdir/g50_ref/`（绝对路径=根目录拼接）：

- `axure-审批列表.png`：顶部页签+下划线指示器；单据类型段标题（左竖色条+右箭头）；卡片内 label/value 行
- `axure-审批详情.png`（同图含详情两态）：主色渐变状态横幅（图标+状态大字）；描述列表 label 左灰/value 右对齐；审批进度时间线（当前节点实心主色、已完成空心、状态文字着色、右侧时间）；底部操作栏
- `axure-个人中心.png`：「某某APP」变体——hero 区（深色底+头像+名字+统计行三格）+ cell 分组卡（图标+文字+右箭头、组内分隔线）
- `axure-数据列表.png`：表头蓝色与信息密度参考（库存查询仅参考配色密度，不采用表格形态）
- `axure-待办事项.png` / `axure-登录页.png` / `axure-Form 表单.png`：辅助参考
- `now-*.png` 5 张：改造前现状（其中 4 张被 m-auth 守卫弹到登录页属预期）

**风格铁则**：企微 H5 定位——截图中的状态栏/胶囊/手机外框/小程序导航栏**一律不引入**（G14 已拍板骨架件不引入）；只取内容区布局层级、卡片形态、配色思路。主色沿用项目 `#1677ff`，禁止新造蓝色。

## 五、任务组

- **T0 备份与锚点核对**：复制 `mobile/` 整目录至 `backup-g50-20260915/mobile/`（保持原相对路径）；确认第四节锚点图 12 张齐全（缺图记失败清单继续，不得联网补抓）。
- **T1 `_m.css` 组件增量**：新增三类组件样式（类名 m- 前缀，不改动既有类语义）——①`.m-banner` 状态横幅（`linear-gradient(135deg,#1677ff 0%,#4d94ff 100%)` 底+白字+状态图标+大字+副标）②`.m-timeline` 审批进度时间线（节点圆点 12px、当前实心 var(--primary)、已完成空心描边、竖连接线 2px var(--border)、状态文字按状态着色、时间右对齐 var(--text-3) 12px）③`.m-hero` 我的页头部（同横幅渐变底+头像 56px+姓名+角色 chip+统计行三格均分、数字 20px/700 白、标签 11px 白 80%）。
- **T2 `待办审批.html`**：保留 v1.1 统计卡三格下钻交互零变更；对齐样板——单据卡片层级（类型徽标+单号+标题+元信息行距/留白对齐 axure-审批列表卡片）、状态徽标配色按状态语义（待审核橙/其他蓝灰）；不新增「审批完成」页签（已处理走 m-done 回显）。
- **T3 `审批详情.html`**：①页首状态改 `.m-banner` 横幅（状态文案=待审核/已通过/已退回，取 m-done 回显或默认待审核）②信息行保持 m-dl 描述列表，校验 label 左灰 value 右对齐层级③新增「审批进度」`.m-timeline` 区块于信息卡之后（静态两节点：提交申请[提交人/提交时间取该 todoItems 行字段]/当前状态节点；行字段不足时只渲染「提交申请/待审核」两节点文案，不新增 demo-data 实体或字段）④底部操作栏保留胶囊主按钮形态（G19a 规范优先，不文字按钮化）。
- **T4 `我的.html`**：①me-card 升级 `.m-hero`（渐变底+头像+姓名+角色 chip 保留）②hero 内统计行三格：待审核=todoItems 行数 / 已处理=m-done 计数 / 单据类型=todoItems 类型去重数（全部现有数据源，JS 实算，不写死数字）③cell 列表改分组卡形态（组内分隔线替代每行独立卡，图标+文字+右箭头视觉对齐 axure-个人中心；**cell 项与功能零增删**，退出登录逻辑不动）④版本号 v1.1→**v1.2·执行当日日期**。
- **T5 `库存查询.html`**：维持卡片列表形态（窄屏表格不可读，axure-数据列表仅参考表头蓝色与密度）；抛光搜索框/chips 横滑（v1.1 单行横滑不回归）/卡片数量高亮与状态徽标配色；零交互变更、零数据变更。
- **T6 `登录.html`**：hero 区视觉抛光（logo 圆角阴影/品牌名与副标层级对齐样板登录页重心）；m-auth 逻辑、企微一键登录、提示卡零改动；页尾版本号 v1.0→**v1.2·执行当日日期**。
- **T7 `_m.js`**：默认零改动；仅当 T2~T6 确需微调时做最小改动，改后必跑 `node --check`。
- **T8 验证与收尾**：按第九节验证门逐项取证贴对话 → 第十节独立验收 → 第十一节回写。

## 六、默认决策表

- 骨架件（状态栏/胶囊/手机外框/小程序导航栏）→ 一律不引入（G14 拍板）
- 主色 → `#1677ff` 不变；横幅/hero 渐变固定 `linear-gradient(135deg,#1677ff 0%,#4d94ff 100%)`；除既有色板变量外禁止新增蓝色 hex
- 状态色映射 → 待审核=var(--warn) 橙 / 已通过=var(--success) 绿 / 已退回=var(--danger) 红 / 其他=蓝灰既有徽标
- 审批进度时间线数据 → 静态两节点（提交申请[取行字段]+当前状态），行字段不足退化为「提交申请/待审核」；不新增实体/字段/演示数据
- 我的页统计三格口径 → 待审核=todoItems 行数、已处理=m-done(localStorage) 计数、单据类型=todoItems 类型去重数；JS 实算
- 操作栏形态 → 保留胶囊按钮（G19a 规范优先于样板文字按钮）
- 库存查询 → 卡片形态不变，仅视觉抛光
- cell 项 → 零增删，仅分组与图标视觉
- 版本号 → 我的/登录两页统一 v1.2·执行当日日期（立项日为 09-15，跨日执行不得沿用立项日）；页面其他计数类文案（115 页等）不随本任务改动
- 复核发现已合规 → 记录「无改动」继续，不算失败
- 拿不准的样式细节 → 以最接近样板的简实现执行，收尾报告「默认决策」节注明

## 七、失败处理策略（无人值守铁律）

任何 assert 失败/锚点不匹配/预期外结构：跳过该项，记入 `agent-handoff/goal-failures-g50.md`（页面+任务+原因），继续下一项。禁止卡住等待、禁止对不确定结构猜测性改写、禁止静默吞掉。收尾报告必须包含失败清单结论（空清单给「无失败项」结论）。

## 八、纪律（违反=返工）

- 基线第四节⚠️历史事故与真相全部逐条适用；按本任务动作类型反查全集：
  - HTML 块改动 → 读取-精确替换+assert 计数，禁裸 `open('w')` 整页写；类名 find 会吃 CSS 段须带标签锚；改后标签配平自检
  - JS 字符串/文件改动 → 必跑 `node --check _m.js`（无 node 时用 osascript JXA 等价）+ 该页 audit（引号丢失→整页挂）
  - Playwright 校验 → 编程点击+导航等待（IAB 管道缺陷）、URL 中文 `unquote()`、文本断言用 `textContent`
  - 备份/还原 → 按原相对路径（丢子目录会产生游离文件被审计死链抓获）
  - 并发 → 原型目录同一时刻仅一 agent 写（开工 mtime≥30 分钟检测，见前置校验 3）
  - 入库 → 只 add 本任务涉及文件与自有产物（mobile 五页+共享件+备份+截图+失败清单+任务书+索引+基线）；**禁 add -A**（免裹挟并行未提交批·沿 G42 纪律先例）；本任务不动 `_data/`，铁律③的 untracked 顾虑不适用；**禁 push**
- 新增 onclick 须自带 DOM 副作用（closeModal 类），免审计判死按钮；`go()` 跳转接管点不得改名
- 类名一律 m- 前缀，不得引入 PC 类名（`.modal`/`.btn`/`.filter-bar` 等）与 PC 样式文件
- m-auth/m-done/go()/mGuard 键名与逻辑零改动

## 九、验证门（全部满足才算完成，证据全部贴进对话）

1. **全量审计**：`python _scan_tmpdir/audit_interaction.py` → 证据=全量 0/0/0 汇总行（页数以执行时实测为准·2026-09-16 口径=**133 页**）+ mobile 五页各自 0/0 + **开工先自跑全量存 `_scan_tmpdir/audit_results_g50pre.json`，收尾重跑与 g50pre 逐键 diff 新增 0**（g41baseline 已滞后一页——转移出库审核.html 系 D-151 产物非本任务新增，若与之对比须豁免注记；diff 输出贴出）
2. **JS 语法**：`node --check P3-R01-包装租赁管理后台原型/mobile/_m.js` → 退出码 0（未改动则跳过并注明）
3. **PW 抽验**：Playwright（先写 m-auth 键再过页）五页断言清单全 PASS + 每页全页截图存 `_scan_tmpdir/g50_after/`：
   - 登录：hero logo/品牌名/两按钮存在
   - 待办审批：统计卡 3 格、item 卡片 ≥1、徽标配色类命中
   - 审批详情：`.m-banner` 存在且含状态大字、`.m-timeline` 节点=2、操作栏按钮 ≥2
   - 我的：`.m-hero` 存在、统计三格数字与 todoItems 行数/m-done 计数/类型去重数逐一相等
   - 库存查询：chips 单行横滑（**7 个 chip** 不换行）、卡片 ≥1、守卫回归（清 m-auth → 跳登录页，URL `unquote()` 后断言）
   - 证据=PW 输出逐页 PASS/FAIL 清单 + 截图 5 张
4. **grep 复评**：mobile/ 内 grep PC 类名（`class="[^"]*\b(modal|btn|filter-bar)\b`）命中 0；grep `go(` 跳转调用在五页保持既有次数不减少（输出贴出）
5. **独立验收**：见第十节，其逐项结论与总判定行原样贴进对话（未贴=未验收）

## 十、独立验收（子 agent 提示词四要素）

自检通过后派子 agent 复验（只读，仅允许往 `_scan_tmpdir/g50_accept/` 写取证输出）：

- **角色**：独立验收员，与执行无关；只读复验，不改任何文件
- **对象**：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\P3-R01-包装租赁管理后台原型\mobile\` 五页+`_m.css`+`_m.js`，及 `_scan_tmpdir/g50_after/` 截图
- **检查清单**：第九节验证门 1~4 逐条（自己跑命令取证，**不引用执行者自述**——audit 自己重跑、PW 断言自己重跑、grep 自己跑）
- **输出格式**：逐项 `[PASS/FAIL] 检查项 —— 证据摘要`；末尾 `总判定：N PASS / M FAIL` + 失败项清单
- 执行者义务：结论原样贴进对话；判 FAIL 修复后重派（同一项最多 2 轮），仍不过记失败清单并保留原状

## 十一、收尾（回写三件+git 批次为硬性）

1. 本文档头部标 ✅ 已完成 + 执行记录节（改动文件清单/验证输出摘要/失败清单结论）+ commit 哈希回填
2. `agent-handoff/_索引.md` G50 行：状态→✅、commit 哈希回填
3. `_AGENT基线.md` 当前基线快照追记 G50 段（mobile v1.2 样板风：banner/timeline/hero 三组件+五页优化）
4. git 一次提交（一 goal 至少一提交·只 add 本任务涉及文件与自有产物·**禁 add -A**），**禁 push**；失败清单结论行已贴出（空清单给「无失败项」）

## 执行记录（2026-09-16 · 无人值守 goal · ZCode 副机 macOS）

### 前置校验
- 基线 G50 命中仅 G41 追记内「P1-R08 挂行（G50 ⏳）」索引待执行行 ✓｜backup-g50-20260915/ 与失败清单开工时不存在 ✓｜互斥：30 分钟窗口内写入均为本会话刚收束的 G42 提交（已 push·无未提交原型改动）——G42 非「正在写」，按前置校验 3/4 条件语义放行（literal 口径出入已在失败清单「过程说明」注明）
- 本机无 node：门 2 以 _m.js 与备份逐字节 diff -q 相等证明零改动，按「未改动则跳过并注明」处理

### T 组完成情况（全 ✅）
- **T0** ✓ 备份 7 件（backup-g50-20260915/mobile/）｜锚点图 12 张计数齐全——但第四节所列 axure-审批详情.png 缺失（实存多出电商域 axure-产品详情.png 未采用）→ R-1 登记，T3 按任务书文字规格施工
- **T1** ✓ _m.css 追加 4330 字符组件块：.m-banner（固定渐变 135deg #1677ff→#4d94ff＋is-pass/is-back 状态变体＋44px 图标圆＋20px/700 状态大字）/ .m-timeline（12px 节点·cur 实心主色+光晕·done 空心 2px 主色描边·2px 竖连线·st-wait/pass/back 状态着色·时间右对齐 12px）/ .m-hero（同渐变＋56px 头像白描边＋姓名/角色 chip 白字化＋统计三格 20px/700 白、11px 白 80%）＋抛光增量（.m-item meta 分隔线与单号字距/.m-item-qty b 主色/.m-search 聚焦环/.m-cell-group 分组卡/.m-login-hero logo 圆角阴影）——既有规则零改动（纯追加＋scoped 增量）
- **T2** ✓ 卡片层级由 CSS 承载（meta 行分隔线上移 8px、单号 letter-spacing、留白对齐样板）；统计卡下钻交互/渲染 JS 零变更；注释 19→20 行（D-151 后）
- **T3** ✓ 页首 .m-banner（状态大字 待审核/已通过/已退回←已驳回映射·图标三态·副标=类型+单号·随通过/驳回动作即时联动）＋「审批进度」.m-timeline 静态两节点（提交申请[提交人+2026-时间取行字段]＋当前状态节点·状态文案着色）＋空态隐藏两件；信息行 m-dl 与底部胶囊操作栏保留；摘要卡状态徽标保留；div 58/58 配平
- **T4** ✓ me-card→.m-hero（渐变+头像首字实取+角色 chip 白字化）＋统计三格 JS 实算（待审核=todoItems 20 行/已处理=m-done 计数/单据类型=类型去重 20·PW 与页面实算逐一相等）＋cell 四行包 .m-cell-group 分组卡（组内分隔线·cell 项零增删·退出登录逻辑不动）＋版本 v1.1→v1.2·2026-09-16
- **T5** ✓ 视觉抛光（搜索框聚焦环/数量 b 主色高亮/状态徽标既有语义配色）·卡片形态与交互数据零变更；chips 维持 6（全部+KC 五状态·任务书门 3「7 个」系计数口径出入→R-2 登记·PW 按 6 单行断言通过）；注释 14→15 行（G37 后）
- **T6** ✓ hero 抛光（logo 圆角 22px+主色投影+品牌名字距·CSS 承载）；m-auth/企微登录/提示卡零改动；版本 v1.0→v1.2·2026-09-16
- **T7** ✓ _m.js 与备份 diff -q 逐字节相同（SHA-256 一致·独立验收复核）→ 零改动跳过 node --check
- **T8** ✓ 验证门 1~4 全过＋独立验收 15 PASS/0 FAIL

### 默认决策命中
- 骨架件零引入 ✓｜主色/渐变按写死值 ✓（is-pass/is-back 变体用既有色板绿/红系浅值·未新增蓝色 hex）｜状态色映射 ✓｜时间线静态两节点退化规则 ✓｜统计三格口径按门 3 字面 ✓｜操作栏胶囊保留 ✓｜库存卡片形态不变 ✓｜cell 零增删 ✓｜版本 v1.2·执行当日 2026-09-16 ✓（115 页计数类文案未动）｜chips=7 口径出入按 T5 零交互变更裁决（R-2）

### 验证门证据
1. 全量审计：开工快照 audit_results_g50pre.json（133 页·0 坏页）→ 收尾重跑 133 页·**坏页 0**·mobile 五页各 0/0/0·**与 g50pre 逐键 diff 新增 0（逐键一致）**
2. JS 语法：_m.js 零改动（diff -q 相同·跳过 node --check 并注明）
3. PW 五页断言（g50_pw.py·m-auth 预置·守卫用净上下文）：**16 PASS / 0 FAIL**（登录 hero+两按钮+v1.2/待办统计 3 格+卡片 20+徽标配色/审批详情 banner 可见+时间线 2 节点+操作钮 2/我的 hero+三格实算相等+分组卡 4+版本/库存 chips 6 单行+卡片 15/守卫回归跳登录页 unquote/五页 JS 错 0）＋截图 5 张 _scan_tmpdir/g50_after/
4. grep 复评：PC 类名宽松式 6 处全为 m-btn 前缀假阳性·负向后顾严格式 **0**；go( 五页计数与备份持平（2/1/3/1/0）
5. 独立验收（只读子 agent·自跑命令取证·_scan_tmpdir/g50_accept/verify.md）：**总判定：15 PASS / 0 FAIL**

### 失败清单结论
**无失败项**（登记性 3 条见 agent-handoff/goal-failures-g50.md：R-1 审批详情锚点图缺失按文字规格施工/R-2 chips=7 口径出入按 6 执行/R-3 门 4 grep 范式假阳性甄别；附过程说明 3 条）
