# G16 任务书：字段字典 A05 + 术语表 V0.5 + 实体关系建模 A06 + 数据字典页联动

> **状态**：✅ 已完成（2026-09-11 · Claude Code 无人值守）｜ **commit**：5bfcd76（工作提交）+ 收尾回写提交
> **执行环境**：实际由 Claude Code /goal 执行（原计划 Zcode，道远改派）
> **上游**：G15 ✅ fca99e8 ｜ **下游**：G17（命名统一执行，独立命令）

## 【第 0 步·上下文获取】

1. 读 `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\_AGENT基线.md` 全文（口径纪律铁律第四节必背）
2. 读 `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\P1-R04-术语表.md`（V0.4 现行版）
3. 读 `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\P3-R01-包装租赁管理后台原型\_data\demo-data.js` 头部 50 行（实体结构形态：`'实体名': { '键': { 'row': {"fields": {...}, "cells": [...], "ops": [...]}, 'title': ... } }`）
4. 项目根（本机）：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\`——本文档所有相对路径以此根拼接

## 【任务总览】

四件合成一轮：T1 字段字典 A05（机器提取）→ T2 术语表 V0.5（词条+待拍板区登记）→ T3 实体关系模型 A06 → T4 数据字典页/物料档案联动（原型 2 页+demo-data 1 处）→ T5 验证+回写+提交。

**不动**：全部页面叫法/列头/菜单（那是 G17 的活）；F01；mobile；A02-A04 既有内容（仅允许追加 A05/A06 引用行）。

## 【T0 前置校验与清场】

1. **并发检查**：原型目录 `_data/` 内文件 mtime 距今 ≥30 分钟（铁律①）。不满足→写失败清单后停止
2. **存量存档**：`git status --short | wc -l` 若 >0（预期约 135 文件=09-11 会话改动），执行 `git add -A && git commit -m "G16前置：09-11会话存量存档（多对多/盘点记录/物料改名/菜单URL修复/归属权修复/4v4弹窗/分期自由笔数/F01按钮）"`——**不 push**。commit 哈希记入执行记录
3. **备份**：目标文件复制到 `_scan_tmpdir/backup-g16-20260911/`（按原相对路径）：`P1-R04-术语表.md`、`P3-R01-包装租赁管理后台原型/_data/demo-data.js`、`P3-R01-包装租赁管理后台原型/系统管理/数据字典.html`、`P3-R01-包装租赁管理后台原型/基础数据/产品档案.html`、`P3-R01-包装租赁管理后台原型/基础数据/弹窗/新建产品.html`、`agent-handoff/_索引.md`、`agent-handoff/_AGENT基线.md`

## 【T1 字段字典 P3-R01-A05】

**产物**：`P3-R01-包装租赁管理后台原型/P3-R01-A05-字段字典.md`

**提取方法（写死，防自造）**：
1. 写脚本 `_scan_tmpdir/g16_t1_extract.py` + `_scan_tmpdir/g16_t1_dump.js`。dump.js 内容：`global.window={}; require('./_data/demo-data.js'); const fs=require('fs'); fs.writeFileSync('../_scan_tmpdir/g16_data_dump.json', JSON.stringify(window.__DEMO_DATA__||window.DEMO_DATA||Object.keys(window).length? Object.fromEntries(Object.entries(window).filter(([k,v])=>k.startsWith('_')===false&&typeof v==='object'&&v!==null)) : null))`——注意 demo-data 实际挂载键名先 grep `window.` 确认（默认决策 D1）；dump 失败则回退纯 Python 正则按 `'实体名':` 分块提 fields 键
2. Python 分析 dump：每实体收 fields 键集合+每键演示值（去重取前 5 个，数值列标数值型、全日期样标日期型、≤8 去重值标枚举）
3. 38 页 renderListPage cfg 提取：grep 各页 `{ label: '([^']+)', field: '([^']+)' }` → 字段键↔中文标签映射表
4. B 类白名单静态页（`agent-handoff/g12-whitelist.md`）表头兜底：grep `<th>` 文本

**文档结构**（A02/A04 同款风格）：
- 头部：版本 V1.0（2026-09-11 G16）、统计行（实体 37/字段总数 N/覆盖页面 M）、维护规则（新增字段先登记）
- 分模块章节：基础资料/项目管理/采购/销售/租赁(租出+租入)/仓储/财务/系统——每实体一张表：`字段键 | 中文标签 | 类型 | 取值域/演示值 | 出处页面`
- 中文标签来源优先级：cfg label > fields 键语义直译（标 `待核`）> 表头文本
- 末节「已知数据缺口」固定三条：①采购订单实体无 project 字段（列表页却有项目筛选）②stockFlows 无供应商维度（租入在库无法按供应商查）③supplier 字段为公司名字符串非客商编码 DW-xxx——出处：2026-09-11 多对多全量评估

## 【T2 术语表 V0.4 → V0.5】

改 `P1-R04-术语表.md`：

1. 版本行：V0.4→V0.5（2026-09-11 G16：物料档案等 6 新词+第五节扩为待拍板区 11 项+废弃词 2 条）
2. 第八节追加 V0.5 词条（表列同现有）：
   - **物料档案**：T7 定稿「物料」后的档案名（2026-09-11 全站改名 51 文件 178 处）；文件名仍 `产品档案.html`（菜单名与文件名分离先例）；实体键仍 products
   - **物料类型**：物料大类维度（器具/零部件 两值）；数据字典新增大类（本任务 T4）；两级制之首级，第二级=物料分类
   - **物料分类**：物料品种维度（围板箱/托盘/料箱/料架/组件）；物料档案「分类」列/筛选
   - **盘点记录**：菜单更名（2026-09-11，38 处侧边栏）；文件仍 `盘点列表.html`；盘盈盘亏支持生成其他出入库+关联单据列（首条+N 省略+无蒙层小弹窗）
   - **上下游多对多**：项目↔供应商多选绑定（2026-09-11 落地，推翻 09-08「本期不实现」——设计变更已记 A03）；绑定弹窗纯多选；新建租入单/采购订单选项目→供应商候选=绑定集合
   - **分期自由笔数**：收付款分期笔数不限（添加一笔/删除/剩余全排/超额拦截）；文案「期」→「笔」（2026-09-11）
3. 第九节废弃词表追加：`~~产品档案~~→物料档案`（2026-09-11 T7 改名全站清零，.prompts 调研存档与历史文档沿用）、`~~盘点~~→盘点记录`（菜单层，文件名与单据词保留）
4. 第八节旧词条 #2「产品档案」行加尾注：`（2026-09-11 更名物料档案，见 V0.5 #1）`
5. **第五节改造为「待拍板区」**，替换现有 3 行为 11 项登记表（G17 消费）：列=编号/口径/现状命名(×次数)/建议统一词/处置建议/状态。11 项数据（出现次数为 09-11 普查值）：
   1. 物料大类维度：物料类型/物料类别(库存查询列头)/类型(BOM)/类别(采购订单弹窗) → 物料类型；建议统一（G17 执行）
   2. 物料品种：分类(物料档案×4) → 物料分类
   3. 器具指代物料：租入单系/采购入库列头「器具」×15 → 物料（行业复合词：租赁器具tab/器具摊销/器具区 保留）【待拍板】
   4. 客商列头：客户名称×9/供应商名称×5/客商名称×3 → 短名（客户/供应商/客商）
   5. 项目：所属项目×27/项目名称×6/项目×2 → 不动（语义有别）
   6. 单据编号：单号×4/单据号×1 → 单据编号（入库单号/出库单号保留）
   7. 状态：当前状态×6 → 状态
   8. 备注：说明×4 → 备注
   9. 制单人/经办人/操作人 → 不动（语境有别）
   10. 仓库 vs 库房：仓库×12（入库/出库仓库、库位档案） → 库房
   11. 单位 vs 计量单位 → 不动（层级不同）；另：水单→银行回单/盈亏→损益/回款→收款 3 条旧待拍板保留原样挂表尾

## 【T3 实体关系模型 P3-R01-A06】

**产物**：`P3-R01-包装租赁管理后台原型/P3-R01-A06-实体关系与状态机.md`（V1.0）

结构：
1. **实体分域清单**：37 实体（以 demo-data 实际键名为准）按 8 模块域分组列表，每实体一行：实体名 | 中文名 | 承载页面 | 行数
2. **核心关系表**（从 demo-data 键值互引+页面关联链推导，每条标证据出处）：
   - projects M:N partners（suppliers 数组+绑定弹窗，2026-09-11 多对多）
   - projects 1:N leaseOrders/rentInOrders/purchaseOrders/stockFlows（所属项目字段）
   - partners 1:N rentInOrders/purchaseOrders/payableBills（供应商字段）
   - bomVersions 自引用父子项（父项/子项编码）
   - 单据 1:N 分期：payableBills/receivableBills 1:N payments/receipts
   - leaseOrders 1:N comboOutbounds/returnInbounds（关联租赁单）
   - locations 1:N stockFlows；todoItems→各单据（docNo 弱引用）
   - 允许补充推导到的其余关系，逐条带出处（页面列名/demo-data 字段）
3. **状态机节**：
   - 单据审核流（术语表第四节）：待提交→待审核→待发货→已完成
   - 库存五态（09-08 租入升第五态后口径=「状态」不带数字）：在库/在途(退租未审核)/客户端(租出)/退租库存/租入
   - 账单/盘点/审批各实体实际演示状态值枚举（从 dump 提取，逐实体）
4. **数据缺口节**：同 A05 末节三条
5. 关系图：**mermaid 代码块文本形态**（不生成独立 HTML，避免扩权；道远要用时再 diagram-design 出图）

## 【T4 数据字典页 + 物料档案联动】

1. **demo-data dictItems 实体**新增「物料类型」大类+两明细：编码沿用现有 DT-xx/简码规则（先 grep 现有 dictItems 明细行的编码形态照抄格式）；明细两项=器具/零部件。**注意基线铁律：实体末条记录结尾无逗号，末位插入须补逗号，改后 `node --check demo-data.js`**
2. **系统管理/数据字典.html**：确认大类切换为数据驱动（G12 接线）——若大类列表也是 dictItems 驱动则仅数据层即生效；若大类列表硬编码在 HTML，则按精确替换加一行大类项（assert 匹配数=现有大类项数）
3. **物料档案「物料类型」字段**：内嵌 createModal（`基础数据/产品档案.html`）与预览副本（`基础数据/弹窗/新建产品.html`）在「分类」行**之前**插入一行：`<span class="form-label"><span class="req">*</span>物料类型</span>` + select（器具 selected/零部件）——样式照抄「单位」行 select-box 结构；编辑入口同弹窗自动获得。**列表不加列**（38 页列宽敏感，默认决策 D5）
4. A05/A06 头部交叉引用对方+数据字典.html

## 【T5 验证门（全部机器判定）】

依次跑，输出全贴执行记录：

1. `node --check P3-R01-包装租赁管理后台原型/_data/demo-data.js` → 退出码 0
2. A05 断言：`grep -c "^| " A05` ≥ 200（字段行下限）；37 实体名逐个 `grep -c "实体名"` ≥1（Python 循环输出 37 行 PASS）
3. 术语表断言：grep 版本行含 `V0.5`；grep `物料档案`/`盘点记录`/`上下游多对多`/`分期自由笔数` 各 ≥1；第五节含 11 行编号
4. A06 断言：37 实体名逐个 grep ≥1；`mermaid` 代码块 ≥1；含「状态机」与「数据缺口」节
5. Playwright（`_scan_tmpdir/g16_t5_verify.py` 留档）：
   - 数据字典页：新大类「物料类型」可见且点击后明细含 器具/零部件 两行
   - 物料档案：打开 createModal（evaluate `openModal('createModal')`）→「物料类型」label 存在+select 2 选项+位于「分类」行之前（DOM 顺序断言）
   - JS 错误 0
6. audit 回归：`python _scan_tmpdir/audit_interaction.py` → 死链 0/JS 0/相对 g14baseline 逐键 diff 新增 0（diff 脚本 `g14_audit_diff.py`）
7. 失败清单 `_scan_tmpdir/g16-failures.md` 结论行（可为「0 未处置失败」）

## 【T5b 收尾三件回写（硬性）】

1. 本文档：头部标 ✅+commit 哈希；追加「## 执行记录」节（T0-T5 逐组结果+验证门输出摘要+偏差清单）
2. `agent-handoff/_索引.md` 追加 G16 行（日期 09-11/任务名/✅/commit 哈希/本文件名）
3. `agent-handoff/_AGENT基线.md` 当前基线快照滚动：文档地图加 A05/A06 两行、术语表 V0.4→V0.5、当前任务块换 G16、dictItems 实体行数更新
4. `git add -A && git commit -m "G16 字段字典A05+术语表V0.5+实体建模A06+数据字典物料类型联动：..."`（要点+验证门证据+偏差）——**不 push**

## 【默认决策表（无需向任何人提问）】

| # | 决策点 | 预定值 |
|---|---|---|
| D1 | demo-data 挂载键名不确定 | 先 grep `window\.` 定实际键名；dump 失败回退 Python 正则分块 |
| D2 | fields 键无对应 cfg 标签 | 中文标签=键名语义直译+标`待核`，不猜业务名 |
| D3 | 取值域枚举阈值 | 去重值 ≤8 视为枚举列全列，>8 取前 5+「…」 |
| D4 | A05/A06 编码 | A05=字段字典、A06=实体关系与状态机，随原型目录 P3-R01- 前缀（A 系列顺延） |
| D5 | 物料档案列表加「物料类型」列 | 不加（列宽敏感），弹窗字段+字典为准 |
| D6 | 字典大类编码顺延不确定 | grep 现有 dictItems 行照抄格式；器具=排序1 零部件=排序2，状态=启用 |
| D7 | 术语表旧「产品档案」词条#2 | 原行保留加尾注指向 V0.5 新词（历史定版不回改，先例=第二节） |
| D8 | A06 关系证据不足 | 宁缺毋滥：已列 8 条为准，能推到的才加，标注出处 |
| D9 | T4 数据字典大类列表若为硬编码 | 精确替换插入（assert 现有大类项数），不重构为数据驱动 |
| D10 | audit 与验证门冲突时间 | 验证门先跑（快），audit 最后跑完再提交 |
| D11 | 存量存档提交信息 | 按 T0.2 写死文案，不另行请示 |
| D12 | 「器具/零部件」默认选中 | 器具（演示主线为围板箱） |

## 【失败处理策略】

- 任一任务组 assert 失败/结构不符：**跳过该组、写入 `_scan_tmpdir/g16-failures.md`（时间/组号/失败断言/现场描述）、继续下一组**；禁止卡住等待、禁止对不确定结构猜测性改写
- T4 原型改动失败可整组跳过（回滚该组 2 页+demo-data 改动从备份），文档组 T1-T3 不受影响照常交付
- 全部组跑完才算执行结束；失败清单结论行必写（0 失败 或 N 失败明细）

## 【纪律（铁律摘引+本任务补充）】

1. 原型目录同一时刻仅本 agent 一个写（T0.1 mtime 检查）；自己用完即关
2. 原型手工维护：批量改动=读取-精确替换+assert 计数，禁裸 open('w') 整页写
3. demo-data 末条记录无逗号坑：插入补逗号+`node --check`
4. 多股改动入库一律 `git add -A`（_data 不随 -u）；**全程不 push**
5. HTML 切块替换带标签锚防吃 CSS 段；改后标签配平自检
6. 新增 onclick 须自带 DOM 副作用（本任务原则上不新增 onclick）
7. 提交前失败清单与验证门输出必须落执行记录——证据先于断言

## 执行记录（2026-09-11 · Claude Code）

### 前置校验
- ①基线存在 ✓ ②任务书存在 ✓ ③demo-data mtime 距今 58 分钟 ✓ ④_索引 G16 行=⏳ 待执行登记行（任务书创建时写入，哈希=—，非已执行态）——按「防重复执行」意图放行，收尾更新为 ✅（偏差#1）

### T0 清场
- 存量 101 文件 `git add -A` 存档提交 **9928240**（不 push）；备份 7 文件 → `_scan_tmpdir/backup-g16-20260911/`

### T1 字段字典 A05
- `g16_t1_dump.js`（node dump 37 实体）→ `g16_t1_extract.py` → **A05**：314 字段行（cfg 标签 154 / info 值反查 36 / 直译待核 112）+ B 类白名单 25 页表头 + 数据缺口 5 条；脚本留档可重跑

### T2 术语表 V0.4→V0.5
- 版本行+日期；6 新词（#18-#23）；废弃词 2（~~产品档案~~/~~盘点~~）；旧词条 #2 尾注；第五节重建为 11 项待拍板区（1/2/4/6/7/8/10=已拍板·G17 执行，3=待拍板 D 表默认，5/9/11=审定不动）+ 旧 3 条挂表尾

### T3 实体建模 A06
- `g16_t3_gen.py` → **A06**：37 实体分域清单 + 29 条关系（逐条证据：dump 互引扫描+页面）+ 状态机 3 组（审核流/库存口径漂移注记/账单生命周期）+ mermaid×3 + 缺口 5 条
- 实测发现：M:N 绑定未落 demo-data（弹窗 0 处 DEMO_DATA 引用）；库存状态数据四值 vs 09-08 五态口径漂移（详见 A06 3.2）

### T4 数据字典+物料档案联动
- `g16_t4_patch.py` 精确替换（assert 全过）：demo-data dictItems +WL-01 器具/WL-02 零部件（node --check 0）｜数据字典.html +物料类型 分类项（计数 2）｜产品档案 createModal+新建产品预览页 +「物料类型」下拉（分类之前·器具默认选中）；标签配平 ✓

### T5 验证门（7 条）
1. node --check 退出码 0 ✓
2. A05：字段表行 351（≥200）· 37 实体名逐一命中 ✓
3. 术语表：V0.5 ✓ · 6 新词各 ≥1 ✓ · 第五节编号 1-11 ✓
4. A06：37 实体命中 ✓ · mermaid×3 ✓ · 状态机+数据缺口节 ✓
5. Playwright（`g16_t5_verify.py`）：数据字典物料类型项可见+计数 2+点击明细 WL-01/WL-02 两行 ✓；createModal 标签序 [物料编码,物料名称,**物料类型**,分类,规格,单位,归属权,租金单价,备注] idx2<idx3 ✓；下拉 [器具,零部件] 器具默认 ✓；预览页同步 ✓；JS 错误 0 ✓
6. audit 113 页：**死链 0 · JS 0**；g14baseline 逐键 diff 新增 2——**G16 引入=0 实证**（库存查询 G16 未触碰；产品档案 git HEAD=G16 前复跑出同款 modal-unreachable 警告）→ 两条均为 09-11 晨会话改动相对 g14baseline 的既有漂移（偏差#2，遗留 G17 基线滚动清偿）
7. `_scan_tmpdir/g16-failures.md` 结论行：**0 未处置失败 · 6 偏差注记闭环**

### T5b 收尾
- 工作提交 **5bfcd76**（含 A05/A06/术语表/原型 4 文件/脚本/失败清单/备份）；注记：该提交按铁律③ `git add -A` 混入 `20260911-G19-移动端规范化与PC登录.md`（13:01 另一会话所建规划文档，非 G16 产物）
- 三件回写：本文档 ✅+本节 ｜ _索引 G16 行 ⏳→✅ 5bfcd76 ｜ _AGENT基线快照滚动（A05/A06 入文档地图+术语表 V0.5+当前任务 G16+dictItems 45→47）
