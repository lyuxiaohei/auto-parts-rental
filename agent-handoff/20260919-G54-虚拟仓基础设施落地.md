# 20260919-G54 · 背靠背直发与虚拟仓完全闭环（任务书）✅

> **状态：已执行完毕（2026-09-19）**——T1→T8 全部完成；T7 六门全过（语法 0 错／数据 23 PASS·0 FAIL／渲染 63 PASS·0 FAIL／审计 18/18 页 0·0·0／grep 收口达标／独立验收 **10 PASS / 0 FAIL**）；失败清单无失败项（登记 8＋待追认 6·见 `goal-failures-g54.md`）；主提交哈希见文末执行记录。

| 项 | 内容 |
|---|---|
| G 号 / 日期 | G54 / 2026-09-19 |
| 任务一句话 | 0918 会后待办第二大部分（#11~#15）原型完全闭环：虚拟仓基础（字典 KW-05＋库位 XNC-ZF 系统内置＋7 处下拉排除）＋租入单类型字段（直发/自发）＋直发链补强（CK-023 回链＋终态链）＋退租链虚拟仓口径（TZRK-009/GHCK-001）＋人工报数维护通道＋标注/文档全量同步＋落账 D-167 |
| 上游依据 | ①道远 09-19 指示（虚拟仓类型进数据字典＋建库位作默认数据不可删改＋详细闭环任务计划·已批准）；②0918 会后待办 #11~#15；③第4次会议（09-14）纪要已拍板「租入入库环节立即转租·自动生成租赁出库」（CK-023 链即其落地）；④09-18 转写原文：退租入库=人工录入（客户报数触发）、归还出库=系统自动生成（纪要「系统生成退租入库」与转写冲突处以录音为准） |
| 已拍板口径 | ①XNC-ZF「直发虚拟仓」系统内置不可删改（ops 置空）；②字典 KW 组 +KW-05；③RZRK-20260903-023 入库库位对齐直发虚拟仓；④类型字段加在**租入单**（袁工 09-18 亲述，09-08 纪要「租赁单加背靠背标记」旧口径废止）；⑤归还出库=系统自动（#13）；⑥**类型命名=直发单/自发单**（道远侧 09-19 确认·袁工 09-18「直发代发是比较规范的」「自发单好一点」）；⑦**终态链=造完整终态链 3 单**（道远侧 09-19 确认·现有链停中间态无终态样本） |
| 待追认口径 | 以下 4 小项预设值入默认决策表，均为文案级可逆，收尾报告列「待追认清单」：CK-023「来源」文案改类型驱动；退租入库新建加库位说明 hint；otherInbounds 造客户报数调整演示单；F01 导航图不画新支线 |
| 改动面 | demo-data.js（dictItems/locations/rentInOrders/comboOutbounds/returnInbounds/rentInReturns/rentInbounds/otherInbounds 共 8 实体段）＋页面 9 处（6 文件 7 处 filterV＋库位档案＋数据字典）＋租入单列表/新建 2 页＋退租入库新建 1 页 hint＋文档（A05/A02/A06/F01 开发规则/P2-R01/拍板登记/会后待办 md）＋A03 json＋notes-data.js＋备份目录。**红线零改动**：stockFlows/库存查询数据、现有 10 库位行、select-source.js/list-generic.js/notes-drawer.js 共享件、转移出库 XNC-ZZ-* 引用（XNC-ZZ-WBX 为物料编码非库位）、mobile、退租入库/租赁出库预埋 filterV 逻辑 |
| 扩容记录 | 2026-09-19 由「虚拟仓基础设施落地」版扩容为完全闭环版（道远侧确认完全闭环诉求；原 T1-T5 并入新 T1/T6/T7/T8，新增 T2-T5） |

## 【第 0 步·上下文获取与前置校验】

1. 项目根＝`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁`（下称 ROOT；其余相对路径以 ROOT 拼接）。原型根＝`ROOT\P3-R01-包装租赁管理后台原型`（下称 P3）。
2. 必读：`ROOT\agent-handoff\_AGENT基线.md`；本任务书全文。
3. 关键现状锚点（执行时按锚文本定位，行号会漂移；以下数值均经 2026-09-19 node 实测）：
   - demo-data.js（P3\_data\）locations 实体（头注释「库位档案 locations：键 = 库位编码」）：**10 键**（RA-A-01-01…末键 **RC-02**，其后插入须给 RC-02 行尾补逗号），fields={wh,ltype,spec,usage,status}；wh 值域＝原料区 RA×3/成品区 RB×5/次品区 RC×2（**无「外购区 RW」**——该值现为孤儿值，本任务全站清零）。
   - dictItems KW 组：**4 值** KW-01 存储位/02 拣选位/03 暂存位/04 不合格品位；fields={category:库位类型,abbr,name,status}，cells=[简码,名称,排序号 td-num span,备注,状态 tag-green]，ops=[{t:编辑},{t:停用,act:openModal('stopModal')}]；abbr 惯例=两字拼音首字母（CC/JX/ZC/BH）。
   - locations 下拉消费点全站恰 **10 处** fillEntity：**已预埋排除（逻辑勿动·注释更新）**＝租赁出库录单:581＋退租入库新建:410（`filterV:function(v){return v.indexOf('虚拟仓')<0;}`；fillEntity field 模式的 v=fields.wh 名称值，XNC-ZF 命名含「虚拟仓」三字可命中）；**本轮补排除 7 处（6 文件）**＝盘点录入:610（盘点库房）、调拨新建:540+541（调出＋调入）、其他入库新建:534、其他出库新建:540、销售出库新建:532、上下游绑定:539（getElementById('bindWh') 形态同参）；**不排除**＝库位档案:743（筛选）。
   - 库位档案.html：renderListPage({entity:'locations'})；静态兜底 tbody 10 行；「库位类型」筛选静态 option（全部/存储位/拣选位/暂存位/不合格品位）；「状态」筛选静态值「待审核/已审核/已完成」与数据值 启用/停用 失配（顺带修复）。
   - **数据字典.html 在 `P3\系统管理\`（非基础数据目录）**：左列表 div.dic-item 静态，「库位类型」cnt 写死 6（实际 4→变 5·顺带修正）；主表数据驱动自动渲染 KW-05 无需改。
   - 库位新建.html:385 页内自带 dictOpts（读 window.DEMO_DATA.dictItems·status≠停用·按 key 排序）——零改动自动带出 KW-05。
   - rentInOrders（租入单）**6 键**：RZD-20260815-003/RZD-20260815-005/RZD-20260902-008/RZD-20260701-001/RZD-20260615-002/RZD-20260910-009；fields 无类型位；cells **10 列**（首列供应商）；formRows 首六行=租入单号|状态|所属项目|供应商|起租日期|止租日期。
   - 租入单列表.html 列头现 11 th（勾选框＋10 数据列：租入单号|供应商|物料|数量|租金|租期起止|押金|累计租金应付|状态|经办人）。
   - 租入单新建.html 已有 .radio 机制（CSS :148-151＋JS 切换 :451）——类型组复用现有机制零新样式。
   - comboOutbounds **10 键**；CK-20260914-023 formRows 14 行已含「来源=立即转租（租入入库登记时勾选·自动生成）」「关联租入库=RZRK-20260903-023」「关联租入单=RZD-20260902-008」「出库库位=—（供应商直发·不经实物入库）」；**chain 为空（唯一缺口）**。RZD-20260902-008 的 chain 已完整三节点（租入单→租入入库 RZRK-023→租赁出库 CK-023）。
   - RZRK-20260903-023（rentInbounds·待入库）：formRows「入库库位」text=「外购区 RW」full:true；timeline 次节点已含「入库后计入租入在库并转租」。
   - returnInbounds **10 键**；TZRK-20260903-009：fields.warehouse=「外购区 RW」（孤儿值）、formRows「入库库位」同值；chain 两节点（本单→归还出库 GHCK-20260903-001）；timeline 4 节点（3 人工＋1 系统待审）。
   - rentInReturns **3 键**；GHCK-20260903-001：chain 三节点（退租入库 TZRK-009→本单→租金应付 AP-20260903-009）完整；ref=RZD-20260815-003；timeline 3 节点。
   - otherInbounds **3 键**/otherOutbounds **5 键**（人工调整单蓝本）。
   - notes-data.js「基础数据/库位档案.html」键现仅 id=1（页头卡·note 提「外购区 RW」库区命名——T5 按默认决策表补虚拟仓半句）。
4. 前置校验（任一不过→停止并写失败清单）：
   - 并行写者：`git log --since=2.hours --oneline`＋`.git` 目录 mtime；有他人新提交或未知新变动→停止留档。
   - 幂等（按缺项断点续做）：locations 无 XNC-ZF 且 dictItems 无 KW-05 且 rentInOrders fields 无 type 键且 CK-023 chain 为空且 TZRK-009 库位含「外购区 RW」→全缺开工；部分已做→从缺项续；全部已在做→验证门复跑通过即收尾。
   - 备份：demo-data.js＋将改 12 页面（库位档案/数据字典/盘点录入/调拨新建/其他入库新建/其他出库新建/销售出库新建/上下游绑定/租赁出库录单/退租入库新建/租入单列表/租入单新建）＋A05/A02/A06/F01 开发规则/A03 json → `ROOT\backup-xnc-20260919\`（原相对路径）。
   - 路径自检：任务书全部绝对路径 `os.path.exists` 实测输出贴对话（含 `P3\系统管理\数据字典.html`）。

## 【T1 虚拟仓基础】数据＋页面（一律 Edit 手动改·每批后 node --check）

1. dictItems +KW-05（插 KW-04 行块后）：fields=`{"category": "库位类型", "abbr": "XN", "name": "虚拟仓", "status": "启用"}`；cells=`["XN", "虚拟仓", "<span class=\"td-num\">5</span>", "背靠背直发账面枢纽", "<span class=\"tag tag-green\">启用</span>"]`；ops=`[{"t": "编辑"}, {"t": "停用", "act": "openModal('stopModal')"}]`（字典项可管理；不可删改守护的是库位行非字典值）。
2. locations +XNC-ZF（插 RC-02 行块后·RC-02 行尾补逗号）：fields=`{"wh": "直发虚拟仓", "ltype": "虚拟仓", "spec": "—", "usage": "系统维护", "status": "启用"}`；cells=`["直发虚拟仓 <span class=\"tag tag-gray\">系统内置</span>", "虚拟仓", "—", "系统维护", "<span class=\"tag tag-green\">启用</span>"]`；ops=`[]`（空数组=无编辑/停用=不可删改）。
3. RZRK-20260903-023 对齐：formRows「入库库位」text「外购区 RW」→「直发虚拟仓（XNC-ZF·背靠背直发不占实体库）」；row/cells/ops/chain/timeline 零动。
4. 7 处 fillEntity(locations) 追加 `filterV:function(v){return v.indexOf('虚拟仓')<0;}`（与预埋逐字同款；上下游绑定 getElementById 形态同参）。
5. 预埋 2 处**注释更新**（逻辑勿动）：租赁出库录单:580 与退租入库新建:407 注释「虚拟仓不维护 D-006·过滤」→「人工单据不选虚拟仓（D-167·直发虚拟仓由系统自动单据流转）」。
6. 库位档案.html：静态兜底 tbody +1 行 XNC-ZF（列值同 cells·操作列写「—」纯文本）；「库位类型」筛选 option 追加「虚拟仓」；顺带修「状态」筛选失配（→启用/停用）。
7. 数据字典.html（系统管理\）：左列表「库位类型」cnt 6→5。
8. 库位新建.html：零改动（验证下拉自动带出）。

## 【T2 租入单类型字段】（#11）

1. rentInOrders 6 单加类型（一律手动 Edit）：
   - fields 各 +`"type"`：**有转租/归还下游链=「直发」**（RZD-20260902-008→CK-023 转租链、RZD-20260815-003→GHCK-001 归还链），**其余 4 单=「自发」**。
   - cells 在「租入单号」列后插类型值（10→11 列·与列表列头同步）。
   - formRows 在「状态」行后插「类型」行（text=直发/自发）。
2. 租入单列表.html：列头在「租入单号」后插「类型」th（11→12 th·勾选框列不动）。
3. 租入单新建.html：加「类型」radio 组（直发单/自发单·复用 .radio 机制·默认选中「自发单」）；选「直发单」时显示行内提示「直发租入：审核通过后系统自动生成租入入库（直发虚拟仓）并自动生成租赁出库，无需实物入库」（对齐入口而非改文案）。
4. 租入单详情/审核页零改动（G53 三卡渲染器按 formRows 数据驱动自动带出）。

## 【T3 直发链补强】（#12）

1. CK-20260914-023 补 chain（照 RZD-008 chain 对称·三节点）：`[{role:"租入单",name:"RZD-20260902-008 · 直发",url:"租入管理/租入单列表.html"},{role:"租入入库",name:"RZRK-20260903-023 · 待入库",url:"租入管理/租入入库列表.html"},{role:"租赁出库（本单·系统自动生成）",name:"CK-20260914-023",self:true}]`。
2. CK-023 formRows「来源」行文案更新：「立即转租（租入入库登记时勾选·自动生成）」→「租入单直发类型 · 系统自动生成」（旧「入库勾选」表述为第4次口径，演进关系写入 F01 沿革注记）。
3. 终态链（默认造·见决策表）：新造 3 张已走完单——租入单 RZD（直发·已完结）→租入入库 RZRK（已入库·库位=直发虚拟仓·chain 回指）→租赁出库 CK（已完成·chain 三节点回指）；单号格式照同实体既有惯例续编（同日期前缀取最大序号+1）；字段结构照同实体既有行克隆精简；chain/timeline 三单互联，timeline 终节点为系统自动文案。
4. RZRK-023 timeline 已含「入库后计入租入在库并转租」——零改动。

## 【T4 退租链虚拟仓口径】（#13·改既有链·默认不造新单）

1. TZRK-20260903-009：fields.warehouse 与 formRows「入库库位」text「外购区 RW」→「直发虚拟仓（XNC-ZF·识别背靠背自动带出）」；timeline 在「验收完好」节点后插 1 节点 `{t:"09-03 11:10",text:"系统识别原租入单 RZD-20260815-003 为直发类型 · 库位自动带出直发虚拟仓",who:"系统"}`（时刻与前後节点自洽）。
2. GHCK-20260903-001：timeline 首节点前插 `{t:"09-03 11:05",text:"系统识别退租入库为背靠背 · 自动生成本单",who:"系统"}`；chain 已完整零动。
3. 退租入库新建.html：入库库位字段旁加一行 hint「直发类型租入件退租时，入库库位由系统自动带出直发虚拟仓，无需手选」（默认决策表·待追认）。
4. 既有 TZRK 实体仓退租链（如 TZRK-20260902-010 BOM 拆散链）保留不动——非背靠背退租入实体仓的对照组。

## 【T5 人工报数维护通道＋标注层】

1. otherInbounds 造 1 张「客户报数调整」演示单（默认造·待追认）：字段结构照同实体既有行克隆；库位字段直写「直发虚拟仓」；备注/摘要表达「客户报数增加 · 账面人工调整入虚拟仓」；单号照实体惯例续编。
2. A03 json＋notes-data.js **双处同步**（G52 抽屉制·不碰页面注入块；aud 缺省=biz）：
   - 「基础数据/库位档案.html」+id=2：title「直发虚拟仓」；note「背靠背直发账面枢纽：直发租入入库入本仓、退租归还自本仓出，不占实体库存；系统内置库位不可删改（字典 KW-05）；人工单据不可手选本仓，自动单据由系统流转」；selector 按既有风格锚新静态行；fp/req 留空。id=1 note 中「外购区 RW」库区表述顺带核对（补虚拟仓口径或维持，见决策表）。
   - 「租入管理/租入单列表.html」+1 条（键无则新增页面键·锚「新增租入单」按钮或卡头）：note「类型=直发单（供应商直发客户·背靠背）/自发单（先入我方库再发）；直发单审核后系统自动生成租入入库与租赁出库（09-18 袁工口径）」。
   - 「租入管理/租入入库详情.html」+1 条（键无则新增）：note「直发链自动化：直发租入单→自动生成本单（入直发虚拟仓·不占实体库存）→自动生成租赁出库并关联（第4次『立即转租』口径 09-18 升级为类型驱动）」。
   - 「租赁管理/退租入库详情.html」+1 条（键无则新增）：note「背靠背退租闭环：客户退租人工录本单→系统识别原租入单为直发类型→库位自动带出直发虚拟仓→自动生成归还出库（GHCK）；客户报数增减亦经出入库单人工调整虚拟仓账面（#15）」。

## 【T6 文档回灌与落账】

1. A05：locations 节头记录数→11＋`_key/wh/ltype` 值域补 XNC-ZF/直发虚拟仓/虚拟仓＋沿革行（D-006 曾删客户虚拟仓→D-167 系统内置直发虚拟仓·ops 置空）；KW 值源注记 4→5；rentInOrders 节 fields 补 type 行＋6 单类型分布；comboOutbounds/returnInbounds/rentInReturns/otherInbounds 相应行沿革补记；登记节 +G54/D-167。
2. A02：库位档案行「含 XNC- 客户虚拟仓」→「含系统内置直发虚拟仓 XNC-ZF（不可删改）」；租入单列表/新建行补类型字段描述。
3. A06：locations 关系行按实况更正＋边注记补 XNC-ZF。
4. F01 开发规则 §12.3：补划界句＋使用规则三句——「直发虚拟仓 XNC-ZF（D-167·#12/#13 背靠背账面枢纽·库位档案系统内置不可删改）区别于客户虚拟仓（D-145 不维护·库存状态维度表达），亦区别于 XNC-ZZ-WBX 转移物料编码；使用规则：①人工单据不可手选虚拟仓（下拉 filterV 排除）②自动单据由系统数据层直写/带出③客户报数增减经出入库单人工调整虚拟仓账面」。
5. **09-08 旧口径残留排查**：grep「背靠背类型标记」「自动生成租入单草稿」于 P2-R01/F01 开发规则/A05——命中则按 09-18 新口径（类型加在租入单）更新并注沿革；会议纪要原文不动（历史材料白名单）。
6. 拍板登记.md：**补登 G54 上游指示条目**（2026-09-19 道远指示：虚拟仓类型进数据字典＋库位默认数据不可删改＋闭环任务计划·已批准→当日扩容为完全闭环执行）＋G54 执行结论条目。
7. P2-R01：头部最后更新行追加 D-167＋附录 A 台账加行（背靠背直发与虚拟仓完全闭环·道远 09-19 拍板扩容）。
8. 会后待办 md（`ROOT\项目沟通记录\2026-09-18 汽车物流包装租赁第5次沟通前-和袁工\2026-09-18 汽车物流包装租赁第5次沟通前 [会后待办].md`）：#11/#12/#13/#15 状态 ⏳→✅（#11 注命名待追认；#15 注「基础设施＋链路口径＋人工维护演示 ✅ G54·D-167·真自动化开发阶段实现」）；关联文档行补 G54 任务书。

## 【T7 验证门】（全部满足才算完成·证据贴对话）

1. **语法门**：node --check demo-data.js＋notes-data.js 退出码 0；A03 json `json.load` OK。
2. **数据门**（node 断言输出贴对话）：locations=11 键且 XNC-ZF.ops=[]；KW 组=5 值且 KW-05.name=虚拟仓；rentInOrders=6 键且 fields 均含 type（2 直发/4 自发）；CK-20260914-023 chain=3 节点且首节点 name 含 RZD-20260902-008；RZRK-023 formRows 入库库位含「直发虚拟仓」；TZRK-20260903-009 库位含「直发虚拟仓」且 timeline 含「自动生成」系统节点；GHCK-20260903-001 timeline 含「自动生成」；终态链 3 新键存在且互联；otherInbounds=4 键且新单库位含直发虚拟仓；stockFlows=18 键（零改动证据）；**demo-data.js 内「外购区 RW」全文=0**（孤儿值清零）。
3. **渲染门**（Playwright 脚本 `P3\_scan_tmpdir\g54_xnc_verify.py`·逐页 PASS 清单贴对话）：库位档案 11 行·XNC-ZF 行操作列空＋含「系统内置」·类型筛选「虚拟仓」命中 1 行；库位新建类型下拉含「虚拟仓」；数据字典 KW 组 5 值；7 页下拉逐页断言无「直发虚拟仓」；租入单列表 6 行含类型列（2 直发/4 自发）；租入单新建 radio 两组＋选直发出提示；租入单详情含「类型」行；租入入库详情（RZRK-023）入库库位=直发虚拟仓且 chain 首节点仍指 RZD-20260902-008；CK-023 详情 chain 三节点；退租入库详情（TZRK-009）库位=直发虚拟仓＋timeline 系统节点；库存查询 tbody=18 行且五态统计卡不变；全上 JS 0。
4. **审计门**：复用 `_run_filter_audit3.audit_page` 跑 ≥15 页（T1 的 11 页＋租入单列表/租入单新建/租入单详情/退租入库详情）problems/dead_links/js 全 0；租入单列表列头数=cells 列数断言（12 th=11 列）。
5. **grep 收口门**：filterV 含「虚拟仓」=9 处；「XNC-ZF」全库分布=预期清单（demo-data/库位档案/文档/A03/notes-data）；「外购区 RW」demo-data=0（页面/标注层残留列清单，白名单外=0）。
6. **独立验收**（不得自证）：自检全过后派**只读子 agent**（general-purpose·除 `_scan_tmpdir\g54_accept\` 外不得写任何文件）复验，逐项 `[PASS/FAIL] 检查项——证据摘要`＋末行 `总判定：N PASS / M FAIL` **原样贴进对话**；FAIL→修复重派同项最多 2 轮，仍不过记失败清单保留原状。
   - 子 agent 提示词四要素：**角色**＝只读独立验收员；**对象**＝ROOT 下 demo-data.js＋库位档案/数据字典/库位新建＋7 下拉页＋租入单列表/新建/详情＋租入入库详情/CK 详情/退租入库详情（只读）；**检查清单**＝①node --check 退出码 ②自抽 6 页渲染（库位档案 11 行·XNC-ZF 无操作按钮＋系统内置 tag·类型筛虚拟仓命中 1；库位新建下拉含虚拟仓；盘点录入与调拨新建下拉无直发虚拟仓；租入单列表类型列 6 行；租入单新建直发提示；TZRK-009 详情库位=直发虚拟仓）③数据断言 locations=11·KW 5 值·rentInOrders 6 键含 type·CK-023 chain 3 节点·stockFlows=18 ④git status 无越界改动（改动面仅 T1-T6 文件＋备份＋g54_* 脚本）⑤grep filterV 虚拟仓=9＋demo-data「外购区 RW」=0；**输出格式**＝逐项 PASS/FAIL+证据+总判定行。

## 【T8 收尾】

1. 失败清单 `ROOT\agent-handoff\goal-failures-g54.md`（无失败也写结论行；登记顺带修复：状态筛选失配/字典 cnt 陈旧/预埋注释过期）。
2. 本任务书头部标 ✅＋执行记录节（各门输出摘要＋**待追认清单**单独列出）。
3. `_索引.md` 挂 G54 行；`_AGENT基线.md` 滚动（D-167·XNC-ZF/KW-05/locations 11 行·租入单类型字段·链补强）。
4. git 两笔提交（只 add 本任务文件·禁 add -A·禁 push）：①数据+页面+备份；②文档+落账。哈希回填任务书头部与索引行。

## 【默认决策表】（拿不准按此执行·不提问·收尾报告注明命中项；标【追认】者列入待追认清单）

| 决策点 | 预定值 |
|---|---|
| KW-05 abbr/排序/备注 | XN / 5 / 背靠背直发账面枢纽 |
| XNC-ZF 系统内置标记 | 名称列尾 `<span class="tag tag-gray">系统内置</span>` |
| 静态兜底行操作列 | 写「—」纯文本无按钮 |
| RZRK-023 改动面 | 仅 formRows 入库库位一处 text（full 属性维持原值） |
| 类型命名 | **直发单/自发单**（已拍板⑥·09-18 转写：袁工「直发代发是比较规范的」「自发单好一点」） |
| 类型补值规则 | 有转租/归还下游链=直发（RZD-20260902-008/RZD-20260815-003），其余 4 单=自发 |
| 类型列位置 | 列表「租入单号」后；formRows「状态」行后；cells 同步插列 |
| 新建页默认选中/提示 | 默认「自发单」；直发提示文案见 T2.3 |
| CK-023「来源」文案【追认】 | 改「租入单直发类型 · 系统自动生成」（旧口径进 F01 沿革） |
| 终态链 | **造 3 张已走完单**（已拍板⑦：直发租入单→已入库→已完成出库）；单号照实体惯例续编（同日期前缀最大序号+1）；字段照同实体既有行克隆精简 |
| TZRK-009/GHCK-001 处置 | 改既有链（库位/文案/timeline），不造新退租链 |
| 退租入库新建 hint【追认】 | 加一行说明（T4.3 文案） |
| otherInbounds 调整单【追认】 | 造 1 张「客户报数调整」演示单 |
| A03 新条目 aud | 库位档案 id=2 及各新条目均不写 aud（biz 默认·? 圆标） |
| A03 库位档案 id=1 note | 「外购区 RW」表述顺带核对：补「＋直发虚拟仓 XNC-ZF（D-167）」半句 |
| F01 导航图【追认】 | 不画背靠背新支线；开发规则 §12.3 文字补口径 |
| 09-08 旧口径处置 | 仅派生文档更新＋沿革注记；会议纪要原文不动 |
| D 号 | D-167 单号全包（本任务一批） |
| 数据字典路径 | `P3\系统管理\数据字典.html` |
| 文档定位 | 锚文本定位禁行号 |
| Edit 锚失配 | 扩上下文重试 2 次；仍失配→失败清单跳过（禁猜测改写） |
| Windows Errno 22 | 同路径重试 3 次＋临时文件 os.replace |
| stockFlows 断言值 | 18（执行时实测≠18 以实测为准并注明） |
| 中断续跑 | 已完成步不重做，从断点续 |

## 【失败处理策略】

跳过→留档→继续；失败清单=`ROOT\agent-handoff\goal-failures-g54.md`（文件/失败原因/现场摘录）；无死路；**禁猜测改写**；轮次上限 150。

## 【纪律】

1. **禁脚本批量刷数据**：demo-data/页面一律 Edit 逐处手动改；脚本仅限验证（T7）与只读提取。
2. Edit 前：锚文本定位→grep -c 确认唯一→改后即验（node --check 按批次）。
3. 备份先行（原相对路径）；demo-data 末键/末条插入补逗号坑（RC-02 及各实体段末条）；改后必 node --check。
4. **列表驱动 cells 插列**：cells 与列头同步改，插列后跑列数断言（「列表驱动 row.cells 陷阱」）。
5. 禁 git add -A；禁 push；禁改共享件；禁动 stockFlows/库存查询数据/mobile/XNC-ZZ-*/预埋 filterV 逻辑。
6. 标注改动只动 A03 json＋notes-data.js 双处，不碰页面注入块（G52 抽屉制）。
7. 同一工作区同一时刻只允许本 agent 一个写者；每批改动前复查 `git log --since=2.hours` 无新提交。

---

## 【执行记录】2026-09-19（无人值守 goal·全缺开工→T8 收尾）

### 第 0 步 前置校验（全过）

- 并行写者：`git log --since=2.hours` 空（无新提交）；`.git` mtime=自身读取触碰，无他人写入。
- 幂等五锚点：locations 无 XNC-ZF ✓／KW 组 4 值无 KW-05 ✓／rentInOrders 6 键无 type ✓／CK-023 chain 空 ✓／TZRK-009 含「外购区 RW」✓ → **全缺开工**。
- 备份：18/18 → `backup-xnc-20260919\`（demo-data＋12 页面＋A05/A02/A06/F01 开发规则/A03 json）。
- 路径自检：31 项全 OK（9 项按实际目录修正：仓储作业/、项目管理/上下游绑定、A02/A03/开发规则真实文件名）。

### T1→T6 执行摘要

- **T1**：dictItems +KW-05（150→151）＋locations +XNC-ZF（10→11·ops=[]）＋RZRK-023 对齐；7 处 fillEntity 补 filterV（6 文件）＋预埋 2 注释更新；库位档案兜底行/类型筛选 option/状态筛选修复；数据字典 cnt 6→5。**顺带**：「外购区 RW」demo-data 全文清零 14→0（直发链 RZRK-023/TZRK-009 row+cells→直发虚拟仓；RZRK-021/022 自发历史行→成品区 RB【待追认】）。
- **T2**：rentInOrders 7 单（含终态链新单）fields+type／cells 插列 10→11／formRows 状态行后插类型行；列表列头 13→14 th＋5 静态行插类型格；新建 radio 组（默认自发单）＋直发行内提示＋联动 JS。
- **T3**：CK-023 补 chain 三节点＋「来源」改「租入单直发类型 · 系统自动生成」；终态链 3 新单 RZD-20260912-010（直发·已完结）→RZRK-20260912-024（已入库·直发虚拟仓·系统自动生成）→CK-20260912-024（已出库·类型驱动），chain/timeline 三单互联。
- **T4**：TZRK-009 库位→直发虚拟仓＋timeline「系统识别原租入单为直发类型」节点（09-03 11:10）；GHCK-001 timeline 首插「系统识别退租入库为背靠背·自动生成本单」（09-03 11:05）；退租入库新建库位旁 pn-hint。
- **T5**：otherInbounds +QTRK-20260912-004 客户报数调整演示单（直发虚拟仓账面·备注表达报数 35/账面 30）；A03 json＋notes-data.js 双处 +4 条（库位档案 id=2 直发虚拟仓·id=1 note 补虚拟仓半句去 RW／租入单列表／租入入库详情／退租入库详情·全 biz）——A03 40→43 键·NOTES_DATA 39→42 键。
- **T6**：A05（locations/rentInOrders/rentInbounds/otherInbounds/comboOutbounds/returnInbounds/rentInReturns 七节刷新＋KW 4→5 值源注记＋dictItems 146→151＋G54 登记节·实体实算 43 实体 490 记录）；A02（行 9/18/155＋头部 v6.10）；A06（关系 #27 更正＋mermaid 边注·md+html 孪生）；F01 开发规则 §12.3 划界句＋使用规则三句；09-08 旧口径排查（「背靠背类型标记」0 命中；「自动生成租入单草稿」5 处全在 P2-R01→正文 4 行注沿革＋D-27 标⊘）；拍板登记 +2 条（上游指示＋执行结论）；P2-R01 V1.10（头部＋D-27⊘＋D-167 台账行）；会后待办 #11/#12/#13/#15 ⏳→✅＋关联文档行补本任务书。

### T7 六门证据摘要

| 门 | 结果 | 摘要 |
|---|---|---|
| 1 语法门 | ✅ | node --check demo-data.js/notes-data.js 退出码 0；A03 json.load OK |
| 2 数据门 | ✅ 23 PASS/0 FAIL | locations=11·XNC-ZF.ops=[]·KW 5 值·rentInOrders=7 键 type 全含（3 直发/4 自发·任务书 6 键系终态链前口径）·CK-023 chain=3 首节点含 RZD-008·RZRK-023/TZRK-009 直发虚拟仓·GHCK/TZRK 系统节点·终态链三键互联·otherInbounds=4·stockFlows=18·「外购区 RW」=0 |
| 3 渲染门 | ✅ 63 PASS/0 FAIL | `_scan_tmpdir\g54_xnc_verify.py`：库位档案 11 行/XNC-ZF 无按钮+系统内置/筛选含虚拟仓 option；库位新建下拉含虚拟仓；数据字典库位类型主表 5 行含 KW-05；9 下拉点（7 页）运行态无虚拟仓；租入单列表 7 行·14 th=行 14 td·3 直发/4 自发；新建 radio 两态+提示；四详情页 chain/库位/系统节点；库存查询主表 15 有效行（18 键−3 组合行）·JS 0 |
| 4 审计门 | ✅ 18/18 页 0/0/0 | `_scan_tmpdir\g54_audit.py` 复用 audit_page；盘点录入首跑 harness 竞态·孤立复跑 2/2 全 0 |
| 5 grep 收口门 | ✅ | filterV 虚拟仓=9 处（8 文件·调拨×2）；XNC-ZF 全库 13 文件=预期清单（demo-data/notes-data/A03/库位档案/文档 9）；外购区 RW demo-data=0·页面/标注残留 11 处白名单化（清单见失败清单二.6） |
| 6 独立验收 | ✅ **10 PASS / 0 FAIL** | 只读子 agent（agent_27cf76fc）四要素提示词；证据 `_scan_tmpdir\g54_accept\`；含 stockFlows 备份逐键 JSON 全等+三共享件 git diff 空（红线铁证） |

**独立验收总判定行（原样）**：`总判定：10 PASS / 0 FAIL`

### 待追认清单（6 项·详见 goal-failures-g54.md 第三节）

CK-023「来源」文案／退租入库新建 hint／QTRK-20260912-004 演示单／F01 不画新支线（以上决策表 4 项）＋RZRK-021/022→成品区 RB 映射／库位档案筛选仅加 option 未接 JS。

### T8 提交

- 提交①（数据＋页面＋备份＋验证脚本）：`01e15fe`（40 文件）
- 提交②（文档＋落账）：`03593af`（12 文件）

### 复核修正（2026-09-19 晚·道远：类型口径一致）

道远复核指出单据类型约定为「直发/自发」，各处需口径一致。当日统一（提交③见下）：①radio「直发单/自发单」→「直发/自发」（命名复核定稿·不带单字）；②CK-023/024 行标签与关联文案统一「直发（系统生成）」系；③RZD-008/RZRK-023 chain 角色改类型驱动；④租赁单新建 D-27「背靠背」类型下拉整行删除；⑤租入入库确认 D-105「立即转租」勾选改「类型：直发」说明行；⑥RZD-009 来源改手动草稿；⑦A03/notes-data 2 条注记＋A05/A02/P2-R01 同步；⑧拍板登记/失败清单第五节留档。

- 提交③（口径统一修正）：`__HASH3__`
