# G19b 任务书：PC 登录页与默认登录态（38 页注入）

> **状态**：✅ 已执行（2026-09-11 无人值守）｜ **日期**：2026-09-11 ｜ **commit**：97f8f7e（工作）+ 收尾回写提交
> **执行环境**：Claude Code /goal 无人值守
> **上游**：G17 ✅ + G18 ✅ + G19a ✅（索引查证——三行齐才开工）｜ 全站口径：114 HTML = PC 109（108+登录页）+ mobile 5（G19a 已规范化）

## 【第 0 步·上下文获取】

1. 读 `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\_AGENT基线.md`（第四节铁律+G14/G19a 现状块）
2. 读 `C:\Users\Administrator\.claude\skills\prototype-design\templates\backend-template.md`（PC 登录页布局参照）
3. 抽样三页顶栏结构（头像 .avatar 位置/层级）：`我的待办.html`、`仓储作业\库存查询.html`、`系统管理\用户权限.html`
4. 项目根：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\`

## 【任务总览】

T1 新建 PC 登录页 → T2 pc-auth.js 默认登录态+38 页注入 → T3 全量验证门 → T4 三件回写。

**核心口径**：首次打开原型=默认已登录（零守卫直开）；仅当用户点「退出登录」后才进登录页；登录成功回默认态。演示零障碍。

## 【T0 前置校验与清场】

1. `_索引.md` 中 G17/G18/G19a 三行均=✅，任一不满足→写失败清单停止
2. 原型 `_data\` mtime ≥30 分钟（铁律①）
3. 存量>0 文件则 `git add -A && git commit -m "G19b前置：存量存档"`（**恢复 add -A 铁律**——本任务不并行），不 push
4. 备份到 `_scan_tmpdir\backup-g19b-20260911\`：T2 注入 dry-run 产出的 38 页清单各文件+`P3-R01-A02-页面类型与入口对照表.md`+`_索引.md`+`_AGENT基线.md`

## 【T1 新建 PC 登录页】

1. 新建 `P3-R01-包装租赁管理后台原型\登录.html`（根级独立页，与 我的待办.html 同级）：
   - 布局参照 backend-template 居中卡片式：左侧品牌区（项目名+一句话定位）/右侧登录表单（账号/密码**预填演示值** liu.dy / ······、记住登录勾选默认勾、登录主按钮、「企微扫码登录」次按钮）
   - 样式沿用 PC 端口径（#1677ff、卡片圆角 8px、btn 高 30px 体系，与 108 页一致）
   - 交互：点登录→空则行内红字提示「请输入账号/密码」（不弹窗）；非空→`localStorage.removeItem('pc-logout')`→跳 `首页\项目看板.html`；企微扫码按钮→toast「演示原型·暂未接入」
2. `P3-R01-A02-页面类型与入口对照表.md` 增订一行（独立页·G19b）

## 【T2 pc-auth.js 默认登录态 + 38 页注入】

**键设计**：`localStorage['pc-logout']`——无键或 ≠'1'=已登录（首次打开/清缓存都直达）；='1'=已退出。

1. 新建 `P3-R01-包装租赁管理后台原型\_data\pc-auth.js`（两件事）：
   - 守卫：`pc-logout=='1'` 且当前页非登录页→跳根级 `登录.html`（相对路径按 `location.pathname` 解析层级计算，不硬编码每页路径）
   - 顶栏注入：头像（.avatar）变可点下拉（JS click 切换——触屏 hover 不可靠），下拉项=当前用户名展示+「退出登录」；点击退出→`localStorage.setItem('pc-logout','1')`→跳登录页
2. **38 业务页注入**（脚本 `_scan_tmpdir\g19b_t2_inject.py`：读取-`</body>` 前插 `<script src="../_data/pc-auth.js"></script>`（根级页为 `_data/`）+assert 注入数=38）：先 dry-run 产出清单再备份再 apply。**不注入**：69 弹窗预览页、F01、mobile 五页、登录页自身（防死循环）
3. mobile `m-auth` 键域独立，PC 不读不写

## 【T3 验证门（全部机器判定，输出贴执行记录）】

1. `登录.html` 存在；A02 含其登记行
2. 注入断言：38 业务页 grep `pc-auth.js` 恰 38 处；弹窗/F01/mobile/登录页=0
3. Playwright（`_scan_tmpdir\g19b_t3_verify.py` 留档，登录态流三段）：
   - **默认已登录**：清 localStorage 后开 我的待办.html→URL 不变不跳登录 ✓
   - **退出流**：点头像下拉→退出登录→URL=登录.html；再开库存查询→被守卫跳登录页 ✓
   - **登录流**：登录页点登录→落 项目看板.html；再开待办→不再跳 ✓
   - 空账号点登录→行内提示出现 ✓；企微按钮 toast ✓；全程 JS 0
4. audit 回归：`_scan_tmpdir\audit_interaction.py` 全量 114 页口径→死链 0/JS 0/相对 g14baseline diff **新增 0**（pc-auth 注入若致探测变化逐条核对，误报记注记；audit 前脚本内预置 `localStorage.removeItem('pc-logout')` 防守卫跳转干扰——G15 f01-fab 事故同款预防）
5. 菜单门 `v32_menu_verify.py` 14 项全过（侧边栏零改动）
6. 失败清单 `_scan_tmpdir\g19b-failures.md` 结论行

## 【T4 收尾三件回写（硬性）】

1. 本文档 ✅+commit 哈希+「## 执行记录」（T0-T3 逐组+验证门输出+偏差）
2. `_索引.md` G19b 行；`_AGENT基线.md` 快照滚动（PC 登录页+pc-auth 机制+113→114 页口径+audit 基线注记+G19a 成果一并入块——G19a 当年未滚基线，此处补齐）
3. `git add -A && git commit -m "G19b PC登录页与默认登录态：..."`——**不 push**

## 【默认决策表（无需向任何人提问）】

| # | 决策点 | 预定值 |
|---|---|---|
| D1 | 「记住登录」勾选 | 视觉件（静态勾选），不接逻辑 |
| D2 | 企微扫码按钮 | 占位（toast「演示原型·暂未接入」） |
| D3 | 登录页放根级还是系统管理 | 根级 `登录.html`（对照 我的待办.html 先例） |
| D4 | 顶栏下拉实现 | JS click 切换；下拉项=用户名展示+退出登录 |
| D5 | PC 与 mobile 登录态打通 | 不打通（pc-logout 与 m-auth 独立键域） |
| D6 | 注入遇页无 .avatar 头像 | 跳过下拉注入、仍注入守卫 script；差异记偏差清单 |
| D7 | pc-auth.js 对 file:// 协议 | pathname 解析对 file:// 成立；审计 harness 接管 go() 页面照常工作 |
| D8 | G19a 若失败未完成（索引非✅） | 本任务停止（前置硬性）；不代跑 G19a |

## 【失败处理策略】

- T1/T2 任一组 assert 失败→跳过+记 `_scan_tmpdir\g19b-failures.md`+继续；T2 注入数≠38→列差异页逐个核对（结构异常页单独记），可部分交付（守卫 script 全量优先于下拉）
- audit/守卫冲突→按 T3.4 预置清键；再冲突→记失败清单
- 全部组跑完才结束；结论行必写

## 【纪律】

1. 单写者（T0.2 mtime 检查）；用完即关
2. HTML 批量改动=读取-精确替换+assert 计数；新增 onclick 自带 DOM 副作用（铁律⑦——下拉/退出点击即有行为）
3. URL/onclick 语境不碰（G15 事故）
4. demo-data.js 零改动；`git add -A`（铁律恢复）；全程不 push
5. 证据先于断言：每道验证门输出贴执行记录后才算过

## 执行记录（2026-09-11 · Claude Code 无人值守 · commit 97f8f7e）

### 前置校验

4/4 过（G17/G18/G19a 三行 ✅ 查证·demo-data 闲置 108min·G19b 行=⏳ 登记行按先例放行）。**执行期间并行事件**：G16a 会话（A06 关系图拆分+渲染页·0d88afb）与本任务窗口重叠——其开工前将本任务进行中工作分股存档 9195ca0 并协作完成 38 页注入（见 B2）。

### T0 清场

存量存档 24f3a73（zip/mermaid.min.js/g16a 探测文件·add -A 铁律恢复）；38 业务页探测（topbar+sidebar 特征）恰 38/38；备份 backup-g19b-20260911（38 页+A02/索引/基线）。

### T1 登录页+A02

`登录.html` 98 行（根级·居中双栏卡片：左品牌区[项目名+定位+三标签]/右表单——预填 liu.dy/123456、记住登录静态勾选 D1、空值行内红字 #errUser/#errPwd、企微按钮 toast「演示原型 · 暂未接入」D2、登录=removeItem('pc-logout')→首页/项目看板.html）；A02 v3.3 版本注记+P-L 登记行（与 G16a 的 v3.4 注记自然合并在同一头部行）。

### T2 pc-auth.js+注入

`_data/pc-auth.js` 80 行：守卫（pc-logout=='1' 且非登录页→跳登录页；currentScript 相对层级解析地址·不硬编码）+头像下拉（.avatar JS click·liu.dy（演示）+退出登录·点外部收起·退出=setItem→跳登录页·铁律⑦自带 DOM 副作用）。
**38 页注入=协作完成（B2）**：本会话 dry-run 38/38 可注入后，G16a 会话分股存档时一并注入（prefix 形态与本会话计划逐字一致）；本会话未重复写回，改为全量验收：**引用恰 38 处·</body> 前正确位置 38/38·尾标签配平 38/38·弹窗/F01/mobile/登录页排除面 0**。

### T3 验证门 6 条（全过）

1. 登录.html 存在+A02 P-L 行 ✅ 2. grep 38 恰+排除面 0（g19b_inject_check.txt）✅ 3. Playwright 三段流 8 断言+JS 0（g19b_t3_pw.txt）：段1 清键直开待办不跳✓/段2 头像下拉退出→登录页+库存查询被守卫拦截✓/段3 空值行内提示双现✓+企微 toast✓+登录落看板✓+再开待办守卫解除✓ 4. audit **115 页**死链 0/JS 0/g17baseline 逐键 diff 新增 0（35 条既有·新页 登录.html/A06 渲染页/mobile登录 全 0 问题·pc-auth 零探测变化）✅ 5. 菜单门 14/14 ✅ 6. g19b-failures.md 结论行=终版 ✅

### T4 收尾

任务文档 ✅+本记录；_索引 G19b 行=✅ 97f8f7e；基线滚动（标题 G19b 后+原型 bullet 113→**115** 口径[G19a 规范化+G16a A06 渲染页+G19b 登录页/pc-auth 机制三件一并入块·**G19a 补滚完成**]+当前任务块+文档地图 A06 V1.1）；记忆库双写。

### 偏差 4 条（B1-B4 全闭环，详见 g19b-failures.md）

B1 页数 114→115（G16a 并行 A06 渲染页 +1·A02 v3.4 已注记）｜B2 38 页注入由 G16a 会话协作完成·本会话全量验收（协作无损·无双重写入）｜B3 diff 基线=g17baseline（任务书写 g14——g14 口径已被 G17 收口取代·35 条既有含 G18 期读数）｜B4 audit 清键预防天然满足（每页独立 context·无 setItem 路径）。

### 注记

- 并行协作时间线：24f3a73（G19b 前置存档）→ 本会话 T1/T2 脚本+登录页+pc-auth.js → 9195ca0（G16a 会话分股存档 G19b 进行中工作+协作注入 38 页）→ 0d88afb（G16a 完成）→ 本会话验收+T3+97f8f7e。
- 默认决策表消费：D1 记住登录=静态勾选、D2 企微=占位 toast、D3 根级、D4 JS click 下拉、D5 键域独立、D7 file:// pathname/currentScript 解析成立、D6 未触发（38 页均有 .avatar）。
