# G06 · F01 导航图修正：租赁四线经库存联系+术语对齐（合并命令任务段 T1）

> **元信息**：G06 ｜ 日期 2026-09-09 ｜ 状态 ✅ 已完成 ｜ commit：66ebd90（执行）+ 回写提交（本提交后追加）
> **双层路由**：公共背景读 `agent-handoff/_AGENT基线.md`；本文件=本任务段全量细则。收尾回写三件：本文件标 ✅+执行记录+commit 回填头部 ｜ `_索引.md` G06 行挂哈希 ｜ 基线「当前任务」行标 T1 完成（goal-creator v7）。
> **需求原文（2026-09-09 道远）**：导航图租赁的四个流程应该和采买一样，也是以库存作为联系，而不是入库后直接租赁；出现很多组合出库，应该是租赁出库，和原型保持一致。
> **现状核实（2026-09-09 实测 F01 v3.0，62850B）**：①B1 买卖线有「库存」节点（采购入库→库存→销售出库），而租赁四线直连——L1「采购订单→采购入库→租赁单→组合出库」、L2「…采购入库→BOM→按 BOM 组合出库→租赁单→组合出库」、L3「租入单→租入入库→租赁单→组合出库」、L4 混合转租同口径（L4 链以文件实际为准），均无库存节点；②标签层「组合出库」16 处（含 L2「按 BOM 组合出库」1 处）、「租赁出库」0 处；href 路径串 组合出库列表.html 8 处（文件名不动）；③版本叙事 v3.0（6 处字样）。

## 前置校验（合并命令统一做，本段另加）

- `git log --oneline | grep -c "G06-F01修正·执行"` =0（≥1 → 本段跳过，续 T2）

## B. 任务组（只改 F01 一个文件）

1. **B1 租赁四线插入库存节点**：L1/L2/L3/L4 各在「入库环节之后、租赁链之前」插入主标「库存」节点（样式/尺寸与 B1 库存节点同构），连线改为 入库→库存→（后续链）——L1：采购入库→库存→租赁单；L2：采购入库→库存→BOM→…；L3：租入入库→库存→租赁单；L4：其入库节点后经库存再入组合/租赁链。副标注明资产口径：L1/L2=自有在库、L3=租入在库、L4=自有+租入在库（字样写死见 C 表）。
2. **B2 标签层 组合出库→租赁出库**：16 处标签全量替换为「租赁出库」（含 L2「按 BOM 组合出库」→「按 BOM 租赁出库」）；href 8 处 组合出库列表.html **不动**（文件名零移动）；页内其他语境（叙事段落/图例）中的 组合出库 一并改 租赁出库，仅路径串除外。
3. **B3 版本叙事 v3.0→v3.1**：头部版本行与 footer 叙事处更新，加一句「v3.1：租赁四线以库存为联系（与买卖线同构）·出库术语与菜单对齐（租赁出库）」；既有 v3.0 历史说明句保留。
4. **B4 验证与收尾**：几何断言+audit+grep（见 D）→ git 一提交（信息含「G06-F01修正·执行」）→ 回写三件套。

## C. 默认决策表

- 库存节点副标写死：L1「自有在库」·L2「自有在库·组合前」·L3「租入在库」·L4「自有+租入在库」；主标统一「库存」
- 「按 BOM 组合出库」→「按 BOM 租赁出库」（组合含义由 BOM 节点承载；有异道远下轮指出即改）
- 库存节点不做成可点实体（无对应单一页面；B1 库存节点若无 href 则同样无 href，样式从同）——先查 B1 库存节点是否带 href，从同处理
- 页面文件/单据层术语不改（本任务只动 F01 图内标签与叙事；页面仍 组合出库列表.html/页题组合出库——G05 术语表词条已注明两层关系）
- 几何断言口径沿 v3.0 批4 先例：新增节点/连线与既有盒零重叠（既有 13 处跨泳道装饰性重叠豁免不动）、箭头端点对齐 0 偏差、新节点不遮挡邻泳道、死链 0；断言脚本无现成则新写 `_scan_tmpdir/f01_v31_check.py`
- 版本号 v3.1 与菜单 v3.x 序列无关（F01 自身版本线：v2.8→v2.9→v3.0→v3.1）

## D. 验证门（全部满足，证据贴对话）

- 几何断言输出行：`==== F01 v3.1 几何断言：N 项全过，失败 0 ====`（N≥8：四线库存节点存在且连线经停/零重叠/端点对齐/既有豁免不动/死链 0）
- grep F01：标签与叙事层「组合出库」0 处（href 路径串 组合出库列表.html 8 处保留·逐形态贴证据）；「租赁出库」≥16 处；v3.1 字样 ≥1
- audit 全量：111 页 死链 0/JS 0（F01 外链噪音 1=豁免）/相对执行前自跑基线新增 0
- git log 新提交号一行（含「G06-F01修正·执行」）+ 本文件头部 ✅ 哈希回填 + `_索引.md` G06 行 ✅
- 失败清单 G06 行（失败 0 项或留档明细）

## 执行记录（收尾回填）

- **执行提交 66ebd90（2026-09-10 · 副机 macOS 会话）**：失败 0 项；几何断言 15 项全过、audit 111 页死链 0/JS 0/相对基线新增 0。
- **B1 四线库存节点**：L1 rect(352,282)「自有在库」/ L2 rect(288,422)「自有在库·组合前」/ L3 rect(352,562)「租入在库」/ L4 rect(336,768)「自有+租入在库」；主标统一「库存」，样式与 B1 库存节点同构（蓝底 rgba(37,99,235,0.08)+#2563eb 描边）。C 表「先查 B1 库存节点是否带 href，从同处理」命中：B1 库存节点实测带 href=仓储作业/库存查询.html → 四新节点从同带 href。L4 按文件实际链路处理：双入库折线（采购入库/租入入库）汇聚库存后再入 混合组装→租赁单→租赁出库 链。
- **B2 标签层**：可见标签/叙事 14 处全量 组合出库→租赁出库（L1/L2/L3/L4 节点 5+T1 泳道「再组合出库」→「再租赁出库」+L2 应收注「按组合出库自动汇总」+头部 sub「按 BOM 组合出库」+footer+S5 支线图内标签「组合出库录单/⟷组合出库列表」×2+SRC_DATA 弹层「泳道止于组合出库」）；「按 BOM 组合出库」→「按 BOM 租赁出库」按 C 表命中。href 路径串不动：组合出库列表.html 8 处+组合出库录单.html 2 处（C 表「文件名零移动」+死链 0 门口径）。**偏差注记**：现状核实「标签层 16 处」实含 组合出库录单.html href 2 处路径串——按 B2「仅路径串除外」与 C 表执行为 14 处替换、10 处路径串保留，grep 终态 组合出库=10 全在 href 内。
- **B3 版本叙事**：v3.0→v3.1（title/eyebrow/头部 sub 版本行/footer 版本行共 4 处升级）；头部 sub 与 footer 各加写死句「v3.1（09-09 修正）：租赁四线以库存为联系（与买卖线同构）·出库术语与菜单对齐（租赁出库）」；v3.0 历史说明句保留 3 处（头部 sub 历史括注/footer 09-08 会议句/SRC_DATA b1）。
- **B4 验证门证据**：①`==== F01 v3.1 几何断言：15 项全过，失败 0 ====`（N=15≥8）；②grep：标签叙事层 组合出库 0（href 形态证据：`组合出库列表.html`×8+`组合出库录单.html`×2 均路径串）、租赁出库 16、v3.1 6；③audit：`111 页 死链 0/JS 0（F01 外链噪音本次 0 条=豁免额度未用）/相对执行前自跑基线逐页 diff 新增 0`（基线留档 audit_results_pre_t1.json，problems 36→36；audit_results.json 已随执行提交入库供 G04 取 G06 版基线 diff）。
- **副机偏差注记**：①重叠豁免基数 rect-盒对口径实测 v3.0=1 处（批4 先例「13 处」为含文本盒宽口径）；断言机制=相对 v3.0 基线重叠集合差不扩，等价覆盖「既有豁免不动」。②audit_interaction.py/v32_menu_verify.py/v32_spotcheck.py 硬编码主机 D:\ 路径 → 新建 _scan_tmpdir/run_audit_mac.py+run_script_mac.py 路径换算包装器（既有脚本零改，符合 T2 纪律）；audit 基线 JSON 页路径为主机反斜杠分隔，diff 时归一化。③现状核实「62850B」与实测 73034B 不符（疑主机早期测量），功能计数（组合出库 24=8 列表 href+16、租赁出库 0、v3.0×6）全部吻合。④记忆库 p3r01-prototype-baseline.md 副机不存在（跨机说明），回写注记跳过。
