# G19a 任务书：移动端 H5 规范化（可与 G17/G18 并行）

> **状态**：待执行 ｜ **日期**：2026-09-11 ｜ **commit**：（收尾回填）
> **执行环境**：Claude Code /goal 无人值守
> **上游**：G16 ✅（5bfcd76）｜ **并行声明**：本任务只写 `mobile\` 7 文件+自有文档，与 G17（PC 命名统一）/G18（PC 文字清理）零文件交集，**允许同时执行**
> **姊妹任务**：G19b（PC 登录页+38 页注入）排 G17/G18 之后

> **⚠️ 移动端定位（道远 2026-09-11 拍板）：企微内嵌 H5，不是小程序。** prototype-design 的小程序规范中，**骨架件不适用**——状态栏（09:41）、胶囊按钮（capsule-btn）、手机外框（phone-frame）、小程序导航栏均**不引入**（顶部导航由企微 App 原生承载，页面内只保留 `m-header` 轻标题条现状形态）；**内容区组件规范适用**——卡片/列表行/表单/按钮/空状态/底部 Tab 的结构、间距、圆角、阴影规范照抄（配色按 D1 用项目色板）。

## 【第 0 步·上下文获取】

1. 读 `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\_AGENT基线.md`（第四节铁律+G14 mobile 现状块）
2. 读样式规范两件：
   - `C:\Users\Administrator\.claude\skills\prototype-design\components\miniprogram-components.md`（卡片/按钮/列表/表单/空状态结构与间距——主施工依据）
   - `C:\Users\Administrator\.claude\skills\prototype-design\templates\miniprogram-template.md`（取内容区/底部 Tab 形态；**跳过** phone-frame/状态栏/capsule-btn/小程序导航栏）
3. 读 `P3-R01-包装租赁管理后台原型\mobile\_m.css` + `_m.js` + 五页
4. 项目根：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\`

## 【T0 前置校验与清场】

1. `agent-handoff\_索引.md` 中 G16 行=✅
2. **仅检查 `mobile\` 目录** mtime ≥30 分钟（并行设计：PC 侧文件即使正被 G17/G18 写入也不影响本任务；**本任务写文件也仅限 mobile\+本任务文档+索引自己的行**）
3. 备份 `mobile\` 7 文件到 `_scan_tmpdir\backup-g19a-20260911\`（原相对路径）

## 【T1 差距清单（留档不改文件）】

逐项对照 miniprogram-components 与 _m.css 现状，输出差距表（组件/规范要求/现状/动作），覆盖：卡片（圆角/阴影/内边距/通栏分组）、按钮（主/次/禁用）、列表行（标题/摘要/元信息层级）、表单（标签/输入/校验态）、空状态、底部 Tab、页面标题条（m-header 保留，仅对齐字号/间距）。骨架件单列一节标「不适用·跳过」：状态栏/胶囊按钮/手机外框/小程序导航栏。落 `_scan_tmpdir\g19a-gap-list.md`。

## 【T2 重写 _m.css + 五页内容区对齐】

1. **重写 `_m.css`**：按 T1 差距表对齐组件结构与间距；配色沿用项目口径 `#1677ff`+#07c160+背景灰/卡片白/文字三级（变量命名照规范 `--primary`/`--card-bg` 等，值用项目色板；**不引入电商红**）。类名保持 `m-` 前缀；max-width 480 居中/375 视口保留
2. **五页结构对齐**：按差距表补齐内容区组件（卡片结构/空状态/表单态），不新增骨架件；`mobile\登录.html` 同步规范化。逐页「读取-精确替换+assert」；`_m.js` 业务逻辑（go/mGuard/m-auth/m-done/toast）**零改动**
3. 不改：业务功能、demo-data 消费、底部 Tab 三项、文件名

## 【T3 验证门（全部机器判定，输出贴执行记录）】

**并行安全设计：本任务不跑全站 audit/菜单门/全量死链**（避免与 G17/G18 的并行改动互相污染基线判定——那些归 G19b 收口）。

1. `_m.css` 断言：含 `--primary: #1677ff`（或等价）；grep `#ff6034|#ee0a24`=0
2. 五页结构断言：每页含内容区组件（卡片容器类）+ `m-header`；骨架件 grep `status-bar|capsule-btn|phone-frame` 五页合计=0；`_m.js` 与改前 git diff 零变化
3. Playwright（`_scan_tmpdir\g19a_t3_verify.py` 留档）：五页加载 JS 错误 0；`m-` 类渲染无缺失（关键容器可见性断言）；截图留档 `g19a-mobile-{页名}.png`×5
4. 失败清单 `_scan_tmpdir\g19a-failures.md` 结论行

## 【T4 收尾（轻量——基线块不动，G19b 统一滚）】

1. 本文档 ✅+commit 哈希+「## 执行记录」（T0-T3 逐组+差距表指针+验证门输出+偏差）
2. `_索引.md`：G19a 行更新（读最新→仅改本行→立即写回；写时若 mtime 距读 <5 秒外且内容非预期，重试一次，再败记偏差——并发防护 D13）
3. `git add "P3-R01-包装租赁管理后台原型/mobile" "agent-handoff/20260911-G19a-移动端H5规范化.md" "agent-handoff/_索引.md" && git commit -m "G19a 移动端H5规范化：..."`——**破 add -A 铁律一次**（并行防互卷，执行记录注记理由；G19b 恢复铁律）。**不 push**

## 【默认决策表（无需向任何人提问）】

| # | 决策点 | 预定值 |
|---|---|---|
| D1 | 规范配色与项目冲突（电商红 vs 企微蓝绿） | 结构取规范、配色留项目：#1677ff+#07c160 |
| D2 | 骨架件（状态栏/胶囊/手机外框/小程序导航栏） | 全部不引入（企微 H5 原生承载）；只留 m-header |
| D3 | mobile 底部 Tab 三项构成 | 不动（G14 定版：待办/库存/我的） |
| D4 | 「美感」判定 | 不可机器判定→结构断言+截图留档替代，人审在收尾报告标注 |
| D5 | _m.css 重写方式 | 手工整文件重写（CSS 允许整写；改前已备份，改后五页 PW 全验） |
| D6 | 工作区发现他人（G17/G18）改动 | 不碰、不提交、不回滚——本任务只管 mobile\ 与自有产物 |
| D7 | 索引并发写冲突 | 重试一次，再败记偏差清单继续（索引行最终由 G19b 收口兜底） |
| D8 | _m.js 意外需要联动改（如新 class 钩子） | 禁止改 _m.js；页面侧用内联适配，偏差清单注记 |

## 【失败处理策略】

- T1/T2 任一 assert 失败→跳过该组+记 `_scan_tmpdir\g19a-failures.md`+继续；T2 单页失败→该页从备份还原，其余页继续
- 全部组跑完才结束；结论行必写（0 失败 或 N 失败明细）

## 【纪律】

1. **本任务写权限边界=mobile\ 7 文件+本任务文档+索引本行**（并行安全基石，越界即停写记偏差）
2. HTML 批量改动=读取-精确替换+assert 计数，禁裸 open('w') 整页写（_m.css 整写除外，D5）
3. URL/onclick 语境不碰（G15 事故）
4. 不 push；证据先于断言
