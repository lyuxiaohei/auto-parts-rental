# 20260910-G12 · 数据驱动查漏收口

> **状态**：⏳ 待执行 ｜ **commit**：（执行后回填） ｜ **起草**：2026-09-10 道远拍板（G11 之后、G13 会议功能补全之前）
> **背景**：道远 09-10 目检发现「数据字段分类、无法交互，没有做到数据驱动」（锚点=数据字典页）。实测漏网规模：**38 业务页中 11 页、69 独立弹窗中 43 个**未引 `_data/demo-data.js`。
> **定位**：基建任务——把存量漏网页面收口到数据驱动底盘，为 G13（会议功能补全 13 项）铺底。G13 多项功能恰好落在本次漏网页面上（数据字典加交互、操作日志/我的待办加审核人筛选）。

## 前置校验（不通过即停止并写失败清单）

1. **单写检查**：原型目录最近文件 mtime ≥30 分钟无变化（尤其 `_data/demo-data.js`）——基线铁律①。
2. **未执行检查**：本文档头部 commit 哈希为空才执行；非空=已执行，停止。
3. **audit 基线自跑**：`python _scan_tmpdir/audit_interaction.py` 自跑存 `audit_results_g12baseline.json`（09-07 教训①：不引用历史报告数字）。
4. **复扫漏网清单**：全量扫描 38 业务页+69 弹窗的 `demo-data.js` 引用，产出 `_scan_tmpdir/g12-scan.json`，与本文档「现状实测」比对——若页面计数已变化（如 G13 并行改动），以实扫为准并在执行记录注记。

## 现状实测（2026-09-10 起草时点，执行时以复扫为准）

- **11 业务页未引**：仓储作业/盘点录入、基础数据/BOM、租赁管理/组合出库录单、系统管理/操作日志、系统管理/数据字典、系统管理/用户权限、财务协同/盈亏报表、采购管理/采购入库录单、项目管理/项目档案、项目管理/项目详情、首页/项目看板。
- **43 弹窗未引**（69 中）：抽样均为 新建/审核/确认 类（其他入库审核、盘点审核、调拨新建、新建产品、新增用户、停用确认、租入单审核 等）。
- demo-data 现有 **31 实体**（含 roles；users/logs/dictItems/boms 是否已存在→T0 复扫时确认，勿重复建）。

## 任务组

### T1 分类判定（只读，产出 g12-classify.json）

对 54 个未引文件逐个判定：

- **A 类=应驱动**：主体内容区含数据表格（≥6 行演示数据）或列表筛选/tab 切换数据展示。
- **B 类=白名单豁免**：纯表单（新建/审核/确认/录单主体）、无数据表格；或表格 ≤5 行演示性质。
- **预分类（供复核，实测以扫描为准）**：
  - A 类候选（6 页）：操作日志、数据字典、用户权限、项目档案、BOM、项目详情。
  - 混合（2 页）：项目看板、盈亏报表——统计卡片区静态保留+主数据表格接驱动。
  - B 类候选（3 页）：盘点录入、组合出库录单、采购入库录单（录单表单主体）。
  - 43 弹窗预判以 B 类为主；逐个复核找出漏网的详情类弹窗（若有→A 类）。

### T2 demo-data 实体补齐（仅 A 类所需，先查重）

- 按需新增实体：logs（操作日志）、dictItems（数据字典）、users（用户权限）、bomList（BOM）等——**先 grep demo-data.js 查重，已存在则复用**。
- 新实体规范：camelCase 复数命名、6-12 行、`cells` 不带外层 td（铁律⑥）、对齐既有实体风格。

### T3 A 类接线（页面改造）

- 列表类接 `list-generic.js` renderListPage 模式；详情类接 demo-data 实体渲染（沿用 26 实体改造先例）。
- **激活筛选交互**（list-generic 既有 match:contains 模式）：数据字典页字段分类筛选为道远点名项，必须可交互生效。
- 不新增页面、不移动文件、不动菜单/侧边栏、不改页面信息架构。
- 新增 onclick 须自带 DOM 副作用（铁律⑦）；写盘后 CRLF→LF 归一（G11 惯例）。

### T4 白名单留档

- 产出 `agent-handoff/g12-whitelist.md`：B 类逐文件登记（路径+豁免理由一句话）。
- B 类文件本体零改动。

### T5 验证门+收尾

- 跑全部验证门（见下）→ 失败清单留档 → 提交 → 推送 → 三件回写。

## 默认决策表（拿不准一律按此执行，不向用户提问）

1. 分类拿不准 → B 类白名单+失败清单注记（保守，不猜改）。
2. 混合页（看板/报表）→ 卡片静态+主表格驱动。
3. 录单页明细表格：演示行 ≥6 接驱动；<6 留静态并记白名单理由。
4. 审核弹窗内"审核记录/流转日志"小表格 ≤5 行 → 静态保留（B 类理由）。
5. 新实体行数 6-12 行；命名 camelCase 复数。
6. 实体已存在 → 复用不重建（字段不齐时增补字段+assert 行数不变）。
7. 表格无 tbody/异形结构 → 跳过留档（失败清单），禁猜测性改写。
8. 数据字典"字段分类"若为页签/分组区 → 接真筛选或真页签切换（演示可交互），样式沿既有。
9. 项目详情若结构复杂（多区块）→ 仅主表格区块驱动化，其余静态（执行记录注记）。
10. 推送代理端口漂移 → 扫描候选（7890 最近可用），全部不通则提交留本地+失败清单注记"待推送"。

## 失败处理策略

- 任一 assert 失败/结构不符：跳过该项 → 写入 `_scan_tmpdir/goal-failures-g12-20260910.md`（文件+条目+原因+处置）→ 继续下一项。
- 禁止卡住等待、禁止对不确定结构猜测性改写、禁止向用户提问。
- 失败清单可为空（0 失败），但文件必须存在且含结论行。

## 纪律（全文=基线 `_AGENT基线.md` 第四节，摘引铁律）

1. 原型目录同一时刻仅一 agent 写（前置校验已查）。
2. 批量改动=读取-精确替换+assert 计数，禁裸 open('w') 整页写。
3. 多股改动入库一律 `git add -A`（_data 不随 -u 入库）；提交信息仿 G07-G11 详尽风格。
4. HTML 切块替换类名 find 会吃 CSS 段，须带标签锚，改后标签配平自检。
5. Playwright 编程点击+导航等待；URL 中文 unquote()；文本断言用 textContent。
6. `row.cells` 存 td innerHTML——新实体 cells 不带外层 td。
7. 新增 onclick 须自带 DOM 副作用免审计判死按钮。
8. 备份：改动前按原相对路径备份至 `backup-g12-20260910/`。
9. 审计/断言数字一律本会话自跑（09-07 教训①）。

## 验证门（全部满足才算完成，证据形态逐条点名）

1. **audit 门**：`python _scan_tmpdir/audit_interaction.py` → 108 页 死链 0/JS 0；相对 `audit_results_g12baseline.json` 逐键 diff 新增 0（贴 diff 输出）。
2. **grep 门**：g12-classify.json 的 A 类清单逐文件含 `demo-data.js` 引用（贴计数输出：A=N，含引用=N，N=N）。
3. **PW 抽验门**（4 页，Playwright，贴 PASS 清单）：数据字典（字段分类筛选交互实测生效：切换后表格行数变化或空态提示）+ 操作日志/用户权限/项目档案（表格行 ≥6+筛选/页签可用）。
4. **白名单门**：`git status` 中 B 类文件零改动（贴 diff --name-only 与白名单交集=∅ 输出）。
5. **回归门**：`python _scan_tmpdir/verify_listfull.py` 三批断言失败 0（贴输出尾行）。
6. **失败清单门**：`goal-failures-g12-20260910.md` 存在+结论行（贴尾行）。
7. **收尾门**：git log 显示 ≥1 提交；推送成功（或失败清单"待推送"注记）。

## 收尾回写（硬性三件，缺一即补）

1. 本文档：头部标 ✅+commit 哈希+「执行记录」节（各门证据摘要+偏差注记）。
2. `_索引.md`：G12 行状态 ⏳→✅+哈希。
3. `_AGENT基线.md`：「当前基线快照」滚动更新（数据驱动漏网数 A 类清零+B 类白名单指针 g12-whitelist.md+demo-data 实体数变化+F01 已升 v3.4+4v4 弹窗已落地+107 页 fab 入口）。

## 执行记录（执行后填写）

（占位）
