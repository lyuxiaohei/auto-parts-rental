# 20260915-G33-退货退款闭环落地（方案 B）

> **状态**：✅ 已完成（2026-09-15 执行·commit a78aee2；执行记录见文末）
> **拆分来源**：G32-T1 竞品调研的落地阶段——G32 已产出调研与方案备选（`_scan_tmpdir/g32_t1_退货退款调研.md`），本包执行其推荐方案 B 的原型落地；**G32-T2 转移出库不处理**（道远 09-15 指示）
> **执行命令**：`_scan_tmpdir/g33_prompt.md`（形态 A·/goal 条件路由——本档＝全量凭据、默认决策表与执行记录处）
> **日期**：2026-09-15（立项）
> **拍板依据**：P2-R01 附录 A.5 —— D-123（退货退款闭环·🔄→本包落地）＋D-109（不红冲·已审核单据不可改，走后续单据调整做平）
> **执行前提**：单写者互斥／git clean；原型改动须备份先行

## 一、拍板口径（道远 2026-09-15 会话）

1. **形态＝方案 B**：在方案 A（双退货单＋负数行冲销＋正逆向互抵开票）之上，**增加独立退款单据**（退款从"散在核销里"升级为"独立可查可审"）。
2. **方向映射**：**采购退货 ↔ 应付退款（对供应商）**；**销售退货 ↔ 应收退款（对客户）**。
3. **岔路不作议题**：退款挂账方向以第 2 条映射为准落账（采购退货→应付退款、销售退货→应收退款），执行中不再另作归属讨论；应收账单既有「供应商应收」行 AR-20260904-015 保持不动。
4. **T2 转移出库不处理**（转下个任务）。

## 二、第 0 步·上下文获取（动手前必做）

1. `agent-handoff/_AGENT基线.md`——第四节（历史事故与真相）＋第七节（新 Agent 上手清单）**逐条适用**（按动作类型反查口径见第六节纪律段）
2. G32-T1 调研文档 `_scan_tmpdir/g32_t1_退货退款调研.md`——方案 B 的行业口径与必答四问答案，本包设计的直接依据
3. 现状骨架（新页一律改造自既有同类型页，**不新造样式**）：
   - 采购侧 → `采购管理/采购入库列表.html`
   - 销售侧 → `销售管理/销售出库列表.html`
   - 财务侧 → `财务协同/付款登记.html`
4. 数据与渲染：`_data/demo-data.js`（三新实体落此处）＋`_data/list-generic.js`（列表渲染器）
5. 工具口径：`_scan_tmpdir/audit_interaction.py`（全量审计）／`verify_listfull.py`（列表断言组）／`v32_menu_verify.py`（菜单组序）／`audit_results_g17baseline.json`（diff 基线）
6. **勿重做**：G31 已落地菜单 v4（本包在其上升 v5）／G32-T1 调研已产出（不重复调研）／**G32-T2 转移出库不处理**

## 三、任务分组（T1-T8）

### T1 双退货单页面（采购／销售）
- 新建 `采购管理/采购退货单列表.html`——骨架参照 `采购管理/采购入库列表.html`（列表驱动 + 状态页签 + 操作列）
- 新建 `销售管理/销售退货单列表.html`——骨架参照 `销售管理/销售出库列表.html`
- 列表列：退货单号／退货类型／关联原单／往来单位（供应商或客户）／物料／数量／金额／状态／日期／操作
- 状态流：**待审核 → 已审核 →（已退款）**；`?audit=1` auto-open 审核弹窗（项目惯例）
- 退货类型两值：**收货拒收**（未入库直接退）／**入库后退货**（对应袁工两场景·G32-T1 调研第三节发现 1）
- 弹窗模板（`弹窗/` 独立可开演示 + 页内嵌双层）：
  - `采购管理/弹窗/新建采购退货单.html`（含「关联原单」选择：下拉/搜索带出原单物料行与可退上限）
  - `采购管理/弹窗/采购退货单详情.html`
  - `采购管理/弹窗/采购退货审核.html`
  - 销售管理同构三个

### T2 退款登记页面（财务）
- 新建 `财务协同/退款登记.html`——骨架参照 `财务协同/付款登记.html`
- **一页双向**：退款类型字段区分「应付退款（对供应商）／应收退款（对客户）」，支持类型筛选
- 列表列：退款单号／退款类型／关联退货单／往来单位／退款金额／资金方向（收/付）／状态／日期／操作
- 弹窗模板：`财务协同/弹窗/退款登记新建.html`、`财务协同/弹窗/退款登记详情.html`
- 状态流：待审核 → 已确认（已到账）

### T3 菜单 v4 → v5（全站侧边栏同步）
- **采购管理组**：`采购入库` 之后 +「采购退货」→ `../采购管理/采购退货单列表.html`
- **销售管理组**：`销售出库` 之后 +「销售退货」→ `../销售管理/销售退货单列表.html`
- **财务管理组**：`付款登记` 之后 +「退款登记」→ `../财务协同/退款登记.html`
- 同步范围＝全部含侧边栏的页面（G31 后为 38 业务页）；菜单项数 33 → 36；`v32_menu_verify.py` 组序与项数断言同步更新
- **A02 对照表同步**：菜单树章节 + 页面类型清单（+3 列表页）+ 弹窗统计（+8）+ 页数口径（117→128）

### T4 demo-data 演示行
- 新增三实体：`purchaseReturns`（采购退货单）／`salesReturns`（销售退货单）／`refunds`（退款登记）
- 演示行：**各 3 行**，覆盖不同状态（待审核／已审核／已退款）与两种退货类型、两种退款类型
- 单号前缀（**须避开已占用前缀**：RK-采购入库／TK-退租入库／FK-付款登记／SK-收款确认）：
  - 采购退货 `CGTH-2026xxxx-xxx`
  - 销售退货 `XSTH-2026xxxx-xxx`
  - 退款登记 `TKD-2026xxxx-xxx`
- 关联字段须与既有实体互链（采购退货单关联采购入库单号、销售退货关联销售出库单号、退款关联退货单号）
- **改后必跑 `node --check`**（末尾追加记录注意逗号）

### T5 待办与审核矩阵
- `todoItems` 16 类 → **19 类**（+采购退货单／销售退货单／退款登记），每类 1 行演示条目，`link` 指向对应列表页 `?audit=1`
- `auditPermMatrix`（`系统管理/弹窗/权限配置.html`）16 项 → **19 项**，与待办一一对应（G25 口径）
- 同步文件：`系统管理/用户权限.html`、`系统管理/角色管理.html`、`我的待办.html`（统计卡与速滤 option）
- mobile 待办页若消费 todoItems 行数，同步（`mobile/` 5 页）

### T6 数据字典
- `dictItems` +2 组：
  - **THC 退货类型**：收货拒收／入库后退货
  - **TKL 退款类型**：应付退款／应收退款
- 总量 119 项 24 组 → **124 项 26 组**；数据字典页 `dic-item` 行与新建弹窗 select 同步
- 主数据引用型（供应商/客户/物料/原单号）**不进字典**（G30 口径）

### T7 标注层与文档
- A03（`P3-R01-A03-标注数据.json`）／A04（`P3-R01-A04-流程链标注数据.json`）为新页补键并重注入（告警 0）
- A05 字段字典补三实体字段行；A06 实体关系如需补关系行则同步
- **F01 业务流程导航图**：采购/销售泳道补退货支线注记（可选·不强制；做则同步 Mermaid 源）

### T8 审计与验证
- 全量审计 `python _scan_tmpdir/audit_interaction.py` → 128 页 死链 0／JS 错误 0／相对基线逐键 diff 新增 0
- 新基线快照 `_scan_tmpdir/audit_results_g33baseline.json`（审计后滚动）
- `verify_listfull.py` 补新页断言组（batch1/2/3 择一），期望值与 demo 行数一致
- `node --check` 全过；Playwright 抽验新 3 页 + 截图留档

## 四、默认决策表（执行中不再提问·逐条按此办）

### 通用
- 术语一律 P1-R04 现行词（「租赁出库」「退租入库」「采购入库」「销售出库」为现行词）
- 中文路径 python 内嵌（`io.open` encoding='utf-8'）；Windows 读写一律 `newline=''`；计数用 `str.count`/regex，禁 `grep -c`
- 文档行尾 LF；禁整页重写式覆盖
- 复核发现已合规：记录「无改动」继续，不计失败

### 页面与样式
- 新页一律改造自既有同类型页骨架（列表页复制后改标题/菜单项/实体名/列头），**不新造样式**；引用 `_data/list-generic.js` 与 `demo-data.js` 相对路径 `../_data/`
- 产线条数：每页演示 3 行；金额带千分位；日期口径 2026-09-xx
- 状态 tag 复用现有色系（待审核 tag-orange／已审核 tag-blue／已退款 tag-green）
- 弹窗宽度与 `max-height` 遵循 G29 规范（92vh 上限、无滚动条）
- 新增 onclick 必须有 DOM 副作用（openModal/closeModal/go 等），免审计判死按钮

### 值域
- 退货类型两值：**收货拒收**（未入库直接退·不产生库存流水）／**入库后退货**（已入库再退·对应袁工两场景）
- 退款类型两值：**应付退款**（对供应商·源自采购退货）／**应收退款**（对客户·源自销售退货）
- 状态流：退货单 待审核→已审核→已退款；退款单 待审核→已确认
- 单据编号前缀见下「命名与编号」

### 命名与编号
- 单号前缀（**须避开已占用**：RK- 采购入库／TK- 退租入库／FK- 付款登记／SK- 收款确认／CK- 出库／LZ- 租赁单／SO- 销售订单／PO- 采购订单）：
  - 采购退货单 `CGTH-2026MMDD-NNN`
  - 销售退货单 `XSTH-2026MMDD-NNN`
  - 退款登记 `TKD-2026MMDD-NNN`
- demo 行日期口径 2026-09-xx；每页 3 行；金额带千分位

### 落账与联动
- 退款方向映射固定：采购退货→应付退款；销售退货→应收退款（不再讨论其他归属）
- 「供应商应收」既有行（AR-20260904-015）**不动**，退货退款不写入该行
- 采购退货审核**不修改**原采购入库单（D-109 已审核单据不可改）；负数冲减以退货单自身为凭
- 待办/矩阵类目顺序：新增三类依次插在「租赁出库」之后

### 兜底自愈
- 骨架页结构不符预期（如缺列表驱动锚点）→ 退用最简静态表格 + 注明，记失败清单
- 断言数字与实测差 1~2（如页数因弹窗合并）→ 以实测为准更新任务书口径并注明，不算失败；差 ≥3 记失败清单
- 标注重注入告警非 0 → 先剥离坏块重注一次，仍非 0 记失败清单并保留原状

## 五、失败处理策略（无人值守铁律）
- 任何 assert 失败／锚点不匹配／预期外结构：跳过该项，记入 `agent-handoff/goal-failures-g33.md`（任务号＋文件＋原因），继续下一项
- 禁止卡住等待、禁止对不确定结构猜测性改写、禁止静默吞掉失败
- 前置校验任一阻塞性不符＝停止执行并写失败清单，禁止带病开工
- 收尾报告必须含失败清单结论（可为空；空则给「无失败项」结论行）

## 六、纪律（违反=返工）

- **基线第四节（历史事故与真相）＋第七节（上手清单）全部逐条适用**——按本包动作类型反查，不得用括号列举法替代：
  - 改 HTML 块 → 带标签锚（类名 find 会吃 CSS 段）＋改后标签配平自检（事故 2、铁律④）
  - 改 demo-data → 逐实体精确替换＋**必跑 `node --check`**（末位追加补逗号·G13 语法事故）
  - Playwright 校验 → 编程点击＋导航等待＋`unquote()`＋`textContent`（铁律⑤·IAB 管道缺陷）
  - 备份/还原 → 按原相对路径（丢子目录产生游离文件，审计死链当场抓）
  - 改内嵌 JS 字符串 → 必跑该页 audit（引号丢失→整页功能挂）
- 全部「读取-精确替换」脚本＋`assert` 计数＋幂等；**禁止裸 `open('w')` 整页写入**（事故 1）
- 备份先行：`P3-R01-包装租赁管理后台原型/` 整体复制至 `_scan_tmpdir/backup-g33-20260915/`（按原相对路径）
- 多股改动一律 `git add -A`（`_data/` 为 untracked，`commit -a`/`add -u` 会漏——事故 7②）
- 环境：中文路径 python 内嵌（`io.open` encoding='utf-8'）／Windows 读写 `newline=''`／行尾判断用二进制读（事故 3）
- 单写者互斥：原型目录同一时刻只允许本 agent 写（前置校验① mtime ≥30 分钟）
- 新增 `onclick` 必须自带 DOM 副作用（openModal/closeModal/go），否则审计判死按钮（铁律⑦）

## 七、验证门（全部满足才算完成·证据形态逐条点名）
1. **新页在位**：三个新列表页与 8 个弹窗模板存在——证据＝文件清单输出 + 各页 HTML 计数
2. **菜单全站同步**：菜单项 33→36、三新项在各自组内——证据＝`v32_menu_verify.py` 输出行 + 全站侧边栏出现次数（各 38 次）
3. **数据校验**：`node --check _data/demo-data.js` 退出 0；三实体行数 3/3/3——证据＝脚本输出行
4. **审计**：`audit_interaction.py` 全量 → 128 页／死链 0／JS 0／相对 g17 基线逐键 diff 新增 0——证据＝审计输出汇总行
5. **断言门**：`verify_listfull.py` 全批通过——证据＝各批 PASS/FAIL 清单行
6. **抽验**：Playwright 新 3 页交互 0 错误＋截图——证据＝脚本输出 + 截图路径
7. **回写**：任务档标完成＋执行记录｜`_索引.md` G33 行挂 commit 哈希｜`_AGENT基线.md` 快照滚动｜P2-R01 D-123 状态推进——证据＝diff 行
8. **失败清单结论行**（空则「无失败项」）
9. **git 批次**：一 goal 至少一提交（提交信息前缀 `G33`·**禁 push**），哈希回填任务书头部与索引行——证据＝`git log --oneline` 输出行

## 八、与其他任务的关系
- **G32-T1**：本包是其推荐方案 B 的落地（调研文档 `_scan_tmpdir/g32_t1_退货退款调研.md` 为准据）
- **D-122 计费红线**：退货/退款**不得触碰计费字段**（G31 红线延续）；若调研方案含计费联动，仅登记不改
- **G32-T2 转移出库**：不处理（道远 09-15 指示）

## 执行记录（开工时填）

## 执行记录（2026-09-15 · 主机 Win · ZCode 无人值守）

**前置校验**：4 项全过（mtime 仅任务书/索引属豁免；G33=⏳；仅 g33_prompt.md 豁免项；git clean）；备份 117 HTML → `_scan_tmpdir/backup-g33-20260915/`。

**T1/T2（三新页+8 模板）**：三列表页分别改造自 采购入库列表/销售出库列表/付款登记（不新造样式）；退货类型两值（收货拒收/入库后退货）、退款类型两值（应付退款/应收退款）、状态流 待审核→已审核→已退款／待审核→已确认；`?audit=1` auto-open ✓；弹窗页内嵌与 `弹窗/` 模板双层同步（新建/审核/详情，详情=openGenericDetail 数据驱动）。合计 117→**128 HTML**（PC 123=41 业务页+79 模板+3；mobile 5）。

**T3（菜单 v5）**：38 既有业务页（37 子目录页+我的待办根页）+3 新页侧边栏同步；菜单项 33→**36**（采购退货/销售退货/退款登记各入组）；v32_menu_verify 更新为 v5 断言基线（⑥b/⑥c/⑥d 新增）。

**T4（demo-data）**：+purchaseReturns/salesReturns/refunds 三实体（各 3 行·CGTH-/XSTH-/TKD-·销售退货关联原单键按 fields.so 实读反查）；dictItems **119→126 项 24→26 组**（THC×2+TKL×2+**DJ-17~19×3**——任务书预估 124·实加 DJ 三项以维持「DJ=todoItems 实读序=审核矩阵同值源」G30 不变量·差 2 按默认决策表以实测为准）；todoItems 16→**19**（三类插「租赁出库」行后）；`node --check` 退出 0。

**T5（待办/矩阵）**：auditPermMatrix 三文件 16→**19** 项一一对应（权限配置/用户权限为 14+3=17 → **发现 G25 漂移**：两文件缺付款登记/收款确认（角色管理有）→ 按 G25 口径 checked 补齐至 19·已在失败清单注记）；我的待办速滤 option +3；mobile 待办动态消费 todoItems（注释 16→19）。

**T6（数据字典）**：dic-item 24→**26 行**（退货类型 2/退款类型 2）+待办单据类型 cnt 16→19+createModal select +2；字典项表数据驱动自动渲染新组。

**T7（标注层/文档）**：A03 +3 页键（每页 2 pin）+A04 +3 页键（每页 1 流程链 pin）→ 双轮重注入（A03 先→A04 后）**告警 0**；修复 BOM维护 #3 pin 锚点残留（「配方行」div 已被 G31 改版移除→改挂 `<th>配方来源</th>`·非本包引入）；A02 v5 增订 15 处（117→128 口径）；A05 头部统计+G33 登记块（3 实体 26 字段行·351 行）；F01 退货支线注记未做（任务书可选项）。

**T8（审计与验证）**：
- `audit_interaction.py` 全量 → **128 页／死链 0／JS 0／相对 g17 基线逐键 diff 新增 0／mobile 5 页各自 0/0**（首轮 4 项新页问题——3×detailModal 不可达+1×只读输入——经 openRetDetail onclick 直达接线+展示框改造清零后复跑 PASS）；`g17_audit_diff.py` 随 G33 更新（117→128+11 新页键）；**g33baseline 落盘**（audit_results_g33baseline.json·128 页）。
- `verify_listfull.py`：+3 断言组入 batch2 → **batch1 103/0 + batch2 118/0 + batch3 43/0 全绿**（batch1 首跑 1 失败=租赁出库 stab 期望 9/1 为 **G31 T6 溯源行未随更新之遗留漂移**·按实测补正 10/2·非本包引入）。
- `v32_menu_verify.py` → **20 项断言失败 0**（⑥d 菜单合计 36·⑥b/⑥c 两新组项）。
- Playwright 抽验三新页 → **21 项断言 0 失败**（JS 0/3 行渲染/stab=3/selected/createModal 开合/?audit=1/详情 onclick），截图 `_scan_tmpdir/g33_shots/`×3。

**红线核对**：零计费字段改动（D-122）✓；未改任何已审核单据、无红冲（D-109·审核备注明示「不修改原入库单」）✓；「供应商应收」AR-20260904-015 未动 ✓；G32-T2 转移出库未处理 ✓。

**失败清单**：见 `agent-handoff/goal-failures-g33.md`（结论：无失败项·3 条非阻塞注记）。
