# G07 · 退租两事件模型与台账合并（F01 T1 重画 + 在租/租出台账并入）

> **元信息**：G07 ｜ 日期 2026-09-10 ｜ 状态 ✅ 已完成 ｜ commit：594df5e（执行）+ 回写提交（本提交后追加）
> **双层路由**：公共背景读 `agent-handoff/_AGENT基线.md`；本文件=本任务全量细则。收尾回写三件：本文件标 ✅+执行记录+commit 回填头部 ｜ `_索引.md` G07 行挂哈希 ｜ 基线「当前任务/快照」行更新（goal-creator v7）。
> **需求原文（2026-09-10 道远，会话讨论后拍板「可以」）**：①退租应该只有 2 种——租赁出库后做退租入库（=客户退租）、或做租入归还（=归还供应商），不应分成 4 种；且纠正理解：退租入库与租入归还不是并列分支，是两个方向不同的事件（租入资产串联：客户退租回库后再归还供应商；租入未转租可直接归还）。②在租台账和租出台账重复，按实用性合并——不必要统计砍掉，两页不必独立存在。

## B. 任务组（已全部执行）

1. **B1 F01 T1 重画两事件模型**：入口改「租赁单『退租』」（href 租赁单列表，去"客户申请"字样）→ 退租入库（副标：客户退租统一入口·验收+缺损核对）→ ①缺损转应收/应付（不变）→ ②完好按资产归属分流两路：自有→在库五态（整器或拆后零件统一按单一产品记录）→循环再出租（租赁单列表）；租入→租入在库（库存查询）→租入归还（关联租入单·同步明细·分批）→租金应付。四路 L1-L4 分支与「↩ 属」注记取消（改 ↩ 自有[L1/L2]/↩ 租入[L3/L4]）；底部三行口径注记（L4=两事件按明细行归属组合 / 租入未转租直接归还不经退租入库 / 入库仅更新库存与财务解耦）。
2. **B2 F01 连带修正**：L4「混合组装」节点改「组合 C · BOM 计算」（副标：自有+租入组件·出库按 BOM 扣减[无组装单]——09-08 结果导向落实，消除图首"四模块移除"与图内组装节点的自相矛盾）；S5「租出台账」节点改「退回进度」（href 租赁单列表）；S6 审核弹窗 18→15 类（与 S6 自身"15 类单据"文字对齐）；版本 v3.1→v3.2（title/eyebrow/头部 sub/footer 4 处+changelog 句）；SRC_DATA t1 追加 09-10 拍板条目、l4 追加 v3.2 条目（09-04「混合组装」历史引语保留）。
3. **B3 台账合并——租出台账并入租赁单列表**：删 `租赁管理/租出台账.html`；租赁单列表列手术：+「退回进度」列（已退回/数量，如 20/60 套）、去「约定归还日期」「租赁天数」列（09-08 去租期口径落实·无数据源）、「起租日期」→「建单日期」（列头+筛选两处）；静态 9 行+demo-data leaseOrders 9 行 cells/info 同步手术（租期 info→退回进度）；审核/详情弹窗「租期 91 天」改「已退回 0 套」（页面+模板双层）；ops 已有 退租入库/出库 按状态分布——零改动。
4. **B4 台账合并——在租台账并入库存查询**：删 `租赁管理/在租台账.html` 与 `租赁管理/弹窗/报废确认.html`（宿主消失；报废走其他出库）；库存查询加「客户在租」行内入口（stockFlows 实体 10 行 ops +静态 10 行）→ rentDrillModal 下钻弹窗（5 行：编码/产品/客户/项目/在租数量/资产来源/状态/操作）→ trackModal 资产轨迹（页内 openTrack(key) 渲染 assetTracks 实体 info/timeline，避免与 flowModal 的 detailTitle/detailBody id 冲突）；口径 hint 追加一行；`弹窗/器具出租履历.html` 模板保留、注入标记改挂靠库存查询（rentTracks 数据源不变）。
5. **B5 菜单 v3.3 与全站侧边栏**：38 页侧边栏移除两菜单项（`../租赁管理/` 形态 37 页 + 根目录形态 我的待办 1 页，各 2 项=76 处，逐文件 assert=1）；租赁管理组「租赁」小标签余 租赁单/租赁出库；`v32_menu_verify.py` 断言⑧随之更新（既有脚本本次属菜单门本体演进，非豁免）。
6. **B6 demo-data 重指向**：url 指针 在租台账→库存查询 32 处（'url' 形态 26+links.4 形态 6）、租出台账→租赁单列表 4 处；role 文案 在租台账→库存查询·客户在租（含 /应收 变体）24 处、租出台账→租赁单列表·退回进度 3 处；自由文本 6 处（cells 循环状态列/name）；assetTracks/rentTracks 两实体头部加宿主变更注记（row.cells/ops 留档不再渲染）。残余 在租台账 2/租出台账 2 均为注记性历史说明。
7. **B7 文档对齐**：A02 v3.1 增订（108 HTML=38 业务页+69 模板+F01；菜单树 v3.3；18/19 行删+20-40 升序重排号；租赁单/租入归还/退租入库/库存查询 4 行注记；结构变更注记⑥）；A03 删租出台账键+FP4-01/02 _meta.codes 改写+2 note 改写；A04 删两页键（20→18）+_meta 注记；P3-R04 5 行场景改写+1 行降 ⚠️（即将到期·无数据源·待客户确认）+更新记录（79✅/2⚠️）；P1-R01 V0.7（FP4-01/02 改写+mermaid RENT 节点+版本行+10.2 两行 G07 句+变更记录⑮）；外发 html 重转（5 图 base64 内嵌保留·banner 双注口径日期）；术语表 V0.4（在租台账/租出台账 废弃标注 3 处+新增 退回进度/客户在租 2 词）；F02 规格重提取 v3.2（节点 74/泳道 12/SRC 7 键；G05 手补演进史段恢复+v3.2 追加——重生成会覆盖头部叙事，须手工补回）。
8. **B8 快照与收尾**：重打 `包装租赁管理后台原型-20260910.zip`（134 项·含 _data 5 件·剔除嵌套旧 zip 20260907·台账/报废残留 0）；git add -A 一提交（594df5e）+ 回写三件套。

## C. 默认决策（执行中已按此处理）

- 退租两事件=「客户退租→退租入库」（全部资产统一入口）+「归还供应商→租入归还」（关联租入单·分批）；是否拆散=退租入库记录粒度（按单一产品/拆后零件）非流程分支；L4=两事件按明细行归属组合
- 混合退回录单=两张纯单据（退租入库录自有行、租入行直接租入归还），不做单入口自动分流联动（纪要未提·不加耦合）；台账/详情行级归属由「资产来源」列承载
- 台账统计砍除清单：平均循环次数/平均租期/超期数量/即将到期(7天)页签/四态卡（库存查询五态已有）/台账编号 ZL（租赁单号即编号）/约定归还列——理由：无数据源（09-08 去租期）或无动作驱动或纯重复
- 报废确认模板随在租台账删除（报废统一走其他出库报废类型）；器具出租履历模板保留改挂库存查询（独立演示价值+rentTracks 数据完整）
- 基线「已拍板」09-06 条「租期到期提醒=在租台账即将到期页签」被本任务实际替代（无数据源）——在基线中以删除线+注记处理，非删除历史
- 菜单版本=**v3.3**（侧边栏变化）；F01 版本=**v3.2**（图自身版本线 v3.0→v3.1→v3.2）；A02 版本=v3.1（增订）；术语表=V0.4；P1-R01=V0.7——五条版本线独立

## D. 验证门（证据）

- **audit 全量**：`108 页 · 死链 0 · JS 错 0 · 问题 34`；相对 HEAD 基线（36 条）逐键 diff：**新增 0**、消失 2（=两删页的 modal-unreachable）✓
- **菜单门**：`==== 菜单 v3.2 验证门：14 项断言，失败 0 项 ====`（⑧ 更新后全过）
- **渲染抽查 g07_spotcheck.py**：26 项 PASS 24、FAIL 2——两处均为断言口径误判非页面缺陷：①行 td 数比对误将 createModal 明细表 th 计入（主表 10 th+行 11 td 含复选列=全站同构正确）；②F01「混合组装」残留仅在 SRC_DATA l4 的 09-04 历史拍板引语（后随 v3.2 修正条目·历史保留口径）
- **grep 证据**：菜单两项目全站 0；租赁单列表 退回进度 th ✓；demo-data url 重指向（库存查询 57 处含既有/租赁单列表 35 处含既有）；原型 HTML 计数=108
- **zip**：`包装租赁管理后台原型-20260910.zip` 134 项、unzip -l 含 `_data/` 6 条目（5 文件+目录）、台账/报废确认 0 残留
- git：执行提交 594df5e + 回写提交（哈希回填头部）

## 执行记录（收尾回填）

- **执行提交 594df5e（2026-09-10 · 副机 macOS 会话·道远会话内讨论拍板后直执行）**：失败 0 项；B1-B8 全过。
- **偏差注记**：①批次执行中三次 assert 中断均为锚点/引号形态勘误（leaseOrders cells 单行形态、F01 heredoc 单引号嵌套、A02 降序重排撞号），全部在写盘前失败零污染，修正后重跑；②外发 html 无 BODY 标记（G05 转换器无注释锚点），改用 banner div 正则替换+5 图 data URI 按序回填；③F02 重生成覆盖了 G05 手补的头部叙事——从 git HEAD 恢复演进史段落+v3.2 追加（此为重提取管线的固有坑，下 agent 注意）；④记忆库 p3r01-prototype-baseline.md 副机不存在，回写跳过注记。
- **遗留登记（待下轮）**：①F01 术语残留待统一清理——B1 泳道仍叫「零部件买卖」、L1/L2 节点副标「器具·资产采购/器具A/B/C」、L3 标题坏句「向供应商/供应商租入」（L6 全站替换所致）、区块标题「业务流程·按第2次沟通纪要」与主标题第3次口径打架；②即将到期提醒待客户确认（若需要须先恢复归还约定类字段）；③P1-R03 工期表范围重估待拍板项追加本次（−2 页−1 模板+退回进度列+客户在租下钻）。
