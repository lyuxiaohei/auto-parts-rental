# 20260920-G55-标注缺口补挂（任务书）✅ 已执行（2026-09-20）

> **执行基线**：开工 HEAD=30a2c26（任务书落盘本身）·完成后主提交哈希=**666a3ba**（＋哈希回填微提交）
> **执行结果速览**：T1~T10 全部落地——4 键改名救活＋14 新键＋26 新条（biz 8/dev 18）＋23 页 26 处 data-note 属性；验证门 1~6 全过＋独立验收门 7 **6 PASS / 0 FAIL**（只读子 agent 自跑取证·原样结论贴对话）；A03 42→56 键·104→130 条·notes-data 全等同步；失败清单=无失败项（T10 一处页面级留档＋登记 12 条·见 goal-failures-g55.md）

> **项目根**：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁`（下文相对路径均以此拼接）
> **来源**：道远 09-20 对话盘点「哪些开发须知未作为标注加入原型」＋拍板「逐个调整，切记不要影响原有的原型和逻辑」
> **现状基线**：HEAD=本任务开工前最新提交（执行时以 `git log --oneline -1` 实录进执行记录）；A03 现有 104 条/42 键（含 5 个弹窗旧路径键）；notes-drawer.js 已含 biz 本体放行修复（471ebd6）
> **红线**：页面文件唯一允许的改动＝给锚点元素加 `data-note="N"` 一个属性；共享件 `notes-drawer.js`/`detail-generic.js`/`demo-data.js`/页面 CSS/JS/结构**零改动**

## 第 0 步·上下文获取

1. 读 `agent-handoff/_AGENT基线.md`（口径与纪律铁律）
2. 读 `P3-R01-包装租赁管理后台原型/P3-R01-A03-标注数据.json` 结构（键=页面路径〔相对原型根〕，值=条目数组；条目字段 id/selector/title/note/fp/req/aud）
3. 读 `C:\Users\Administrator\.zcode\skills\原型标注\SKILL.md`（双轨规范与施工纪律）
4. 前置校验：`git status --porcelain` 为空；`.git` 目录 mtime 与 `git log --since=2.hours` 无并行写入（有则停止并写失败清单）

## 任务（T1~T10，逐个手动 Edit，禁脚本刷页面/数据）

### T1 死键救活（4 键 6 条·A03+notes-data 键名改现行路径）

页面 data-note 已在（上下游绑定除外），只改数据键名：

1. `租赁管理/弹窗/租赁单新建.html` → `租赁管理/租赁单新建.html`（2 条：#1 押金预留 dev、#2 免费借用 dev）
2. `租入管理/弹窗/租入单新建.html` → `租入管理/租入单新建.html`（1 条 biz 租入计费）
3. `租入管理/弹窗/归还出库新建.html` → `租入管理/归还出库新建.html`（1 条 biz 分批归还）
4. `项目管理/弹窗/上下游绑定.html` → `项目管理/上下游绑定.html`（1 条 biz 多对多）——**该页现无 data-note**：给页面「上下游绑定」卡片标题（h3.card-title，无 onclick）加 `data-note="1"`

改后逐页 PW 验证：租赁单新建 2 个 dev 锚（无 pn-biz 类，开 ?notes=1 点角标开抽屉）；租入单新建/归还出库新建/上下游绑定各 1 个 ? 圆标。

### T2 客商域 2 条

1. `基础数据/客商新建.html`：开票资料卡内「结算周期」form-label 加 `data-note="2"`，biz：title「结算周期与发票类型值源」note「发票类型、结算周期均取数据字典统一值源（发票类型／结算周期组），与物料档案供应商税率区共用同一套结算周期选项，改字典全站生效」
2. `基础数据/客商详情.html`：壳卡标题 `#dtTitle` 所在 card-head 的 h3 加 `data-note="1"`，dev：title「详情数据驱动」note「本页内容全部由 demo-data partners 按 formRows 数据驱动（信息区 sec 拆卡＝客商信息/开票资料/收货信息三卡＋明细卡＋流转卡）；调字段改数据键，不动页面 HTML」

### T3 计费口径 2 条

1. `租赁管理/退租入库新建.html`：计费/日期相关卡片标题加 data-note，biz：title「计费天数规则」note「计费天数＝止租日−起租日＋1，且不足 2 天按 2 天计（当天起租当天退回＝2 天）；止租日当天仍按持有量计租」
2. `租赁管理/租赁单新建.html`：T1 救活后新增 id=3 biz：title「计费方式直录」note「按时间周期计费的直录日租金，按次计费的录次单价；三段式租价仅作物料档案参考价，单据侧不做换算」——锚点选计费方式表单区标签（非交互）

### T4 财务守卫 2 条

1. `采购管理/采购退货单列表.html`：列表卡片标题加 data-note（新 id），biz：title「退货退款守卫」note「已付款的退货可发起退款，退款累计不超已付额；未付款的退货不立退款单，冲减应付按实际入库量重算」
2. `财务协同/付款登记.html`：卡片标题加 data-note，biz：title「付款与应付口径」note「应付账单由采购入库/租入周期生成；付款登记核销应付，多付部分转预付留抵，不自动退款」

### T5 采购订单列表 1 条 biz

`采购管理/采购订单列表.html`：卡片标题 data-note，biz：title「交期口径」note「列表日期列为要求到货日期（交期），非下单日期；供应商提前到货属正常业务，不作异常处理」

### T6 销售出库列表 1 条 biz

`销售管理/销售出库列表.html`：卡片标题 data-note，biz：title「出库关联应收」note「已完成的销售出库自动挂接应收账单，一张出库对应一条应收明细，可从行内追溯；待审核出库不挂应收」

### T7 机制件 dev 注记 5 组

1. 库存校验件：grep `data-tax="qty"` 定位实际页面（预期采购入库录单/租赁出库录单等），每页 1 条 dev：title「库存校验统一件」note「明细数量输入须挂 data-tax=qty；可用库存列由 .stock-cell 紧挂数量列渲染，量源 STOCK_MAP 单源；新页克隆勿漏」
2. 列显示设置件：`基础数据/客商管理.html` 新 id=2、`基础数据/产品档案.html` 新 id，dev：title「列显示设置件」note「列显隐由 CSS nth-child 实现，勾选状态存 localStorage（键 ksgl/wlda）；恢复默认须带可见反馈；克隆到新页须换存储键并逐列对齐 th 文本」
3. 批量设置库位件：grep `batchLocModal` 定位使用页，1 条 dev：title「批量设置库位件」note「按 th 文本找列注入 select；克隆到含下拉列的页面必须补 select 分支，否则整列控件被毁」
4. 参考价脱钩：`基础数据/产品档案.html` 新 id，dev：title「参考价与单据价脱钩」note「四参考价仅档案参考，与全部录单点单价零联动（单价手工录入）；联动规则与 6 条待定事项悬置未拍板，勿自行实现联动」
5. 库位档案：新 id=4 dev：title「类型筛选待接线」note「类型筛选下拉当前为静态装饰未接过滤逻辑，属待追认项；接线前勿据其做数据断言」

### T8 悬置提示 dev 2 条

`财务协同/应收账单.html` 新 id、`财务协同/应付账单.html` 新 id=2，dev：title「详情三卡化待拍板」note「本模块详情走专用渲染器（期段化账单），尚未切 formRows 三卡版式；切换前勿按三卡口径改造本模块数据」

### T9 归还出库列表 1 条 dev

`租入管理/归还出库列表.html`：卡片标题 data-note，dev：title「更名沿革」note「原『租入归还』，09-18 会议后更名『归还出库』（与租入入库成对）；文件名/URL/单据前缀 GHCK 不变，仅展示名变更」

### T10 其他入库列表 1 条 biz

`仓储作业/其他入库列表.html`：卡片标题 data-note，biz：title「报数调整单」note「客户报数调整类入库单用于对账面数量的客户报数校准，走人工录入，不与库存盘点勾稽」

## 默认决策表（拿不准按此执行，收尾报告注明命中项）

1. 轨道判定：面向最终用户的功能/业务规则=biz；拍板沿革/实现机制/开发约束/悬置提示=dev
2. biz 文案禁过程词（D 号/会议/拍板/人名/日期）——验证门有正则
3. 锚点选择顺序：卡片标题 h3.card-title → 表头 th → form-label/div 标签；**禁挂 button/a/带 onclick 元素**（dev 点击会开抽屉劫持功能；biz 虽已放行仍避免圆标贴在按钮文字后误触）
4. 新条目 id＝该页现有最大 id＋1；fp/req 留空字符串
5. selector 字段＝锚点元素实际 outerHTML 前 80 字符左右（照 A03 现有条目格式）
6. A03 与 notes-data.js 逐字同步（键名/条目/字段全等）
7. T7 机制件 grep 定位：命中多页时逐页都挂（每页独立条目）；grep 零命中＝该子项跳过留档
8. mobile 页本轮不挂（盘点未拍板·留档说明即可）
9. 页面锚点理想元素不存在时：换默认决策 3 的次选；全都不合适＝跳过留档
10. 与现有条目语义重复时：并入现有条目（note 追加句号衔接），不新增 id；留档说明

## 失败处理策略

- 任何 assert 失败/结构不符：跳过该项、写入 `agent-handoff/goal-failures-g55.md`（编号/现象/原因/处置）、继续下一项；禁止卡住等待、禁止猜测性改写
- 页面改动后 `git diff` 发现超出 data-note 属性（多了别的行）：立即 `git checkout -- <页面>` 还原该页，该项留档跳过
- 全部任务走完后失败清单可为空，但文件必须存在（含「无失败项」结论行）

## 纪律

- 手动 Edit 逐条施工（道远 0920 标注施工纪律：禁脚本加标注/改文案）；验证用只读脚本
- 页面 HTML 行尾保持原样（CRLF/LF 不动）；A03/notes-data 保持各自现行行尾
- 精确替换＋唯一性 assert（锚文本必须先 count==1 再改）
- 幂等：重复执行同一任务组必须零新增改动
- 同一工作区同一时刻仅本 agent 一个写者
- 提交不 push

## 验证门（全过才算完成；证据输出贴对话）

1. `node --check _data/notes-data.js` 退出码 0
2. A03 JSON 可 load；A03↔notes-data 键集与逐条目（id/title/note/aud）diff＝0 差异（只读脚本输出「N 键 M 条 全等」）
3. PW 渲染门：T1~T10 涉及页面逐页打开——biz 条断言 .pn-q 数＝该键 biz 条数；dev 条 ?notes=1 开启后点击角标开抽屉并定位（抽验 3 条）；全页 JS 错误 0
4. 功能回归门：新建客商/新建项目/自动匹配/上下游绑定入口 4 个按钮点击仍执行原功能（URL 跳转或原行为），抽屉不被误开
5. 改动面门：`git diff --stat` 中页面 HTML 改动行数＝新增 data-note 属性个数；`git diff _data/notes-drawer.js _data/detail-generic.js _data/demo-data.js` 输出为空
6. biz 新条目过程词正则（D-\d|会议|拍板|道远|袁工|王总|09-1|09-2|2026-）0 命中（输出 grep 行数）
7. **独立验收**：派只读子 agent 按上述 1~6 复验（不引用执行者自述、自查取证），输出逐项 PASS/FAIL＋总判定行，原样贴进对话；FAIL 项修复后重派（同一项最多 2 轮）

子 agent 提示词四要素——角色：只读验收员；对象：项目根+本任务书验证门 1~6；检查清单：逐项自跑命令取证；输出格式：`[PASS/FAIL] 检查项 —— 证据摘要`＋末行`总判定：N PASS / M FAIL`

## 收尾

1. 提交（信息含 G55·任务名·条目数·验证结论·不 push），哈希回填本文件头部与 `_索引.md` G55 行
2. `_索引.md` 追加 G55 行；`_AGENT基线.md` 滚动 A03 计数（104→实数）与「标注缺口已补挂」现状
3. 任务文档标 ✅＋执行记录（各任务组结果/失败清单结论/默认决策命中）
4. A03 `_meta.date` 追加一句沿革注记（2026-09-20 G55：死键救活+缺口补挂 N 条）

## 执行记录（2026-09-20）

**基线实录**：开工 `git log -1`＝30a2c26（0920 G55 任务书落盘）；前置校验 porcelain 非空两项＝用户备份 zip 放名（mtime 11:35 早于任务书提交）·裁定非并行写者放行（证据链见 goal-failures-g55.md 前置节）·提交显式列文件不卷入 zip。

**各任务组结果**：

| 组 | 结果 | 要点 |
|---|---|---|
| T1 | ✅ 4 键 5 条 | 键名改现行路径（租赁单新建×2/租入单新建/归还出库新建/上下游绑定）；上下游绑定页卡片标题补 `data-note="1"`；旧弹窗模板确认已不存在（G36 B 批退场） |
| T2 | ✅ 2 条 | 客商新建 id=2（结算周期 form-label·照任务书字面键内自 2 起）；客商详情 id=1 dev（#dtTitle 壳卡 h3） |
| T3 | ✅ 3 条 | 退租入库新建 id=1 biz（退租入库信息卡）；租赁单新建 id=3 biz（**锚改挂明细表计费方式 th**·表单区同名标签实为应收生成方式 radio·语义修正登记） |
| T4 | ✅ 2 条 | 采购退货单列表 id=3；付款登记新键 id=2（页有孤儿锚 1·编号避开） |
| T5 | ✅ 1 条 | 采购订单列表新键 id=2（同上孤儿锚避让） |
| T6 | ✅ 1 条 | 销售出库列表新键 id=2（同上） |
| T7 | ✅ 14 条 | 库存校验统一件×6（定位签名改 STOCK_MAP+.stock-cell 三件套·见登记 #4：租赁单新建#4/租赁出库录单#3/销售订单新建#1/销售出库新建#1/租赁单列表#6/销售订单列表#2）；列显示设置件×2（客商管理#2/产品档案#3）；批量设置库位件×4（锚避让库位 th 内批量设置链接→明细卡标题：退租入库新建#2/其他出库新建#1/其他入库新建#1/采购入库录单#3 挂基本信息卡）；参考价脱钩（产品档案#4 挂参考价 th）；库位档案#4（类型筛选 ff-label） |
| T8 | ✅ 2 条 | 应收#3/应付#2 dev——**零页面改动**：按任务书指定号绑定既有行锚（孤儿转活） |
| T9 | ✅ 1 条 | 归还出库列表新键 id=3（页有孤儿锚 1/2） |
| T10 | ⚠️ 落库留档 | A03/notes-data 条目＋`data-note="1"` 已挂·但该页**缺共享件两行引用**（改前状态）标注不渲染——红线禁加引用行·留档待追认（goal-failures-g55.md T10 节） |

**施工量**：23 页 25 行对 **26 处 data-note 属性**（租赁单新建单行双属性）＋A03/notes-data 各 18 个 Edit 块（4 改名+14 追加/插入）逐字镜像。

**默认决策命中项**（收尾注明义务）：

- 决策 1（轨道判定）：全部照任务书轨道（biz 8/dev 18）
- 决策 3（锚点次序）：命中多处——卡片标题被占/避让交互子元素时降级 th/form-label（库存校验挂 stock th·参考价挂 th·类型筛选挂 ff-label·批量库位挂明细卡标题）；**dev 锚含 onclick 子元素=劫持红线**（471ebd6 同型）主动避让 4 页
- 决策 4（id=max+1）：**页面孤儿锚号纳入 max 计算**（付款 2/采购订单 2/销售出库 2/归还出库列表 3/租赁单列表 6/销售订单 2/应收 3/应付 2）——drawer 按号全元素绑定·同号必撞
- 决策 7（grep 多页逐页挂）：库存校验 6 页·批量库位 4 页
- 决策 8（mobile 不挂）：本轮零 mobile 页
- 决策 9（次选锚）：采购入库录单批量库位→基本信息卡
- 决策 10（语义合并）：未触发

**验证门实录（1~6·脚本留档 `_scan_tmpdir/g55_verify.py`）**：

1. `node --check _data/notes-data.js` 退出码 0 ✅
2. A03↔notes-data：**56 键 130 条 全等**（键集 diff 无·逐条 id/title/note/aud 及全字段 0 差异）✅
3. PW 渲染门 26/26 PASS：biz 圆标数=该键 biz **活锚**数（3 页既有死锚按登记口径断言：采购退货 1/3·产品档案 0/1·库位档案 1/2·其他入库列表 0/1 缺引用）；dev 抽验 3/3（租赁单新建#4/客商管理#2/应付#2 开抽屉+定位高亮全中）；全页 JS 错误 0 ✅
4. 功能回归门 4/4 PASS：新建客商/新建项目/上下游绑定跳页正常·自动匹配建议勾选 2 行生效·抽屉零误开 ✅
5. 改动面门：23 页 HTML 25 行对 26 属性·**逐行 diff 仅含 data-note 插入**（净增 26·脚本复核 0 违例）；`notes-drawer.js`/`detail-generic.js`/`demo-data.js` diff 空 ✅
6. biz 新条目过程词正则 `D-\d|会议|拍板|道远|袁工|王总|09-1|09-2|2026-` 命中 **0** ✅
7. 独立验收：**6 PASS / 0 FAIL**——只读子 agent 逐项自跑取证（不引用执行者自述）：门1 node EXIT=0｜门2 56 键 130 条全等（id/title/note/aud 520 次比对 0 不符·biz 86→94/dev 18→36 实算）｜门3 26 页圆标=活锚数（合计 19 圆标·4 活锚口径页实测吻合）+JS 0+dev 抽验 3/3｜门4 四按钮跳页/勾选生效+抽屉零误开｜门5 23 文件 25 行对·25 hunk 剥离 data-note 后逐字相等·净增 +26·三共享件 diff 空｜门6 以 HEAD↔工作区差分独立识别新 biz 条目（8 条+迁移 3 条）正则 0 命中。结论原样已贴对话。

**失败清单结论**：`agent-handoff/goal-failures-g55.md`——**无失败项**（T10 页面级留档 1＋登记性 12 条·全部非阻塞）。

**插曲与处置**：归还出库新建 tbody 锚圆标被运行时行渲染刷掉（PW 首轮 0）→补挂归还明细卡标题稳定锚（登记 #7）；采购退货列表静态行锚被 renderListPage 重写→门 3 按活锚口径（登记 #8）。

**收尾三件套**：本文件 ✅＋执行记录（本节）｜`_索引.md` G55 行（哈希回填）｜`_AGENT基线.md` 滚动（A03 计数 42→56 键/104→130 条·标注缺口已补挂现状）＋A03 `_meta.date` 沿革句已追加。提交不 push。
