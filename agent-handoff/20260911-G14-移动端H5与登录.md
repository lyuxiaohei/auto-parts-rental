# 20260911-G14 · 移动端 H5 与登录

> **状态**：⏳ 待执行（前置 G13 已 ✅ f68fa90）｜ **commit**：（执行后回填）｜ **起草**：2026-09-11 道远拍板（三命令链 G12 数据驱动→G13 功能补全→G14 移动端之末环）
> **会议依据**：09-08 纪要十.2-4——移动端基于若依开源框架开发 **H5 页面**，绑定**企业微信**（王总强调：企微收消息点开即审）；**免二次登录**（首次登录后记忆，不要每次打开都验证）。道远 09-10 补充："额外生成一个移动端的 html，另外少了登录 html"。
> **定位**：原型层移动端演示——消费 demo-data 终态（G12/G13 后 37 实体），不改 PC 端任何页面。

## 前置校验（不通过即停止并写失败清单）

1. **G13 已完成**：`_索引.md` G13 行=✅ f68fa90（重验，被回退则停止）。
2. 单写检查：原型目录最近文件 mtime≥30 分钟无变化（尤其 `_data/`）。
3. 本文档头部 commit 哈希为空才执行。
4. audit 基线自跑存 `audit_results_g14baseline.json`（此时 mobile/ 尚不存在，应为 108 页口径；禁引用历史数字）。

## 任务组

### T1 移动端骨架（新建目录与共享件）

1. 新建 `P3-R01-包装租赁管理后台原型/mobile/` 目录。
2. `mobile/_m.css`：企微 H5 风格——375 视口基准、max-width 480px 居中（桌面可预览）、白底卡片、主体色沿 PC 主色、企微元素用 #07c160、底部 tab 三项（待办/库存/我的）。
3. `mobile/_m.js`：引 `../_data/demo-data.js`；渲染助手（卡片列表/徽标）；**免二次登录守卫**——检查 `localStorage('m-auth')`，未登录访问受护页自动跳 登录.html。

### T2 五个页面（中文文件名，沿 PC 惯例）

1. **登录.html**：账号+密码输入（空值行内提示）+「登 录」主按钮+「企业微信一键登录」按钮（企微绿）+免二次登录说明文案；登录成功→写 `m-auth` 标记→跳 待办审批.html；已登录访问本页自动跳待办。
2. **待办审批.html**：消费 `todoItems`（12 行）——顶部统计（待审 N）+卡片列表（单据类型徽标/标题/审核人/时间/状态），点卡片→审批详情。
3. **审批详情.html**：单据摘要卡+明细行（演示数据）+底部「驳回 / 通过」双按钮——点击后状态变化+toast+返回待办；无底部 tab（详情态）。
4. **库存查询.html**：消费 `stockFlows`（13 行）——搜索框+状态筛选 chips（全部/在库/客户端(租出)/客户端(转租)/退租待入库）+卡片行（编码/名称/数量/状态徽标）。
5. **我的.html**：登录人卡片（演示：王琳·商务主管·users/roles 实体取）+退出登录（清 `m-auth`→回 登录.html）+版本信息行。
6. 五页底部 tab：待办审批/库存查询/我的 三项互切（登录与审批详情无 tab）。

### T3 PC 端隔离（零改动承诺）

- 不改 PC 任何页面/侧边栏/菜单/F01/弹窗；mobile 不进 PC 菜单体系。
- demo-data.js **只读**（G12/G13 终态 37 实体复用，不新增实体；如确需字段由失败清单留档，禁直改）。

### T4 文档对齐

- A02 页面清单增 mobile 5 页（新分类「移动端 H5」）；P1-R01 附录 10.2 增 G14 记录句。

## 默认决策表（拿不准一律按此执行，不向用户提问）

1. 视口 375×812 基准、max-width 480px 居中、桌面浏览器可预览。
2. 主体色沿 PC 主色（系统一致性）；仅企微登录按钮/图标用 #07c160。
3. 免二次登录=localStorage('m-auth') 演示：登录写/我的清/四受护页守卫跳转。
4. 审批详情数据=todoItems 行摘要+静态明细演示，不深挖单据实体（最简演示形态）。
5. 登录表单=空值提示演示，不接真认证；企微一键登录=同样跳转（按钮语义演示）。
6. 中文文件名+`<title>P3-R01-M0x · 页名</title>`（M01 登录~M05 我的）。
7. audit 页数口径预注：**108→113**（mobile 5 页纳入扫描）；diff 口径=既有 108 页相对 g14baseline 新增 0+新 5 页各自死链 0/JS 0；F01 外链断网噪音豁免口径沿 G13。
8. mobile 页不参与 verify_listfull 三批与菜单门（PC 专属），但两门照常跑作回归。
9. 样式自包含（_m.css 一份共享+页内少量内联），不引外链 CDN（断网可开）。
10. 推送代理全不通→提交留本地+失败清单注记"待推送"（沿 G13 口径）。

## 失败处理策略

- assert 失败/结构不符：跳过该项→`_scan_tmpdir/goal-failures-g14-20260911.md` 留档→继续；禁卡等、禁猜测改写、禁提问；清单可为空但必须存在+结论行。

## 纪律（全文=基线 `_AGENT基线.md` 第四节，摘引铁律）

1. 原型目录同一时刻仅一 agent 写；2. 新建文件可直接 Write（非改 PC 既有页，无需精确替换；但若涉及改 PC 文件则回到精确替换+assert）；3. 入库 `git add -A`；4. 新 JS 写盘后 `node --check`（G13 逗号事故纪律）；5. Playwright 编程点击+导航等待、URL unquote()、文本断言 textContent；6. 审计数字本会话自跑。

## 验证门（全部满足才算完成，证据形态逐条点名）

1. **audit 门**：audit_interaction.py → **113 页** 死链 0/JS 0（F01 外链断网噪音豁免沿 G13 预注）+既有 108 页相对 `audit_results_g14baseline.json` 逐键 diff 新增 0+mobile 5 页各自 0/0（贴 diff 输出）。
2. **grep 门**（贴逐项命中）：mobile/5 页存在；登录.html 含「企业微信」与「m-auth」；待办审批.html 含 todoItems 渲染；库存查询.html 含 stockFlows 与「客户端(转租)」chip；审批详情.html 含「通过」「驳回」；我的.html 含「退出登录」。
3. **PW 门**（5 项贴 PASS 清单）：①未登录直访 待办审批→守卫跳 登录 ②登录填账号点登录→跳待办+localStorage 写入+列表渲染 ≥10 卡片 ③审批点「通过」→状态变化+toast ④库存 chips 筛选「客户端(转租)」→行数变化 ⑤我的点退出→清标记回登录。
4. **回归门**：verify_listfull.py 三批失败 0（103/88/43）+v32_menu_verify.py 14/14（PC 零改动佐证）。
5. **失败清单门**：goal-failures-g14-20260911.md 存在+结论行（贴尾行）。
6. **收尾门**：git log ≥1 提交；推送成功或注记"待推送"。

## 收尾回写（硬性三件，缺一即补）

1. 本文档：标 ✅+commit 哈希+「执行记录」（各门证据+偏差注记）。
2. `_索引.md`：G14 行 ⏳→✅+哈希。
3. `_AGENT基线.md`：快照滚动（108→113 页+mobile 目录结构与免二次登录机制+audit 新口径）。

## 执行记录（执行后填写）

（占位）
