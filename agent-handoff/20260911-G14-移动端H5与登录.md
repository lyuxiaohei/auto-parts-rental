# 20260911-G14 · 移动端 H5 与登录

> **状态**：✅ 已完成（2026-09-11·六门全过）｜ **commit**：c64a43e（已推送）｜ **起草**：2026-09-11 道远拍板（三命令链 G12 数据驱动→G13 功能补全→G14 移动端之末环）
> **会议依据**：09-08 纪要十.2-4——移动端基于若依开源框架开发 **H5 页面**，绑定**企业微信**（王总强调：企微收消息点开即审）；**免二次登录**（首次登录后记忆，不要每次打开都验证）。道远 09-10 补充："额外生成一个移动端的 html，另外少了登录 html"。
> **定位**：原型层移动端演示——消费 demo-data 终态（G12/G13 后 37 实体），不改 PC 端任何页面。

## 前置校验（不通过即停止并写失败清单）

1. **G13 已完成**：`_索引.md` G13 行=✅ f68fa90（重验，被回退则停止）。
2. 单写检查：原型目录最近文件 mtime≥30 分钟无变化（尤其 `_data/`）。
3. 本文档头部 commit 哈希为空才执行。
4. audit 基线自跑存 `audit_results_g14baseline.json`（此时 mobile/ 尚不存在，应为 108 页口径；禁引用历史数字）。

## 任务组

### T1 移动端骨架（新建目录与共享件）

1. 新建 `P3-R01-包装租赁管理后台原型/mobile/` 目录。
2. `mobile/_m.css`：企微 H5 风格——375 视口基准、max-width 480px 居中（桌面可预览）、白底卡片、主体色沿 PC 主色、企微元素用 #07c160、底部 tab 三项（待办/库存/我的）。
3. `mobile/_m.js`：引 `../_data/demo-data.js`；渲染助手（卡片列表/徽标）；**免二次登录守卫**——检查 `localStorage('m-auth')`，未登录访问受护页自动跳 登录.html。

### T2 五个页面（中文文件名，沿 PC 惯例）

1. **登录.html**：账号+密码输入（空值行内提示）+「登 录」主按钮+「企业微信一键登录」按钮（企微绿）+免二次登录说明文案；登录成功→写 `m-auth` 标记→跳 待办审批.html；已登录访问本页自动跳待办。
2. **待办审批.html**：消费 `todoItems`（12 行）——顶部统计（待审 N）+卡片列表（单据类型徽标/标题/审核人/时间/状态），点卡片→审批详情。
3. **审批详情.html**：单据摘要卡+明细行（演示数据）+底部「驳回 / 通过」双按钮——点击后状态变化+toast+返回待办；无底部 tab（详情态）。
4. **库存查询.html**：消费 `stockFlows`（13 行）——搜索框+状态筛选 chips（全部/在库/客户端(租出)/客户端(转租)/退租待入库）+卡片行（编码/名称/数量/状态徽标）。
5. **我的.html**：登录人卡片（演示：王琳·商务主管·users/roles 实体取）+退出登录（清 `m-auth`→回 登录.html）+版本信息行。
6. 五页底部 tab：待办审批/库存查询/我的 三项互切（登录与审批详情无 tab）。

### T3 PC 端隔离（零改动承诺）

- 不改 PC 任何页面/侧边栏/菜单/F01/弹窗；mobile 不进 PC 菜单体系。
- demo-data.js **只读**（G12/G13 终态 37 实体复用，不新增实体；如确需字段由失败清单留档，禁直改）。

### T4 文档对齐

- A02 页面清单增 mobile 5 页（新分类「移动端 H5」）；P1-R01 附录 10.2 增 G14 记录句。

## 默认决策表（拿不准一律按此执行，不向用户提问）

1. 视口 375×812 基准、max-width 480px 居中、桌面浏览器可预览。
2. 主体色沿 PC 主色（系统一致性）；仅企微登录按钮/图标用 #07c160。
3. 免二次登录=localStorage('m-auth') 演示：登录写/我的清/四受护页守卫跳转。
4. 审批详情数据=todoItems 行摘要+静态明细演示，不深挖单据实体（最简演示形态）。
5. 登录表单=空值提示演示，不接真认证；企微一键登录=同样跳转（按钮语义演示）。
6. 中文文件名+`<title>P3-R01-M0x · 页名</title>`（M01 登录~M05 我的）。
7. audit 页数口径预注：**108→113**（mobile 5 页纳入扫描）；diff 口径=既有 108 页相对 g14baseline 新增 0+新 5 页各自死链 0/JS 0；F01 外链断网噪音豁免口径沿 G13。
8. mobile 页不参与 verify_listfull 三批与菜单门（PC 专属），但两门照常跑作回归。
9. 样式自包含（_m.css 一份共享+页内少量内联），不引外链 CDN（断网可开）。
10. 推送代理全不通→提交留本地+失败清单注记"待推送"（沿 G13 口径）。

## 失败处理策略

- assert 失败/结构不符：跳过该项→`_scan_tmpdir/goal-failures-g14-20260911.md` 留档→继续；禁卡等、禁猜测改写、禁提问；清单可为空但必须存在+结论行。

## 纪律（全文=基线 `_AGENT基线.md` 第四节，摘引铁律）

1. 原型目录同一时刻仅一 agent 写；2. 新建文件可直接 Write（非改 PC 既有页，无需精确替换；但若涉及改 PC 文件则回到精确替换+assert）；3. 入库 `git add -A`；4. 新 JS 写盘后 `node --check`（G13 逗号事故纪律）；5. Playwright 编程点击+导航等待、URL unquote()、文本断言 textContent；6. 审计数字本会话自跑。

## 验证门（全部满足才算完成，证据形态逐条点名）

1. **audit 门**：audit_interaction.py → **113 页** 死链 0/JS 0（F01 外链断网噪音豁免沿 G13 预注）+既有 108 页相对 `audit_results_g14baseline.json` 逐键 diff 新增 0+mobile 5 页各自 0/0（贴 diff 输出）。
2. **grep 门**（贴逐项命中）：mobile/5 页存在；登录.html 含「企业微信」与「m-auth」；待办审批.html 含 todoItems 渲染；库存查询.html 含 stockFlows 与「客户端(转租)」chip；审批详情.html 含「通过」「驳回」；我的.html 含「退出登录」。
3. **PW 门**（5 项贴 PASS 清单）：①未登录直访 待办审批→守卫跳 登录 ②登录填账号点登录→跳待办+localStorage 写入+列表渲染 ≥10 卡片 ③审批点「通过」→状态变化+toast ④库存 chips 筛选「客户端(转租)」→行数变化 ⑤我的点退出→清标记回登录。
4. **回归门**：verify_listfull.py 三批失败 0（103/88/43）+v32_menu_verify.py 14/14（PC 零改动佐证）。
5. **失败清单门**：goal-failures-g14-20260911.md 存在+结论行（贴尾行）。
6. **收尾门**：git log ≥1 提交；推送成功或注记"待推送"。

## 收尾回写（硬性三件，缺一即补）

1. 本文档：标 ✅+commit 哈希+「执行记录」（各门证据+偏差注记）。
2. `_索引.md`：G14 行 ⏳→✅+哈希。
3. `_AGENT基线.md`：快照滚动（108→113 页+mobile 目录结构与免二次登录机制+audit 新口径）。

## 执行记录（2026-09-11 · 执行 agent 回填）

### 前置校验（4/4 过）

1. `_索引.md` G13 行=✅ f68fa90（重验命中）。
2. 原型目录最近文件 mtime=2026-09-10 20:55（demo-data.js），距今 ~11 小时 ≥30 分钟，无并行写入。
3. 本文档头部 commit 哈希为空 ✓。
4. 基线自跑 `audit_interaction.py` → 复制为 `audit_results_g14baseline.json`：**108 页·死链 0·JS 错 0·既有问题 34 条入基线**（F01 外链噪音本次未现，网络正常）。

### 任务执行（T1-T4 全落地）

- **T1**：`mobile/` + `_m.css`（企微 H5 风：375 基准·max-width 480 居中带桌面预览阴影·白底圆角卡片·主色沿 PC `--primary` #1677ff·企微元素 #07c160·底部 tabbar 三项；类名一律 `m-` 前缀避免与 PC 类名体系冲突，规避 audit 清扫类 `.tab/.stab/.radio/.checkbox/.sm-link/.modal-overlay`）+ `_m.js`（`go()` 全局跳转=审计 harness 接管点·`mGuard()` 600ms 延迟守卫[防 goto 中断]·`m-auth`/`m-done` localStorage 读写·`mToast`·状态/单据类型徽标配色·`mQtyFromCells` 合计取数）。`node --check` ✅。
- **T2**：五页全部 Write 直建，内联 JS 逐页提取 `node --check` 5/5 ✅、div/span/button/a/script 标签配平自检 0 偏差：
  - M01 登录：账号+密码（空值行内提示）+「登 录」+「企业微信一键登录」（#07c160）+免二次登录说明绿条；成功内联写 `m-auth`（字面量在本页，grep 门要求）→500ms 跳待办；已登录访问反跳待办（`mGuardAuthed`）。
  - M02 待办审批：todoItems 12 行卡片（类型/状态双徽标·单号·摘要·审核人/提交人/时间）+统计 8/4/12；点卡→审批详情（?id=）。
  - M03 审批详情：摘要卡（6 行 m-dl）+明细卡（首行 summary 正则解析+2 静态演示行，页面注明演示数据）+底部固定「驳 回/通 过」双按钮：点击→徽标 待审核→已通过/已驳回+toast+双钮禁用+900ms 返回待办；结果写 `m-done` 待办回显（退出登录时清除）；无底部 tab。
  - M04 库存查询：stockFlows 13 有效行（3 个 ZH-* 组合行无 fields.status 不计入）+搜索（编码/名称/库区）+五 chips（全部/在库/客户端(租出)/客户端(转租)/退租待入库）+合计数量取 cells 含 `<b>` 列（13/13 行验证）。
  - M05 我的：王琳·商务主管卡片（角色名 roles 实体校验取数——users 实体无王琳，见偏差 1）+企微绑定/数据来源/PC 端/版本四信息行+退出登录（清 `m-auth`+`m-done`→600ms 回登录）。
  - 底部 tab：待办/库存/我的三项 `<a href>` 互切（登录与审批详情无 tab）。
- **T3**：PC 零改动——`git status` 仅 mobile/ 与文档/工具文件；demo-data.js 未触碰（sha 前后一致）；verify 三批+菜单门作回归佐证。
- **T4**：A02 头部范围 108→113+日期 v3.2 增订行+新增「七、移动端 H5」节（5 页表）+变更注记第 10 条；P1-R01 附录 10.2 P3-R01 长行尾追加「09-11·G14 移动端 H5 与登录」记录句+A02 台账行 v3.2 注记。

### 验证门（6/6 全过，证据形态=本会话自跑输出）

1. **audit 门 PASS**：post `audit_results.json` **113 页**（PC 108+mobile 5）·全站死链 0·JS 错 0（F01 外链断网豁免 0 条用到）·既有 108 页 problems 逐键新增 **0**（`g14_audit_diff.py`）·mobile 5 页各自 **死链 0/JS 0/问题 0**；受护 4 页 nav_anomaly=True=守卫跳登录属预期（audit 每页独立上下文无 m-auth，harness 实际清扫登录页 0 问题，守卫行为由 PW 门①独立验证）。
2. **grep 门 13/13**：5 页存在+登录「企业微信」「m-auth」+待办 todoItems+库存 stockFlows 与「客户端(转租)」+详情「通过」「驳回」+我的「退出登录」全命中。
3. **PW 门 5/5**（`g14_pw_gate.py`，编程点击纪律）：①未登录直访待办→守卫跳登录 ✓ ②登录→跳待办+m-auth 写入+12 卡片（≥10）✓ ③点「通过」→待审核→已通过+toast「已通过·单据进入下一环节…」+返回待办后已通过徽标 ✓ ④chips 客户端(转租) 13→3 ✓ ⑤退出→m-auth=None 回登录 ✓。
4. **回归门**：verify_listfull batch1/2/3 = **103/88/43 断言失败 0**；v32_menu_verify **14/14**（PC 零改动佐证）。
5. **失败清单门**：`_scan_tmpdir/goal-failures-g14-20260911.md` 存在，**0 失败·6 偏差注记**（users 无王琳按 roles 取/nav_anomaly 预期/m-auth 字面量首轮 MISS 改内联即中/F01 豁免未用到/stockFlows 13 有效行口径/明细静态演示），结论行在文件尾。
6. **收尾门**：commit `c64a43e`（git add -A）；**推送成功**——直连 `git push origin main` 通（网络已恢复），`0996252..c64a43e` 共 **11 个积压提交一并推送**（G12 前存档 29e4496 → G14 落地 c64a43e）；四代理端口 7890/10808/10809/14157 仍无监听（直连已可用，代理不再必要）。

### 偏差处置（详见失败清单 6 条）

最有信息量两条：①users 实体无「王琳」（王琳=todoItems 审核人）→登录卡片按 roles 实体取「商务主管」，未改 demo-data（T3 只读承诺）；②登录页 m-auth 字面量初版封装在 _m.js mLogin() 致 grep MISS → 改登录页内联写标记（语义更贴任务书），_m.js mLogin 保留为共享 API。
