# G54 失败清单（背靠背直发与虚拟仓完全闭环 · 2026-09-19）

**结论：无失败项。** T1→T8 全部执行完毕；T7 六门全过（语法门 0 错／数据门 23 PASS·0 FAIL／渲染门 63 PASS·0 FAIL／审计门 18/18 页 0·0·0／grep 收口门全达标／独立验收 10 PASS·0 FAIL）；以下均为登记性记录与顺带修复说明。

## 一、顺带修复（任务书 T1 明列的既有缺陷）

| # | 项 | 现场 | 处置 |
|---|---|---|---|
| 1 | 库位档案「状态」筛选失配 | 静态 option 为「待审核/已审核/已完成」，数据值域=启用/停用 | 改为「全部/启用/停用」（T1.6） |
| 2 | 数据字典「库位类型」cnt 陈旧 | 左列表写死 6，实际组值 4→（本任务后）5 | 改为 5（T1.7） |
| 3 | 预埋 filterV 注释过期 | 「虚拟仓不维护 D-006·过滤」与 D-167 新口径矛盾 | 两处注释更新为「人工单据不选虚拟仓（D-167·直发虚拟仓由系统自动单据流转）」（T1.5） |

## 二、登记性差异（任务书数字/口径 vs 实测·非缺陷）

1. **任务书 T7.2「rentInOrders=6 键」**：系终态链（T3.3）新增 RZD-20260912-010 之前的口径；实测 **7 键**（3 直发/4 自发），「fields 均含 type」通过。任务书内部 T3 与 T7 数字未对齐，以 T3 造单结果为准。
2. **任务书 T7.4「租入单列表 12 th=11 列」**：实际列头 13 th→插「类型」后 **14 th**（勾选框+租入单号+11 数据列+操作），行 td=14 与 cells 11 列配平（键列+11 cells+勾选+操作）。任务书算术系约数。
3. **任务书 T7.3「类型筛选『虚拟仓』命中 1 行」**：库位档案筛选 select 为**装饰性静态控件**（无 JS 接线·全站筛选卡先例·任务书第 0 步锚点自证「筛选静态 option」）；T1.6 原文=「option 追加『虚拟仓』」已照做。未擅自接线 JS（不得自行扩范围）；如需筛选真过滤待另行拍板（见待追认清单）。
4. **任务书 T7.3「库存查询 tbody=18 行」**：主表实际 **15 有效行**（stockFlows 18 键−3 个 ZH-\* 组合行无 fields.status 不计·G25 既有口径）；stockFlows 数据本体 18 键零改动（独立验收已用备份逐键 JSON 全等证明）。
5. **审计门盘点录入首跑竞态**：`Page.evaluate: Execution context was destroyed`（harness framenavigated 竞态），孤立复跑 2/2 全 0——非页面缺陷，计入 18/18。
6. **「外购区 RW」页面/标注层残留清单（白名单）**：demo-data 侧已清零；以下 11 处为静态兜底/历史档案（运行时被数据驱动渲染覆盖或为不可变历史材料），不在 G54 改动面、不清理——①其他入库新建/其他出库新建/调拨新建×2/销售出库新建/采购入库录单（批量库位弹窗）共 6 处静态 `<option>外购区 RW</option>`（无 JS 兜底）②我的待办 1 处调拨摘要文本「外购区 RW → 成品区 RB 300 只」③租入入库列表静态兜底 3 行/退租入库列表静态兜底 1 行④A04-流程链标注数据.json 1 条（G52 后归档层）⑤文档沿革白名单（P2-R01 D-154 沿革/A05 沿革注记/P3-R05/agent-handoff 历史任务档案×5/拍板登记 G54 条目自述）。
7. **RZRK-021/022 库位映射值**：任务书 T1.3/T4.1 只点名 3 处替换文案，T7.2 门要求 demo-data「外购区 RW」全文=0，两处合计 11 处未指定替换值——自发单历史行按「映射到合法实体库区」自定 **成品区 RB**（待追认）；直发链行（RZRK-023/TZRK-009 的 row/cells）改「直发虚拟仓」（与点名处口径一致）。
8. **会后待办 md 为未跟踪新文件**（非对已跟踪文件修改），独立验收按新增文件口径核过。

## 三、待追认清单（决策表【追认】4 项＋自定默认值 2 项）

| # | 项 | 预定值（已按此执行·文案级可逆） |
|---|---|---|
| 1 | CK-023「来源」文案 | 「租入单直发类型 · 系统自动生成」（旧「立即转租勾选」第4次口径进 F01 沿革） |
| 2 | 退租入库新建库位 hint | 「直发类型租入件退租时，入库库位由系统自动带出直发虚拟仓，无需手选」 |
| 3 | otherInbounds 演示单 | 造 QTRK-20260912-004「客户报数调整」（其他类型+备注表达·库位直发虚拟仓） |
| 4 | F01 导航图 | 不画背靠背新支线；开发规则 §12.3 补划界句与使用规则三句 |
| 5 | RZRK-021/022 孤儿值映射 | 「外购区 RW」→「成品区 RB」（自发单历史行） |
| 6 | 库位档案类型筛选 | 仅追加「虚拟仓」option·未接 JS 过滤（筛选卡为装饰性静态·全站先例；如需真过滤另行拍板） |

## 四、过程小事故（即修·零残留）

1. RZRK-023 首次 Edit 误将 formRows 行拼进 row 行尾（不相邻两行合一个 old_string）→ 当即 Read 回滚错位行并在正确位置重做（中间态未跑任何验证·未提交）。
2. P2-R01 头部首编辑锚失配（误写 0918·实为 0919）致同批第二编辑孤立生效（D-166 结尾 ✅\*\* 暂缺）→ 两笔 Edit 修复闭合，终态 markdown 结构完整。

## 五、09-19 复核修正（道远：类型口径一致）

道远指出「单据类型约定的直发和自发，各处要保持口径一致」。排查发现四类不一致并当日统一（详见拍板登记 09-19 复核条目）：

1. **radio 带「单」字**：租入单新建 radio 曾按任务书已拍板⑥写「直发单/自发单」，与数据值/列表列（直发/自发）不一致 → 复核定稿**直发/自发（不带单字）**，radio/A03 注记/RZD-010 备注同步。
2. **租赁出库行标签旧词并存**：CK-023「租入直发（立即转租）」（G31 时代词）与 CK-024「租入直发（类型驱动）」（G54 新词）→ 统一「直发（系统生成）」；zl/关联租赁单/关联销售订单文案同步「—（直发系统生成）/—（租赁出库·直发）」；RZD-008/RZRK-023 chain 角色同步。
3. **租赁侧 D-27 废止口径残留**：租赁单新建「单据类型：常规/背靠背」下拉+红字提示（09-08「租赁单加背靠背标记」D-27 已⊘）→ 整行删除；租入单列表/租赁单列表 G13 toast 注释去「背靠背」。
4. **D-105 勾选触发残留**：租入入库确认「立即转租」勾选行 → 改「类型：直发」说明行（类型驱动·D-167）；RZD-20260910-009 来源「背靠背自动生成」→「新建草稿·需求来源 ZL-036」（经办人 系统→陈锋）。
