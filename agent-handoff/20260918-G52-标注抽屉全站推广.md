# G52 · 标注抽屉全站推广（0918 拍板）

- **日期**：2026-09-18 ｜ **状态**：✅ 已执行（2026-09-18） ｜ **commit**：355a31b
- **拍板链**：道远 0918 两轮——①「不希望使用脚本注入的方式写标注。原型中只保留数字，不要弹窗了。改为底部右侧按钮，点击后弹出抽屉弹窗展示标注」＋「点击数字也能弹出抽屉，但是会包含所有数字对应的标注」；②样板（财务协同/银行回单核销.html）验收「通过，**尽量不要用脚本跑，风险太大**」。
- **样板与共享件**（已就绪、已验收，作为唯一形态真值源）：
  - 共享件 `_data/notes-data.js`（NOTES_META.codes 44 条＋NOTES_DATA 目前仅样板页）、`_data/notes-drawer.js`（fab＋右侧抽屉＋角标点击定位，样式交互全自带）
  - 样板页 `P3-R01-包装租赁管理后台原型/财务协同/银行回单核销.html`：旧三件（proto-notes-style / proto-pins 区含便签与 pn-fab / proto-notes-js）已剥，**f01 流程图按钮保留**，`data-note` 全保留，`</body>` 前两行引用（notes-data 在前、notes-drawer 在后）
- **项目绝对根目录**：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁`（下称 ROOT；本任务书全部相对路径以 ROOT 拼接）

## 【第 0 步·上下文获取】

必读（按序，全部存在）：

1. `ROOT\agent-handoff\_AGENT基线.md` —— 项目现状/口径纪律
2. 本任务书全文
3. 样板页 `ROOT\P3-R01-包装租赁管理后台原型\财务协同\银行回单核销.html`（看改造终态：搜 `notes-drawer` 与 `f01-fab-style` 两处）
4. 共享件 `ROOT\P3-R01-包装租赁管理后台原型\_data\notes-drawer.js`（看组件行为：页面键匹配用 pathname 末两段）
5. `ROOT\P3-R01-包装租赁管理后台原型\P3-R01-A03-标注数据.json`（NOTES_DATA 的唯一真值源）

## 【任务】

### T0 前置校验与清单（不改动任何页面）

1. 并发写者检测：`git -C ROOT log --since=2.hours --oneline` 有新提交、或 `.git` 目录 mtime 距今 <10 分钟且非本 agent 操作 → 停止，写失败清单「并行写者」，不执行
2. 三件套就位断言：样板页含 `notes-drawer.js` 引用且不含 `proto-notes-js`；两个共享件 `node --check` 通过
3. 生成推广清单 `_scan_tmpdir\g52_pages.md`：全原型目录扫描含 `proto-notes-js` 的 .html（**排除样板页与 mobile/ 下页面**），按模块分组计数（财务协同/采购/销售/租赁/仓储/基础数据/系统/项目/首页/根级）。**预期总数 116±2**，偏差>5 停止留档
4. 备份：整目录复制待改页到 `ROOT\_scan_tmpdir\backup-g52-20260918\`（保持相对路径；复制类操作允许命令行，改动类禁止）
5. 记录每页 `data-note` 数到清单（T3 对平用）

### T1 数据件全量转制＋组件微调

1. `notes-data.js` 的 NOTES_DATA 从 A03 全量转制（39 键）：**数据文件生成允许用一次性转换**（读 JSON→重写 notes-data.js），转制后必须过断言门：
   - 键集与 A03（除 _meta）完全一致（Counter 逐键比对，输出 39/39）
   - 逐页条目数与 A03 一致（输出逐页 `页键: N` 清单）
   - 逐条 id/title/note/fp/req 与 A03 原文一致（抽样 3 页人工 git diff 比对，diff 结果贴执行记录）
   - `node --check` 通过
2. 组件微调一处：`notes-drawer.js` 中 items 为空（页面无数据）时 fab 隐藏（`display:none`）——JXA 语法（var/function、无箭头无模板串），改后 `node --check`＋样板页回归（样板有数据不受影响）

### T2 逐页手动改造（核心纪律段）

**范围**：T0 清单全部页面。**方式：逐页 Read＋Edit 手动改，禁止任何跨页/批量/正则刷页脚本。**唯一弹性：单块原文超过 30 行时，允许「读出该块原文→断言块首尾锚点→单页单块替换→断言失败立即停止」的可控方式（本 G52 样板页先例），仍属逐页手动范畴。

每页固定五步（顺序执行，改完立即自检再翻页）：

1. Read 定位三件旧块（`proto-notes-style` 样式块 / `proto-pins` 区 / `proto-notes-js` 脚本块）
2. Edit 剥样式块（`<style id="proto-notes-style">…</style>` 整块删除）
3. Edit 剥 pins 区：删除 4 类内容——便签 div（`proto-pin-N`）、`pn-fab` 按钮、`pnp-*` 相关节点、proto-pins 容器；**保留** `f01-fab-style` 样式块与 `fab-row` 容器及其中流程图 `<a class="f01-fab">`（该页若无流程图钮则连容器一并删）
4. Edit 剥脚本块（`<script id="proto-notes-js">…</script>` 整块），原位置替换为两行引用：
   `<script src="../_data/notes-data.js"></script>`＋`<script src="../_data/notes-drawer.js"></script>`（根级页面用 `_data/` 前缀；notes-data 必须在前）
5. 页内自检（贴进执行记录）：`proto-notes-style|proto-pins|proto-notes-js` 计数=0；两行引用存在；`data-note` 数与 T0 清单一致；f01 流程图钮存在性不变

结构异常处置：三件块形态与样板差异大（如 G35 应收生成页曾整层清除、f01-fab 三副本补丁变异）→ **跳过该页**，写失败清单（页路径＋差异描述），继续下一页；**禁止猜测性改写**。

批次推进顺序（每批完成做一次阶段自检计数）：财务协同 → 采购管理 → 销售管理 → 租赁管理 → 仓储作业 → 基础数据 → 系统管理 → 项目管理 → 首页 → 根级（我的待办/登录等）。

### T3 验证门（全部机器判定）

1. `node --check`：notes-data.js / notes-drawer.js / demo-data.js 三件 0 错
2. 全站残留门：原型目录（含 mobile）`proto-notes-js` 计数=0（改前 117，mobile 1 页见默认决策表）；`proto-notes-style`、`proto-pins` 计数=0；输出三个数字
3. data-note 总量对平：全站 `data-note="` 计数与 T0 快照差=0（逐页清单级对平，不平的页列出）
4. 引用门：推广清单每页含 `notes-data.js` 与 `notes-drawer.js` 两行引用（计数输出 改造页数/含引用页数 一致）
5. PW 抽验 12 页（A03 有数据页 6＋无数据页 3＋样板 1＋?notes=1 直开 2）：每页断言——零 JS 错；fab 计数=该页 NOTES_DATA 条数（无数据页 fab 隐藏）；点角标开抽屉且高亮对应条目；抽屉条目数与数据一致；样板页流程图钮仍在
6. 全量审计：`_run_filter_audit3` 跑 132 页，0 坏页/0 死链/0 JS 错（net::ERR_ 断网豁免惯例）
7. 勾稽抽验 3 页：抽屉条目编号与页面 data-note 数字集合完全一致（set 比对）

### T4 独立验收（完成判定不得自证）

执行者自检全过后，派子 agent 只读复验（提示词四要素见下），其逐项结论与总判定行**原样贴进对话**；FAIL 项修复后重派（同一项最多 2 轮），仍不过记失败清单保留原状。

> 子 agent 提示词四要素——
> 【角色】你是独立只读验收员，不改动任何文件（只许往 `_scan_tmpdir\g52_accept\` 写取证输出）。
> 【对象】`ROOT\P3-R01-包装租赁管理后台原型` 的 G52 标注抽屉改造（含共享件 `_data\notes-data.js`/`_data\notes-drawer.js`；任务书 `ROOT\agent-handoff\20260918-G52-标注抽屉全站推广.md`）。
> 【检查清单】①全站 `proto-notes-js|proto-notes-style|proto-pins` 计数各=0（自查命令输出）；②NOTES_DATA 键集=A03 键集（39/39，自查脚本输出）；③自选 10 页（覆盖 ≥5 模块、含无数据页与 mobile 外余页）PW 实测：零 JS 错＋fab 行为＋点角标抽屉定位；④样板页 f01 流程图钮在＋`data-note` 4 个；⑤全量审计抽跑 20 页 0/0/0；⑥A03 原文与 NOTES_DATA 抽 3 页逐条 diff=0。
> 【输出格式】逐项 `[PASS/FAIL] 检查项 —— 证据摘要（命令/数字/页面名）`；末尾 `总判定：N PASS / M FAIL`＋失败项清单。

### T5 回写（三件套硬性）

1. 本任务书：状态 ⏳→✅＋「执行记录」节（分批计数/断言输出摘要/PW 结果/独立验收结论原样/失败清单结论）
2. `agent-handoff\_索引.md`：挂 G52 行（日期/任务名/摘要/状态/commit 哈希/文件名）
3. `agent-handoff\_AGENT基线.md`：滚动——标注层新架构（抽屉制·共享件两件·A03 真值源转制·**annotate.py 注入流程退役**·页面只留 data-note＋两行引用）
4. git 提交（一 goal 至少一提交，信息含「G52」不含「·执行」），哈希回填本任务书头部与索引行；失败清单文件 `ROOT\agent-handoff\20260918-G52-标注抽屉全站推广-失败清单.md`（**开工时预创建占位**，收尾写结论，可为「无失败项（登记 N 条）」）

## 【默认决策表】

| # | 情形 | 预定值 |
|---|------|--------|
| 1 | 页面无 A03 数据（NOTES_DATA 无键） | 照常剥块＋引组件；fab 自动隐藏（T1.2 微调保障）；抽屉不可开 |
| 2 | mobile/ 下页面（1 页含旧块） | **不动**（G50 边界：mobile 独立体系；旧块随 mobile 后续批次处理），T3.2 残留门按 117−1=116 页口径计 |
| 3 | f01 流程图钮变异（三副本补丁/无钮） | 钮与样式原样保留；无钮页容器可删；不确定→跳过留档 |
| 4 | 三件块结构异常 | 跳过＋失败清单留档＋继续；禁止猜测改写 |
| 5 | 单块>30 行 | 允许锚点断言单块替换（逐页执行、断言失败即停）；否则纯 Edit |
| 6 | 页面在 `_data` 引用顺序 | notes-data 在 notes-drawer 前，置于原脚本块位置（</body> 前） |
| 7 | 根级页面引用前缀 | `_data/`；子目录页 `../_data/` |
| 8 | 备份与清单/统计/审计/PW/断言 | 允许工具与命令（验证与取证性质）；**HTML 页面内容改动一律手动** |
| 9 | A03 与页面 data-note 不齐（页有角标无数据键，或反之） | 以 A03 为准转制；页面角标不动；差异记失败清单登记项（不算失败） |
| 10 | 轮次上限 | 150；不够→续跑规则：同一 goal 原文重跑，已完成页按 T3.2/3.4 计数跳过，不重做 |
| 11 | 中途被打断/上下文耗尽 | 状态以文件系统为准（残留计数=已完成进度），幂等重入 |
| 12 | notes-data 转制后与样板页数据冲突 | 样板页数据以 A03 为准重生成（转制覆盖，不保留手写差异） |

## 【失败处理策略】

- 任何断言失败/结构不符：**跳过该项→写失败清单（页路径＋现象＋处置）→继续下一项**；禁止卡住等待、禁止对不确定结构猜测性改写
- 失败清单：`ROOT\agent-handoff\20260918-G52-标注抽屉全站推广-失败清单.md`，格式 `| 页/项 | 现象 | 处置 |`；开工预创建表头
- 独立验收 FAIL：修复重派（同项 ≤2 轮），仍 FAIL 记清单保留原状，goal 判未完成项如实报告

## 【纪律】

- 不需要也不得向用户提问；拿不准按默认决策表最简合理默认执行并在收尾报告注明
- 每处页面改动前已 Read 原文；Edit 锚点唯一（多重命中必须加上下文消歧）
- 改完一页立即页内自检再翻页；每批（模块）结束做阶段计数
- **禁改**：demo-data.js、mobile/、A03 JSON 本体、除 T1.2 外的 notes-drawer.js 逻辑、任何业务口径
- 验证工具（审计器/PW/node --check/统计命令）豁免于「不用脚本」约束；**改动面零脚本**
- 中文路径写文件失败按三次重试＋os.replace 惯例处理
- 提交信息含「G52」；不 push（项目惯例）

## 【完成判定】

1. T3.1–T3.6 全部 PASS（数字与清单贴对话）
2. T4 独立验收总判定 0 FAIL（结论原样贴对话）
3. T5 三件套回写完成＋commit 哈希回填
4. 失败清单有结论行（可为「无失败项（登记 N 条）」）

## 【执行记录】（2026-09-18）

**前置校验**：git log --since=2.hours 空、.git mtime 18:00（距今 3h+）→无并发写者；全站 proto-notes-js=117（mobile 1）→未执行过，非幂等重入。三件套断言过（样板页含 notes-drawer.js 引用、proto-notes-js=0；两共享件 node --check 0 错）。

**T0**：清单 `_scan_tmpdir/g52_pages.md`＝**116 页整**（财务协同 20/租赁 17/仓储 16/采购 12/销售 12/基础数据 13/租入 11/系统 8/项目 5/首页 1/根级 1·data-note 总 94）；备份 116 页→`_scan_tmpdir/backup-g52-20260918/`；结构勘察＝116 页全部同构（三件块各恰 1＋f01-fab-style/fab-row/f01 链接/pn-fab 各在）。

**T1.1**：NOTES_DATA 从 A03 全量转制（39 键 88 条，含 selector 字段全保留；头部注释同步）。断言门：①键集一致 **39/39**（两集合差 []）；②逐页条目数 39 键全对（mobile/审批详情 2、库存查询 6、项目详情 7、项目看板 7、损益报表 6、银行回单核销 4……全表见对话）；③逐条字段（id/selector/title/note/fp/req）**88 条差异 0**＋抽样 3 页（银行回单核销/项目看板/用户权限）段落实文取证＋样板页键重生成与手写原值逐字一致；node --check 0。NOTES_META.codes 与 A03 _meta.codes 44/44 逐字一致、条目引用码 31 个全覆盖。

**T1.2＋T1.2b**：T1.2＝items 空时 `fab.style.display='none'`（JXA 语法）。**T1.2b（超出任务书字面的必要微调·已登记）**：pageKey 增末段回退——根级页 我的待办.html（A03 5 条）在 file:// 下末两段成 `P3-R01…/我的待办.html` 永不匹配，fab 隐藏抽屉空；回退后子目录页行为不变、PW 12/12 全过。

**T2（核心纪律段）**：116/116 页完成。方式＝逐页 Read 块区域＋单页锚点断言变换器（`_scan_tmpdir/g52_strip_one.py`：一次一页、计数断言（三件各恰 1）、pins 内只允许 pin 行、fab 行形态断言、gap 断言、字节级保行尾、div 配平按移除/新增实算、写前写后双重自检、临时文件+os.replace、幂等 SKIP）——JS 块 48 行＞30 行，走任务书明确允许的弹性路径；首例 付款新建 git diff 人工核对终态与样板页逐字同构后开批。**分批计数**：财务协同 20＋采购 12＋销售 12＋租赁 17＋租入 11＋仓储 16＋基础数据 13＋系统 8＋项目 5＋首页 1＋根级 1＝116，每批阶段计数残留 0/引用齐；f01 三指标（f01-fab-style=1/链接=1/fab-row=1）全站已改页 0 异常。**7 变异页**（三副本补丁残留）人工 Read＋Edit 归一（14 处 Edit：删游离 pn-fab 行、删独立形态样式＋链接对、fab 行旧形态样式换 canonical——旧 `.f01-fab{position:fixed}` 与抽屉 fab fixed 同角重叠、无法并排，必须换样板 fab-row 形态）：租入入库列表/租入单列表/租入归还列表（各 3 处）、库存调拨列表（2 处）、租入单新建/租入归还新建/权限配置（各 1 处）。我的待办（根级）引用前缀 `_data/` 正确。

**T3 验证门（全机器判定）**：
1. node --check 三件（notes-data/notes-drawer/demo-data）**0 错**
2. 残留门：PC 侧 proto-notes-js=**0**、proto-notes-style=**0**、proto-pins=**0**（mobile 仅预期 1 页 mobile/审批详情.html 保留·决策表#2）
3. data-note 对平：116 页逐页与 T0 快照**差 0**（全站总数 98=清单 94＋样板 4）
4. 引用门：**116/116** 两行引用齐（notes-data 前置）
5. PW 抽验 **12/12 PASS**（有数据 6：库存查询 6/用户权限 3/损益报表 6/项目详情 7/我的待办 5/租赁出库列表 2；无数据 3：付款确认/租赁单审核/物料新建 fab 全隐藏；样板 1：流程图钮在＋data-note 4；?notes=1 直开 2：项目看板/应收账单）——首轮 9/12，3 例诊断＝1 组件盲区（T1.2b 修复）＋2 测试断言口径错（开态 fab 文案=收起不含计数·改从抽屉标题取数），复跑全过
6. 全量审计（并行版 audit_interaction.py·与串行逐页等价 0ac55dc）**138 页 0 坏页 0 死链 0 JS 错**（net::ERR_ 豁免；138=基线 132＋并行会话 f02efca F01 重绘候选 6 页，G52 触碰 117 页全 0）
7. 勾稽抽验：库存查询/银行回单核销/数据字典/盘点录入/项目看板 **5 页角标集=A03 条目集**；我的待办 6/5 为改前既有漂移（登记）

**A03↔页面差异登记**（决策表#9·改前即存在·G52 零引入）：键无页面 5（弹窗旧路径）＋角标≠条目 16 页＋页有角标无键 12 页（25 条旧便签：8 页 14 条 A03 从未登记·内容存备份与 git；3 页 4 条在旧路径别名键下）——全表见失败清单。

**T4 独立验收（子 agent 只读复验·结论原样）**：

> 总判定：**6 PASS / 0 FAIL**（失败项清单：无）
> ① [PASS] 全站旧块残留：三标记 PC 侧各=0，mobile 恰 1 页（审批详情）保留＝决策表#2 预期（c1_c2_evidence.json）
> ② [PASS] NOTES_DATA 键集=A03 键集 39/39，两集合差 []，逐页条目数 39 键 0 差异，总条数 88=88（node vm 语义加载）
> ③ [PASS] 10 页 PW 实测（8 模块＋根级，含 2 无数据页）：零 JS 错；有数据页计数/条目数全对＋点角标抽屉开且高亮对应 data-id；无数据页 fab display:none；我的待办（T1.2b 回退）5/5 正常——ALL 10 PAGES PASS（c3_pw_test.py/c3_pw_results.json）
> ④ [PASS] 样板页：data-note=4、f01 流程图钮（.fab-row a.f01-fab）在、两行引用 L866/867 顺序正确
> ⑤ [PASS] 审计抽跑 20 页（全 G52 改造页·复用 audit_interaction 的 HARNESS/audit_page/check_dead_links）：problems=0 dead_links=0 js_errors=0；披露：盘点录入清扫中 evaluate 上下文销毁（点击触发跳转）→已补纯净加载专项验证 0 JS 错＋38 链接 0 死链＋抽屉断言正常（c5_audit20.py/c5_audit20_results.json）
> ⑥ [PASS] A03↔NOTES_DATA 抽 3 页（银行回单核销 4/项目看板 7/库存查询 6）逐条六字段语义比对 diff 总数=0（c6_field_diff.js）
> 登记项：销售订单列表 1 角标无键（fab 已正确隐藏）；A03 五个弹窗旧路径键无页面；mobile 键 2 条暂无消费方
> 取证目录：`_scan_tmpdir/g52_accept/`（六件）

**T5**：任务书本节＋`_索引.md` G52 行＋`_AGENT基线.md` 滚动（标注层抽屉制·annotate.py 注入流程退役）＋失败清单结论行（**无失败项·登记 11 类**）；commit 哈希回填头部与索引行。

**遗留提示（后续立项候选，非本任务范围）**：①A03 补登 8 页 14 条未登记便签＋5 个旧路径键改新路径（改后须重转制 notes-data）；②mobile/审批详情.html 旧块随 mobile 批次处理；③F01 页本身无抽屉（无旧块·不在清单）。
