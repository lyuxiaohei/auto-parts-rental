# 20260916-G43-枚举接线与下拉数据源化

> **状态**：✅ 完成（2026-09-17 执行·见文末执行记录）｜**commit**：执行收尾回填
> **项目绝对根目录（本机 mac）**：`/Users/bailey/Desktop/xiaohei-workplace/auto-parts-rental/`（Win 主机等价 `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\`·路径分隔符等价）
> **失败清单**：`agent-handoff/goal-failures-g43.md`（沿 g38~g42 惯例随 git 入库）
> **来源**：基线六.17「枚举接线任务（G30 登记·待立项）」＋道远 09-14 拍板 D-93「写死枚举值收敛数据字典管理」延续＋底稿 `_scan_tmpdir/g30_接线差集.md`（09-14 时点·**G36 弹窗页面化/G38 字段口径/G39 计费/G42 缺口修复后部分过时·T0 须重扫**）

## 一、终态与范围

- **终态**：PC 全站 `<select>` 下拉完成数据源化——**B 类**（已入字典仍写死的组）改字典渲染；**A 类**（主数据引用型）改实体渲染；残留演示占位清零。下拉值域与字典/实体单一真值源一致，正式版免「改一处值跟几十页」。
- **范围**：PC 功能页（含 G36 页面化新页）＋新增共享件；**零改动**——mobile/ 五页（chips 不接线）、业务口径与值域、`?audit=1`/打印机制、G30 白名单文件既有演示注记。
- **明确跳过**：①计费方式 BF＋周期单位 ZQ（G21 三段控件两层口径·D-93 注记零改动）②库位类型 KW（G30 已接线样板·演示扩展值待正式版归并不动）③物料分类 FL（G38 已并入 WL·不复现）④银行账户（无实体·保留写死＋title 注记「正式版银行账户主数据」·A 类 #19 降级注记）。

## 二、前置校验（任一不符→写失败清单后停止）

1. `agent-handoff/_索引.md` G43 行=⏳ 且 `_AGENT基线.md` 无 G43 完成追记
2. `backup-g43-20260916/` 不存在；`agent-handoff/goal-failures-g43.md` 不存在
3. 单写者互斥：`P3-R01-包装租赁管理后台原型/`（尤其 `_data/`）最近文件 mtime ≥30 分钟；`git log --oneline | head -5` 无 G43 提交
4. `git status --short` 存档 `_scan_tmpdir/g43_gitstatus_pre.txt`；若存在他人未提交原型改动→允许执行但提交与执行记录注明「携带并行未提交改动」

## 三、任务组

- **T0 差集重扫＋陈旧遗留销账（先行）**：
  1. 写扫描器 `_scan_tmpdir/g43_scan.py`（Playwright 全 PC 页遍历：每个 select 的 label/现 option 文本/所在页/表单 or 筛选），产出 `_scan_tmpdir/g43_接线差集_v2.md`（页面×select×现值×目标源矩阵·区分 B 类/A 类/跳过类）——以重扫结果为准修正底稿 09-14 旧口径（模板已删/FL 并入/ARB 7 值/DJ 20 值等）
  2. 陈旧遗留销账（纯文档·逐条带证据划线）：基线六.5（P1-R04 三术语·G40 已做）／六.10（工期范围重估·D-94 V20260914/15 已覆盖）／六.11（客户转租独立菜单·G37 转移出库 3 页已承载）／六.15②（无订单预付款退款场景·G42 TKD-20260916-004 预收退回已表达）／`_索引.md` G32 行 T3 🔄（G37/D-146 已落地）——改写为 ✅＋沿革注记，不动业务
- **T1 共享件 `_data/select-source.js`**：三函数——`dictOpts(cat)`（dictItems 按组取 name 序列·照抄 G34 页面局部样板实现）／`fillDict(sel, cat, keepFirst)`（清空 select 后按字典渲染·keepFirst=保留「全部」首项）／`fillEntity(sel, entity, opts)`（opts：{label=取 fields 指定键拼文本·filter=如 partners 按 type 过滤·append=['其他（演示）']·emptyKeep=true 空实体守卫保留原 option}）；文件头注释载明用法与 G43 来源；JXA `new Function` 语法门
- **T2 B 类字典渲染**（按 T0 重扫实有组·预估 14 组：WL/DW/ZF/KC/RKU/CKU/ARB(7 值)/APB/FY/FP/KST/SJQ/PDK/DJ(20 值)）：目标页引入 `<script src="../_data/select-source.js"></script>`（挂 demo-data.js 后）＋一行 `fillDict` 调用（挂页面脚本尾部/renderListPage cfg 后·G35 先例）；**措辞统一**：选项文本=字典 name 原文（短式），页筛原括号口径说明并入 `title` 属性（ARB-03「丢损赔偿（客户赔付我方）」→文本「丢损赔偿」+title 全句；APB/FP 同法）；DJ 组「我的待办」筛选与操作日志 6 值子集**不裁剪不接线**（操作日志按 module 域语义保留·登记性注记）
- **T3 A 类实体渲染**（20 组值源映射·T0 重扫定位全部出现页）：

  | # | 组 | 值源与拼式 |
  |---|---|---|
  | 1-2 | 客户/供应商 | partners·filter type=客户/供应商·文本=名称全称 |
  | 3 | 供应商（明细含「其他」） | partners 供应商＋append「其他（演示）」 |
  | 4-5 | 物料（编码长串/短编码/名称） | products·文本=`编码 · 名称 规格`（长串页）/`编码`（短页）/名称（表单页）·按 T0 现形态就近 |
  | 6 | 所属项目 | projects·文本=`编码 名称`（新建页 G42 已立全拼式）/`编码`（筛选页裸码照旧） |
  | 7 | 库房 | locations·取 wh 去重·文本=仓库名 |
  | 8 | 库区 | locations·取 area 去重（G31 后仅档案域残存·以 T0 为准） |
  | 9 | 转租客户 | partners 客户＋append「其他终端用户（手输）」 |
  | 10-12 | 仓管员/盘点人/项目负责人/审核人/角色 | users（人名去重）/roles（name）·users 无王琳→todoItems 审核人并集（G22 口径） |
  | 13 | 角色 | roles·name |
  | 14-18 | 关联单五组（租入/租赁/销售订单/应收/应付） | 对应实体·文本=`单号 · 摘要首段`·按单号倒序取前 30 条防下拉超长 |
  | 19 | 银行账户 | **跳过**（见一.跳过④·仅注记） |
  | 20 | 账期 | 动态生成近 12 期（执行月倒推 `YYYY-MM`）·不新增实体 |

- **T4 残留占位清洗**：「待定选项（演示数据）」全站=0（09-16 实测 6 处 5 页：字典项新建/开票新建/收款新建/用户新建/付款新建·以 T0 重扫为准）；数据字典 createModal「字典分类」旧占位复核清洗；清洗处若为必填校验锚点→以首个真实 option 替代不破坏校验
- **T5 验证门取证**（见五）→ **T6 回写三件＋A05/A02/P2-R01**（A05 概念索引各组补「值源=字典/实体」注记；A02 登记 select-source.js 共享件；P2-R01 附录 A **D-153** 立行）

## 四、默认决策表

1. 既有 5 页局部 `dictOpts`（物料新建/客商开票资料/库位新建/客商新建/上下游绑定）**不回归不删除**（避免无谓重测）；新页一律走共享件
2. 措辞统一方向=字典 name 原文为准·原页筛括号说明并入 title（不新增字典值）
3. 「其他（演示）」「其他终端用户（手输）」＝实体值后 append 固定 option（演示语义保留）
4. 演示扩展值（虚拟仓等）一律保留不动
5. 账期近 12 期·执行月倒推；单号引用组前 30 条倒序
6. fillEntity 空实体守卫=保留原静态 option（页面不炸·登记性注记）
7. 表单页已选默认值（如 radio/selected 首项）清洗后以首个渲染 option 为默认·不造新默认语义
8. 每页改动=读取-精确替换＋assert 计数＋标签配平；改后即跑该页 audit（_run_filter_audit3 单页）
9. 拿不准的形态细节按最简合理默认（就近现有形态）执行·收尾「默认决策」节注明
10. 提交信息前缀 `G43`·**禁 push·禁 add -A**（只 add 本任务涉及文件与自有产物·含 T0 销账文档）
11. 轮次上限 150；任务幂等可续跑不重做（重扫差集 v2 为续跑真值源）

## 五、验证门（证据形态逐条写死·全部满足才算完成）

1. **命令级**：`_data/select-source.js` JXA `new Function` 编译退出 0（本机无 node 先例）；`demo-data.js` 语法门照常 0
2. **全量审计**：开工自跑全量存 `_scan_tmpdir/audit_results_g43pre.json` → 收尾重跑全量 **0 坏页**（页数执行时实测·2026-09-16 口径 133）＋与 g43pre 逐键 diff **新增 0**；改动页单页 audit 随改随跑
3. **断言级**：python 一键脚本逐组断言——重扫矩阵中每个「已接线」select：页面运行时 option 文本集合 = 运行时字典组值/实体值集合（Playwright evaluate·Counter 比对·「全部/其他（演示）/其他终端用户」白名单）；「待定选项（演示数据）」全站 grep=0——逐行 `[PASS/FAIL]` 贴对话
4. **渲染级**：PW 抽验 ≥10 页（覆盖 B 类≥5 组＋A 类≥5 组·含新建表单与列表筛选两类）下拉展开值域截图 ≥6 张存 `_scan_tmpdir/g43_shots/`
5. **独立验收**：只读子 agent 按门 1~4 复验（自跑命令取证·不引用执行者自述·仅可写 `_scan_tmpdir/g43_accept/`）——结论逐项 `[PASS/FAIL]`＋`总判定：N PASS / M FAIL` **原样贴对话**；FAIL 修复重派（同项最多 2 轮）
6. **收尾硬性**：任务书 ✅＋执行记录＋哈希回填｜索引 G43 行 ✅ 挂哈希｜基线追记 G43 段（含六.17 销账）｜A05/A02/P2-R01 D-153｜失败清单结论行（空清单给「无失败项」）

## 六、失败处理策略

assert 失败/结构不符/锚点不匹配：跳过该项→记 `agent-handoff/goal-failures-g43.md`（项号/现象/证据/原状）→继续下一项。禁止卡住等待、禁止对不确定结构猜测性改写、禁止静默吞掉。单页接烂即从 `backup-g43-20260916/` 回滚该页。

## 七、纪律（违反=返工）

- 基线第四节历史事故逐条适用：精确替换+assert 计数·类名 find 带标签锚·HTML 删块后配平自检·`_data` 改动每轮语法门（失败即回滚本轮）·Playwright 编程点击+unquote+textContent·备份按原相对路径·同一时刻单写者（执行中共享文件 mtime 突变→停手重读）
- T0 开工备份：触碰页＋`_data/` 快照→`backup-g43-20260916/`（先备份后施工）
- 新增 onclick 自带 DOM 副作用；`go()`/m-auth/m-done/mGuard 零触碰；类名规范沿各页现状（本任务不加新类名组件）
- **禁 add -A**；一 goal 至少一提交；**禁 push**

## 执行记录（2026-09-17 执行·当日单会话完成）

> **状态**：✅ 完成｜前置校验四项全过（索引 ⏳/基线无追记/无备份无失败清单/单写者互斥·git clean 存档 g43_gitstatus_pre.txt）

- **T0 重扫差集**：`_scan_tmpdir/g43_scan.py`（Playwright 运行时·128 PC 页 228 select 0 报错）→ `g43_scan_raw.json` → **`g43_接线差集_v2.md`**（续跑真值源：B 类 20 处＋A 类 40 项＋跳过类三节·分类原则=筛选 select 值源取本页渲染实体字段保匹配〔G35 ent() 先例〕·项目/客商筛选=主数据实体·表单=主数据/字典；接线方式=G35 先例静态 option 不动＋页面尾 fill 块运行时重渲染）。底稿过时项销账：数据字典 createModal 占位（G36 后该页已无 select）／新建库位模板孪生（已删）／关联租赁单（D-106 清退后无此 select）。
- **T0 陈旧遗留销账**：基线六.5（G40 术语三条）/六.10（工期 D-94 两版）/六.11（G37 转移出库承载）/六.15②（G42 TKL 预收退回）划线＋索引 G32 行 T3 ⏳→✅——五处纯文档带证据销账。
- **T0 开工备份**：54 页＋`_data/` → `backup-g43-20260916/`（库存调拨列表系断言暴露后补接页·前态自 git HEAD 补入）。
- **T1 共享件**：`_data/select-source.js`——dictOpts/fillDict（opts.value 保匹配映射·opts.title 措辞注记·opts.only 子集）/fillEntity（field 字段去重＋行键列表双模式·数组实体并集〔products∪bomList〕·filter/filterV/desc/limit/append/emptyKeep）/fillPeriods（近 N 期）/byLabel/byFormLabel（定位助手）；fill 保持原选中值。JXA `new Function` 编译 **退出 0**。
- **T2+T3+T4 施工**：`_scan_tmpdir/g43_wire.py`（manifest 驱动·读取-精确替换＋锚点预检＋T4 计数 assert＋script 配平自检＋幂等）共 **54 页接线**：B 类字典 20 处（含 ARB/APB/FP 措辞统一=字典 name＋括号入 title·APB 筛选 valueMap 保 contains 匹配·cmBtype 双页语义 value 键保留不破坏 sync 分支）；A 类实体（partners 客/供 22 处·projects 全键筛选 11 页/非已完结全拼表单 5 处·locations.wh 表单 8 处·本页实体字段筛选〔warehouse/frm/to/scope/checker/area/loc/operator/appliance/auditor/user/owner〕·roles/users 人名·关联单七组倒序前 30〔riSelect 保 data-key 联动＋fill 后重跑 syncReturnItems·排除新建草稿〕·账期动态 12 期 2 页·字典分类 28 组·物料明细 products〔租赁单新建∪bomList〕）——明细物料选择器收窄 `.g39mat`/`[data-tax="prod"]` 防 BF/ZQ 红线误伤（g39mode 计费 select 零触碰）；T4 占位清洗 6 处 5 页（「待定选项（演示数据）」全站 grep=0·银行两页补真实第二选项过 select-few-options 门·title 注记）。补挂采购入库录单/盘点录入缺失的 demo-data.js include。**mobile 五页/demo-data.js/业务数据零改动**。
- **T5 验证门**（证据全在案）：
  1. 命令级：select-source.js JXA **0**＋demo-data.js JXA **0**（未改动仍复验）；
  2. 全量审计：g43pre（=G50 收尾快照·git HEAD 089f8fb 在案——首次后台审计与施工并发即中止〔F-01〕·语义等价）→ 收尾重跑 **133 页坏页 0/死链 0/JS 0（net::ERR_ 断网类豁免）**＋逐键 diff **新增 0**（g43_audit_diff.py 判定 PASS·中途 F-03 银行 select-few-options 修复后复跑归零）；
  3. 断言级：`g43_assert.py` 逐组 Counter 比对（期望值页面内独立遍历 DEMO_DATA 计算·不经 SSEL 避免自证）**103 PASS / 0 FAIL**（首轮 2 FAIL 暴露漏接即补〔F-02〕）；「待定选项（演示数据）」全站 grep **0**；行为级抽测：我的待办审核人=赵磊→1 行（ZY-005）·应收账单丢损赔偿→1 行·应付账单无订单预付款（value=预付）→1 行；
  4. 渲染级：`g43_pw.py` **14 PASS / 0 FAIL**＋截图 14 张 `_scan_tmpdir/g43_shots/`（B 类 6＋A 类 8·表单与筛选两类·筛选卡展开后截取）；
  5. 独立验收：只读子 agent 自跑取证（仅写 _scan_tmpdir/g43_accept/）——门 1~4 逐项 **[PASS]×4**·**总判定：4 PASS / 0 FAIL**（g43_accept/verify.md）。
- **T6 回写**：本执行记录＋索引 G43 行 ✅＋基线追记 G43 段（含六.17 销账）＋A05 G43 登记节（B/A 类值源表＋概念索引值源注记）＋A02 _data 七件（+select-source.js）＋P2-R01 附录 A **D-153**＋失败清单 goal-failures-g43.md（**结论无失败项**·登记 5 条）。

### 默认决策落地注记（任务书四.9 收尾交代）

1. 筛选值域=本页实体实值（无数据死选项随源消失·盘点范围复合串原筛选失配经接线即修复）；2. 表单项目下拉=projects 非已完结恰为六在执行项（零变化）；3. 库房表单按 A#7 统一 locations.wh（华东中心仓系列为库区时代残值）；4. 仓管员/盘点人接 users 8 人（张帆系 purchaseInbounds.maker 域不在 users·登记）；5. 租入归还关联单排除「新建(草稿)」；6. 应收生成 cmBtype 6/7 子集（预付款（保证金）无表单分支·登记）；7. g35PeriodSel 已接不动（应付账单期段与动态 12 期并存·登记）；8. 销售退货新建关联原单拼式统一「单号 · 摘要」。

### 统计

HTML 133 不变（零增删）·触碰 54 页＋_data/select-source.js 新 1 件·demo-data.js 零改动·dictItems 150 项 28 组不变·既有 51 处 JS 接线（G34 五页/G35 族/页面局部）不回归不删。
