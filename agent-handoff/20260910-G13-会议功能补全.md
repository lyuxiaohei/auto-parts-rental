# 20260910-G13 · 会议功能补全（09-08 会议未实现项 13 项）

> **状态**：✅ 已完成（2026-09-10）｜ **commit**：f68fa90（功能主体）+ 收尾回写提交 ｜ **起草**：2026-09-10 道远拍板，**v2 修订**基于 G12 终态（A 类 9 页接线/demo-data 37 实体/todoItems 已建/verify 三批 100·88·43）
> **来源**：09-08 会议三份材料交叉核对（纪要最终版+视频纪要+音频转写全文），道远两轮修正后定稿清单。
> **定位**：业务功能层——建立在 G12 数据驱动底盘之上；G14（移动端 H5+登录）依赖本任务的数据结构终态。

## 前置校验（不通过即停止并写失败清单）

1. **G12 已完成**：`agent-handoff/_索引.md` G12 行状态=✅（现=✅ d9aa5b1，重验；若被回退则停止）。
2. 单写检查：原型目录最近文件 mtime≥30 分钟无变化（尤其 `_data/demo-data.js`）。
3. 本文档头部 commit 哈希为空才执行。
4. audit 基线自跑存 `audit_results_g13baseline.json`（禁引用历史数字）。
5. 读 `agent-handoff/g12-whitelist.md` 知悉 B 类 46 文件清单——**白名单豁免的是数据驱动改造，不是冻结**：G13 的 13 项功能落点含多个 B 类表单弹窗（付款登记新建/付款确认/新建销售订单/租入归还新建/权限配置/上下游绑定等），按功能需求正常改动；收尾时在 g12-whitelist.md 增订一节「G13 改动的 B 类文件」注记。

## 任务组（13 项，按页面域分四组）

### T1 库存与成本（2 项）

1. **库存查询加库存状态「客户端(转租)」+筛选**（道远会后讨论口径，替代独立菜单）：demo-data stocks 加该状态演示行（2-3 行，客户 A 转租给终端用户语境）；库存查询状态筛选加 option；与既有「客户在租」下钻弹窗衔接（转租行为=在租的子状态，注记一句话）。
2. **库存体现成本**（转写 21:16 徐蔚承诺）：库存查询加「成本单价」列（未税口径，沿税率三件套语言）；演示值取该货品最近采购入库未税单价；列表页与客户在租下钻弹窗同步加列。

### T2 财务（3 项）

3. **付款方式字段**（转写 31:58 袁丽晶）：付款登记新建+付款确认弹窗加「付款方式」下拉（银行转账/承兑/现金/票据 4 项，默认银行转账）。
4. **应收按实际使用量生成**（纪要八.3）：应收账单生成弹窗加「生成方式」单选（按月定期[现有默认]/按实际使用量）；选使用量时显示 数量×单价 说明区+「按客户对账量录入」文案；demo-data 应收演示行 1 条 usage 模式。
5. **少退/丢损对比视图**（纪要八.4）：租赁单列表操作列加「退回对比」弹窗（列：货品/出库量/已退回/未退回差异，静态演示数据 3-4 行）；最小侵入，不动既有列。

### T3 租赁流程（4 项）

6. **租出计费方式**（纪要 7.2.2）：租赁单新建+租赁单详情加「计费方式」单选（按月定期生成应收/按次套数对账）；明细表格上方；demo-data leaseOrders 行加 billing 字段演示。
7. **新建租赁单+销售订单优先查库存**（纪要 7.2.1+转写 51:04 扩展到销售订单）：两处新建弹窗选产品后行内显示「可用库存 N 套」；不足时红色提示条+两按钮「生成采购订单」「生成租入单」（道远拍板：不是草稿，是可选择生成采购订单或租入单；按钮 onclick 演示跳转/提示，标注去向单据页）。
8. **背靠背自动生成租入单草稿**（纪要 7.1.1，保留原口径）：租赁单审核确认后 toast「已生成租入单草稿 RI-xxx，供应商待选」；租入单列表加 1 条「新建(草稿)」状态演示行+操作列「选供应商→修改→提交」三步演示入口。
9. **租入归还单页面级改造**（纪要 7.1.4/L3）：归还新建弹窗加「关联租入单」下拉→选中后 JS 同步该单明细表格（列：货品/租入数量/已归还/本次归还数量[输入框]）；表尾注记「支持分批：多个归还单对应一个租入单」；demo-data rentInOrders 明细供同步源。

### T4 销售与系统（4 项）

10. **销售订单附件上传**（纪要五.2）：新建销售订单+销售订单详情加「订单附件」区（PDF/邮件截图；假上传=文件名输入+「添加」按钮+附件列表行含删除；不做真 file API）。
11. **审核按角色配置**（纪要十.1）：权限配置弹窗加「可审单据」勾选区（采购订单/销售订单/各类出入库/退租入库 等矩阵，复用角色权限矩阵样式）；审核弹窗审核人行显示「按角色匹配」注记。
12. **审核列表按审核人筛选**（转写 15:39 袁丽晶）：我的待办加「审核人」筛选下拉（王琳/袁丽晶/徐蔚等演示名单）——**基于 G12 已重建的 todoItems 实体（12 行）+filterTodo 函数扩展**，勿另起炉灶；可选：操作日志 opLogs 同款筛选。
13. **项目上下游一对多/多对多**：仅设计注记——上下游绑定弹窗加一行灰字注记「设计预留：上下游支持一对多/多对多（2026-09-08 王总提出，本期不实现）」；不写任何多对多逻辑。

## 默认决策表（拿不准一律按此执行，不向用户提问）

1. 所有新字段/弹窗/列：样式沿 admin-ui-spec 既有规范，不发明新组件。
2. 演示数据量：每新增演示行 1-3 行（够演示即可，不铺量）。
3. 库存状态值文案=「客户端(转租)」（道远原词，不简写）。
4. 成本列名=「成本单价」，取值演示写死（最近采购未税单价），不做真推导。
5. 按钮演示形态：onclick 弹 toast/alert 或跳转对应单据页（按钮文案即去向），自带 DOM 副作用免审计判死按钮。
6. 归还明细同步：JS 读 demo-data `rentInOrders`（G12 终态 37 实体之一，已复核在库）；permission 弹窗改动注意双层同步（角色管理页定义+用户权限页引用同源）。
7. 附件假上传：不引 file API，文件名列表演示。
8. 新增筛选/下拉 option 沿既有 match:contains 模式激活真筛选。
9. 拿不准的落点→最简演示形态+失败清单注记。
10. 不动：菜单/侧边栏/F01/页面文件名；不新增页面（对比视图等一律弹窗承载）。

## 失败处理策略

- assert 失败/结构不符：跳过该项→写入 `_scan_tmpdir/goal-failures-g13-20260910.md`（条目+原因+处置）→继续下一项。
- 禁卡住等待、禁猜测性改写、禁向用户提问；失败清单可为空但必须存在且含结论行。

## 纪律（全文=基线 `_AGENT基线.md` 第四节）

1. 原型目录同一时刻仅一 agent 写（前置校验已查）。
2. 批量改动=读取-精确替换+assert 计数，禁裸 open('w') 整页写。
3. 入库 `git add -A`（含 _data）。
4. HTML 切块替换带标签锚+标签配平自检。
5. Playwright 编程点击+导航等待；URL unquote()；文本断言 textContent。
6. 新实体/行 cells 不带外层 td；新增 onclick 自带 DOM 副作用。
7. 备份按原相对路径至 `backup-g13-20260910/`；写盘 CRLF→LF 归一。
8. 审计/断言数字本会话自跑。

## 验证门（全部满足才算完成，证据形态逐条点名）

1. **audit 门**：audit_interaction.py → 108 页 死链 0/JS 0+相对 g13baseline 逐键 diff 新增 0（贴 diff 输出）。**预注豁免**：F01 页 `Failed to load resource: net::ERR_CONNECTION_CLOSED` = 断网环境下外链资源加载失败（非脚本错误；G06 先例「F01 外链噪音既有豁免」；2026-09-10 晚独立复跑实证）——diff 与 JS 计数时剔除该条，失败清单注记即可。
2. **grep 断言门**（13 项逐项，贴每项命中行数）：①库存查询含「客户端(转租)」②库存查询含「成本单价」③付款登记新建含「付款方式」④应收账单生成含「按实际使用量」⑤租赁单列表含「退回对比」⑥租赁单新建+详情含「计费方式」⑦租赁单新建+新建销售订单含「可用库存」⑧租入单列表含「草稿」状态演示行⑨租入归还新建含「关联租入单」⑩新建销售订单+详情含「订单附件」⑪权限配置含「可审单据」⑫我的待办含「审核人」筛选⑬上下游绑定含多对多注记。
3. **PW 抽验门**（4 页贴 PASS 清单）：库存查询（客户端(转租)筛选生效：切换后行数变化）+租赁单新建（选产品出库存提示）+租入归还新建（选关联单后明细同步行数>0）+权限配置（可审单据勾选区渲染）。
4. **回归门**：verify_listfull.py 三批失败 0（batch1=100/batch2=88/batch3=43，G12 修复后口径）+菜单门 v32_menu_verify.py 14/14。
5. **失败清单门**：goal-failures-g13-20260910.md 存在+结论行（贴尾行）。
6. **收尾门**：git log ≥1 提交；推送成功或注记"待推送"。

## 收尾回写（硬性三件）

1. 本文档：标 ✅+commit 哈希+「执行记录」节（各门证据摘要+13 项逐项完成状态+偏差注记）。
2. `_索引.md`：G13 行 ⏳→✅+哈希。
3. `_AGENT基线.md`：快照滚动（13 项落地状态+demo-data 实体/字段变化+P1-R01 附录 10.2 记录句「G13 会议功能补全」）。

## 执行记录（2026-09-10 执行完毕·commit f68fa90+回写提交）

### 13 项逐项完成状态（全 ✅）

| 项 | 落点 | 状态 |
|---|---|---|
| ①客户端(转租) | stockFlows +3 演试行（XNC-ZZ-WBX/PLT/BTC·客户安吉智行转租终端用户）+既有 10 行补 fields.status；库存查询新增筛选卡（物料编码/库存状态两控件真筛选）+rentDrill 转租行+注记 | ✅ |
| ②成本单价 | 库存查询主表 13 列（总量后插成本单价·未税）+客户在租下钻弹窗同步加列；演示值=最近采购未税价写死 | ✅ |
| ③付款方式 | 付款登记新建（模板+宿主）下拉 银行转账/承兑/现金/票据；付款确认（模板+宿主）drow 行 | ✅ |
| ④按实际使用量 | 应收账单生成（模板+宿主）生成方式单选+usageHint 说明区（数量×单价/按客户对账量录入）；receivableBills 增 AR-2026-09-PRJ2603-U1 | ✅ |
| ⑤退回对比 | 租赁单列表 cmpModal（静态 4 行：货品/出库量/已退回/未退回差异）+leaseOrders ops×9 首位插「退回对比」 | ✅ |
| ⑥租出计费 | 租赁单新建（模板+宿主）radio 行+切换注记；leaseOrders 9 行 billing 字段+详情 info「计费方式」行；详情模板注释锚 | ✅ |
| ⑦优先查库存 | 租赁单新建+新建销售订单（模板+宿主 4 文件）：STOCK_MAP 14 项行内「可用库存 N 套」提示（change/input 委托刷新）+不足红色提示条+「生成采购订单」「生成租入单」双按钮（go 去向单据页） | ✅ |
| ⑧背靠背草稿链 | 租赁单审核（模板+宿主）确认提交 toast「已生成租入单草稿 RZD-20260910-009，供应商待选」；租入单列表草稿演示行（三步入口 选供应商/修改/提交=showToast）+新建(草稿) stab+筛选 option | ✅ |
| ⑨归还关联同步 | 租入归还新建（模板+宿主）关联租入单下拉 data-key×3+syncReturnItems 明细表（货品/租入数量/已归还/本次归还数量[输入框]）+分批注记；rentInOrders returnItems×3（独立模板无 demo-data 走静态回退） | ✅ |
| ⑩订单附件 | 新建销售订单（模板+宿主）假上传交互区（文件名输入+添加+列表 2 行+删除）；销售订单详情 info「订单附件」行×8+注释锚 | ✅ |
| ⑪可审单据 | 权限配置模板+角色管理 roleModal（数据权限范围后矩阵 14 项·默认勾 8）+用户权限 roleModal 同源段；审核人（按角色匹配）注记落 租赁单审核/付款确认/销售订单审核（模板+宿主） | ✅ |
| ⑫审核人筛选 | 我的待办审核人下拉（王琳/袁丽晶/徐蔚/王强/李国栋/陈金/袁明）+filterTodo 扩展 data-auditor+重置联动；todoItems 12 行 fields.auditor | ✅ |
| ⑬多对多注记 | 上下游绑定（模板+项目档案+项目详情 3 处）灰字设计预留注记 | ✅ |

### 六道验证门证据（全部本会话自跑）

1. **audit 门** ✅：108 页 死链 0/JS 错 0+相对 g13baseline 逐键 diff 新增 0（`_scan_tmpdir/g13_audit_diff.py` 输出：`post 页数 108｜死链合计 0｜JS 错合计 0（豁免 0·其余新增 0）｜problems 逐键新增 0`）；本轮网络正常，F01 ERR_CONNECTION_CLOSED 豁免未触发（预注口径保留于 diff 脚本）。
2. **grep 门** ✅：13/13 全命中（`g13_grep_gate.py`：①=2②=2③新建1/确认1④生成2/应收2⑤=2⑥新建2/详情2/列表2⑦各5⑧=2⑨新建2/列表6⑩新建2/详情2/列表2⑪配置1/角色1/用户2⑫=1⑬各1）。
3. **PW 抽验门** ✅：4/4 PASS（PW1 库存查询转租筛选 13→3 行｜PW2 租赁单新建切 PLT-1210W 提示「可用库存 60 块」｜PW3 租入归还切 RZD-…-003 明细同步租入 30 只｜PW4 权限配置矩阵 14 项/默认勾 8/可取消勾）。
4. **回归门** ✅：verify_listfull 三批失败 0——batch1=**103**（库存查询新增筛选卡激活 select/input/重置 3 项可选断言，全过；正偏差）/batch2=88/batch3=43；v32_menu_verify 14/14。batch2 租入单（全部 6+新建(草稿) 1）与应收账单（全部 14/未开票 7）stab 期望值随新演示行同步（G12 修工具先例）。
5. **失败清单门** ✅：`_scan_tmpdir/goal-failures-g13-20260910.md` 存在，结论行=「G13 失败 0 项·偏差 6 条留档；13 项功能全部落地，六道验证门全过」。
6. **收尾门** ✅：git log ≥1（f68fa90）；**推送不通**——7890/10808/10809/14157 四端口 socks5 全部 Could not connect（无代理监听）+直连 443 超时，**待推送**（连同积压共 7 提交留本地，代理恢复后 `git -c http.proxy=socks5://127.0.0.1:<端口> push origin main` 一键带走）。

### 过程事故与处置（防再犯）

- **demo-data.js 语法事故**：4 处新记录插入点（stockFlows/rentInOrders/receivableBills/assetTracks 实体末条后）原文末条记录结尾 `}` 无逗号，插入后缺分隔逗号致整文件 SyntaxError、全站列表渲染断（node --check 定位 775 行）；g13_fix_commas.py 修复+复检通过。**教训：实体末位追加记录须先给前一条补逗号（或统一在倒数第二条后插入）；demo-data 改动后必跑 node --check 再进页面验证。**
- 冒烟脚本初版定位器笔误（未取 .first 计入全部表格 tbody）致 8 项假失败，修正后 43/43 全过（页面本体无误）。

### 偏差 6 条

见失败清单（stocks→stockFlows 落点/详情侧 info 行形态/verify batch1=103/用户权限矩阵同源段/草稿号 RZD 前缀/F01 豁免未触发）。
