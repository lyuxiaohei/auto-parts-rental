# G50 失败清单（移动端样板风样式优化 · 2026-09-16）

> **结论：无失败项**。以下为登记性记录（锚点素材/口径出入/断言甄别），均不阻塞完成判定，处置已在执行记录注明。

## R-1 · 锚点图 `axure-审批详情.png` 缺失（任务书第四节清单与实存不符）

- 现象：`_scan_tmpdir/g50_ref/` 12 张计数齐全，但第四节所列 `axure-审批详情.png`（注「同图含详情两态」）不存在；实存清单多出 `axure-产品详情.png`（经图像分析为电商域样板：商品/景点/直播详情，与审批域无关，未采用）。
- 处置：按 T0「缺图记失败清单继续，不得联网补抓」执行；T3 横幅/时间线完全按任务书 T1 组件参数（渐变值/节点 12px/连线 2px/配色映射）与 T3 结构规格（静态两节点/提交人提交时间取行字段/退化文案）文字规格施工，无视觉锚点缺口。
- 依据：任务书第四节文字规格已完整写死样式参数。

## R-2 · 验证门「chips=7 个」与实况 6 个不符（任务书内部口径矛盾）

- 现象：验证门 3 要求库存查询「7 个 chip」，实况＝全部＋KC 字典五状态（在库/客户端(租出)/客户转租出/退租待入库/租入）＝**6 个**，且 T5 明文「零交互变更、零数据变更」——补造第 7 个 chip 即违反 T5 且无数据源（stockFlows 运行时值域＝5 状态，无第 6 状态）。
- 处置：按 T5 约束与字典值域维持 6 chip；PW 断言按「6 个·单行横滑不换行」执行并通过（行数=1）。登记为任务书计数口径出入（沿「页数以执行时实测为准」同类先例）。
- 证据：`g50_probe.js` 运行时 dump（stockFlows 有效 15 行·状态 5 种）＋ KC 字典五值。

## R-3 · 验证门 4 grep 范式假阳性（`\bbtn\b` 命中 `m-btn`）

- 现象：任务书给定范式 `class="[^"]*\b(modal|btn|filter-bar)\b` 在 mobile/ 命中 6 处，逐处核验全为 `m-btn`（连字符构成词边界导致 m- 前缀类被误报）。
- 处置：以负向后顾严格式 `(?<![\w-])(modal|btn|filter-bar)` 复扫＝**0 命中**（PC 类名零混入成立）；范式出入登记不改判。

## 附 · 过程说明（非失败）

- 前置互斥 literal 口径：开工时原型目录 30 分钟窗口内的写入均为本会话刚收束的 G42 提交（已 push、无未提交原型改动），G42 非「正在写」，无并发写者——按前置校验 3/4 的条件语义放行并在此注明。
- 注释计数刷新（非行为改动）：待办审批「todoItems 19 行」→20（D-151 后）、库存查询「stockFlows 14 有效行」→15（G37 后）。
- `node` 本机不可用：验证门 2 以 `_m.js` 与备份逐字节 `diff -q` 相等证明零改动，按任务书「未改动则跳过并注明」处理。
