# 20260912-G23-PRD文档生成

> **状态**：⏳ 待执行（收尾时改 ✅ 并回填 commit 哈希）
> **commit**：（待回填）
> **日期**：2026-09-12
> **项目根（全部相对路径以此拼接）**：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\`
> **失败清单（绝对路径）**：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\goal-failures-g23.md`
> **形态**：精简命令路由（G21/G22 先例）——执行命令=`_scan_tmpdir/g23_prompt.md`，一切写死口径/计数预期/默认决策以本文档为准；本文档=该 goal 的完整凭据与执行记录处
> **前置依赖**：G22 已执行完成（✅ 4ff7f5a+66df14b+192d840，索引已挂行；2026-09-12 已合并至本地 82b031f）——本 goal 提取一律以 **G22 后现状**为准（租赁出库称呼/待办 16 类/comboOutbounds 9 行）；对原型仍只读

## 背景（2026-09-12 道远拍板）

1. **目的**：构建一份 PRD（产品需求文档），记录**最新版本的需求**——以 G22 后原型现状为需求基线，收拢 09-08 会议最终纪要与历次拍板
2. **章节格式**：按 `prd-auto-generator` 技能模板（道远点名）；模板在主机 `C:\Users\Administrator\.agents\skills\prd-auto-generator\`（7 文件在位已实测，见第 0 步清单）
3. **硬约束（道远原话「不要改原型，只生成文档即可」）**：原型目录对本 goal **只读**；产出物=1 份新文档+收尾回写四件（详见任务四组与纪律节白名单）
4. **产出编码**：`P2-R01-产品需求文档.md`（项目根）——P1-R01 附录 10.1 已预留「P2 = 功能调研/PRD（启用时在此声明）」，本 goal 即为启用动作；同步 10.1 启用声明+10.2 加行（收尾 T4）

## 需求来源与优先级（=第 0 步阅读顺序；冲突时序号小者胜）

1. **原型现状=需求真值**：`P3-R01-包装租赁管理后台原型/` 115 HTML（38 业务页+69 弹窗模板+F01+mobile 5）+`_data/demo-data.js`（38 实体）
2. **`agent-handoff/_AGENT基线.md`**——「已拍板不复议」「待客户方答复」「挂起决策与已知遗留」三清单
3. **`2026-09-08 汽车物流包装租赁第3次沟通原型演示/…[会议纪要-最终版].md`**——最新客户拍板（唯一声明「与原始 docx 冲突以本文档为准」）
4. **`P1-R04-术语表.md` V0.6**——命名唯一口径+废弃词表（第五节）
5. **`P1-R01-需求梳理与功能框架.md` V0.7**——REQ 21/FP 38/US 18 基线；注意其头部「信息来源」仍只列第 1/2 次沟通（滞后），以本表 1-3 为准
6. **原型支撑材料**：`P3-R01-A02-页面类型与入口对照表.md`（页面清单/入口真值）、`P3-R01-A05-字段字典.md`（字段全量真值源）、`P3-R01-A06-实体关系与状态机.md`（ER+状态机源）、`P3-R01-F01-业务流程导航图.html` v3.6（主线流程源）
7. **`P3-R01-包装租赁管理后台原型/_data/demo-data.js`**——实体字段与单据链样例

## 模式与章节结构（写死）

- **模式=标准模式**（技能 V1.2 模式选择表：常规 PRD 生成→模块级 3 段「用例→流程→数据」；评审模式需评审议题触发，本任务无）
- **章节结构**（按技能 §章节结构·标准模式）：

```
一、范围边界            In/Out Scope 表（含模块代号列；Out 列明已移除模块与不做的能力）
二.1 角色列表           角色/代号/说明（角色源=demo-data roles 9 角色；客户/供应商/路凯为外部角色）
二.{代号}.2 功能用例与验收   UC+AC 强绑定（Given-When-Then）；首个模块输出测试账号（全文仅一次）
三.{代号} 核心流程       Mermaid 主流程 + 状态流转表（含守卫条件列）+ 异常与边界（非功能并入）
四.{代号} 数据流向       CRUD 矩阵（角色代号列头·中文动词）+ 字段说明表（统计口径作为字段行，来源列填公式）
五、单据流转            [可选·有核心实体→产出]
六、接口时序            [可选·跨系统（路凯/吉客云）→产出，按注记口径]
七、实时通信            [判定不产出——无 WebSocket 推送，判定记录写明即可]
```

- **模板文件（主参考，主机实测在位）**：`SKILL.md`（V1.2）+`templates/version-prd.md`（V7.2 骨架/编号体系）+`templates/prd-module-section.md`（V1.0 模块 3 段/字段表列定义）+`rules/structure-rules.md`/`writing-rules.md`/`flowchart-rules.md`/`material-checklist.md`
- 执行机若上述路径不存在 → 按本节结构+技能 Anti-patterns 要点执行并记偏差，不阻塞

## 模块代号表（写死 10 模块；页面归属以 A02 现行版+菜单 v3.5 现状为准，一页只归一个模块）

| 代号 | 模块 | 页面域（摘要） |
|---|---|---|
| PRJ | 项目管理 | 项目看板/项目列表/项目详情 |
| SAL | 销售管理 | 销售订单（列表/新建/审核/详情）/销售出库 |
| LEA | 租赁管理 | 租赁单四件套（销售管理目录）/租赁出库/退租入库（含新建）/租入单/租入入库/租入归还/库存查询（客户在租下钻+资产轨迹） |
| PUR | 采购管理 | 采购订单/采购入库 |
| WHS | 仓储管理 | 其他入库/其他出库/库存调拨/盘点（列表+录入）/BOM 维护 |
| BAS | 基础数据 | 产品档案（供应商税率+四价三段式）/客商档案/库位/物料类型字典 |
| FIN | 财务管理 | 财务看板/盈亏报表/应收账单/应付账单/付款/回款/开票/水单核销（预收预付冲抵） |
| SYS | 系统管理 | 角色管理（权限配置+可审单据矩阵）/用户权限/数据字典（49 项）/操作日志 |
| WF | 待办与审批 | 我的待办 16 类（含租赁出库审核）+审核流（审核人/背靠背草稿链） |
| MOB | 移动端与登录 | PC 登录页/mobile 五页 M01-M05（企微 H5·免二次登录） |

- **模块编排序**（=章节推进序）：PRJ→SAL→LEA→PUR→WHS→BAS→FIN→SYS→WF→MOB（业务主线序）；与菜单组序冲突时以 A02/菜单现状为准并记偏差

## 前置校验（先于一切，任一不符的处理写死）

1. **单写者互斥**：python 扫 `P3-R01-包装租赁管理后台原型/` 与 `agent-handoff/` 最近文件 mtime，距今 ≥30 分钟无变化才动手（本任务书与失败清单自身 mtime 豁免——建档入库属任务下达动作，G21 偏差①先例）；不符 → 停止，失败清单记「疑似并行会话在写，30 分钟后可重跑」
2. **防重复执行**：`_索引.md` 无 `| G23 |` 行；项目根无 `P2-R01-产品需求文档.md`——任一存在 → 停止，失败清单记「疑似已执行」
3. **技能文件在位**：第 0 步清单 7 文件 `os.path.exists` 全过；缺失 → 记偏差，按本任务书内嵌结构执行
4. **材料存在性**：需求来源 1-7 项路径断言存在；某项缺失 → 该源降级「尽力提取」并记失败清单，不阻塞
5. **口径基数记录**：原型 HTML 总数（预期 115）、demo-data 实体数（预期 38）、todoItems 待办键数（预期 16）、A05 头部统计读数——与预期不符以实际数为准记偏差，验证门基数随之（HTML=115 与待办=16 为 2026-09-12 合并 G22 后实测）
6. **工作区存档**：`git status` 有未提交改动 → 先 `git add -A && git commit -m "存档：G23 开工前快照（不 push）"`
7. **开工时间戳落盘**：`_scan_tmpdir/g23_starttime.txt`（ISO 格式；验证门 2「原型零改动」依据）

## 第 0 步·上下文获取（动手前必做）

1. `agent-handoff/_AGENT基线.md`——第四节（历史事故与真相）+第七节（上手清单）逐条适用
2. 本文档全文——前置校验 7 条 / 任务四组 / 默认决策表 12 条 / 验证门 6 条，一切写死口径以本文为准
3. 需求来源 2-7 项（见「需求来源与优先级」节，序即优先级）
4. prd-auto-generator 技能 7 文件（路径见「模式与章节结构」节）
5. `agent-handoff/20260911-G22-租赁出库称呼统一与杂项清理.md`——G22 已执行，通读其执行记录了解称呼/计数改动面（组合出库→租赁出库·待办 15→16 类·opLogs 五组映射·租入-路凯来源口径），避免沿用旧称呼旧计数

## 任务一：材料收集与版本级章节（T1）

1. 按需求来源优先级通读 7 源；产出「一、范围边界」——In Scope=代号表 10 模块页面全集（以 A02 现行版为准）；Out Scope 至少含：退租申请/组装/拆卸管理/丢损赔偿单（四移除模块）+客户端自助下单+扫码+采购计划（暂缓）+归属权字段（退场）+路凯系统替换（仅对接）
2. 产出「二.1 角色列表」——角色源=demo-data roles（9 角色）；客户/供应商/路凯/吉客云为外部角色列入
3. **分模块追加写**：每完成一个章节/模块立即落盘保存（禁全文最后一次性生成——中断可续写）

## 任务二：模块 3 段 ×10（T2，每模块三段齐才进下一模块）

1. **功能用例与验收**：UC 表+AC 紧邻（Given-When-Then；AC 表含「对应用例」列强绑定）；**每条用例必须能在原型找到对应页面/弹窗/操作**（禁臆测——技能 Anti-pattern 首条）；每模块三段开头 1-2 句概括内注明对应原型页面路径（叙述体溯源）
2. **待确认项处理**：基线「待客户方答复」+「挂起决策」中影响需求口径的（计费单价/押金/赔偿标准、租入资产入库方式、吉客云/路凯账号、即将到期提醒等）→ 不臆测结论，PRD 内标「待确认」并汇总入「待确认清单」节（挂范围边界之后）；界面已有静态示例表达的（如预收预付冲抵）按现状写并注记「原型按静态示例表达，正式版做收付类型+冲抵逻辑」
3. **核心流程**：每模块 ≥1 张 Mermaid flowchart 主流程+状态流转表（含守卫条件列，状态机源=A06 三组状态机）+异常与边界子节（非功能需求并入，禁单列章节）
4. **数据流向**：CRUD 矩阵+字段说明表（列结构以 `templates/prd-module-section.md` V1.0 现行定义为准）；每模块取核心单据/档案实体全列，表尾注记「全量字段以 P3-R01-A05-字段字典.md 为准」；统计口径指标（如盈亏报表/库存统计）作为特殊字段行，来源列填公式
5. **计费与单据口径以 G22 后现状写死**：租价=计费方式（按时间周期/按次）+周期单位（年/月/日·默认月）+数值；**无日租金**；应收生成=按月定期/按次套数；退租两事件模型（退租入库=客户退租统一入口/租入归还=归还供应商独立事件）；台账合并后无独立在租/租出台账；出库单据称「租赁出库」（组合出库为旧称呼，全文禁用）；租赁出库有审核环节（待办 16 类之一）
6. **测试账号**：全文仅首个模块（PRJ）「二.PRJ.2」验收表后输出一次（演示角色口径，源=demo-data users/roles）；其余模块引用「测试账号见二.PRJ.2」

## 任务三：可选章节与质量检查（T3）

1. **五、单据流转**：核心单据链 Mermaid+文字——销售订单→销售出库/租赁单→租赁出库→退租入库→应收；采购订单→采购入库→应付；租入单→租入入库→租入归还→应付；预收/预付冲抵支线
2. **六、接口时序**：路凯（数据对接：下单+结算）、吉客云（自购 SaaS）——仅按纪要/基线注记口径写，标注「对接细节待客户方答复（账号/基础资料清单/单据样例未取得）」
3. **七、实时通信**：判定不产出（原型无 WebSocket 推送），判定记录一句话写明
4. **质量检查 3 项**（技能 §质量检查）：①编号——标题序号连续无跳号，UC/AC/代号全局唯一；②追溯——每条 AC「对应用例」列可回链 UC；③范围——In Scope 每模块都有 3 段，测试账号全文仅一次，页面清单覆盖（标准模式下=In Scope 模块全覆盖）——先跑 T4 验证门 1 结构断言再下结论

## 任务四：收尾回写（T4，硬性）

1. 本文档标 ✅+执行记录（任务逐段结果/验证门证据摘要/默认决策命中/偏差闭环）+commit 哈希回填头部
2. `_索引.md` 追加 G23 行：日期/G 号/任务名/状态/commit 哈希/文件名
3. `_AGENT基线.md` 快照滚动：当前任务块更新+文档地图加 P2-R01 行+**顺带更正文档地图 P1-R01 版本行 V0.6→V0.7**（建档时已核实的滞后，借收尾滚动清偿）
4. `P1-R01-需求梳理与功能框架.md` 附录两处（仅此两处，其余正文零改动）：10.1「预留：P2 = 功能调研/PRD」行改启用声明（启用日期+P2-R01）；10.2 文档清单加 P2-R01 行（编码/路径/说明）
5. 失败清单结论行贴出；`git add -A && git commit`（信息前缀 G23，**禁 push**）；哈希回填本文档头部与索引行

## 默认决策表（可预见小决策全部预置；表外情形→最简合理默认+收尾注明，禁止提问）

1. 模式=标准模块级 3 段（技能路由表「常规 PRD 生成」项；评审模式无评审议题不触发）
2. 输出=项目根 `P2-R01-产品需求文档.md` 单文件（项目编码规则优先于技能默认 `PRD/` 目录命名）；文档内版本标 V1.0；**只产 md，不产 HTML 外发版**（外发另行任务）
3. 需求冲突裁决序=需求来源优先级表（原型现状>基线拍板>09-08 纪要>P1-R01 V0.7）；裁决不了的进「待确认清单」
4. 模块代号用 2-3 字母助记码（PRJ/SAL/…）——技能示例单字母在 10 模块下撞码（Project/Purchase 同 P），编号体系 `UC-{代号}-{序号}` 兼容多字母
5. 模块编排序=PRJ→SAL→LEA→PUR→WHS→BAS→FIN→SYS→WF→MOB；与 A02/菜单现状冲突以现状为准记偏差
6. 字段说明表列数以模块模板现行定义为准（模板正文 9 列：字段中文名/字段名/类型/校验规则/取值范围/来源/去向/错误提示/说明；SKILL.md 概述称 8 列系版本差，以模板为准记偏差）
7. 术语一律用术语表 V0.6 现行词，废弃词禁用（验证门 grep=0）；本 goal 不新增术语条目、不改术语表（确需新词→PRD 待确认清单+失败清单注记）
8. 移动端 MOB 纳入 In Scope（纪要十.2-4 拍板域：企微 H5+免二次登录）；PC 登录页归 MOB
9. Mermaid 图内节点文本照术语表现行词；图不引外网 CDN（md 由阅读器渲染，不内嵌 mermaid.js）
10. G22 已执行（4ff7f5a+66df14b+192d840）：提取以 G22 后现状为准；「组合出库」等旧称呼不得进 PRD 正文
11. PRD 语言=中文；篇幅不设上限以覆盖为准，UC 数量按原型操作面自然界定不凑数不缩水
12. 需求基线快照口径：文档头部写明「基线=G22 后原型（115 HTML·demo-data 38 实体·待办 16 类）+2026-09-08 会议纪要最终版+基线拍板至 2026-09-12」

## 失败处理策略（无人值守铁律）

任何 assert 失败/材料缺失/预期外结构：跳过该项，记入失败清单 `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\goal-failures-g23.md`（字段：任务号/文件/原因），继续下一项。禁止卡住等待、禁止对原型没有的流程臆测编造、禁止静默吞掉。收尾报告必须含失败清单结论行（空清单给「无失败项」）。

## 纪律（违反=返工）

- `agent-handoff/_AGENT基线.md` 第四节+第七节逐条适用，不复述
- **原型只读铁律（本 goal 特有·最高优先）**：`P3-R01-包装租赁管理后台原型/` 全目录（含 `_data/`、A03/A04 json、F01、mobile/）**零写入零改名零删除**；提取只用只读方式（python 读源码足够，无需 Playwright）
- **新建文件白名单**：`P2-R01-产品需求文档.md`、`_scan_tmpdir/g23_verify.py` 及 g23_* 过程留档、`_scan_tmpdir/g23_starttime.txt`、失败清单；**修改白名单**：T4 收尾四件（本任务文档/_索引/_AGENT基线/P1-R01 附录两处）——白名单外零改动
- 「原型手工维护·禁裸 open('w')」铁律**不适用于** P2-R01 新建文档（新文件整篇生成为允许操作）；但对 P1-R01 附录两处追加必须精确插入+assert 计数+幂等
- 中文路径一律 python 内嵌处理（禁依赖 shell 中文参数）；验证数字一律自己重跑不引用历史读数；多股改动入库一律 `git add -A`（`_data/` untracked 坑）；禁 push

## 验证门（全部满足才算完成；证据=命令输出贴收尾报告）

1. **结构断言**：`_scan_tmpdir/g23_verify.py`（新建留档）逐行 PASS——
   - `P2-R01-产品需求文档.md` 存在且 ≥500 行
   - 章节计数：`一、范围边界`=1、`二.1 角色列表`=1、`待确认`清单节=1
   - 10 代号逐一断言：`功能用例与验收`/`核心流程`/`数据流向` 标题各=1（regex 带 `{代号}`）
   - UC 编号 regex `UC-[A-Z]{2,3}-\d+` 全局重复=0；AC 行「对应用例」列含 `UC-` 的比率=100%
   - mermaid 代码块 ≥11（10 模块流程+单据流转）；每模块 ≥1
   - 废弃词 grep=0（词表=执行时从 `P1-R04-术语表.md` 第五节废弃词行现场提取，逐词输出命中数）+`组合出库`=0（G22 旧称呼不得回潮进 PRD）
   - `测试账号` 全文出现次数=1；Out Scope 含 退租申请/组装/拆卸/丢损赔偿 各≥1；`计费单价`/`租入资产入库方式`/`吉客云` 各≥1（待确认覆盖证据）
2. **原型零改动门**：python 校验 `P3-R01-包装租赁管理后台原型/` 全文件（含 `_data/`）mtime ≤ `g23_starttime.txt` 时刻 → 违例清单=空行贴出
3. **git 门**：`git status --porcelain` 输出中 `P3-R01-包装租赁管理后台原型/` 前缀行数=0
4. **质量检查 3 项**：编号/追溯/范围 结论行逐项 PASS 贴出
5. **回写四件证据**：本任务文档 ✅ 行、`_索引.md` G23 行、`_AGENT基线.md` 含 P2-R01 行、`P1-R01` 10.1 启用行+10.2 P2-R01 行——grep 输出贴出
6. **git 批次**：commit 完成（G23 前缀）+哈希回填任务文档与索引行；未 push（`git log` 本地领先即可）

## 执行记录（收尾时填）
