# 20260915-G37-转移出库落地（含转租登记退场）

> **状态**：⏳ 已立项待执行
> **来源**：D-106「转移出库」（第4次沟通 41:50-47:46＋会后确认）＋ 袁工 09-15 结算回复（D-132）＋ 道远 09-15 拍板「转租登记退场，并入转移出库单」
> **调研依据**：`_scan_tmpdir/g32_t2_转移出库调研.md`（路凯/CHEP/金蝶调拨/通用 ERP 4 家×6 维度）＋ `_scan_tmpdir/g37_转租转移业界处理调研.md`（会计 CAS21／RapidWorks／RentalPoint Bill To／专利 CN115578146B／T-JFLP 002-2021）
> **执行命令**：`_scan_tmpdir/g37_prompt.md`（形态 A）
> **执行前提**：单写者互斥／git clean；备份先行
> **红线**：不勾稽原单（D-106）｜不碰计费字段（D-122）｜已审核单据不可改（D-109）

## 一、拍板口径（道远 09-15）

1. **形态＝转移出库单**：独立列表页＋新建/详情弹窗；**不勾稽原租赁单**（转移单不引用原单，只记转出方→接收方）
2. **结算方式两值**（D-132·袁工回复「两种情况」）：
   - **按租出结算**（默认·主流）——租金仍向**直接客户**计收；**转移单不进财务链路**（纯库存与货权记录）
   - **按终端结算**（特例·个别早期项目）——转移生效后**后续账单主体切换为终端客户**（历史账单不动）
   - 默认值取**项目档案「转租结算方式」属性**，转移单可**单据级覆盖**
3. **转租登记退场**：库存查询行内「转租登记」「转租还回」两个弹窗**删除**，改为行内「转移出库」按钮**跳转转移出库录单页**（带出物料与转出客户）；库存状态「客户转租出（客户的客户）」**保留**，改由**转移出库单审核后驱动**
4. **转租还回**：在转移出库单上做「**终止转移**」动作（转移可逆），状态回到「在客户（租出）」
5. **T0 落账**：P2-R01 取执行时当时最大 D 号＋1（D-133：转移出库形态＋转租登记退场）

## 二、第 0 步·上下文获取（动手前必做）

1. `agent-handoff/_AGENT基线.md`——第四节＋第七节**逐条适用**
2. 两份调研：`_scan_tmpdir/g32_t2_转移出库调研.md`（形态备选与行业对照）｜`_scan_tmpdir/g37_转租转移业界处理调研.md`（结算责任与单据流取证）
3. 现状实读：`仓储作业/库存查询.html`（zzRegModal/zzBackModal 两个弹窗与其 pn-hint 定义·L913-952）｜`基础数据/产品档案.html` 与 `项目管理/项目档案.html`（项目档案加属性处）｜`_data/demo-data.js`（stockFlows 的「客户转租出」演示行 XNC-ZZ）
4. 规范与样板：`skills/admin-ui-spec/references/common/surface-selection.md`（载体选择）＋`form.md`（表单细则）；样板页＝`采购管理/采购订单新建.html`
5. **勿重做**：G33 三页、试点页、G35/G36 范围外内容不动

## 二·补 D 组任务（随本任务一并执行·道远 09-15 采纳）

### D1 页面内术语三条统一（D-142）
把菜单已定版、页面正文滞后的三条术语全站统一（纯文本替换·零业务影响）：
1. **水单 → 银行回单**（~53 处）
2. **盈亏 → 损益**（~35 处·菜单已是「项目损益」）
3. **回款 → 收款**（~42 处·菜单已是「收款登记」）
- 白名单：**P1-R04 术语表本身、F01/A05 的沿革注记、需求文档与会议材料**不动
- 替换后原型目录 grep 残留 0（白名单除外）

### D2 审核角色调整为 6 个（D-143）
- `roles` 实体 7 → **6 个**：**系统管理员／财务／商务／物流／采购／项目经理**
- `users` 实体 role 字段同步（主管并入对应部门，如「商务主管」→「商务」）
- 角色管理页、用户权限页、审核矩阵的角色名同步
- 补齐**采购**与**项目经理**（后者＝订单「项目经理代下」的承载角色）

### D 组口径
- D1 用**精确字符串＋assert 计数**；白名单不动
- D1 范围＝原型目录（各页 HTML／_data/*.js／A 类文档）
- D2 只改**角色维度**，不动审核矩阵的单据类型项

## 二·补 字段口径任务（随本任务一并执行·道远 09-15 指示）

> **边界**：全站字段口径统一归 **G38**（`agent-handoff/20260915-G38-字段口径统一.md`）。本节只收**与本任务同文件的 3 条**——避免同一页面两次进出，且新实体「建立即规范」不欠新账。

### F1 库存查询页用词收敛（与 T3 同文件·一次改完）
- 列头「**名称**」→「**物料名称**」（主表；下钻表已有「物料」列，勿重复改名）
- 列头/筛选项「**仓库**」→「**库房**」（库房级；「库区」层已在 G31 T3 从模型删除，不入用词）
- 顺带校对 A03 标注层 pin 文案中的同词

### F2 新实体 `transferOutbounds`「建立即规范」
- 字段键与列头按默认决策表【字段口径】节命名，**不留待 G38 回改**
- 演示数据 `ZY-*` 的 `cells` 顺序必须与 thead 列一一对应（防 G36 B1 那类错位事故）

### F3 项目档案「转租结算方式」属性命名对齐（T2 同文件）
- 新增属性标签与该页既有列头一律用现行词（项目/客户/供应商/状态/日期口径）
- 复核发现已合规 → 记「无改动」继续，不计失败

> **执行顺序（单会话内完成·不拆批）**：T0 落账 → T1 转移出库单页面 → T2 结算方式＋项目档案（含 F3 命名对齐）→ T3 转租登记退场（含 F1 库存查询用词）→ T4 演示数据（含 F2 `transferOutbounds` 建时即规范）→ T5 验证 → **D1 术语三条全站替换** → **D2 角色 6 个** → 全量审计＋回写＋独立验收
>
> 本任务为**一个 goal 会话**内完成，**不拆批**。若中途无法完成某项：按失败处理策略记失败清单并在收尾汇报，**不要停下等待**；可后续以**同一 goal 续跑**，已完成项不重做。

## 三、任务分组

### T1 转移出库单页面
- 新建 `租赁管理/转移出库列表.html`＋`租赁管理/弹窗/转移出库新建.html`、`转移出库单详情.html`（页内嵌＋弹窗模板双层）
- 列表列：转移单号／转出方（直接客户）／接收方（终端客户）／物料／数量／**结算方式**／转移日期／状态／操作
- 字段：转出方（带出）／接收方（客商档案选或手工录入·对应 D-108）／物料（字典/主数据）／数量／转移日期（`type=date`）／关联项目（可选）／**结算方式**（默认带项目档案值·可覆盖）／备注（textarea·表单卡末尾）
- 状态流：**待转移 → 已转移**（终端不进系统·无"确认签收"环节）；「**终止转移**」动作（已转移 → 已终止·状态回「在客户（租出）」）
- 菜单：租赁管理组加「转移出库」（菜单 v5 → v6·全站侧边栏同步）

### T2 结算方式字段与项目档案属性
- 项目档案（`项目管理/项目档案.html`＋其弹窗）加「**转租结算方式**」属性（两值·默认「按租出结算」）
- 转移出库单新建时从所选项目带出默认值，允许单据级覆盖
- 按终端结算时：**后续应收账单主体切换为终端客户**（历史账单不动）——落地程度＝演示数据可表达（不新建财务模块）

### T3 转租登记退场（D-78 更新）
- 删除 `仓储作业/库存查询.html` 的 `zzRegModal`／`zzBackModal` 两个弹窗及其专属脚本（`zzToast` 等若仅服务这两个弹窗一并清）
- 行内操作改为「**转移出库**」→ `go('转移出库新建页')`（带出该行物料与在租客户）
- **库存状态「客户转租出（客户的客户）」保留**，驱动方由弹窗改为转移出库单审核
- 同步：A03/A04 标注层（`proto-pin-4` 客户转租说明改述）＋A02 对照表＋A05 字段字典

### T4 演示数据
- 新增 `transferOutbounds` 实体（≥3 行·前缀 `ZY-`·避已占用前缀）
- 造**两个项目各一种结算方式**（一个按租出、一个按终端），数量与既有 stockFlows「客户转租出」行一致
- 转移单与库存状态联动可见（列表可查、库存查询状态为「客户转租出」）

### T5 验证与回写
- 审计 0 死链 0 JS 错误 diff 新增 0＋`node --check`＋PW 抽验（新建/终止/跳转/结算方式带出）
- 回写三件＋A02/A05/A03 同步

## 四、默认决策表（执行中不再提问·逐条按此办）

### 命名与编号
- 转移单号前缀 **`ZY-2026MMDD-NNN`**（避已占用：RK-/TK-/FK-/SK-/CK-/LZ-/SO-/PO-/CGTH-/XSTH-/TKD-/PD-/GH-/QT-/DB-/RZD-）
- 列表页与弹窗命名：`转移出库列表.html`／`弹窗/转移出库新建.html`／`弹窗/转移出库单详情.html`

### 结算方式
- 两值固定文案：**「按租出结算」／「按终端结算」**（与 D-132 口径一致）
- 默认值＝项目档案「转租结算方式」；项目档案未设时默认「按租出结算」
- 「按租出结算」时转移单**不生成任何应收/账单**；「按终端结算」时后续账单主体切换但**历史账单不回改**

### 形态与样式
- 照 `采购管理/采购订单新建.html` 样板：控件宽 380px／日期 `type=date`／备注 textarea 并入表单卡末尾／提交条按钮居中／页顶不放「返回列表」
- 下拉值域动态取（字典 `dictItems`／主数据实体），不写死
- 新增 `onclick`/`onchange` 须自带 DOM 副作用

### 字段口径（本任务内联·G38 全站口径落地前适用）
- 物料列一律 `物料｜物料名称｜物料编码`；**禁「货品」「商品」**
- 库房层级：库房级→`XX库房`；库位级→`XX库位`；**「库区」不入用词**
- 时间列：只到日→`XX日期`；精确到分→`XX时间`
- 金额列：含税/未税必须显式（`含税金额(元)`／`未税金额(元)`），**禁裸「金额(元)」**
- 价格四件套（本任务若涉及金额列）＝`未税单价(元)｜税率｜含税单价(元)｜含税金额(元)`
- **只改标签与列头，不动数据键名与数值**（G34 先例）；不动计费字段（D-122 红线）

### 范围边界
- **不勾稽原单**：转移单无「关联租赁单」字段（D-106）
- **终端客户**：客商档案可选或手工录入（D-108 口径），不做强制建档
- 删弹窗前 grep 全站确认无引用；删后跑全量审计
- 状态机只加「待转移/已转移/已终止」三态，不新增库存状态

### 兜底自愈
- 项目档案加属性处结构不符 → 改在转移单内做默认值（并记失败清单）
- 库存状态联动取不到 → 保留静态状态、记失败清单
- 复核发现已合规 → 记「无改动」继续，不计失败

## 五、失败处理策略（无人值守铁律）

- 任何 assert 失败／锚点不匹配／结构异常：跳过该项，记入 `agent-handoff/goal-failures-g37.md`（任务号＋文件＋原因），继续下一项
- 禁止卡住等待、禁止猜测改写、禁止静默吞掉失败
- 前置校验任一阻塞性不符＝停止并写失败清单
- 收尾必须给失败清单结论（可为空；空则「无失败项」）

## 六、纪律（违反=返工）

- **基线第四节＋第七节全部逐条适用**（按动作类型反查：改 HTML 块→带标签锚＋配平；改 demo-data→精确替换＋`node --check`；Playwright→编程点击＋`unquote()`＋`textContent`；备份还原→按原相对路径；改内嵌 JS→必跑该页 audit）
- 读取-精确替换＋`assert` 计数＋幂等；禁裸 `open('w')` 整页写入既有页面
- 备份先行：`P3-R01-包装租赁管理后台原型/` 整体复制至 `_scan_tmpdir/backup-g37-20260915/`
- 多股改动 `git add -A`；工作区有非本 agent 改动 → **只提交精确路径**
- 中文路径 python 内嵌／Windows `newline=''`／行尾与宿主一致
- 新增 `onclick` 必须自带 DOM 副作用；弹窗双层同步（本任务为新建页＋新建弹窗）

## 七、验证门（证据全部贴进对话）

> **验收方式**：按 goal-creator v9——执行者自检通过后派**独立子 agent**（只读·自查取证）逐项复验，结论原样贴进对话

1. **页面在位**：转移出库列表页＋2 弹窗模板存在——证据＝文件清单
2. **菜单同步**：租赁管理组新项在全站侧边栏出现——证据＝菜单验证脚本输出（v32_menu_verify）
3. **结算方式字段**：两值可选＋默认从项目档案带出＋可覆盖——证据＝PW 断言
4. **财务口径**：按租出时转移单不生成账单；按终端时后续账单主体为终端——证据＝PW/数据断言
5. **转租登记退场**：库存查询无 `zzRegModal`/`zzBackModal`；行内按钮跳转移出库页——证据＝grep 计数 0＋PW 跳转断言
6. **状态联动**：审核后库存状态为「客户转租出」；终止后回「在客户（租出）」——证据＝PW 断言
7. **演示数据**：两项目各一种结算方式——证据＝数据输出
8. **审计**：`audit_interaction.py` 全量—死链 0／JS 0／diff 新增 0——证据＝汇总行＋新基线
9. **node --check** 退出 0＋页数变化核对——证据＝输出行
10. **回写**：任务档执行记录｜`_索引.md` G37 行挂哈希｜`_AGENT基线.md` 快照｜A02/A05/A03 同步——证据＝diff 行
11. **独立验收**：子 agent 逐项结论与总判定行原样贴进对话
13. **D1 术语清零**：三条术语在原型目录残留 0（白名单除外）——证据＝逐词 `str.count` 输出
14. **D2 角色 6 个**：roles 6 行·users role 同步·页面对照——证据＝数据与 PW 断言
15. **字段口径**：库存查询页「名称」「仓库」残留 0；`transferOutbounds` 字段键与列头符合内联口径；`ZY-*` 的 cells 数＝thead 列数——证据＝`str.count` 输出＋字段清单＋列数断言
12. **失败清单结论行**＋**git 提交**（前缀 G37·禁 push）——证据＝输出行

## 八、与其他任务的关系

- **G36**：**必须待 G36 全部批次（B2 收尾→B3→B4→B5）完成后执行**——G36 逐批重建页面载体（删弹窗模板＋从样板页建新页），若本任务先改（D1 术语三条属全站替换·菜单 v6 属全站侧边栏），G36 后续批次重建时会**把改好的文案覆盖回旧版**。G36 在进行中时不得开工
- **G38（字段口径统一·待执行）**：全站字段口径统一归 G38；本任务只做与其同文件的 **F1-F3**（见补节）。**两者串行**——G38 已先行则本任务以现状为准只补差集；本任务先行则 G38 开工时以现状为准
- **G35**：筛选区/脱敏已覆盖本任务涉及页面的术语与名称，执行时以现状为准
- **D-78**：本任务落地后，「库存查询行内转租登记保留不动」被**替代**（改由转移出库单承载）；D-78 状态在落账时更新
- **D-108**：终端客户仍按「直接输入简称/全称」；若选「按终端结算」需引导补档（记设计注记，不强制）

## 执行记录（开工时填）
