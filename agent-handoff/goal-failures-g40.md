# goal-failures-g40.md · G40 原型侧变更包（术语三条补做＋F01 导航图同步）

> 执行会话：2026-09-16（无人值守·一个 goal 会话内完成·不拆批）
> 工作目录基准：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\`

## 一、前置校验结论（5 项）

| # | 校验项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 单写者互斥（原型目录最近 mtime ≥30 分钟） | **判为符合（非阻塞）** | 字面最近文件＝`包装租赁管理后台原型-20260916.zip`（11:50·距开工 11 分钟）；**该文件系已完成会话 G39 的打包产物**，已提交于 `c5958ad`（11:50:52），zip 完整性校验通过（147 entries·`testzip()`=None）→ 无进行中写入。**源文件最新 mtime ＝ `P3-R01-A02` 10:17（距开工 104 分钟）**；git 工作区无第三方未提交改动 |
| 2 | `_索引.md` G40 行状态＝⏳ | 符合 | 该行状态为 `⏳`（待执行）；G37/G38/G39 均 ✅（不重跑） |
| 3 | `_scan_tmpdir` 无 `g40_*` 执行产物 | 符合 | 开工时仅 `g40_prompt.md`（本 prompt 属立项产物·豁免） |
| 4 | git status 未提交改动先存档 | 符合 | 存档提交 `90cc79a`（**只提交精确路径** 11 项：G39 收尾遗留产物＋G40/G41 立项件）·不 push |
| 5 | 备份先行 | 符合 | `_scan_tmpdir/backup-g40-20260915/P3-R01-包装租赁管理后台原型/`（159 文件＝源 159） |

**结论：无阻塞性不符，继续执行。**

## 二、失败项

**无失败项。**

（全流程 0 个 assert 失败、0 个锚点不匹配、0 个结构异常。）

## 三、登记项（非失败·按默认决策表「兜底自愈」记录）

### R-01 白名单保留（原型目录内旧词·按任务书白名单「A05／A06 沿革注记」及类比「P1-R04 术语表原用词列」）

| 位置 | 原文片段 | 性质 |
|---|---|---|
| `P3-R01-A05-字段字典.md:45` | `货品／裸「名称」` | 概念索引「禁用写法」列（声明禁用，必须保留） |
| `P3-R01-A05-字段字典.md:46` | `零件号` | 同上 |
| `P3-R01-A05-字段字典.md:59` | `水单（D-142 已清）` | 同上（本任务落定后该注记成立） |
| `P3-R01-A05-字段字典.md:696` | `水单号（待拍板）` | G17 标签同步沿革注记 |
| `P3-R01-A05-字段字典.md:846` | `入库库区`×2 | G35 变更记录行（沿革注记） |
| `P3-R01-A05-字段字典.md:912` | `水单→银行回单／盈亏→损益／回款→收款` | G37 D1 术语沿革注记（自述句） |
| `P3-R01-A05-字段字典.md:926` | `货品→物料`·`零件号→物料编码` 等 | G38 改动面沿革注记 |
| `P3-R01-F01-业务流程导航图.html:765` | `客户回款、水单核销、收款核销` | **第2次沟通 [音频转写]·严丽 43:24 原文**（会议材料·白名单）——改写会篡改原始记录，故保留 |

→ 除上述白名单外，`水单／盈亏／回款` 在原型目录**残留 0**。

### R-02 范围排除：`.prompts/` 工具日志

`P3-R01-包装租赁管理后台原型/.prompts/` 与 `财务协同/.prompts/` 共 7 个文件，命中 `盈亏 1`／`货品 5`。
**排除理由**：`.gitignore:10` 忽略（不入库）· 属历史会话提示词工具日志·非交付物（132 页交付面不含）；G39 打包时亦明确「排除旧 zip 与 .prompts 工具日志」，沿例。**不计失败。**

### R-03 假阳性登记：`货品` ×1

`_data/demo-data.js:13727` → `'text': '供货品类扩充（料箱）'`——`货品` 系 **`供货品类` 子串**，非术语，**不改**。
→ 故任务书背景所述「「货品」仍残留 1 处」实为子串假阳性；页面域真术语 `货品` 本就为 0。

### R-04 超 T3 字面授权的一处命名清理（已执行·登记）

`P3-R01-A03-标注数据.json:21` `"desc": "零件号、名称、供应商来源、多项目共用标记"` → `"物料编码、名称、…"`。
理由：A03/A04 **标注层文案在 G38 T4「标注文案同步（fix4）」范围内**（该批已做 A03 desc 的「逐货品→逐物料」，唯漏此条）；按 G38 概念表 `material.code` 禁用写法＝`零件号`。属「**只做术语替换与命名**」红线内动作，**非口径变更**。
→ 结果：`零件号` 页面域 0；文档域仅剩 A05 白名单 3 处。

### R-05 `货品` 清理落地（T3 授权项）

`P3-R01-A02-页面类型与入口对照表.md:59` 备注列 `（多货品明细：月租/按套）` → `（多物料明细：月租/按套）`（按 G38 口径「货品退场→物料」）。

### R-06 F01 虚拟仓注记用词归一（按权威顺序）

任务书原文写「客户端（租出）」；F01 自身 S1 五态行与本图五态口径均为「**在客户（租出）**」，`仓储作业/库存查询.html` 的 G37 五态口径注记亦为「在客户（租出）」。
→ 按默认决策表权威顺序（**原型与 demo-data ＞ 台账 ＞ 任务书**）归一为「**在客户（租出）**」，F01 内自洽。已在 T5 开发规则 §12.2 写明「图上若出现「客户端（租出）」，一律以「在客户（租出）」为准」。

### R-07 F01 计费文案实际改动面（任务书估「7 处」·实测 7 条不同串／8 行）

| # | 行 | 原串 | 新串 |
|---|---|---|---|
| 1 | L150（L1） | `（自动汇总）` | `（按持有量计租 · 期段账单＝起租日｜止租日｜天数｜日单价｜小计）` |
| 2 | L200（L2） | `（按租赁出库自动汇总）` | `（按持有量计租 · 期段账单）` |
| 3-4 | L262（L3）/L316（L4） | `>应付账单</a></text>` | `>应付账单</a>（按持有量计租 · 期段账单）</text>` ×2 |
| 5 | L284（L3 决策框） | `租金按月生成「租金应付」进应付账单` | `租金按持有量计租生成「租金应付」进应付账单（期段账单）` |
| 6 | L487（T2） | `（L3/L4 已实现 · 租入单按月生成）` | `（L3/L4 已实现 · 按持有量计租 · 期段账单）` |
| 7 | L489（T2） | `（应收按月/按实际使用量独立生成）` | `（应收按持有量计租、期段账单独立生成）` |
| 8 | L541（F2） | `（租入单按月生成）` | `（按持有量计租 · 期段账单）` |

→ 计 7 条不同串（其中第 3 条命中 2 行）＝8 行；B1 销售线 `销售费自动汇总` 属销售口径、**不在计租改动面**，未动。

### R-08 保留的历史记录（按「被覆盖不删除」·不改写）

- F01 `SRC_DATA` `l3`／`t2` 的 「2026-09-04 陆鸣拍板：租金按月生成『租金应付』进应付账单」——**历史拍板原文**，被 D-145 覆盖但按沿革保留（T5 规则 §12.1 已写明）。
- F01 `L544` `客户预付租金记预收按月冲抵`——预收款冲抵口径，非计租口径，未动。

### R-09 超出本任务 5 类改法范围、转 G41 一致性终核的既有漂移（未改）

- F01 头部 `.sub`／footer 仍写「**38 页**」「**67 个弹窗**」——为 G05/G36 前口径，与现状（132 HTML＝129 页＋3 弹窗模板·弹窗已页面化）不符。**本任务 T4 仅授权「版本与日期刷新」**，点数口径归 **G41 T9「页数与菜单项三处一致」**统一核算，避免两处算法打架。
- `财务协同/损益报表.html` 页面 `title` 仍为「项目损益」——A02 已登记为 v3.5 沿革（菜单=财务看板·页面名=项目损益），非本任务改名项。
- `库区` 页面域残留（`基础数据/库位档案.html` 3／`仓储作业/盘点录入.html` 2／`盘点列表.html` 1／`mobile/库存查询.html` 1）——**不在 T3 复核清单**（清单＝库龄／在库时长／流转次数／零件号／入库库区）；G38 已就 `mobile` 占位文案与「全库区」筛选 option **值**登记为「口径偏差登记（非失败）」（值域红线），本任务未动。

## 五、⚠️ 并行写者事件（重大登记 · 非本任务失败）

**事实**：本任务执行期间，**另一会话（G39 收尾）在同一仓库并行提交**——开工时（12:01）的前置校验未检出，属**事后发现**。

| 时点 | 事件 | 证据 |
|---|---|---|
| 12:02 | 本任务存档提交 | `90cc79a`（本 agent·只提交精确路径） |
| 12:04 | 本任务 T1 术语替换（129 文件 mtime） | 本任务会话 |
| 12:05 | 本任务 T2 `git mv` 5 处 | 本任务会话 |
| 12:06 | 对方产出 `_scan_tmpdir/g39b_after.png` | 非本 agent 产物（本任务未生成） |
| **12:08** | **对方提交 `2762601`**「G39 修复：转移出库新建页三处下拉组件叠框」 | `git log` 显示该提交非本 agent 所为（本 agent 未提交过任何原型修复） |
| **12:10** | **对方提交 `dc9bce0`**「G39 重打包：20260916.zip 覆盖 11:50 过期包（**该包早于并行会话 12:04 文件改名**…）」——**该提交把我的 5 处 `git mv` 重命名一并纳入**（`{回款登记.html => 收款登记.html}` 等 5 行·0/0），并重打 zip | `git show --numstat dc9bce0` |

**影响评估（已核）**：

1. **对方已显式识别本会话**——`dc9bce0` 提交信息原文写「该包早于并行会话 12:04 文件改名」并据此重打包，**对本任务是配合性动作**，非冲突。
2. **重命名已被对方提交**（HEAD 内已是新文件名）；**本任务的 T1／T2 内容改动仍全部未提交**（`git diff HEAD -- P3-R01-包装租赁管理后台原型/ = 128 files changed`，逐条 `git diff` 核对为 T1/T3/T4/T5 的真实内容变更，非行尾噪音）→ 本任务提交仍能完整落地。
3. **本任务未发现被对方覆盖的改动**：对方两次提交只涉及「租赁管理/转移出库新建.html」与 zip 两个对象（前者当前工作区**干净**、后者**未修改**），与本任务改动面（术语/改名/F01/A 类文档）**零重叠**。
4. **本任务的验证结论仍然有效**：审计／PW／截图均在**对方最后一次提交（12:10）之后**、以当前工作区实际内容跑出（见 `_scan_tmpdir/audit-g40.log`、`g40_pw.json`、`g40_f01_*.png`），反映的是**对方提交＋本任务改动**的合并态。

**处置**：
- 本任务提交**只提交精确路径**（本任务改动面＋本任务证据件），**排除**对方产物（`_scan_tmpdir/backup-g39b-select/`、`g39b_repack.py`、`g39b_after.png`、zip）。
- **不 push**（沿任务约定）。
- **重申纪律**：后续任务开工前应把 mtime 判读**同时**用于 `.git` 目录与 `git log --since`（本次仅查了原型目录 mtime，未查 git log，故未能在开工时发现对方写者）。此条已回写基线「口径纪律铁律」。

**结论**：**不构成任务失败**（对方提交与本任务零内容重叠、且为配合动作），但**前置校验漏检一次**，按登记项记录并回写纪律。

## 四、失败清单结论行

**无失败项**（登记项 9 条＋并行写者事件 1 条，均为白名单／范围排除／假阳性／口径归一／超范围转派／并行写者登记，非执行失败）。
