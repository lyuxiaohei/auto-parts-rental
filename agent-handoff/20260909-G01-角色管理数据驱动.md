# G01 · 角色管理数据驱动化（菜单重组 v3.2 三连跑 1/3）

> **元信息**：G01 ｜ 日期 2026-09-09 ｜ 状态 ⏳ 待执行 ｜ commit：〈收尾回填〉
> **双层路由**：公共背景（口径/纪律/现状/工具）读 `agent-handoff/_AGENT基线.md`；本文件=本 goal 任务全量细则。收尾回写三件：本文件标 ✅+执行记录+commit 回填头部 ｜ `_索引.md` G01 行挂哈希 ｜ 基线「当前任务」行标 G01 完成（goal-creator v7）。
> **需求原文（2026-09-09 道远验收反馈①）**：新开发的角色管理是数据驱动的吗？需要和其他列表、弹窗保持一致，目前使用后无法演示。
> **现状核实（2026-09-09，v3.1 提交 24fb61f 后）**：`系统管理/角色管理.html` 为 v3.1 B1 静态口径——未接 renderListPage、demo-data.js 无 roles 实体；「新增角色」与行内「权限配置」均 `openModal('roleModal')`，而 roleModal=自用户权限页照抄的**角色列表弹窗**（标题"角色管理"）→ 两个按钮弹出同一列表 = "无法演示"根因。既有模板 `系统管理/弹窗/角色管理.html`（角色列表·roleModal）服务用户权限页双层，**不随本任务改**。renderListPage 接线基准=产品档案.html（三 script 引入+filters 配置+tbody 骨架化保留兜底行）。

## 前置校验（不过即停）

1. `git log --oneline | grep -c "G01-角色管理·执行"` =0 → 可执行；≥1 → 往 `_scan_tmpdir/goal-failures-menuregroup32-20260909.html` 追加「G01 已执行，跳过」一行后停止（**全史锚，不限 HEAD**——三连跑顺序投喂，前序提交会顶掉 HEAD）
2. 原型目录 `P3-R01-包装租赁管理后台原型/_data/` mtime ≥30 分钟无变化（资源互斥：同一工作区同一时刻仅本 agent 一个写）；工作区非 clean 先 `git add -A` 留档
3. 备份 `_scan_tmpdir/backup-menuregroup32-20260909/` 已存在（e70efe3 建·117 文件），校验存在即可、不重建

## B. 任务组

1. **demo-data.js 增 `roles` 实体 9 条**：键 RL-01~RL-09；fields：name/desc/scope/accts；row.cells 五列=角色(b)/说明/数据权限/账号数(td-num)/操作，操作列=`<a onclick="openRolePerm('角色名')">权限配置</a>`；内容照 v3.1 静态表 9 行原文。
2. **角色管理.html 接线（产品档案同构）**：引入 `_data/demo-data.js`+`_data/list-generic.js`（本页无详情弹窗，不接 detail-generic）+`renderListPage({entity:'roles', filters:[{label:'角色名称',field:'name'},{label:'数据权限',field:'scope'}]})`；tbody 骨架化（保留 9 行无 JS 兜底）；筛选卡改与产品档案同构两字段+查询/重置。
3. **roleModal 语义修正为权限配置弹窗**：标题 span id="rolePermTitle"（默认「权限配置 · 系统管理员」）；体=角色信息 dgrid（角色名/说明/账号数）+「菜单权限」九组 .checkbox 网格（组名=v3.2 九组：项目管理/我的待办/基础资料/采购管理/销售管理/租赁管理/仓储管理/财务管理/系统管理）+「数据权限范围」三个 .radio（全部项目/所属客户/所属供应商）；footer 取消/保存（onclick=closeModal）；页面级 JS：`window.ROLE_PERM` 勾选矩阵+数据权限映射+`openRolePerm(name)`（设标题+按矩阵勾选+置 radio+openModal）。
4. **「新增角色」改 `openModal('createModal')`**：createModal=新增角色表单（角色名称*input/说明 input/数据权限范围 select 三项+取消/保存）。
5. **双层同步**：新建 `系统管理/弹窗/权限配置.html` 与 `系统管理/弹窗/新增角色.html` 独立模板（独立可开、内容与页面内嵌一致）；`弹窗/角色管理.html` 与 `用户权限.html` **不动**。
6. **批末验证与收尾**：audit 全量+verify 三批+新写 `_scan_tmpdir/verify_roles_v32.py`（12 项，明细见 C 表）→ git 一提交（信息含「G01-角色管理·执行」）→ 回写三件套（头部元信息/索引 G01 行/基线当前任务行）。

## C. 默认决策表

- roles 实体内容=v3.1 静态 9 行原文（系统管理员 2/财务 1/财务主管 1/商务 2/商务主管 1/物流 1/物流主管 1/客户账号 7/供应商账号 3），不新造角色
- ROLE_PERM 勾选矩阵（菜单权限·九组）：系统管理员=全部九组；财务/财务主管=我的待办+财务管理；商务/商务主管=我的待办+基础资料+采购管理+销售管理+租赁管理；物流/物流主管=我的待办+仓储管理；客户账号/供应商账号=我的待办
- 数据权限 radio 映射：系统管理员与六内部角色=全部项目；客户账号=所属客户；供应商账号=所属供应商
- 权限配置/新增角色弹窗保存按钮=closeModal（原型不落库）；checkbox/radio 视觉态走 ia-fix 全局脚本（不另写切换 JS）
- verify_listfull 三批不动（100/88/55 保持）；本批新增 `_scan_tmpdir/verify_roles_v32.py` **12 项写死**：①roles 实体存在 ②行数=9 ③列结构 th=5 且每行 td 数一致 ④首行逐格=cells（系统管理员）⑤末行=供应商账号 ⑥筛选"财务"→2 行（财务/财务主管）⑦权限配置点击→roleModal show+标题含「权限配置 · 财务」⑧矩阵抽查：财务=勾 2 组含财务管理·不含仓储管理 ⑨新增角色→createModal show+表单含 角色名称/说明/数据权限范围 三字段 ⑩弹窗关闭恢复 ⑪角色页 0 JS 错 ⑫两新模板独立打开 0 JS 错+标题正确
- audit 页数口径=109+2 新模板=**111 页**；基线=执行前自跑快照逐页 diff（新增 0）
- 失败清单 `_scan_tmpdir/goal-failures-menuregroup32-20260909.html`（三连跑共享，G01 行回填）

## D. 验证门（全部满足，证据贴对话）

- `==== 角色管理数据驱动验证门：12 项断言，失败 0 项 ====`
- audit 全量：死链 0 / JS 错 0（F01 外链噪音 1 条=既有豁免口径）/ 相对执行前基线新增 0（111 页）
- verify 三批：`==== batchN 验证门：X 项断言，失败 0 项 ====` 三行（100/88/55）
- grep：demo-data.js 命中 `roles`；ls `系统管理/弹窗/权限配置.html`、`系统管理/弹窗/新增角色.html` 存在
- git log 新提交含「G01-角色管理·执行」；grep 本文件头部状态=✅ 已执行 且 commit 已回填；`_索引.md` G01 行=✅
- 失败清单 G01 行（失败 0 项或留档明细）

## 执行记录（收尾回填）

（空·待执行后填写：提交哈希/各门证据/偏差与默认决策命中）
