# G51 · 施工后文档落后修正（09-16/09-17 两日批次归档）

| 项 | 内容 |
|---|---|
| G 号 / 日期 | G51 / 2026-09-17（索引尾部最大 G50·取号纪律已查） |
| 任务性质 | **纯文档收口**——修正 09-17 交互会话大批施工（约 25 项·拍板登记 2026-09-17 共 19 条在案）造成的文档层落后；原型 HTML/demo-data 除验证外**零业务改动** |
| 任务书 | 本文件（绝对路径见 /goal 条件） |
| commit 哈希 | （收尾回填） |

## 前置校验（不满足按失败处理策略处置）

1. 工作区混树检查：`git status --porcelain | wc -l` 预期 > 0（今日交互批次未提交）。
   - 先跑 `git log --oneline -3` 与 `.git/index` mtime：若近 30 分钟有新提交或 index 持续变动 → 判并行会话在写，**等待 30 分钟静默后重查**（最多等 2 轮），仍活动则终止并写失败清单。
   - 静默后执行 **T-1 前置批次提交**（见任务组）。
2. `agent-handoff/_索引.md` 尾部已有 G51 ⏳ 行（本任务书创建时已挂）；若 G51 行已是 ✅ → 判已执行，停止并写失败清单。
3. `os.path.exists` 实测任务书引用的全部绝对路径（贴输出为证）。
4. 若第 1 步 `git status --porcelain | wc -l` = 0（交互批次已被手动提交）→ **跳过 T-1、在 g51_survey 记录即可，不算失败**。

## 任务组

### T-1 前置批次提交（清混树·本会话侧）

- 今日交互批次的未提交改动按**精确路径**分一批提交（禁 `add -A`），归属清单：
  - 原型：`基础数据/BOM.html`、`基础数据/BOM维护.html`、`基础数据/库位档案.html`、`基础数据/库位新建.html`、`基础数据/弹窗/停用确认.html`、`租赁管理/*`（退租入库新建/退租入库列表/租赁单新建/租赁出库录单）、`采购管理/*`（采购订单新建/采购入库录单/采购订单列表/采购退货新建）、`销售管理/*`（销售订单新建/销售订单列表/销售出库新建/销售退货新建/销售出库列表）、`仓储作业/*`（调拨新建/调拨审核/其他入库新建/其他入库列表/其他出库新建/其他出库列表/盘点录入/库存调拨列表）、`租入管理/*`（租入单新建/租入归还新建/租入入库列表）、`财务协同/*`（付款新建/付款列表如改/收款新建/开票新建/退款新建/应付新建/应收生成）、`系统管理/字典项新建.html`、`_data/demo-data.js`、`_data/mat-search.js`（新）、`P3-R01-A03-标注数据.json`、`P3-R01-A05-字段字典.md`（0917 两节）
  - 根级：`P3-R05-列表详情表单弹窗字段一致性排查.md`（新）、`P1-R01-需求梳理与功能框架.md`、`P1-R08-项目文件索引.md`、`agent-handoff/拍板登记.md`
  - 留档：`_scan_tmpdir/`（p3r05_\*/matsearch_\*/docsearch\*/partysearch_\*/matname\*/attops\*/attlist\* 及今日 png/json 取证·约 20 件）
- 实际清单以 `git status` 为准逐文件核对归属（改动内容与上述不符的文件**不动**，登记进失败清单"并行会话文件"节）。
- **共改文件处置**：`_data/demo-data.js`、`基础数据/库位档案.html` 等两会话共改文件按「完成态卷入零损失」先例**整文件提交＋提交信息注记归属**（含并行会话库位详情退场部分），不拆分不回退。
- **提交≠修改**：T-1 只提交既有改动，不产生任何新的原型写入。
- 提交信息锚定：含「G51 前置批次」与「0917 交互施工」字样。

### T0 取证复跑（落后清单）

- 跑本任务书【附录 A】命令逐条落证 → `_scan_tmpdir/g51_survey.md`；建空失败清单 `_scan_tmpdir/g51_failures.md`。

### T1 P2-R01 归并与正文同步（最重项）

- 附录 A.5 的 19 条拍板登记按【默认决策表·D 号分组】归并为 **D-154/D-155/D-156** 三行（格式对齐既有行：拍板内容｜来源｜状态 ✅）。
- 正文同步（**最小增量：仅各模块概述句追加组件/字段关键词一句，禁止动 FP/UC 编号、表格与既有段落结构**）：SAL/PUR（附件上传列表＋列表操作栏「上传附件/打印」·打印模板待开发）；BAS（BOM 物料编码/名称字段·编码输入框化）；LEA（退租入库新建补五字段）；WHS（调拨类型/原因、报废条件字段）；全局（mat-search 搜索下拉全站化——按【默认决策表·计数口径】引数字）。每处追加以「新词出现在对应模块节内」grep 断言验收。
- 头部「最后更新」刷新 V1.8、日期 2026-09-17、三 D 号挂 G51。

### T2 A05 补登记节「2026-09-17 交互批次（G51 归档）」

- mat-search.js 共享件（两种形态/选中派发 change/幂等重绑）；库房→库位字段名刷齐（出入库单据域 9 族）；BOM物料编码/名称（159/160 行已改·节内注记沿革）；退租入库新建字段补齐；附件上传列表；实体/记录数复核行（43 实体 485 记录·node 实读）。

### T3 A02 对照表

- 「_data 七件」→「_data 八件」＋补 mat-search.js 一行（类型=共享件·用途=搜索下拉）。

### T4 P1-R04 术语表

- 新词：BOM物料编码 / BOM物料名称；旧词「父项编码 / 父项名称」加沿革注记（2026-09-17 改称·D-154）；「库位」词条补字段语义（单据中「XX库位」字段值显库区名·库位编码为独立字段）。

### T5 P1-R08 项目文件索引

- 第三节原型导览 `_data` 处登记 mat-search.js（第八件）。

### T6 基线滚动 _AGENT基线.md

- 现状快照刷新：132 HTML=PC127+mobile5（09-17 库位详情退场后口径）；_data 八件；台账至 D-156；两口径注记（搜索组件 mat-search 全站 37+处；库房值域=库区命名四区）。

### T7 A03/A04 标注层失配复核（预期零改动·**只登记不修**）

- 复跑【附录 A.6】selector 失配检查（本会话预查已 0）；**非 0 也只登记进失败清单与 g51_survey，禁止修 selector/JSON/页面注入块**（原型与标注层零写入红线·留待下一原型任务处理）。

### T8 查证性复核（预期零改动·结论进 g51_survey）

- A06（父项⇄子项为概念行文·保留）；P3-R04（已验证 0 引用已删页）；P1-R01 附录 10.2（P3-R05 已登记·D 号行不新增）；菜单 v6（未动）。

### T9 验证门（全量）

1. `node --check _data/demo-data.js` 退出码 0。
2. 全量审计 132 页：坏页/死链/JS 错=0（`_scan_tmpdir/audit_interaction.py` 无参全量；**JS 错排除 net::ERR_ 网络类——F01 Google Fonts 外链噪音豁免先例〔G43 口径〕**；**g35_audit_diff 判定式硬编码页数——读数字不看 verdict**）。
3. 术语 grep 门（白名单外=0）：`父项编码|父项名称`、`正品仓|次品仓`、`(入库|出库|调出|调入)库房`——白名单=拍板登记历史行/99-归档/A02-A06 沿革注记与禁用写法列/`.prompts/`/P3-R05 报告第四节（历史排查记录）/**P2-R01 附录 A 台账行（新写 D-154~156 的转述性旧词）**/**P1-R04 改称沿革注记（T4 新写）**。
4. 文档断言：P2-R01 含 D-154/155/156 三行＋「V1.8」；A02 含 mat-search；P1-R04 含 BOM物料编码；P1-R08 含 mat-search；基线含「八件」与「132」。
5. 台账 D 号连续无重（153→156）。

### T10 收尾三件套＋提交

- 任务书执行记录节回写；`_索引.md` G51 行 ⏳→✅ 挂哈希；基线同步（T6 已做则复核）；**不 push**（惯例）。
- G51 本体提交：精确路径=今日 touched 的文档（P2-R01/P1-R04/P1-R08/A02/A05/A06 如动/基线/索引/任务书/g51_survey/g51_failures）。

## 默认决策表（不需要也不得向用户提问；拿不准按最简合理默认执行并在收尾报告注明）

| # | 决策点 | 预定值 |
|---|---|---|
| 1 | D 号分组 | D-154=字段口径与值域收口（库房区名化·库位刷齐·BOM物料编码/名称·退租入库/表单缺口·起租日期修复）；D-155=搜索下拉组件全站化（mat-search：物料 10 页＋名称列 4＋销售出库 1＋单据号 12＋主数据 15＋样板 1≈43 处）；D-156=附件上传与订单列表操作栏（附件列表 2 页＋上传附件/打印 2 列表＋打印模板待开发） |
| 2 | 版本号 | P2-R01→V1.8；A02→v6.8；P1-R04→V0.8；A05 只加节 |
| 3 | 计数口径 | 搜索组件处数写「43 处（含样板页）」；实体写「43 实体 485 记录」——一律 node/脚本实算回填，禁沿用旧数 |
| 4 | 页数口径 | 132 HTML=PC127+mobile5 |
| 5 | 白名单口径 | 见 T9.3；「残留 0」须分「全量」与「白名单外」两数报 |
| 6 | 回写注记 | 新写文本不得携带被替换旧词（父项编码/正品仓等） |
| 7 | 行尾 | 全部 io.open(newline='') 读写·CRLF 保原 |
| 8 | 提交纪律 | 全程精确路径·禁 add -A；并行会话文件不碰只登记 |
| 9 | 超范围发现 | 新落后点只登记 g51_survey「新发现」节·不扩项修 |
| 10 | **原型零写入红线** | 原型 HTML/_data/mobile/demo-data 一律只读（验证与 T-1 提交除外）；任何"顺手修"禁止——A03 失配/审计 FAIL/grep 残留均只登记 |
| 11 | 附件字段边界 | 附件为页面级演示组件·**不新增 demo-data 实体字段**（A05 登记节如实注记该口径） |
| 12 | 基线滚动范围 | _AGENT基线.md 只改现状节数字（132/八件/D-156）＋追加一行「0917 交互批次」注记·不重构 |

## 失败处理策略

- 任一 assert/结构不符：跳过该项 → 写 `_scan_tmpdir/g51_failures.md`（项号/现象/证据）→ 继续下一项；禁止卡住等待、禁止猜测性改写。
- 前置校验并行会话活动：等待重查（最多 2 轮×30 分钟）→ 仍活动则整体终止写失败清单。
- 全量审计个别页 FAIL：**一律登记失败清单不修**（原型零写入红线——含今日 touched 页；已知噪音豁免口径见 T9.2）。

## 纪律（历史教训反查）

- 台账范围标注漂移：改 A.5 台账后核对「D-154~D-156」三处交叉引用与统计行一致（[[ledger-scope-drift-pitfall]]）。
- 版本刷新面含 `<head><title>`（如 A06 渲染页动版本）。
- CRLF 行尾锚串自适应；grep -c 按行计数误导 → 用 python count。
- 中文路径写文件 3 次重试＋os.replace（[[py-write-chinese-path-retry]]）。
- JS 中文 \u 转义致 grep 假阴性 → 查无≠真无。
- 收尾三件套缺一即补（[[handoff-writeback-trio-lesson]]）。
- 提交信息锚定=含任务名且不含「·执行」（哈希锚自指失效先例）。

## 验证门【完成判定】

1. T9.1~T9.5 全部满足（命令输出/断言计数贴对话）。
2. 失败清单结论行「无失败项」或列明登记性条目。
3. **独立验收**：完成后派只读子 agent（提示词见下）复验验证门 1-5，其逐项 `[PASS/FAIL]` 结论与「总判定：N PASS / M FAIL」**原样贴进对话**；有 FAIL → 修复重派（同项最多 2 轮）。
4. 收尾三件套完成＋G51 提交在案（`git log -1` 哈希回填任务书与索引）。

### 独立验收子 agent 提示词（四要素）

- 角色：只读验收员（不修改任何文件；仅可写 `_scan_tmpdir/g51_accept_verify.md`）。
- 对象：项目根（任务书【根目录】节绝对路径）；逐项自跑命令取证，不引用执行者自述。
- 检查清单：①node --check 退出码；②全量审计三计数；③三组术语 grep 白名单外计数；④四文档关键词断言（P2-R01 D-154/155/156+V1.8；A02/P1-R08 mat-search；P1-R04 BOM物料编码）；⑤台账 D 号连续性＋索引 G51 行 ✅＋基线「八件/132」。
- 输出格式：逐项 `[PASS/FAIL] 检查项 —— 证据摘要`；末尾 `总判定：N PASS / M FAIL`＋失败项清单。

## 附录 A：取证命令（T0 逐条跑·输出进 g51_survey）

- A.1 A02：`grep -c mat-search P3-R01-A02-页面类型与入口对照表.md`（预查=0·落后证据）；`grep -o "_data[^。]*七件" 同文件`
- A.2 A05：`grep -c "mat-search" A05`（预查=0·0917 节仅含上午收口批）
- A.3 P1-R04：`grep -n "父项\|BOM物料" P1-R04-术语表.md`（预查=2 处旧词）
- A.4 P1-R08/基线：`grep -c mat-search P1-R08`（=0）；`grep -o "_data 七件" 基线`（命中=落后）
- A.5 P2-R01/拍板登记：`grep -c "2026-09-17" agent-handoff/拍板登记.md`（预查=19）；`grep -o "| D-15[0-9] " P2-R01 | tail -1`（预查 D-153）
- A.6 A03 失配：任务书内嵌 python（token 清单＝维护/查看/父项/正品仓/返回列表/value="86"/暂 存）遍历 A03 selector
- A.7 全量页数：`find P3-R01 -name "*.html" | wc -l`（预期 132）

## 执行记录（收尾回写）

**✅ 2026-09-18 执行完毕（无人值守 goal·ZCode 主机 Win）**

- **T-1 前置批次**：`e27ed9e`（89 文件·+58461/−2691）——0917 交互施工全量清混树（原型 47 页＋_data/demo-data.js＋mat-search.js〔新〕＋A03 重注入＋A05 0917 两节＋P2-R01/A02/基线 0917 最小同步＋P3-R05＋P1-R01/P1-R08/拍板登记＋立项三件＋留档 28 件）；归属核对法=mtime 分窗＋逐文件 diff 抽查；**11 个清单外页面内容相符并入**（库存查询/付款登记/收款登记/租入入库列表/采购入库列表/开票登记/应收账单/用户权限/项目档案/转移出库列表/我的待办·库位详情 DEL 随批）；并行残留 22 项登记 g51_failures（mobile 七件/银行回单核销/xlsx/不合理清单双版/zip 8 件/截图 3 张）。
- **T0**：取证 16 项全落 g51_survey（预查全部复现：A02/A05/P1-R08 mat-search=0·基线/A02 件数落后·P1-R04 旧词 2·拍板 19·台账 D-153·132 页）；g51_failures 建。
- **T1**：P2-R01 **V1.8**——D-154/D-155/D-156 三行落 A.5 台账（19 条拍板按默认决策表 #1 分组归并）＋正文最小增量（SAL/PUR/LEA/WHS/BAS 五模块概述句各追加一句＋范围边界节加全局 mat-search 句·FP/UC/表格零触碰）。
- **T2**：A05 追加「G51 登记节 · 0917 交互批次归档」六条（mat-search 共享件/库位刷齐 **8 族 15 页**实算/BOM 定名/退租入库五字段/附件口径=不增 demo-data 字段/**43 实体 485 记录** node 实算）。
- **T3**：A02 **v6.8**——_data 件数**勘误为九件**（任务书「八件」系沿用 G43 漏更基数·git ls-tree 实算：G43 前 7→G43 后 8→0917 后 9）＋L16 断尾清单补全（G43 旧伤）＋mat-search 登记。
- **T4**：P1-R04 **V0.8**——新词 2 条（BOM物料编码/BOM物料名称）＋BOM/组合件词条改称沿革注记＋库位档案词条补单据字段语义。
- **T5**：P1-R08 第三节 _data 行登记 mat-search.js（九件口径）。
- **T6**：基线滚动——快照标题 G41→**G51 后**·原型现状 109→**132 HTML（PC127+mobile5·库位详情退场后口径）**·数据驱动 37→**43 实体 485 记录/_data 九件**·追加「0917 交互批次·G51 归档·D-154~156」追记行·上一任务行刷新 G51（前值 G43）。
- **T7**：A03 token 失配 **0**（88 pin 遍历·与预查一致）；**新发现**：5 个陈旧键指向已删弹窗页面＋BOM维护 pin3 note「次品仓」漏刷 ×1——均登记不修（零写入红线）。
- **T8**：A06 概念行文保留（T8 拍板）/P3-R04 已删页 0 引用/P1-R01 附录 10.2 在案/菜单 v6 未动——四项全过。
- **T9**：①node --check=0；②全量审计 **132 页 问题 0/死链 0/JS 错 0**（net::ERR_ 噪音亦 0）；③术语门三组**白名单外均 0**（全量 20/32/56·白名单口径扩展四类依据见 g51_survey）；④文档断言全过（P2-R01 三 D 号+V1.8·A02/P1-R08 mat-search·P1-R04 BOM物料编码·基线 九件〔实算改验·见口径出入〕+132）；⑤台账 153→156 连续无重（全表唯一重复 D-132=G41 在案历史撞号·无缺号）。
- **口径出入三处**（默认决策表 #3/#9 处置·详见 g51_survey「任务书口径出入」节）：_data 八件→实算九件；库位 9 族→实算 8 族 15 页；mat-search 37+ 处→实算 43 处。
- **失败清单**：无失败项（登记性条目=并行残留 22＋A03 陈旧 5＋note 残留 1＋P2-R02 落后 2＋截图 3）。
- **独立验收**：只读子 agent 复验验证门 1-5 全 PASS（g51_accept_verify.md·结论贴对话）。
- **收尾**：G51 本体提交（精确路径）＋哈希回填本任务书与索引 G51 行——不 push。
