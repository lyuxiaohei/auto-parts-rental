# G55 失败与留档清单（2026-09-20）

> 任务书：`agent-handoff/20260920-G55-标注缺口补挂.md`｜处置原则＝跳过留档继续·禁止卡死
> **结论行：无失败项（T10 一处页面级留档·登记性条目 12 条·全部非阻塞）**

## 前置校验裁定（第 0 步）

- `git status --porcelain` 非空两项＝**用户备份 zip 改名**（旧 `包装租赁管理后台原型-20260920.zip` 删除＋新 `包装租赁管理后台-20260920-2.zip` untracked·mtime 11:35 **早于**任务书落盘提交 30a2c26 11:59:40）
- 最后提交＝30a2c26（G55 任务书落盘·上游前置会话提交属预期·[并行会话窗口判别]放行口径）·此后零新提交·`.git` mtime 12:01:05＝本 agent 自身 `git status` 刷新 index
- 裁定：**非并行写者，放行**。处置：提交时显式列文件（不用 `git add -A`），不卷入用户 zip 产物

## T10 留档（唯一页面级留档）

- **仓储作业/其他入库列表.html 缺共享件两行引用**（改前状态：G52 只给有标注页挂两行引用，本页从无标注故无引用）→ `data-note="1"` 属性已挂、A03/notes-data 条目已落，但**标注不渲染**（pageKey 落单段回退且 NOTES_DATA 未加载）。
- 不补引用行依据：红线「页面唯一允许的改动＝加 data-note 属性，git diff 不得出现其他行」。
- 复活路径（待追认）：按 G52 样板在 `</body>` 前补 `_data/notes-data.js`＋`_data/notes-drawer.js` 两行引用（`../` 前缀）即亮。

## 登记性条目（编号/现象/原因/处置）

| # | 现象 | 原因 | 处置 |
|---|---|---|---|
| 1 | T1 实数 4 键 **5** 条 ≠ 任务书「6 条」 | 任务书起草约数（G54 同型先例） | 按实算执行并登记 |
| 2 | 客商新建条目 id 自 **2** 起（键内无 id=1） | 任务书字面写 `data-note="2"`·页无任何既有锚 | 照任务书字面执行（功能等价·留 id=1 余量） |
| 3 | T3.2 锚改挂**明细表计费方式 th**（非表单区标签） | 表单区 369 行「计费方式」实为应收生成方式 radio（按月/按次对账）·与「直录日租金/次单价」语义不符；真口径在明细表 g39mode 列 | 改挂同页非交互 th·登记 |
| 4 | T7.1 定位改用 **STOCK_MAP+.stock-cell 三件套**签名（6 页） | `data-tax="qty"` 被 G39 计费联动共用（G43 收窄口径）非统一件独占签名；任务书预期页「采购入库录单」实测**无**三件套（data-tax 属其他用途） | 6 页＝租赁单新建/租赁出库录单/销售订单新建/销售出库新建/租赁单列表/销售订单列表（后两页含 STOCK_MAP 脚本属消费者·默认决策 7 逐页挂）；采购入库录单不挂库存校验件 |
| 5 | T7.3 批量库位 4 页锚**改挂明细卡标题**（非库位 th） | 库位 th 内含 `<a onclick="openBatchLoc">批量设置</a>` 子链接——dev 锚宿主会在默认态吞掉子链接点击（471ebd6 同型劫持） | 4 页挂 退回明细/出库明细/入库明细 卡标题 |
| 6 | 采购入库录单批量库位锚挂**基本信息卡** | 明细卡「到货明细」已被 data-note=1 占用·库位 th 含批量设置链接（见 #5） | 默认决策 3 次选 |
| 7 | 归还出库新建 id=1 **双锚**（tbody#riItemsBody 既有＋归还明细卡标题新增） | tbody 运行时被行渲染 innerHTML 重写·? 圆标随行内容被刷掉（PW 实测 .pn-q=0） | 补挂稳定卡标题锚·既有 tbody 属性按红线不删·静息态圆标恒 1 |
| 8 | 采购退货单列表 biz 活锚=1（biz 条数 3） | 既有死锚 2 处：id1 行锚被 renderListPage 运行时重写·id2 锚=新建退货单按钮 openModal 已页面化（G33 后） | 门 3 按活锚数断言·死锚登记（改前既有） |
| 9 | 产品档案 biz 活锚=0（biz 条数 1） | id1/id2 锚=税率维护区已按 0914 拍板迁新建物料页（改前既有） | 同上登记 |
| 10 | 库位档案 biz 活锚=1（biz 条数 2） | id2 锚=locations 运行时行·demo-data 无 note 字段（G54 既有·demo-data 本轮零改动红线内不修） | 同上登记 |
| 11 | 新键编号自孤儿锚号+1（付款 2/采购订单 2/销售出库列表 2/归还出库列表 3/租赁单列表 6/销售订单列表 2/应收 3/应付 2） | 各页存在 G52 登记的改前孤儿锚（如租赁单列表行锚 1~5）·notes-drawer 按号全元素绑定·同号新条目会误绑行锚 | 编号避开既有锚号·T8 应收/应付即按任务书指定号绑定既有行锚（孤儿转活·零页面改动） |
| 12 | A03/notes-data `_meta.date` 沿革注记 | 收尾义务 | 已追加「2026-09-20 G55」句 |

## 既有孤儿锚盘点（本轮未动·供后续补登参考）

付款登记 td1｜采购订单列表 td1｜销售出库列表 td1｜归还出库列表 td1/td2｜租赁单列表 td1~td5｜销售订单列表 td1｜应收账单 td3（本轮转活为 T8 id3）｜应付账单 td2（转活为 T8 id2）/td3/卡标题5｜采购退货单列表 td1（运行时重写）｜产品档案 id1/id2 锚元素不存在｜库位档案 id2 运行时无 note 字段
