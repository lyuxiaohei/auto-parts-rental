# 20260915-G39-按天计租落地（含事件流结构化）

> **状态**：✅ 完成（2026-09-16·执行会话 ZCode·主机 Win·D-148）
> **来源**：道远 2026-09-15 拍板——①计费口径改定（单据侧直录日租金·不做三段式换算）②止租日来源采纳「**按持有量计租**」路线（不勾稽原单）③库龄不加
> **依据**：`_scan_tmpdir/g31_t0_计费梳理.md`（计费字段现状与冲突面）｜`_scan_tmpdir/g37_前置调研_计费库龄虚拟仓.md`（行业「箱天」计费取证）｜`P1-R07-租赁管理行业调研.md` §4.4（按天×数量·起租＝发货审核完成日·止租＝退租验收完成日）
> **执行命令**：`_scan_tmpdir/g39_prompt.md`（形态 A）
> **执行前提**：单写者互斥／git clean；备份先行
> **红线**：**不勾稽原单**（D-106·本任务不推翻）｜**不红冲已审核单据**（D-109）｜**不加库龄/在库时长/流转次数列**（道远 09-15）｜虚拟仓不维护（D-006）｜不碰字段命名口径（以 G38 为准）

## 一、拍板口径（道远 2026-09-15）

1. **单据侧不做换算**
   - 计费方式＝**按时间周期** → 业务**直接录入日租金**
   - 计费方式＝**按次** → 录**次单价**（不录日租金）
   - **三段式只在物料档案录参考价时体现**（G21 控件保留·仅参考价使用）
   - 推论：**「月按几天折」的换算基数问题作废**——系统不做周期→日的折算
2. **天数算法＝首尾双端计入、最低 2 天**：天数＝**max(2, 止租日 − 起租日 ＋ 1)**——首尾各计一天，**当天起租当天退租＝2 天**（道远 09-15）
3. **计费路线＝按持有量计租**（本任务核心）
   - 租金＝Σ（**每客户 × 每物料 的每日在租数量 × 日租金**）
   - 出库＝在租数量增、退租＝在租数量减，**事件日期**决定变化点
   - **不按"单据对单据"勾稽**——D-106「退租不勾稽原单」口径**原封不动**
   - 同客户同物料单价相同时**合并总量计算**；仅当多批次单价不同才需批次核销（本任务不做）
4. **库龄不加**：不做库龄／在库时长／流转次数列；库存展示维持现状（客户端(租出) 数量列＋客户在租下钻）
5. **参考价六条**：按单据性质对应带出（采购/销售/租入/租赁各对应）｜**可改**（不带留痕）｜组合件租价**录单时手填**｜**计费方式随料带出**（**组合件须补「计费方式」字段**，否则闭环断在组合件上）｜四参考价按物料类型通用（不按器具·零部件二分）｜列头保留「参考」二字
6. **T0 落账**：P2-R01 取执行时当时最大 D 号＋1

## 二、第 0 步·上下文获取（动手前必做）

1. `agent-handoff/_AGENT基线.md`——第四节（历史事故与真相）＋第七节（上手清单）**逐条适用**
2. **依据文档**：`_scan_tmpdir/g31_t0_计费梳理.md`（现状与冲突面 C1-C8）｜`_scan_tmpdir/g37_前置调研_计费库龄虚拟仓.md` §1（箱天口径）
3. **现状实读点**（开工先逐处确认，勿凭记忆）：
   - **断链点**：`returnInbounds`（退租入库）fields＝customer／project／appliance／dest／warehouse／date／result／status——**无租赁单号、无出库单号、无数量**（数量埋在 `appliance` 文本里）
   - 已勾稽侧：`comboOutbounds`（租赁出库）有 `zl`→租赁单；`rentInReturns`（租入归还）有 `ref`→租入单
   - `stockFlows`：14 行·**0 行带日期**·`cls` 值域＝「租赁器具／零部件」（与 `products.cls` 五值不一致）
   - `rentTracks`：有 `date`（起租）与 `back`（约定归还）
   - 金额载体：`comboOutbounds.combo`（如「ZH-2603-B × 30 套」）、`salesOutbounds.summary`（如「箱盖 ABS 吸塑×1,500」）——**数量在文本里**
4. **工具**：`_scan_tmpdir/audit_interaction.py`｜`_scan_tmpdir/g28c_modal_scan.py`｜`node --check`
5. **勿重做**：G37（转移出库＋D/F 组）｜G38（字段口径统一）｜G33 三页｜试点页｜G36 正在执行的批次范围

> **执行顺序（单会话内完成·不拆批）**：T0 落账 → T1 事件流结构化 → T2 在租量时间序列（`stockEvents`＋每日余额）→ T6 演示数据（多次出库＋部分退租）→ T3 单据侧计费字段（含组合件补「计费方式」字段）→ T4 账单期段化 → T5 生成方式改造 → T7 验证＋回写
>
> 本任务为**一个 goal 会话**内完成，**不拆批**。若中途无法完成某项：按失败处理策略记失败清单并在收尾汇报，**不要停下等待**；可后续以**同一 goal 续跑**，已完成项不重做。

## 三、任务分组

### T1 事件流结构化（本任务的地基）
- 出库／退租／归还单据的明细：**物料编码、数量、单位**从文本提为字段（`combo`／`summary`／`appliance` 不再当数量载体）
- 同步 `demo-data` 与页面明细表；`node --check` 必过

### T2 在租量时间序列
- **默认＝新增 `stockEvents` 实体（事件流水）**：`stockFlows` 保持「当前快照」职责**不变**（不动其现有消费页与库存查询的项目闭环逻辑），每日余额由事件流水派生
- 若实读发现 `stockFlows` 已具备事件维度，则改为就地升级并记偏差
- 每条事件＝日期＋客户＋项目＋物料＋数量＋方向（出库/退租/归还/调拨/其他出入库）
- 可复算：**指定客户 × 物料在任意日期区间的每日在租量**＝期初＋区间内事件累计

### T3 单据侧计费字段
- **计费方式随料带出**（按时间周期／按次）
- **按时间周期** → 录**日租金**；**按次** → 录**次单价**；列头随计费方式**联动切换**（`日租金(元/天)` / `次单价(元/次)`）
- 租入单／租赁单：起租日＋天数展示（租期两层口径沿用 D-117）
- **组合件补「计费方式」字段**（`bomList` 现无该字段；道远 09-15 指令「补充，不然无法闭环」）——补后「计费方式随料带出」对组合件同样生效
- 参考价四类单据**带出并可改**（采购/采购入库→参考采购价；销售/销售出库→参考销售价；租入/租入入库→参考租入价；租赁/租赁出库→参考租赁价）

### T4 账单按持有量计租
- 应收／应付账单明细行＝**起租日｜止租日｜天数｜日单价｜小计**（P1-R07 §4.4 口径）
- 期段租金＝Σ（每日在租数量 × 日租金）；天数＝**max(2, 止−起＋1)**
- 起租日＝租赁出库单**发货审核完成日**；止租日＝退租入库单**验收完成日**；租入侧对称（起租＝租入入库日／止租＝租入归还验收日）

### T5 生成方式改造
- 应收 `gen`（现「按租赁出库自动汇总」）改为「**按持有量×天数**」
- 历史已生成账单**不回改**（D-109 口径）；仅新生成走新算法

### T6 演示数据
- 造「**同一客户同一物料多次出库 ＋ 部分退租**」场景（否则持有量曲线退化成单点，验证不了）
- 两个项目各造一种计费方式（按时间周期／按次）各一条链
- 事件与既有单据日期自洽（出库日 ≤ 退租日）

### T7 验证与回写
- 页内可复算：`日租金 × 天数 × 数量 ＝ 金额`（逐行断言差额 0）
- 全量审计 ＋ `node --check` ＋ PW 抽验 ＋ 回写三件

## 四、默认决策表（执行中不再提问·逐条按此办）

### 天数与期段
- 天数＝**max(2, 止租日 − 起租日 ＋ 1)**（首尾双端计入·**最低 2 天**）
- 边界：当天起租当天退租＝**2 天**（道远 09-15 定）
- 跨月／跨年期段**按同一日租金**连续计算，不分段（因不折算周期，无月租特例）

### 计费与列头
- 列头文案随计费方式切换：按时间周期→`日租金(元/天)`；按次→`次单价(元/次)`（**两态共用一字段**）
- 计租公式：期段租金＝Σ(每日在租数量 × 日租金)；**单价相同的批次合并总量**，不做批次核销
- 兜底（仅在总量法覆盖不到时）：账单生成时**人工指认期段**（路 C）——记失败清单并在页面注记

### 起止日来源
- 起租日＝**租赁出库「发货审核完成日」**；止租日＝**退租入库「验收完成日」**
- 租入侧：起租＝租入入库日；止租＝租入归还「验收完成日」（`ref` 已可追溯）
- **不给退租入库加「关联租赁单」字段**（守 D-106）

### 字段与命名
- 字段名以 **G38 概念表口径**为准（物料／物料名称／基本单位／日租金／状态／日期）
- 本任务新建字段**建时即按该口径**，不留待回改
- 金额列显式含税/未税（`含税金额(元)`），禁裸「金额(元)」

### 数据模型
- 在租量事件＝**新增 `stockEvents` 实体**（**不动** `stockFlows` 的现有职责与消费页）；每日余额由事件派生
- 事件方向枚举＝**出库／退租／归还／调拨／其他出入库**；数量以**物料基本单位**计（G38 口径）

### 不做清单（各记关闭·不实施）
- 库龄／在库时长／流转次数列（道远 09-15）
- 客户虚拟仓维护（D-006）
- 历史数据迁移（同批关闭）
- 批次核销（先租先还）——仅当多批次单价不同才需要，本任务不做

### 兜底自愈
- 某单据明细无法结构化 → 跳过该单、记失败清单、继续
- 每日余额算不平 → 保留原快照、记失败清单（不猜测改写）
- 复核发现已合规 → 记「无改动」继续，不计失败

## 五、失败处理策略（无人值守铁律）

- 任何 assert 失败／锚点不匹配／结构异常：跳过该项，记入 `agent-handoff/goal-failures-g39.md`（任务号＋文件＋原因），继续下一项
- 禁止卡住等待、禁止猜测改写、禁止静默吞掉失败
- 前置校验任一阻塞性不符＝停止并写失败清单
- 收尾必须给失败清单结论（可为空；空则「无失败项」）

## 六、纪律（违反=返工）

- **基线第四节＋第七节全部逐条适用**（按动作类型反查：改 HTML 块→带标签锚＋配平自检；改 demo-data→精确替换＋`node --check`；Playwright→编程点击＋`unquote()`＋`textContent`；备份还原→按原相对路径；改内嵌 JS→必跑该页 audit）
- 读取-精确替换＋`assert` 计数＋幂等；禁裸 `open('w')` 整页写入既有页面
- 备份先行：`P3-R01-包装租赁管理后台原型/` 整体复制至 `_scan_tmpdir/backup-g39-20260915/`（按原相对路径）
- 中文路径 python 内嵌（`io.open encoding='utf-8'`）／Windows `newline=''`／行尾与宿主一致／计数用 `str.count` 禁 `grep -c`
- 多股改动 `git add -A`；工作区有非本 agent 改动 → **只提交精确路径**
- 新增 `onclick` 必须自带 DOM 副作用（`go()`/`closeModal()` 等）

## 七、验证门（证据全部贴进对话）

> **验收方式**：按 goal-creator v9——执行者自检通过后派**独立子 agent**（只读·自查取证）逐项复验，结论原样贴进对话

1. **事件流结构化**：出库／退租明细逐行含物料编码＋数量＋单位——证据＝字段清单＋打印
2. **在租量可复算**：指定客户×物料在指定区间的每日在租量由事件算出——证据＝断言输出
3. **天数算法**：`max(2, 止−起＋1)` 逐例打印（含跨月样例＋当天起当天止＝**2 天**）
4. **计费对平**：期段租金＝Σ(日租金×在租数量)＝账单金额，逐行差额 0——证据＝断言输出
5. **列头联动**：按时间周期→`日租金(元/天)`；按次→`次单价(元/次)`——证据＝PW 断言
6. **计费方式随料带出**——证据＝PW 断言
7. **参考价带出且可改**：四类单据各一例——证据＝PW 断言
8. **不勾稽守线**：退租入库／转移单**无**「关联租赁单」字段——证据＝`grep` 计数 0
9. **库龄未加**：库存查询无「库龄／在库时长／流转次数」列——证据＝`grep` 计数 0
10. **审计**：`audit_interaction.py` 全量——死链 0／JS 0／相对上一基线 diff 新增 0——证据＝汇总行＋新基线落盘
11. **`node --check`** 退出 0＋`g28c_modal_scan.py` 无溢出——证据＝输出行
12. **回写**：任务档执行记录｜`_索引.md` G39 行挂哈希｜`_AGENT基线.md` 快照｜A02／A05 同步——证据＝diff 行
13. **失败清单结论行**（空则「无失败项」）
14. **独立验收**：子 agent 逐项 `[PASS/FAIL]` 与总判定行原样贴进对话
15. **git 提交**（前缀 `G39`·**禁 push**）——证据＝`git log --oneline` 输出行

## 八、与其他任务的关系

- **G38（字段口径统一·待执行）**：**本任务先行方向相反**——字段命名以 G38 概念表为准，故**G38 应先于本任务**（否则本任务新建字段会被 G38 回改）；若 G38 未完成即执行本任务，则按本任务书【默认决策表·字段与命名】节的内联口径命名，并在 G38 开工时以现状为准
- **G37（转移出库落地）**：同守「不勾稽」口径（D-106），互不干扰；转移出库不改变在租量的客户归属（按租出结算时）——按终端结算时转移单审核驱动客户维度变化（G37 T3）
- **D-125（库存展示规则）**：**库龄部分本任务关闭**（道远 09-15「库龄不加」）；「租出后库存如何显示」维持现状口径（客户端(租出) 数量列＋客户在租下钻）
- **D-131（库龄并入计费）**：随本任务**关闭**（结论＝不处理）
- **D-006（客户虚拟仓）**：**不维护**（道远 09-15）——库位档案虚拟仓行退场、客户在租由状态维度表达
- **D-144（出货单打印）**：已落地，不冲突
- **G36（弹窗页面化·执行中）**：不涉及；其未完成批次涉及的页面跳过并记失败清单

## 执行记录（开工时填）

> **执行**：2026-09-16 · ZCode 无人值守 goal 会话 · 按「执行顺序」T0→T1→T2+T6→T3→T4→T5→T7 全量完成 · 前置校验全过（互斥门 OPEN／索引 ⏳／无 g39 产物／git 存档 79b6bde／备份 backup-g39-20260915·158 文件）

### T0 落账
- P2-R01 V1.3→**V1.4**＋附录 A 台账 **D-148**（按天计租落地执行·D-145 承接）＋头部「最后更新」刷新。

### T1 事件流结构化
- **demo-data**：comboOutbounds 10 行／returnInbounds 9 行（含新 TZRK-20260908-011）／rentInReturns 3 行——fields 全量增 `mat/matName/qty/unit`（21+4 键）；退租入库 fees feeCols [物料,来源,单位,数量,去向]→[**物料编码**,物料名称,来源,单位,数量,去向]（fees 首列补码·15 行）；租入归还 feeCols [序号,器具,…]→[序号,**物料编码**,物料名称,…]。
- **页面**：退租入库新建——明细 [物料,退回数量,缺损数量]→[物料编码,物料名称,单位,退回数量,缺损数量]（GB-800 隔板→LJ-C300 围板）；租入归还新建——明细 +物料编码/单位列（syncReturnItems 按物料名前缀拆码）。
- salesOutbounds 免改（明细 fees 本已含物料编码/数量/单位——summary 仅展示文本）。`node --check` 0。

### T2 stockEvents 事件流水（新实体·不动 stockFlows）
- **42→43 实体**：`stockEvents` 22 行（客户在租 16＋租入持有 6）——每条＝date/customer/project/mat/matName/qty/unit/**dir**（出库/退租/归还/调拨/其他出入库）/doc/side/note。
- 复算口径：**出库/入库当日计入；退租/归还当日仍计、次日起减**（与天数=首尾双端计入一致）。期初以「其他出入库·期初结转」事件承接（不做历史迁移·D-145）。
- A05 新增 stockEvents 节（12 字段行）＋五实体增量字段登记（comboOutbounds/returnInbounds/rentInReturns mat 四键·bomList billing·products buyPrice/salePrice）——统计 实体 43·字段行 394。

### T6 演示数据
- **多次出库＋部分退租链**：华骏×ZH-2601-A——期初 200（08-14）→退租 30（08-20）→出库 120/180（08-29/30）→循环出库 60（09-03）→**部分退租 120（TZRK-20260908-011 新行·09-08·已入库·整套退回不拆散）**；每日在租 09-01~09-10＝470,470,530×6,410,410。
- **两项目两计费方式**：PRJ-2601 华骏×ZH-2601-A 按时间周期（bomList.billing）／PRJ-2603 星途×ZH-2603-C 按次。
- bomList 6 行补 `billing`（ZH-2601-A/B·JP-3105·WBX-1210L 按时间周期；ZH-2603-C·ZT-2201 按次）；products 15 行补 `buyPrice/salePrice`（参考价带出数据源）；dictItems +**BF-06 按时间周期**（145→146）＋BF-05 释义更新（「本期无日租金业务」口径作废）；PLT-1210W 补 rentInMode=按次 6.00（租入域按次演示·cells/info 同步）。

### T3 单据侧计费字段
- **租入单新建**：计费方式 select 按月/按次→**按时间周期/按次**＋`g39mode` 类；价格列头联动（`#g39PriceTh`：按时间周期→**日租金(元/天)**／按次→**次单价(元/次)**·两态共用一字段）；物料切换→计费方式随料带出（rentInMode 映射）＋参考租入价预填（WBX-1210L 45.00·不换算直录·可改）；**起租日期＋计租天数输入＋止租日期自动**（=起＋天数−1·最低 2 天）；明细首行静态值 400/452/13,560→45.00/50.85/1,525.50。
- **租赁单新建**：明细 +**计费方式**列（TH 8→9·footer colspan 7→8）；联动＋随料带出（单件 products.rentalMode／组合件 bomList.billing·ZH-* 租价不预填=手填）；参考租赁价预填（PLT-1210W 15.00）；表单 +起租日期行（口径注记：起租=发货审核完成日·止租=退租验收完成日·当日仍计·最低 2 天）。
- **租赁出库录单**：明细 +计费方式静态列（.ctl sel·ZH-2601-A/ZH-2602-B=按时间周期）＋价格列头→日租金(元/天)（组合件手填值 2.40/0.15 维持跨页一致）。
- **采购订单新建**：物料编码 change 监听扩展——buyPrice 预填＋含税/金额重算（可改）；**销售订单新建**：+监听（salePrice 预填）。
- **起租日＋天数展示**：leaseOrders 9 行 info +起租日期（计租天数 G31 已有）；rentInOrders 5/6 行（period 解析）+起租日期/止租日期/计租天数（1 行新建草稿无租期·跳过）。
- **决策注记（最简合理默认）**：①租赁单 doc 级 radio「按月定期生成应收/按次套数对账」**维持不动**——语义=出账周期（按月出账）与按天计价兼容（g37 调研 §1.1「计价按天·结算按月」行业常态），leaseOrders.billing 历史值域亦不回改；②租入单历史明细「月租」值维持（历史单据不回改·D-109 同精神），新口径落在新建造表单；③散件名→编码映射演示近似：折叠隔板/隔板/护角→LJ-F600、箱体→WBX-1210L、金属料箱 800×600→BTC-6040（无独立 SKU）。

### T4+T5 账单期段化＋生成方式
- **应收 +AR-2026-09-PRJ2601-D1**（租赁费·**按持有量×天数**·gen/btype/feeType 三层新值·期段 09-01~09-10·4,940 套天×2.00=9,880.00）；**+AR-2026-09-PRJ2603-D1**（按次套数·60 套×1 次×4.50=270.00）；**应付 +AP-20260911-013**（租金应付·按持有量×天数·环通×WBX-1210L 期段 08-16~09-03·760 只天×1.50=1,140.00）。
- 明细行＝**物料编码|物料名称|单位|起租日期|止租日期|天数|量·套天(只天)|日租金(元/天)|小计(元)**（按次：数量|次数|次单价(元/次)|小计）。
- **渲染器**：receivable-bill-detail.js／payable-bill-detail.js +`segCols` 分支（期段表·历史账单走原 5 列**不回改**）。历史 15 应收/12 应付行 gen/btype/金额零改动。
- 手术事故自愈：应付块末记录无尾逗号／transferOutbounds 实体闭合被吞／buyPrice 误插 row 层——三处均已当轮修复（node --check 0）；D1/D2 ops 银行回单核销.html→银行水单核销.html（真实文件名·G37 文件名保护）。

### T7 验证（证据全文贴执行对话）
- g39_verify.py：门 1/2/3/4/7/8 全 PASS（字段清单 22 行｜每日在租序列断言｜天数 5 例含当天起退=2｜对平差额 0×3｜勾稽 grep 0｜库龄 grep 0）；g39_pw.py：**26/26 PASS**（列头联动两态/随料带出 单件+组合件/参考价带出可改×4 类/期段化渲染/退租新建无勾稽/归还明细编码）。
- field_gate 终扫 **0/0/0/0**（g39_field_gate_final.md）；audit 全量 132 页 **0/0/0**＋g38baseline 逐键新增 0＋**g39baseline 落盘**；node --check 0；g28c 18 弹窗 0 溢出；listfull **103+118+43 全绿**（退租入库 9/应付 13/应收 17 期望随数据补正）。
- **工具修复（存量缺陷·非 G39 引入）**：verify_listfull 步骤 3 对 G36 B5 起 detailFn 跳转页（应付/应收账单）会挂起（#detailTitle 等待 30s）——补跳转分支（URL 带键断言→回列表·弹窗页维持原断言）；G33 后 batch2 未再全量跑过故未暴露。

### 回写
- 任务档（本节）｜_索引 G39 行 ✅ 挂哈希｜_AGENT基线 快照追记 G39 块｜A02 v6.2｜A05 G39 登记节＋记录数订正（returnInbounds 9/receivableBills 17/payableBills 13/dictItems 146/comboOutbounds 10）＋price.mode 概念行更新｜P2-R01 V1.4＋D-148。HTML 132 口径不变（零增删）。

