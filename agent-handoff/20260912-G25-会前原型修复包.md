# 20260912-G25-会前原型修复包

> **状态**：✅ 已完成（2026-09-12·ZCode 无人值守·主机 Win 会话）
> **commit**：423a8b1（主体·62 文件）＋收尾回写另段提交
> **日期**：2026-09-12（建档）
> **项目根（全部相对路径以此拼接）**：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\`
> **失败清单（绝对路径）**：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\goal-failures-g25.md`
> **形态**：精简命令路由（G22/G23/G24 先例）——执行命令=`_scan_tmpdir/g25_prompt.md`，一切判定口径/计数预期/默认决策以本文档为准；本文档=该 goal 的完整凭据与执行记录处
> **前置依赖**：G24 已执行（✅ 7665a86+3fc8af6，P2-R02 检查报告已产出，修复建议 R-01~R-07 为本包依据）；原型现状=G22 后（115 HTML·demo-data 37 实体·待办 16 类·菜单 v3.5·F01 v3.6）
> **硬时限**：**2026-09-14 客户沟通**——本 goal 须在 09-13 内完成；G26 文档收尾包（P3-R04 刷新/PRD 增量/zip）随后另行建档

## 背景（2026-09-12 道远拍板·四项）

1. **R-03 客户转租**：降级采纳——不建独立菜单、维持最简化（拉表改状态+可查+能还回·现行动线=库存查询行内「转租登记/转租还回」），**不升审核**（推翻 09-08 独立菜单子项的口径变化已登记；本 goal 零改动，落法在 G26 文档包刷新 P3-R04/F01 注记为正式动线）
2. **R-04 可审单据矩阵对齐待办 16 类**：道远拍板「该单据需审核，需闭环」+采纳对齐 16 类——**G25 实测修正**：矩阵 14 项已含租入单与租赁出库，真实差集=「付款登记」「收款确认」两类财务单（待办有、矩阵无）→ 本包补这两项（14→16）
3. **R-07 库存「租入」态**：选 A——补演示行+筛选选项，维持五态口径（09-08 拍板）；「租入」态语义=从供应商租入、在库未转租的资产
4. **F01 旧术语清理**：采纳（基线六节 12 遗留）——客户沟通演示入口页，09-13 前必须完成

## 改动依据（优先级序）

1. `P2-R02-决策落实与场景闭环检查报告.md` 第七节 R-01/R-04/R-07 + 3.1/3.3 证据（改动面与现行结构参照）
2. `agent-handoff/_AGENT基线.md` 第六节 12（F01 四处旧术语）/14⑤（审核矩阵差异·G22 遗留）+ G18 遗留项 + G11「29 弹窗模板注入残块」登记
3. 原型真值：`P3-R01-包装租赁管理后台原型/`（改动前逐文件读结构，禁凭记忆改）

## 前置校验（先于一切）

1. **单写者互斥**：原型目录与 agent-handoff/ 最近文件 mtime ≥30 分钟（本任务书与失败清单豁免）→ 不符停止记失败清单
2. **防重复**：`_索引.md` 无 `| G25 |` 行；`_scan_tmpdir/` 无 g25_* 执行产物（建档文件除外）
3. **G24 已执行**：项目根存在 `P2-R02-决策落实与场景闭环检查报告.md`；`_索引.md` 有 G24 行
4. **工具就绪**：`node --version` 可用（v24.14.0·demo-data 改后必跑 node --check）；`_scan_tmpdir/g17_audit_diff.py` 与 `g17baseline` 在位
5. **工作区存档**：git status 有改动 → `git add -A && git commit`（不 push）
6. **备份**：`P3-R01-包装租赁管理后台原型/backup-g25-20260912/` 按 G22 先例建（F01+库存查询+角色管理+应收账单+demo-data 改前拷贝）

## 任务（T1-T6·按会前优先级序）

### T1 F01 旧术语清理（最高优先·演示脸面）

1. 备份后改 `P3-R01-F01-业务流程导航图.html`，四处映射（G25 建档实测锚点）：
   - 「零部件买卖」×6 → 「物料买卖」（含头部叙事/无障碍 desc/泳道标签/页脚沿革叙述中的现行语境处；**沿革记录句中的历史词保留并注「（原文）」**）
   - 「向供应商/供应商租入」×2 → 「向供应商租入」（L3 标题坏句+节点标签）
   - 「业务流程 · 按第2次沟通纪要」×1 → 「按第3次沟通纪要」（区块标题与主标题口径对齐）
   - 「器具」×7 逐处判定：**单独指物料概念的改「物料」**（「器具 · 资产采购」「单一器具 · 直接出租」）；**「零部件/器具」并列（物料类型 WL-01/WL-02 两值语境）保留**；判定不定的留失败清单不改
2. 改动均在 SVG `<text>`/HTML 文本节点——**逐处 assert 计数**，改后标签配平自检；mermaid 源块/SRC_DATA/决策注记数组内同步检查（含旧词才改）
3. A03/A04 标注 json grep 旧词（零部件买卖/向供应商/供应商租入）：命中才同步改并双轮重注入零告警；不命中零动作
4. 改后：死链 0 + JS 0（audit 单页口径）+ PW 打开截图 1 张留档 `_scan_tmpdir/g25-f01-after.png`

### T2 R-07A 库存「租入」态演示行+筛选选项

1. `_data/demo-data.js` stockFlows 增 1 行（先读现有行结构同构新增·末位插补逗号·node --check 必过）：键顺流水号格式；fields.status=`租入`；物料=WBX-1210L 围板箱（路凯租入·在库未转租）；来源 tag=租入；数量 500；库位=上海一号库；与五态注记「租入＝租入在库未转租」口径一致
2. `仓储作业/库存查询.html` 库存状态筛选 select 在「退租待入库」后追加 `<option>租入</option>`（仅页面层，无弹窗双层）
3. `仓储作业/弹窗/` 无涉及；主表若数据驱动渲染（G12 renderListPage）则新行自动入列——listfull 相关断言同步**先红后绿**

### T3 R-01 应收「供应商应收」演示行

1. demo-data receivableBills 增 1 行（先读一条「丢损赔偿」AR 行完整结构同构）：键=`AR-2026-09-PRJ2601-SUP`；btype=供应商应收；客商=路凯包装运营（上海）有限公司（供应商赔付我方·租入器具损坏）；金额 18,600；费用分类=丢损缺损赔偿；状态=未收款；ref=—（无关联单据手填形态·与 radio supplier 语境一致）；详情时间线含「供应商赔付确认」节点；cells 格数与列数断言一致（**不带外层 td**）
2. 与应付侧 AP-20260905-013（对客户应付）形态对称；筛选 option「供应商应收」已存在零改动
3. verify_listfull 应收账单期望值同步（14→15 行·先红后绿）

### T4 R-04 可审单据矩阵 14→16

1. `系统管理/角色管理.html` auditPermMatrix 在「租入归还」后追加两 span（checked 态与既有项一致）：`data-audit="付款登记"`、`data-audit="收款确认"`
2. 全站 grep「14 项」（含 A03/A04 标注 json、G13 注记弹窗、「可审单据矩阵（14 项」表述）→ 16 项同步；命中标注 json 则重注入零告警
3. 待办/矩阵两套口径差异销账口径：矩阵 16 项=待办 16 类一一对应（付款登记/收款确认对应财务确认审核）

### T5 G18 遗留项销账（零原型改动）

- G18 待拍板项「应付账单类型 option 内括号注释×2」按**保留**销账：option 括号=口径注释（两类赔付方向说明），下拉展开态非页面默认态文案，范本页验收态保留先例（G18 D5 保留类同性质）；登记于本档执行记录+基线六节滚动，失败清单不记

### T6 G11「29 弹窗模板注入残块」清理（可降级）

1. 按 G11 登记清单逐文件检查弹窗模板（`*/弹窗/` 目录）：空 `<script>`+孤立 `</style>` 坏块、注释残段——只清残块，逐文件 assert 标签配平+改动前后行数差登记
2. **降级条款**：09-13 12:00 前未启动本任务 → 整体降级至 G26，失败清单记偏差一条，不阻塞收尾

## 默认决策表（表外→最简合理默认+收尾注明，禁止提问）

1. **执行序**=T1→T2→T3→T4→T5→T6（会前优先级；T5 随时可做；T6 可降级）
2. **术语纪律**：叙述层一律 P1-R04 V0.6 现行词；「组合出库」叙述层禁用（原文注记除外）；「器具/零部件」作为物料类型值（WL-01/WL-02）合法保留
3. **F01 沿革句**：v3.x 决策注记/页脚沿革记录中的历史词保留（加「（原文）」注记方式参照 G22 白名单先例）
4. **R-01 行数据**：单号/金额/费用分类按本档预设；渲染字段名以现有行实测结构为准（fields 键名不同则按实际同构）
5. **R-07A 行数据**：同上；若 stockFlows 行含结存字段则按流水口径推算一致值
6. **矩阵新增两项**默认 checked；「14 项」同步含一切数字表述（14→16）
7. **demo-data 追加坑**：末位插行补逗号；改后 `node --check`（主机 node v24.14.0 在位已验）；mobile 页消费口径不受影响（stockFlows 13→14 有效行·todoItems 不动）
8. **验证数字自重跑**：不引用历史 audit/listfull 读数；断言同步一律先红后绿（G22 先例）
9. **A03/A04 同步**：仅当 grep 命中被改文本才同步+重注入；重注入零告警为过门条件
10. **原型改动白名单**（验证门核对）：`P3-R01-F01-业务流程导航图.html`、`仓储作业/库存查询.html`、`财务协同/应收账单.html`（若 cells 需页面侧配列）、`系统管理/角色管理.html`、`_data/demo-data.js`、A03/A04 json（命中才动）、`*/弹窗/*.html`（仅 T6）——白名单外原型文件零改动
11. **不改**：P2-R01/P2-R02/P3-R04/zip（G26 域）；我的待办（16 类不动）；菜单结构（v3.5 不动）
12. **git**：两段提交（主体+收尾回写）；`git add -A`（_data untracked 不随 -u）；**禁 push**
13. **收尾回写四件**：本档 ✅+执行记录+哈希｜_索引 G25 行｜_AGENT基线（当前任务块滚动+第六节销账：12 F01 清理/R-04 差异/G18 项/R-01/R-07 挂账闭环+G24 条目 15 对应项标记处置）｜P1-R01 附录 10.2 仅当新增文档（本包无新文档→零改动）
14. **失败处理**：跳过-留档-继续；T1-T4 任一失败须在 09-13 内补救重跑（会前硬约束）；收尾报告含失败清单结论行

## 失败处理策略（无人值守铁律）

assert 失败/结构不符/预期外：跳过该项记入失败清单（任务号/文件/原因），继续下一项；禁止卡死等待、禁止臆测改写、禁止静默吞掉。T6 触发降级条款按默认决策 11 执行。

## 纪律（违反=返工）

- `_AGENT基线.md` 第四节+第七节逐条适用：批量改动=读取-精确替换+assert 计数，禁裸 open('w') 整页写；HTML 切块类名 find 须带标签锚；新增 onclick 自带 DOM 副作用；cells 不带外层 td；中文路径 python 内嵌；计数 str.count/regex 禁 grep -c
- 工具留档 `_scan_tmpdir/g25_*.py`；备份先行（backup-g25-20260912）；原型目录同一时刻单写者
- Playwright 编程点击+导航等待；URL unquote()；文本断言 textContent

## 验证门（全部满足才算完成；证据=命令输出贴收尾报告）

1. `python _scan_tmpdir/g25_verify.py`（新建留档）逐行 PASS：
   - F01：叙述层「零部件买卖」=0（沿革句原文注记除外）、「向供应商/供应商」=0、「按第2次沟通纪要」=0；「物料买卖」≥5
   - 库存查询：筛选含「租入」option；demo-data stockFlows 含 status='租入' 行 ≥1；五态注记在
   - 应收：demo-data receivableBills 含 btype=供应商应收 行=1；应收账单页面 option「供应商应收」在
   - 角色管理：auditPermMatrix data-audit 计数=16（含付款登记/收款确认）；全站「可审单据矩阵（14」=0
   - demo-data `node --check` 退出码 0
2. `verify_listfull.py` 三批断言全过（同步后终态绿）
3. `audit_interaction.py` 115 页：死链 0/JS 0/相对 g17baseline diff 新增=0（或新增⊆白名单对账 PASS）
4. `v32_menu_verify.py` 14/14（菜单未动应原样过）
5. PW 抽验：F01 打开无 JS 错+截图；库存查询/角色管理/应收账单三页渲染无 JS 错；截图留档
6. 原型改动白名单门：`git status --porcelain` P3-R01 前缀行 ⊆ 白名单（默认决策 10）；demo-data node --check 已含
7. git 两段提交完成+哈希回填本档与索引行；未 push（本地领先即可）

## 执行记录（收尾时填 · 2026-09-12 · commit 423a8b1）

### 前置校验（6/6 过）
单写者互斥：原型最近 mtime 641 分钟/基线 189.5 分钟（≥30）✅；_索引无 G25 行、_scan_tmpdir 无 g25_* 执行产物（g25_prompt.md 建档豁免·预检脚本自身为首个产物）✅；G24 已执行（P2-R02 在+索引 G24 行）✅；node v24.14.0+g17_audit_diff+g17baseline 在位 ✅；git 会话起点 clean（仅预检脚本 untracked·随主体提交）✅；备份 5 文件建（后迁 `_scan_tmpdir/backup-g25-20260912/`，见偏差 2）✅

### T1 F01 旧术语清理（12 处替换全过）
- 改动 12 处：零部件买卖×6→物料买卖（行 69 头部叙事/行 79 无障碍 desc/行 90 泳道区块注释/行 91 泳道标签/行 669 mermaid 源块 subgraph/行 725 SRC_DATA b1 头）；L3 坏句×2（行 255 泳道标题「向供应商/供应商租入→转租客户」→「向供应商租入」+行 269 节点标签）；「业务流程 · 按第2次沟通纪要」→「按第3次」（行 73）；器具单指概念×3→物料（行 157「物料 · 资产采购」/行 178「单一物料 · 直接出租」/行 207「物料A/B/C 采购」）
- 保留 4 处：行 102「零部件/器具 · 两类公用」+行 117「采购订单零部件/器具两类公用」（WL-01/WL-02 并列语境）；行 322「自购辅材 · 器具类」（WL-01 类型值）；行 730 SRC_DATA v3.0 决策注记沿革句「器具+零部件合并物料档案」（默认决策 3·v3.x 决策注记历史词保留）——「零部件买卖」6 处全为现行语境无沿革句，「（原文）」注记条款未触发
- 终态：零部件买卖=0/向供应商/供应商=0/按第2次沟通纪要=0/物料买卖=6/按第3次=1/器具=4；标签配平改前后一致（text 273/div 8/script 2/svg 2/pre 1/details 1）；A03/A04 grep 旧词零命中零动作
- **偏差 3（CRLF 事故·已闭环）**：首轮脚本默认换行读+`newline=''` 写致 F01 整文件 CRLF→LF（822 行），即时发现并从备份以 `newline=''` 重放（g25_t1_fix_crlf.py），终态 CRLF=822、git diff 12 行干净

### T2 R-07A 租入态演示行+筛选（完成）
- demo-data stockFlows 末位插入 `'RZRK-20260910-024'`（键顺租入入库流水号格式·RZRK-20260816-021/022、RZRK-20260903-023 之后顺延）：fields.status=租入·物料 WBX-1210L 围板箱 1200×1000×970（路凯租入·在库未转租）·500 只·上海一号库·cells 11 格（在库 500/总量 500/成本单价 340.00 沿物料）·来源 tag=租入（info「资产来源」tag 走 detail-generic STATUS_CLS→tag-gray）·chain 租入单 RZD-20260815-003·路凯→本批（self）→转租客户（未发生）·timeline 09-10 租入入库 +500·与五态注记「租入＝租入在库未转租」口径一致；node --check 过
- 库存查询库存状态筛选「退租待入库」后追加 `<option>租入</option>`（页面层·无弹窗双层）
- 验证：listfull batch1 103 项全过（主表 14 行/13 列/新行详情弹窗逐行实点过）；PW 实证筛选「租入」→主表恰 1 行 RZRK-20260910-024（其余行为隐藏弹窗表不受筛选影响）；batch1 断言全动态（行数=实体条数自适应），无静态期望需先红后绿同步
- 主表数据驱动（renderListPage entity=stockFlows）新行自动入列，页面零结构改动

### T3 R-01 供应商应收演示行（**前提修正·零改动**·偏差 1）
- 按 T3.1 预设完成插入（AR-2026-09-PRJ2601-SUP·18,600·未收款·手动建单形态）后，终态断言发现 seg 内 btype=供应商应收=2——**既有行 AR-20260904-015 已存在**（billType=供应商应收·路凯赔付我方 2,850·租入单 RZD-20260815-005 关联·09-04 时间线含「直接生成供应商应收（不走赔偿单）」·scenario「F1 · 供应商应收（4 来源）」·应收账单表格首行正常渲染）
- 根因：该键为 receivableBills 实体**首键且 5 空格缩进**（`     'AR-20260904-015': {`），G24 检查与 G25 首轮键扫描（4 空格缩进正则）均漏检——G24 报告「receivableBills 无行」系误判
- 处置：按「最简合理默认+禁止臆测改写+单号即外键」**精确回退插入**（g25_t3_revert.py·键声明 0/billNo 0/双空行残留修复·字节级一致·node 计数 rows=14 sup=1）；验证门「btype=供应商应收 行=1」由既有行满足 PASS；listfull 应收期望 14 行不变三批全绿（先红后绿同步不适用·无数据变化）；G24 报告 R-01 条目待 G26 更正为「已满足」
- 预设行数据留档于 g25_t3_apply.py（若后续拍板需第二条手填形态演示行可直接取用）

### T4 R-04 矩阵 14→16（完成）
- 角色管理 auditPermMatrix「租入归还」后追加 `<span class="checkbox checked" data-audit="付款登记">` 与 `data-audit="收款确认"`（默认决策 6 checked）；data-audit 14→16·标签配平过·PW 实证 16 项/新两项 checked
- 全站 grep「14 项/可审单据矩阵（14/14 类/共 14」等变体（排除备份）：原型内**零命中**——计数表述仅存在于基线文档，T4.2 文字同步=零改动；A03/A04 亦无命中零重注入
- 销账口径登记：矩阵 16 项=待办 16 类一一对应（付款登记↔FK/收款确认↔SK 对应财务确认审核）；G22 遗留「租入单是否补入审核矩阵」随矩阵已含租入单一并闭环

### T5 G18 遗留销账（零改动·按拍板保留）
应付账单类型 option 内括号注释——「赔付应付（赔付供应商）」「丢损赔偿（赔付供应商）」（账单类型筛选 select）+新建弹窗同构 value option×5 组：按**保留**销账（括号=两类赔付方向口径注释，下拉展开态非页面默认态文案，范本页验收态保留先例）；G18 任务书遗留项与本档双登记，失败清单不记

### T6 G11 弹窗残块清理（完成·30 文件）
- 实测口径：`*/弹窗/` 69 HTML 中 34 个含 `<style id="proto-notes-style">` 块；其中 **30 个纯残块**（style 块在·fab/注入 script/块外 data-note 属性均无——首轮扫描 data-note=True 系 CSS 选择器 `[data-note]` 误报，实测块外属性 0），**4 个活标注层保留**（租入单新建/租赁单新建/权限配置/上下游绑定·有 fab+script+data-note 属性）；G11 登记 29·实测 30（口径差·含 G22 改名文件）
- 清理：逐文件删除孤立 style 块+紧随 EOL（防双空行）；逐文件 assert 块恰 1→0、`<style>`/`</style>` 配平各 -1、EOL 风格保持（LF 文件 26/CRLF 文件 4）；行数差登记：LF 文件块 20 行 2450 字节/CRLF 文件 21 行 2472 字节
- 空 `<script>`+孤立 `</style>` 坏块扫描：**0**（2026-09-03 历史坏块无回归）

### 验证门（7/7 全过·证据=各命令输出）
1. `g25_verify.py` 16/16 PASS（F01 三零+物料买卖 6/租入 option+status 租入 1+五态注记/供应商应收行=1[既有]+option 在/矩阵 16 含两项/全站「可审单据矩阵（14」=0/node --check 0/T6 残块 0）
2. `verify_listfull` 三批重跑 103+88+43=234 项 0 失败（batch1 含新行 14 行·batch2 应收 14 行回退正确·batch3 不变）
3. `audit_interaction.py` 115 页：死链 0/JS 0/`g17_audit_diff` 既有页逐键新增 0/页键改名对账 PASS/mobile 5 页各自 0/0（首轮 119 页=备份目录污染·迁移后复跑全绿，见偏差 2）
4. `v32_menu_verify.py` 14/14（菜单未动原样过）
5. PW 抽验：F01/库存查询/角色管理/应收账单四页 JS 0+关键词全命中+截图×4 留档（g25-f01-after.png/g25-kucun-after.png/g25-juese-after.png/g25-ys-after.png；角色管理首轮 innerText 检查因矩阵在默认关闭弹窗内不含隐藏文本误报，改 DOM textContent+矩阵项计数复验过）
6. 白名单门：git status P3-R01 前缀 34 项 = F01+_data/demo-data.js+库存查询+角色管理（白名单 4）+弹窗 30（T6 域）⊆ 默认决策 10 白名单；应收账单/A03/A04/菜单/P2 文档零改动
7. git 两段提交（主体 423a8b1·收尾见本段）·未 push（本地领先）

### 失败清单结论
**无失败项**；偏差 3 条闭环（①T3 R-01 前提不成立·既有行满足零改动 ②T6 备份位置迁 _scan_tmpdir·审计 115 口径 ③T1 CRLF 事故自愈重放）——详见 `agent-handoff/goal-failures-g25.md`

### 后续（G26 域·不属本包）
R-01 更正（G24 报告条目）、R-03 降级落法（F01 注记/P3-R04 刷新）、R-05 P3-R04 文档包、R-06 术语收口、zip 打包（须含 _data/）
