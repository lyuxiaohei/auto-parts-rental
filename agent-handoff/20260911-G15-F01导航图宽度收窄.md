# G15 · F01 导航图宽度收窄（1280→880 · 支线两行 · 主线右半收编）

> 状态：✅ 已完成（2026-09-11 · 执行方=kimi-code K3 · B 形态长任务命令）
> commit：fca99e8（工作批次；三件套回写批次随其后）
> 命令版本：2026-09-11 起草（道远拍板：备份一份→支线改两行→主线右半收编→整体收窄 ~880→审计复验）

## 一、背景与目标

- 道远反馈：F01 页面宽度过大——主线泳道节点链只占左半（最长到 x≈822），支线 chip 链拉满全宽（S1 七个 chip 到 x=1136），右侧大片稀疏。
- 目标：整页收窄 **1280→880**（`.wrap` max-width 与两个 SVG viewBox 同步），支线 S1/S5 chip 链改两行，主线右半的决策备注框/右对齐注记收编到 x≤840。
- **内容零损失铁律**：所有 `<a href>`、节点文案、注记文字、SRC_DATA、JS 一律不改不删；只动坐标/宽度/换行/viewBox/CSS 容器宽。字号一律不许改。
- 改动对象仅 1 个文件：`P3-R01-包装租赁管理后台原型/P3-R01-F01-业务流程导航图.html`（816 行，c0c64cb 提交态）。其余 112 页零改动。

## 二、现状坐标事实（锚点清单 · 行号为 c0c64cb 版参考，以锚点字符串为准）

### CSS 与 viewBox（3 处全局）

| 锚点 | 现值 | 目标值 |
|---|---|---|
| `.wrap{max-width:1280px` | 1280 | 880 |
| 主 SVG `<svg viewBox="0 0 1280 2168"` | 1280×2168 | 880×2184（图例改两行 +16） |
| 支 SVG `<svg viewBox="0 0 1280 724"` | 1280×724 | 880×846（±20 容差，见段偏移表） |

### 主 SVG 内需收编的元素

| 类别 | 锚点特征 | 数量 | 处置 |
|---|---|---|---|
| 泳道分隔横线+图例线 | 正则 `(x1="40"[^>]*?x2=)"1240"` | 主 SVG 9 条 | x2 批量 1240→840 |
| 右对齐注记 | `x="1240"` 且 `text-anchor="end"`（L1/L2/L3/L4/T1/T2/F 通道等） | 9 处 | x=1240→840；若与 16px 泳道标题估宽重叠（标题右缘>注记左缘），注记下移至标题下方 y+16 |
| B1 决策备注虚线框 | `<rect x="560" y="116" width="680"` + 内文 2 行 | 1 | 框改 x=516 w=324；内文 8px 重排 ≤38 全角当量/行，行距 14（2 行拆 3-4 行） |
| L3 决策备注虚线框 | `<rect x="560" y="800" width="680"` + 内文 2 行 | 1 | 同上：x=516 w=324 |
| L4 决策备注虚线框 | `<rect x="1112" y="1104" width="128"` + 内文 2 行 | 1 | 框改 x=532 w=308（右缘 840；L4 节点链止于 516，无碰撞） |
| T1 长侧注 | 「自有 → 应收（客户赔）·…详见 S4」（x=556 y=1458，估宽 ~330 溢出） | 1 | 从「·」拆两行 y=1458/1472，每行 ≤36 全角 |
| T1 适用注记 ×2 | 「适用：L1/L2（自有资产）」(854,1534)、「适用：L3/L4（租入资产）」(854,1610) | 2 | 分别移至 ②/③ 节点框正下方：x=682 y=1572 / x=682 y=1646 |
| T2 双入口注记 ×2 | (880,1700)/(880,1716) | 2 | 移至 x=524，从「——」/「（」处拆行，两行 ≤34 全角，y=1744/1758（不与 y=1740 横线元素碰撞则保持，碰撞则 y=1748/1762） |
| F1 来源注记 | (680,1952) 估宽 ~330 | 1 | 680+330=1010 溢出→拆两行 y=1952/1966 |
| F2 侧注 ×2 | (368,2038)/(368,2054) | 2 | 复核估宽 ≤472 即不溢出（368+472=840），超则拆行 |
| 底部押金/预收注记 ×2 | (40,2088)/(40,2104) | 2 | 同上复核，超宽拆行 |
| 主图例 | swatch 行 y=2138-2148，横向到 x=1140 | 1 组 | 改两行：y=2138 与 y=2154 各 ≤5 项，横向从 x=120 起按估宽排，全部右缘 ≤840 |

### 支 SVG 内需重排的元素

| 段 | 现状 | 目标 |
|---|---|---|
| S1 | 7 chip 单行 x=200/336/472/608/744/880/1016 y=20；来源注记 (1016,72)；底注 (336,72)/(200,86) | 两行 **4+3**：row1 x=200/336/472/608 y=20（右缘 728），row2 x=200/336/472 y=64；来源注记→(608,76)；底注→(336,116)/(200,130) |
| S2 | chips y=140，箭头 y=158，注记 (760,162) 溢出 | 整段 +44；注记→(200,238) |
| S3 | chips y=260，注记 (624,282) 不溢出（复核） | 整段 +44；注记随标题 (624,326) |
| S4 | 标题 386/框 380/分支 364·416/口径注记 (624,408) 溢出 | 整段 +44；口径注记拆两行→(200,514)/(200,528)，每行 ≤36 全角 |
| S5 | 6 chip 单行（5×w120+1×w170 到 x=1050），注记 (1082,522) | 两行 **3+3**：row1 x=200/336/472 y=544，row2 x=200/336/472 y=588（第 6 个 w=170 右缘 642）；注记→(200,638)；整段 +44 |
| S6 | chips y=620，箭头 y=638，右侧三行注记 (760,626/640/654) 溢出 | 整段 +76；注记→(200,706/720/734) |
| 支图例 | 线 y=684，项 y=694-704 | 整段 +108：线 y=792，项 y=806 起（若 S6 注记 y 到 734 则图例线 ≥760，取 792） |
| 支 viewBox 高 | 724 | **846**（±20 容差：以纵向重叠门=0 与图例完整可见收敛） |

**段偏移速查**：S1 +0（内部加行）｜S2/S3/S4/S5 +44｜S6 +76｜支图例 +108。位移必须覆盖该段全部 `y=`/`y1=`/`y2=`/`path d` 坐标形式。

## 三、任务组（T0-T7 · 每组完成即写状态文件）

- **T0 前置+备份+状态文件**：并发检查（原型目录最近文件 mtime≥30 分钟）→ 建备份 → 写状态文件。备份=复制 F01 至 `backup-f01-20260911/P3-R01-包装租赁管理后台原型/P3-R01-F01-业务流程导航图.html`（按原相对路径）。幂等：备份已存在则跳过复制。
- **T1 CSS 收窄**：`.wrap` max-width 1280→880（精确替换 assert=1）。
- **T2 主 SVG 内容收编**：按第二节表逐项（横线 x2、右对齐注记、三个决策框、长侧注拆行、适用注记移位、F1/F2/底部注记复核拆行）。全部"读取-精确替换+assert 计数"。
- **T3 主 SVG viewBox 与图例**：图例两行重排 → viewBox 改 `0 0 880 2184`。
- **T4 支 SVG 两行重排**：S1 4+3、S5 3+3、各段偏移、注记去向、图例位移 → viewBox 改 `0 0 880 846`（或重叠门收敛值）。
- **T5 机器门自检**：溢出门（两个 SVG 全元素 bbox 右缘≤880）+ 支线纵向重叠门（rect×rect 相交=0，箭头线段豁免）+ 内容零丢失门（改前后：`<a href>` 列表一致 + 全部 text 规范化拼接一致）。脚本存 `_scan_tmpdir/g15_gates.py`，输出逐门 PASS/FAIL。FAIL→按默认决策表修→重跑（≤3 轮，仍 FAIL 记失败清单）。
- **T6 审计复验**：`python _scan_tmpdir/audit_interaction.py`（全量 113 页）→ `python _scan_tmpdir/g14_audit_diff.py`。期望：死链 0 / JS 新增 0 / 既有 108 页 diff 新增 0（F01 外链 ERR_CONNECTION_CLOSED 断网噪音沿 G13/G14 预注豁免）。
- **T7 收尾**：截图 `_scan_tmpdir/g15-f01-after.png`（PW 全页）→ 回写三件套 → `git add -A` 提交（**不 push**）→ 哈希回填任务文档头部与索引行。

## 四、默认决策表（无人值守 · 拿不准按此执行）

1. 目标宽 880：右缘 840，左边距 40 不变；两个 viewBox 宽与 `.wrap` 三者同步。
2. 拆行规范：注记文本从「·」或「——」分隔符处拆；行距 14；每行 ≤36 全角当量（半角按 0.6 折算）；拆行后逐行复核右缘 ≤840。
3. S1 切 4+3、S5 切 3+3；chip 尺寸/宽度档（120/170）不动。
4. 右对齐注记与泳道标题重叠 → 注记下移标题下方 y+16，不改字号。
5. 段偏移档与 ±20 容差：以纵向重叠门与 bbox 门收敛为准；冲突时该段再 +12 递增重排。
6. 注记估宽公式：全角 8px/字、半角 4.8px/字（8px 等宽字体口径）。
7. F01 Google Fonts 外链断网（ERR_CONNECTION_CLOSED）= 环境噪音，豁免留档，不算失败。
8. 任何"内容零损失"与"宽度目标"冲突 → 宽度让位（宁可该行溢出记失败清单，不删不缩字）。
9. Mermaid 折叠区（`<details>`）文本不动——信息记录，不参与宽度收敛。
10. 不 push；commit message 前缀「G15 F01 导航图宽度收窄」。
11. 其余拿不准 → 最简合理默认 + 失败清单注明。

## 五、失败处理策略

- 任何 assert 失败/锚点不匹配：跳过该项记入失败清单 `_scan_tmpdir/goal-failures-g15-20260911.md`（格式：`| T组 | 锚点/元素 | 原因 | 处置 |`），继续下一项。
- 幂等重入：精确替换的 old_string 找不到 **且** new_string 已在文件中 → 判"该项已完成"，不算失败。
- 禁止卡住等待、禁止对不确定结构猜测性改写、禁止静默吞掉。
- 收尾报告必须含失败清单结论（空清单写"无失败项"）。

## 六、纪律（违反=返工 · 按任务动作类型反查基线第四节全集）

- **手工维护**：全部"读取-精确替换"式脚本+assert 计数；禁裸 `open('w')` 整页写。
- **坐标位移**：块内位移覆盖 `y=`/`y1=`/`y2=`/`path d` 全形式；改后标签配平自检。
- **备份还原**：按原相对路径（含子目录）；备份只增不覆盖。
- **Playwright**：编程点击+导航等待；URL 中文 `unquote()`；文本断言用 `textContent`；截图全页 `full_page=True`。
- **中文路径**：一律 Python `Path(r"...")` 内嵌，不经 shell 转义。
- **资源互斥**：同一工作区同一时刻只允许本 agent 一个写；开工查原型目录 mtime≥30 分钟。
- **git**：`git add -A`（`_data` untracked，`-u` 会漏）；禁 push。
- **验证门自己重跑**，不引用历史报告数字。

## 七、验证门（全部满足才算完成 · 证据贴进对话）

1. grep 门：`grep -c "max-width:880px" F01`=1；主 viewBox `0 0 880 2184`=1；支 viewBox `0 0 880`=1；`x2="1240"` 残留=0；`x="1112"` 残留=0。
2. 内容零丢失门（T5 脚本输出行）：`href 列表一致 PASS` + `text 全文拼接一致 PASS`（含 `<a>` 计数改前=改后）。
3. 溢出门：`溢出元素 0`（两个 SVG 全元素 bbox 右缘≤880）。
4. 支线重叠门：`rect 重叠对 0`。
5. 审计门：g14_audit_diff.py 输出含 `全站死链合计: 0` 且 `新增 0`（diff 新增 problems=0）。
6. 截图存在：`_scan_tmpdir/g15-f01-after.png`（人验用）。
7. 收尾三件套+commit 哈希已回填（索引新行 G15）。

## 八、执行记录（执行方回填）

- T0（10:05-10:15）：前置并发封锁——另一会话 65b73626 正实施 M:N 上下游绑定改造（09:38 道远指示其直改），G15 按命令停等并起监视器；道远现场确认其已停止后解除封锁，其未提交工作按基线§七.0② 单独存档 86675fc；备份 backup-f01-20260911（88948 字节）；状态文件建立
- T1：.wrap max-width 1280→880（assert=1）
- T2：24 项精确替换全 OK——分隔线 x2×9→840；右对齐注记×7→x=840 并下移避让📎标签（6 处 +16 至泳道标签行；T2 +26 避交棒虚线 y=1696）；B1/L3 决策框→x516 w324 内文改 3 行；L4 决策框→x532 w308；T1 长侧注拆两行；适用注记×2 移至节点下方；T2 双入口移至 (524,1724/1740)+既有注记 1744→1760 避让；F1 来源注记拆两行。F2 侧注/底部注记复核右缘 ≤840 不动
- T3：主图例改两行（5+4·行2 自 x=120 重排·右缘 ~522）+主 viewBox 0 0 880 2184，8 项全 OK
- T4：51 项全 OK——S1 切 4+3（来源/底注随排）、S2/S3/S4/S5 +44、S5 切 3+3、S6 +76（注记落 200,746/760/774 避 chips）、支图例 +108、支 viewBox 0 0 880 846；S4 口径注记拆两行
- T5：g15_gates.py 首轮 ALL PASS——门1 grep 全中；门2 href 92=92+text 拼接 3137 字一致+Mermaid/script/footer 逐字节一致；门3 溢出元素 0；门4 rect 重叠对 0；标签配平自检 text +5（拆行新增）闭合配平、行数 817→822
- T6：audit_interaction.py 全量 113 页死链 0/JS 0（F01 外链断网豁免本次未触发）；g14_audit_diff.py：全站死链合计 0、既有 108 页 diff 新增 0、mobile 5 页各自 0/0 → PASS
- T7：PW 全页截图 g15-f01-after.png（1000×3414）+五区人工复核（B1 框/T2 簇/主图例/S1/S5 无叠无溢出）；工作批次 fca99e8（11 文件）；三件套回写
- 失败清单结论：无未处置失败项（前置 3 行并发封锁及解除 + 偏差 8 行均当场闭环，详见 _scan_tmpdir/goal-failures-g15-20260911.md）
- 默认决策命中：#2 拆行规范（B1/L3/T1/F1/S4 五处·分隔符留行尾·行距 14）；#4 扩展适用（碰撞源实为📎标签非泳道标题）；#11×4（T2 双入口落点/适用注记③避交棒虚线/S6 注记避 chips/grep 门口径）；#5/#8 未动用；F01 页脚版本沿革未追加（内容零损失口径不动文案，沿革记于交接文档）
