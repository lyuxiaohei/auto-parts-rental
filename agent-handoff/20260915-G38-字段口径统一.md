# 20260915-G38-字段口径统一

> **状态**：✅ 完成（2026-09-16·执行会话 ZCode·主机 Win）
> **来源**：道远 2026-09-15 指示（字段统一方案一~四）＋「不能并入 G37 的生成 G38」
> **依据**：`_scan_tmpdir/参考价与单据单价梳理.md`（7 组异名实读＋2 处缺列缺陷＋参考价 6 条待定规则）｜`_scan_tmpdir/g31_t0_计费梳理.md`（计费字段现状·D-122 输入）
> **执行命令**：`_scan_tmpdir/g38_prompt.md`（形态 A）
> **执行前提**：单写者互斥／git clean；备份先行
> **红线**：**零业务口径变更**（只改名字／列头／列序配平；不动状态流、单号前缀、审批层级、计费字段与逻辑）｜不碰计费字段（D-122）｜已审核单据不可改（D-109）｜不改数据键名（G34 先例）

## 一、立项口径（道远 09-15）

1. **目的三条**：①把"同一概念多个名字"收敛（实读 7 组）②把"表头有列、数据缺格"的缺陷堵住（2 处）③把字段真值源从「实体→字段」单层升级为「实体＋概念」两层
2. **承接边界**：**G37（转移出库落地）只做功能与载体**；字段口径统一归本任务。唯一例外——G37 新建的 `transferOutbounds` 实体「建立即规范」，其字段键与列头按内联口径命名（见 G37 任务书），本任务回头只做核对不重做
3. **不承接**：
   - 单价带出与计费方式联动 → **计费议题任务**（D-122 派生·三段式＋结算换算日租金）
   - 主数据写死 option（租入单新建／租赁单新建硬编码下拉）→ **D-134 枚举接线**
   - 列表渲染机制推广（剩余 30 页）→ **D-133**（与本任务串行，见第八章）
   - 库存查询页的功能改造（删转租弹窗·行内跳转）→ **G37**
4. **物料类型值域收敛**（道远 09-15 指示）：按行业口径取 **10 个平铺物料类型**在**数据字典维护**——依据 WB/T 1058-2015／GB/T 31150-2014《汽车零部件物流器具分类及编码》（标准采用「材质×结构×用途」三维分类）；同时修正现状错归（零部件被归入「组件」）与 `stockFlows.cls` 双值域问题
5. **T0 落账**：P2-R01 取执行时当时最大 D 号＋1（字段口径统一立项＋参考价规则采纳＋物料类型 10 值）

## 二、第 0 步·上下文获取（动手前必做）

1. `agent-handoff/_AGENT基线.md`——第四节（历史事故与真相）＋第七节（上手清单）**逐条适用**
2. **依据文档**：`_scan_tmpdir/参考价与单据单价梳理.md`（本任务的问题清单来源）｜`_scan_tmpdir/g31_t0_计费梳理.md`（计费字段现状·勿越界）
3. **现状实读点**（开工先逐处确认，勿凭记忆）：
   - 两处缺列：`采购管理/采购入库录单.html`（表头 14 列／数据行 10 格）｜`租赁管理/租赁出库录单.html`（12／8）
   - 价格四件套破例：`采购管理/采购退货单列表.html`、`销售管理/销售退货单列表.html`、`采购管理/弹窗/新建采购退货单.html`、`销售管理/弹窗/新建销售退货单.html`（用裸「单价(元)／金额(元)」）
   - 物料异名：「货品」5 文件（租入单列表＋弹窗、租入归还列表＋弹窗、租赁单列表下钻）｜裸「名称」2 处（`仓储作业/库存查询.html`、`仓储作业/盘点录入.html`）
   - 库房层级：「入库库区」2 处（`采购管理/采购入库列表.html`、`租入管理/租入入库列表.html`）｜「仓库」2 处（`基础数据/库位档案.html`、库存查询页归 G37）
   - 字段真值源：`P3-R01-包装租赁管理后台原型/P3-R01-A05-字段字典.md`（实体 41·字段行 351·**115 处〔待核〕**）
4. **工具与规范**：`_scan_tmpdir/audit_interaction.py`（全量审计）｜`_scan_tmpdir/g28c_modal_scan.py`（弹窗溢出）｜`node --check`｜`skills/admin-ui-spec/references/components/form.md`
5. **勿重做**：G37 范围（转移出库三页·库存查询功能改造）｜G33 三页｜试点页｜G36 正在执行的批次范围｜P1-R04 术语表第五节执行记录

> **执行顺序（单会话内完成·不拆批）**：T0 落账 → T1 字段闸工具＋全站首扫 → T2 概念表 → T3 A05 回填与清账 → T4 表单／录单／弹窗页口径 → T5 列表页表头口径 → T6 两处缺列修复 → T7 入库计量删托列 → T7·补 物料类型 10 值 → T8 字段闸终扫＋全量审计 → T9 回写
>
> 本任务为**一个 goal 会话**内完成，**不拆批**。若中途无法完成某项：按失败处理策略记失败清单并在收尾汇报，**不要停下等待**；可后续以**同一 goal 续跑**，已完成项不重做。

## 三、任务分组

### T1 字段闸工具（先做·成本最低·立刻能拦缺陷）
- 新建 `_scan_tmpdir/field_gate.py`，四项检查：
  1. **列数配平**：静态表＝表头列数（colspan 计入）等于数据行格数；数据驱动表＝表头列数等于 `(勾选框?)＋1(单号)＋cells.length＋(操作?)`（`noCheckbox`/`noOps` 变体）
  2. **悬空表头**：某表头列下全部数据行为空或错位特征（列值类型与表头语义不符）
  3. **同义异名**：概念白名单（T2 产物）之外的写法直接报错
  4. **未登记字段**：页面/数据出现而 A05 无记录的字段
- 全站首扫报告 `_scan_tmpdir/g38_field_gate_scan.md`（修复前基线）
- **工具本身进验证门**：本任务收尾时终扫必须比首扫少掉全部已知项

### T2 字段概念表（真值源中间层）
- 落 A05 新节「**字段概念索引**」：一概念一行＝`概念 id`／`标准名`／`列内短名（表格内允许）`／`禁用写法`／`单位与口径注记`
- 概念 id 用「域.语义」短码：如 `price.excl`／`price.rate`／`price.incl`／`price.amount`／`qty.base`／`qty.carton`／`date.occur`／`wh.warehouse`／`wh.location`／`material.name`／`material.code`／`doc.no`／`status`
- 覆盖 40~60 个概念即可；**本节是后续所有页面命名的引用源**

### T3 A05 回填与清账
- A05 每字段行加「概念 id」列并回填（351 行）
- 清 **115 处〔待核〕**：能由页面/数据确认的落标准名，不能确认的降级为注记（不臆造）

### T4 表单／录单／弹窗页口径收敛
- 价格四件套（见默认决策表）
- 物料列（货品／裸名称）
- 库房层级（库区→库房）
- 时间列（日期 vs 时间）
- 金额列口径显式化
- **只改标签与列头，不动数据键名与数值**

### T5 列表页表头口径收敛
- 与 T4 同口径，范围＝各列表页 thead 与 `renderListPage` 的 cfg 列定义（两处必须同步，否则触发 G35 那类 label 脱钩）
- 含「金额(元)」按真实口径改显式（订单类含税→`含税金额(元)`）

### T6 两处缺列缺陷修复
- `采购管理/采购入库录单.html`：补回 `未税单价(元)｜税率｜含税单价(元)｜含税金额(元)` 四格（值按默认决策表带出）
- `租赁管理/租赁出库录单.html`：同上（组合件无档案价，按默认决策表取跨页一致值）
- 修后两页必须过 `field_gate` 的「列数配平」与「悬空表头」两项

### T7 入库计量改以基本单位为主（道远 09-15 拍板·不按托·**删托列**）
- 采购入库录单＋租入入库录单：**主计量＝物料基本单位**，数量列必填（带 `*`）；「单位」列→「**基本单位**」（值取 `products.unit`）
- **删除 `托数`／`每托数量`／`入库总数` 三列**（道远 09-15「删掉」）——列头＋表单行＋该页 `renderListPage` cfg 同步删除；数量一律以基本单位计
- 行内说明与 A03 标注文案同步（原「以零配件、以托为单位入库」表述退场）
- 双层同步（录单页＋宿主页内嵌块）

### T7·补 物料类型值域收敛（10 值·道远 09-15 指示「看行业通用·给 10 个示例·在数据字典维护」）
- **数据字典 `物料类型` 组 6 → 10 值**（见默认决策表·物料类型节），`系统管理/数据字典.html` 同步
- **`products.cls` 重新归类**：现 6 个零部件（锁扣／铰链／围板／箱盖／底托架＝LJ-A100~E500）被**错归「组件」**→ 改「**零部件**」；`内衬`（LJ-F600）单列「**内衬**」；「组件」留给 BOM 组合件（`bomList` 的 ZH-*）
- **`stockFlows.cls` 值域收敛**：现「租赁器具／零部件」2 值 → 收敛到 10 值口径（器具行按形态归类、零部件行→「零部件」）
- A05 值域行同步；演示数据按新值域补示例（**卡板箱／金属托盘／料架** 各至少 1 行，避免值域空转）

### T8 验证
- `field_gate` 终扫（对比首扫）＋全量审计 ＋`node --check` ＋ PW 抽验（两录单页＋两列表页）

### T9 回写三件
- 任务档执行记录｜`_索引.md` G38 行挂哈希｜`_AGENT基线.md` 快照滚动；A02／A05 同步

## 四、默认决策表（执行中不再提问·逐条按此办）

### 价格四件套（全站统一）
- 明细表一律 `未税单价(元)｜税率｜含税单价(元)｜含税金额(元)`；**禁裸「单价／金额」**
- **计费口径联动（道远 09-15 拍板）**：单据侧不做三段式换算；按「时间周期」计费时该列**语义即日租金**，按「次」计费时为次单价——列头随计费方式切换：按时间周期→`日租金(元/天)`、按次→`次单价(元/次)`（两态共用一个字段）
- 退货单四处：`单价(元)`→`未税单价(元)`、`金额(元)`→`含税金额(元)`（**值不动，只改列头**）
- 列表页「金额(元)」：订单类（采购订单／销售订单）→`含税金额(元)`；账单类已显式者不动
- 含税单价保留两位小数＝未税×（1＋税率）；含税金额＝数量×含税单价（与既有算法一致）

### 物料列
- 一律 `物料｜物料名称｜物料编码`；「货品」（5 文件）→`物料`；裸「名称」→`物料名称`

### 库房层级（不新增/删除层级·只对齐用词）
- 库房级→`XX库房`；库位级→`XX库位`
- 「库区」**不入用词**（该层已在 G31 T3 从模型删除）：`入库库区`（值＝原料区 RA／成品区 RB／外购区 RW）→`入库库房`（2 处）
- `库位档案` 列头「仓库」→「库房」；`盘点录入`「库位」保留（概念表登记为短名）
- 库存查询页**不在本任务范围**（G37）

### 物料类型（10 值·行业口径）
- 字典 `物料类型` 组 10 值：**围板箱｜料箱｜卡板箱｜料架｜塑料托盘｜木托盘｜金属托盘｜内衬｜组件｜零部件**
- 依据：WB/T 1058-2015／GB/T 31150-2014《汽车零部件物流器具分类及编码》——行业标准按「材质／结构／用途」三维分类并给 15 位编码；**本项目取器具形态名平铺为一维列表**（便于筛选项与列头显示），材质/结构若后续需要另作独立属性，**不拆类型**
- 语义边界：「**组件**」专指 BOM 组合件（`bomList` 的 ZH-*）；**零部件**＝锁扣/铰链/围板/箱盖/底托架等；**内衬**单列
- 值域唯一来源＝字典；页面筛选/列头一律按字典渲染，**禁写死**

### 时间列
- 只到日→`XX日期`；精确到分→`XX时间`（`data-tax`/值形态判粒度）；同一实体同一语义禁两种写法

### 概念 id 与概念表
- 概念 id 格式＝`域.语义`（小写·下划线分隔），域限：price／qty／date／wh／material／doc／status／partner／project／amount_extra
- 概念表未收录的写法**不许出现在页面**（field_gate 第三检会报）

### T7 入库计量（道远 09-15 拍板：以基本单位为主·不按托·**删托列**）
- 主计量＝**物料基本单位**：数量列必填（带 `*`）；「单位」列→「**基本单位**」，值取 `products.unit`
- **删除** `托数`／`每托数量`／`入库总数` 三列（列头＋表单行＋cfg 三处同步删）
- 不用 `class="auto"`（本项目 audit 无该豁免）；只读列一律 `td-num auto-cell`

### T6 缺列修复取值
- `采购入库录单` 三行按**参考未税采购价**带出：LJ-A100＝6.80／LJ-B200＝4.20／LJ-D400＝36.00；税率 13%；含税单价＝7.68／4.75／40.68；含税金额＝入库总数×含税单价＝36,864.00／15,200.00／29,289.60
- `租赁出库录单` 组合件 ZH-2601-A **无档案价**→取跨页一致演示值（与 `租赁单新建` 同物料同价）：未税 2.40／税率 13%／含税 2.71／出库数量 180／含税金额 487.80（第二行同理按 `租赁单新建` 行 2 值）

### 范围边界
- 只改**标签／列头／列序配平**；**不动数据键名、不动数值口径、不动状态流与单号前缀**
- 白名单：`P1-R04-术语表.md` 本身｜A05 沿革注记｜P2-R01 需求文档与会议材料
- 改 `demo-data.js` 后必跑 `node --check`；改 thead 后必须同步 `renderListPage` cfg（G35 教训）

### 兜底自愈
- 某页结构不符／锚点不匹配→跳过该页、记失败清单、继续
- 概念表定不下某概念→**保留现状**并记失败清单（不臆造标准名）
- 复核发现已合规→记「无改动」继续，不计失败

## 五、失败处理策略（无人值守铁律）

- 任何 assert 失败／锚点不匹配／结构异常：跳过该项，记入 `agent-handoff/goal-failures-g38.md`（任务号＋文件＋原因），继续下一项
- 禁止卡住等待、禁止猜测改写、禁止静默吞掉失败
- 前置校验任一阻塞性不符＝停止并写失败清单
- 收尾必须给失败清单结论（可为空；空则「无失败项」）

## 六、纪律（违反=返工）

- **基线第四节＋第七节全部逐条适用**（按动作类型反查：改 HTML 块→带标签锚＋配平自检；改 demo-data→精确替换＋`node --check`；Playwright→编程点击＋`unquote()`＋`textContent`；备份还原→按原相对路径；改内嵌 JS→必跑该页 audit）
- 读取-精确替换＋`assert` 计数＋幂等；禁裸 `open('w')` 整页写入既有页面
- 备份先行：`P3-R01-包装租赁管理后台原型/` 整体复制至 `_scan_tmpdir/backup-g38-20260915/`（按原相对路径）
- 中文路径 python 内嵌（`io.open encoding='utf-8'`）／Windows `newline=''`／行尾与宿主一致／计数用 `str.count` 禁 `grep -c`
- 多股改动 `git add -A`；工作区有非本 agent 改动 → **只提交精确路径**
- 术语一律以 P1-R04 现行词为准（D-142 三条术语由 G36/G37 执行，本任务不重复做）

## 七、验证门（证据全部贴进对话）

> **验收方式**：按 goal-creator v9——执行者自检通过后派**独立子 agent**（只读·自查取证）逐项复验，结论原样贴进对话

1. **字段闸终扫**：`field_gate.py` 四检对全站——列数配平 0 处不符、悬空表头 0 处、异名 0 处、未登记字段 0 处——证据＝扫描输出行＋首扫/终扫对比
2. **异名清零**：7 组逐词 `str.count` 输出（白名单除外）
3. **缺列修复**：两页「表头列数＝数据行格数」计数打印＋四价格列逐格有值
4. **概念层**：概念表行数＋A05 回填覆盖行数＋〔待核〕残留数——证据＝打印计数
5. **审计**：`audit_interaction.py` 全量——死链 0／JS 0／相对上一基线 diff 新增 0——证据＝汇总行＋新基线落盘
6. **弹窗扫描**：`g28c_modal_scan.py` 无溢出——证据＝扫描输出
7. **`node --check`** 退出 0＋页数变化核对（本任务不新增/删除页面，期望与开工基线一致）——证据＝输出行
8. **PW 抽验**：两录单页四价格列有值＋两列表页列对齐——证据＝断言输出
9. **回写**：任务档执行记录｜`_索引.md` G38 行挂哈希｜`_AGENT基线.md` 快照｜A02／A05 diff 行——证据＝diff 行
10. **失败清单结论行**（空则「无失败项」）
11. **独立验收**：子 agent 逐项 `[PASS/FAIL]` 与总判定行原样贴进对话
12. **git 提交**（前缀 `G38`·**禁 push**）——证据＝`git log --oneline` 输出行

## 八、与其他任务的关系

- **G37（转移出库落地）**：字段口径归本任务；G37 内的三条例外＝①`transferOutbounds` 新建即规范②项目档案「转租结算方式」属性命名对齐③库存查询页用词随其功能改造顺手收敛。**两者串行执行**，避免同页交叉；本任务开工时若 G37 已改过的页面以现状为准
- **G36（弹窗页面化·执行中）**：**必须待其全部批次完成后执行**——本任务属「全局性文本与列头改动」，而 G36 逐批重建页面载体（删模板＋从样板页建新页）；若先改，G36 后续批次重建时会**覆盖回旧版文案**。G36 进行中时不得开工（不是"跳过页面"，而是整体排后）
- **D-133（列表数据驱动推广）**：**串行·本任务先行**（先把口径定死，D-133 逐页重建 thead 时直接按新口径写，避免二次改）；若 D-133 已开工，则本任务只补差集
- **D-134（枚举接线）**：主数据写死 option 属 D-134，本任务只登记不实施
- **计费议题任务（D-122 派生）**：单价带出与计费方式联动属该任务；本任务只统一列头文案，**不碰计费字段**
- **D-142（术语三条统一）**：由 G36/G37 执行，本任务不重复

## 执行记录（2026-09-16 · 单会话完成·不拆批）

**前置校验全过**：模块 mtime ≥30min（demo-data 519min）｜索引 G38=⏳｜无 g38_* 执行产物｜git clean｜G36=✅｜备份 backup-g38-20260915（132 HTML）。

**T0 落账**：P2-R01 A.4 +D-147（字段口径统一立项＋物料类型 10 值·参考价六条采纳并入 D-145③注记）；头部「最后更新」→09-16；完成时状态 🔄→✅。

**T1 字段闸工具＋首扫**：`_scan_tmpdir/field_gate.py` 四检（列数配平＝静态 th/td 配平＋数据驱动公式「勾?+单号1+cells+操?」；悬空表头＝列全空·控件格按 input value 取值·勾选格记 [cb]；同义异名＝字段名位置〔th/cfg label/ff-label/feeCols/info label〕禁用写法＋时间粒度错配；未登记字段＝demo-data fields 键 vs A05·按实体.字段去重）。工具自检三轮迭代修 bug（tbody 60000 字符越窗归错表／input 空值误报／innerHTML 运行时整写豁免）。**首扫冻结 `g38_field_gate_scan.md`：检1=5／检2=0／检3=158／检4=17**——检1 精确命中两处已知缺列（租赁出库录单 12/8×2 行·采购入库录单 14/10×3 行）；检3 命中 7 组全部＋新发现（invoices feeCols 裸金额×6·时间粒度 13 处）；检4＝A05 缺登记清单（1 实体+15 字段去重后 17 项）。

**T2 概念表**：A05 新节「字段概念索引」**60 概念**（price 11/qty 5/date 6/wh 2/material 5/doc 9/status 4/partner 3/project 2/amount_extra 12·域限九域），列＝概念 id／标准名／列内短名／禁用写法／单位与口径注记；含维护规则三条（禁用写法由 field_gate 检3 机检·渲染元字段不挂概念记 —）。

**T3 回填与清账**：42 表头加「概念 id」列＋**367 行回填**（351 原＋拆 salesReturns 组合行 1→5＋补登记 12）；**〔待核〕清账 112 行**（页面标题/明细区标题等渲染元字段 60+ 落注记·业务字段按取值域落标准名：单据编号→单据号·账单编号→账单号·金额→账单金额·已付→已付/已收金额等）；残留 2 处〔待核〕为维护规则/图例的机制说明（沿革注记·白名单）＋1 处版本行自述；退场注记 2 行（locations.area 库区·salesOrders.mode 下单方式〔09-14 删域〕）；标签同步 16 行（库区→库房·纯日期 时间→日期·器具→物料·出库时间→出库日期）；概念 id 未挂（—）111 行＝渲染元/权限/看板等无对应概念。补登记 12 行＝检4 清单（products.model/innerCode/rentInMode/rentInPrice/rentalMode/rentalPrice·opLogs.result·projects.settle·purchaseInbounds.maker/area·stockFlows.loc/qtyByProject）。**A05 统计行更新：实体 42｜字段行 367｜概念 60**。

**T4/T5 页面口径收敛（fix1·18 文件全过 0 失败）**：裸「单价(元)/金额(元)」清零（退货两新建页→未税单价/含税金额·订单两列表→含税金额·分期 4 页〔付款新建/应付新建/应付账单/应收生成〕→分期金额(元)）；货品→物料（租入归还新建/租赁单列表 th·租入单新建 hint 多货品→多物料）；时间粒度（BOM/产品档案/客商管理/项目档案/租入归还列表 纯日期值→「日期」）；**两列表页 thead+cfg+ff-label 三处同步**（采购入库列表/租入入库列表 入库库区→入库库房）。

**T6 缺列修复（fix3）**：采购入库录单 thead 重写 12 列（零件号/物料编码→物料编码·单位→基本单位·托三列→数量*·四价格列在位）＋3 行各补四价格格（6.80/13%/7.68/36,864.00·4.20/13%/4.75/15,200.00·36.00/13%/40.68/29,289.60＝按参考未税采购价·行3 实值 6 托×120=720）；租赁出库录单 2 行各插四价格格（2.40/13%/2.71/487.80＝与租赁单新建同物料同价·行2 0.15/13%/0.17/0.00〔数量 0 空态行〕）；两页格数断言 12×3/12×2＋标签配平自检一致。**注**：任务书默认决策表「出库数量 180」与页面实值核符（460 系可用组合库存列·180 为数量 input·决策表无误）。

**T7 托计量退场＋物料类型 10 值（fix2/fix4）**：
- 采购入库列表删「到货托数」列（thead＋demo-data cells 8→7·8 行托格删除〔7 托格＋1「80 件（折叠隔板）」格〕·cells 均 7 断言）；
- feeCols：零件号→物料编码 ×15（purchaseOrders 7＋purchaseInbounds 8）·托数 × 件数→数量 ×8·退货两实体 裸单价/金额→四件套 ×6+6·invoices 金额→开票金额 ×6·rentInOrders 多货品→多物料 ×5；
- info 标签：入库库区→入库库房 ×22·出库库区→出库库房 ×20·调出/调入库区→库房 ×3+3·stockFlows 库区→库房 ×16·returnInbounds 入库时间→入库日期 ×8·salesOutbounds 制单时间→制单日期 ×5（1 条含时分 XSCK-20260902-015 保留「时间」·标签随值）·partners 更新时间→更新日期 ×8·products 裸「名称」→「物料名称」 ×12；
- **物料类型 10 值**：dictItems +WL-07 卡板箱/WL-08 金属托盘/WL-09 内衬/WL-10 零部件（**141→145 项**）·WL-06 释义「锁扣内衬等散件」→「BOM 组合件（ZH-* 母件）」；products 重归类 LJ-A100~D400 组件→零部件·LJ-E500/F600→内衬（**E500 名为「内衬（金属）」按语义归内衬·任务书「A100~E500→零部件」系笔误从语义**）＋新增 3 示例行 **KBX-1040M 卡板箱/PLT-1210G 金属托盘/KJ-2701 料架（12→15 行·四参考价齐）**；stockFlows.cls 租赁器具→形态值 11 行（围板箱 6/塑料托盘 3/料箱 2）·LJ-F600 零部件→内衬；产品档案/物料新建 物料类型 option 6→10；库存查询种子行 tag 同步 5 行（租赁器具 清零）；
- **标注文案同步（fix4）**：A03 JSON（FP3-01 desc 按零配件以托为单位→按物料基本单位入库〔D-145〕·到货明细标题与 note·逐货品→逐物料）＋A04 ×1＋全站注入块 **75 文件**（73 页共享 pin＋归还 2 页）·HTML 残留终检 0；
- demo-data 行尾整体归一 CRLF（既有 10 行 LF 一并）·**node --check 退出 0**。

**口径偏差登记（非失败·按最简合理默认）**：①salesOutbounds 制单时间数据混粒度（5 纯日期+1 含时分）→标签随值分置，数据级治理另议；②rentInbounds/rentInReturns fields 纯日期而 cells 含时分→列标签维持「时间」（按显示值），fields 粒度怪癖登记不改值；③mobile 库存查询搜索占位符含「库区」（占位文案非字段名·gate 不检·移动端改号归 G50）；④盘点录入/盘点列表「全库区」为筛选 option 值（值不动红线·保留）；⑤G36 前 B类白名单快照两行表头同步为新口径（其余路径沿革为 G36 登记债）。

**T8 验证**（证据全部打印于会话）：
1. **field_gate 终扫 `g38_field_gate_final.md`：检1=0／检2=0／检3=0／检4=0**（首扫 5/0/158/17→全零；终扫前两轮迭代：混合粒度列回滚 2 页＋闸门粒度判据改「列值优先」）；
2. 异名清零 str.count 终检：货品 0（HTML th/label 位）·库区 0·零件号 0·托数 0·单价(元)/金额(元) 裸名 0（详见会话验证段）；
3. 缺列修复：两页「表头列数=数据行格数」12=12×3/12×2＋四价格列逐格有值（PW 实读 input value）；
4. 概念层：概念表 60 行·A05 回填 367 行·〔待核〕残留 0（行级）；
5. **audit_interaction.py 132 页：死链 0／JS 0／problems 逐键对 g37baseline 新增 0·页数 132=132**（g38baseline 落盘）；
6. g28c_modal_scan：**18 弹窗 0 溢出·0 页异常**；
7. node --check 退出 0＋页数 132 不变（无增删）；
8. **PW 抽验 10/10**（g38_pw_check.py：两录单页四价格格值＋两列表页列对齐〔采购入库列表 thead10=td10·入库库房在位〕）。

**T9 回写**：任务档本节｜索引 G38 行✅＋哈希｜基线快照追记｜A02 v6.1｜A05 G38 登记节（12 条改动面）｜P2-R01 D-147 ✅＋头部；P1-R08 零改动（无文件增删）；失败清单 goal-failures-g38.md＝登记性记录 5 条·结论**无失败项**。

**工具留档**：field_gate.py（四检·入验证门）｜g38_field_gate_scan.md（首扫）/g38_field_gate_final.md（终扫）｜g38_fix1_labels.py/g38_fix2_demo.py/g38_fix3_t6t7.py/g38_fix5_revert.py（幂等修复链）｜g38_audit_diff.py（对 g37baseline）｜audit_results_g38baseline.json｜g38_pw_check.py（10 断言）。
