---

## 变更记录 · 2026-09-08（详情弹窗数据驱动全量推广 · 第十一节任务书三批连跑）

**背景**：第十一节任务书 E 节 goal 连跑版（D 段分批停等作废，3 批连跑、批末硬门、git 一批一提交、失败回滚记清单）。开工前置检查过：原型目录 mtime 503 分钟无变化、工作区先 git add -A 留档、基线审计自跑（16 条＝10 readonly 豁免＋6 modal-unreachable 误报，死链 0/JS 错 0）。

**改动**：
1. `_data/demo-data.js` 新增 26 实体 171 条，合计 33 实体 212 条：批次1 租赁主线 8 实体 51 条（leaseOrders/rentInOrders/comboOutbounds/returnInbounds/returnApplies/damageOrders/rentInReturns/rentInbounds）；批次2 买卖+仓储 8 实体 48 条（purchaseOrders/salesOrders/purchaseInbounds/salesOutbounds/otherInbounds/otherOutbounds/assemblyOrders/disassemblyOrders）；批次3 库存与档案 10 实体 72 条（stocktakes/transfers/stockFlows/rentTracks/assetTracks/partners/appliances/parts/locations/bomVersions）。单号全部沿用列表行与 L3/L4 数据链/财务实体（不自造）：RZD/RZRK/ZL/TZSQ/TZRK/GHCK/ZZ/CK/CGRK/XSCK/AP/AR/BS/QTRK/QTCK/PD/DB/DW 等链上单号跨实体引用 6~16 次/号，互溯链可点
2. `_data/detail-generic.js` 扩展：STATUS_CLS 三批扩充（已退租/在租/履行中/盘点中/启用/超期未还 等状态色）；`openGenericDetail(entity,key,base,modalId)` 与 `wireDetailModal(entity,{anchorText,modalId})` 加可选参数（向后兼容）——批次3 特殊弹窗（库存查询 flowModal·锚「库存流水」、租出台账 trackModal·锚「详情」、在租台账 trackModal·锚「资产轨迹」、BOM维护 bomViewModal·锚「查看」）经结构核对均为四段式，直接参数化套通用渲染器，未写专用渲染器
3. 25 个列表页＋24 个弹窗模板双层骨架化（#detailTitle/#detailBody＋wireDetailModal 一行接线/模板预览行；列表行 HTML 零改动；LF 行尾文件按文件探测保持）；在租台账 trackModal 与租出台账共用 `弹窗/器具出租履历.html` 模板、两页各自接线（A 表 #20）；在租台账 scrapModal 报废确认弹窗不动
4. 前缀键防误匹配：BTC-6040S 先于 BTC-6040、WBX-1210L(旧) 先于 WBX-1210L 插入（wireDetailModal 行文本 indexOf 匹配的包含陷阱，实体键插入序即匹配序）
5. 09-07 业务补充新增行一并建实体（键=行单号，HTML 行零改动）：退租申请 TZSQ-20260904-009（部分退租）、其他出库 QTCK-20260905-005（赔偿核销）、组装 ZZ-20260904-007（拆散件再组装承接 CX-20260902-006）、租出台账 ZL-20260903-034（退租回库件循环再出租，履历含前手 ZL-20260610-015）

**验证**（三批批末硬门全过）：
- Playwright 逐行实点（http.server + 标题 textContent 断言）：批次1 48/48、批次2 47/47、批次3 81/81，合计 176/176 PASS（151 个行内触发锚＋25 模板预览页含共用模板；加载 0 JS 错）
- 全量审计三连跑（125 页）：死链 0 / JS 错 0；modal-unreachable 随数据驱动页 6→14→22→32 同步增长，全部为已有 wireDetailModal 接线页的已知误报（第四节⑦口径）；readonly 预填 10 条豁免不变；其余新增问题 0
- demo-data.js 校验：每实体键在实体段内唯一（批脚本断言）；跨实体同名键为合法设计（receivableBills 赔偿联动行键=赔偿单号、多页共用物料编码）；node --check 语法通过
- git 三批提交：bdcbf78（批次1）/ dac267b（批次2）/ 批次3 见 git log；改前备份 `_scan_tmpdir/backup-detail-批次1|2|3-20260908/`（原相对路径）；失败清单 `_scan_tmpdir/goal-failures-detaildriven.html` 空清单（失败 0 项）

**默认决策记录**（E 节表外，按最简合理默认）：
- 器具/客商/零部件/库位档案四段语义映射按 E 节第 1 条执行（档案信息/规格参数/往来轨迹/操作记录），映射内容均能对上行数据，未写专用渲染器
- A 表标注 3 个特殊弹窗（库存流水/器具出租履历/BOM版本查看）结构核对均为四段式，套 detail-generic.js（modalId/anchorText 参数化），未写专用渲染器
- 租赁单列表 2 行（ZL-20260823-033/ZL-20260901-032）无「详情」按钮为页面原有状态，实体数据仍按全行建模
- 财务实体（应付 refs）引用的 CGRK-20260825-011 等单号与采购入库现行列表行不完全一致（财务实体先建时的历史数据），本轮按列表行数据为准、未回改财务实体
- BOM 版本配方：V2.1 按 BOM维护查看弹窗现行 4 子件行照抄；V2.0/V1.0 按版本变更说明反推（围板 5→4、锁扣 6→4 演进）

**遗留**：
- audit 的 modal-unreachable 检测维度仍待补 JS 动态绑定识别（第四节⑦；本轮已知误报增至 32 条）
- 根目录 zip 快照未重打（对外发版需重打，必须含 _data/ 目录，第四节⑦教训②）
