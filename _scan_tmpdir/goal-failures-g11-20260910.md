# G11 失败清单（2026-09-10）

（空清单 = 全过；单项失败格式：`页面/文件 | 阶段 | 现象 | 处置[恢复备份/跳过注记]`）

处置外偏差注记（非失败，均已闭环）：
- B3 首轮注释配平断言把 JS 注释计入 `<!--` 计数致 4 文件误报——断言修正后 10/10 全过（4 文件首轮未写盘零污染）
- B5 Playwright 库存查询首跑用 textContent 断言（含隐藏注入块文本）误判——改 innerText（技能文档口径）后 3/3 PASS
- 迁移 pin 落点 4 条改落 A04（annotate 单数据文件整注替换机制，落 A03 会被 A04 重注覆盖）——见 g11-disposition.md 默认决策命中注记

结论：失败 0 项。
