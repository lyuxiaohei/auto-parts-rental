# G12 失败/偏差清单（2026-09-10）

> 任务：数据驱动查漏收口 ｜ 规则：任一失败=跳过+留档+继续；文件必须存在且含结论行

## 偏差与失败记录

### 1. 前置校验1（单写检查）字面偏差——判定通过，继续执行
- **现象**：原型目录最近文件 mtime=5~7 分钟（F01-业务流程导航图.html 17:53:11 / F01-开发规则.md 17:54:53），< 30 分钟门槛。
- **核实**：①120 秒复采样 mtime 无推进（写入者已停手，非活跃并发）；②`_data/demo-data.js` 149 分钟静默；③F01 两文件不在 G12 改动范围（54 文件清单外）；④F01 改动为未提交 WIP（+673 行，另一会话 v3.4 规则化重写产物）。
- **处置**：按基线七.0.② 协议——先将存量未提交改动单独存档提交（29e4496，与 G12 改动分股），再开始 G12 作业；audit 基线在当前状态自跑。
- **结果**：无冲突发生；audit 基线/终跑 F01 读数均 0/0/0。

### 2. 复扫与任务书现状差异——以实扫为准（前置校验4 口径）
- 实扫未引业务页 = **12 页**（任务书 11 页 + 多出「我的待办.html」）；弹窗 43 与任务书一致。
- **处置**：我的待办.html 按同一分类标准判定（15 行表+筛选交互）→ A 类接线；任务书「现状实测」差异已在执行记录注记。

### 3. verify_listfull.py batch3 工具清单陈旧（G07 遗留，非 G12 造成）
- **现象**：batch3 引用 G07 已删除的 租出台账.html/在租台账.html → ERR_FILE_NOT_FOUND 崩溃。
- **处置**：工具修复——租出台账条目改挂合并宿主 租赁单列表.html（entity leaseOrders）；在租台账条目删除（宿主 库存查询 batch1 已覆盖）；注释同步。修复后 batch3 43 项断言失败 0。

### 4. list-generic.js 增补 noOps 开关（基建小改，向后兼容）
- **原因**：操作日志/盈亏报表两页表格无「操作」列，原 rowHTML 强制渲染 ops 列会列错位，且 ops 缺失时 forEach 崩溃。
- **处置**：cfg.noOps=true 跳过 ops 列渲染 + (r.ops || []) 防御；既有页面（flag 未设）行为不变，batch1-3 回归全绿佐证。

### 5. A 类页内局部静态块（非文件级白名单，已登记 g12-whitelist.md 附注）
- 用户权限第二表「角色管理」9 行：ops 引用 openRolePerm 仅角色管理页定义，驱动会产生 JS 错 → 保持静态（roles 实体在角色管理页已驱动）。
- 数据字典「计费方式」卡片 3 行（≤5 行规则）、看板/盈亏统计卡片区（混合页规则）、盈亏按月/按客户页签（聚合演示）、用户权限「状态」与盈亏「统计口径」两个演示占位下拉未接线。

### 6. 推送不通（默认决策表第 10 条口径）——提交留本地，注记「待推送」
- **现象**：候选端口 7890/10808/10809/14157 中仅 7890 开放，但 socks5/http/socks5h × schannel/openssl 四种组合均 TLS 握手失败（上游断）；直连 github.com:443 超时；扩展扫描 30 端口无其他开放。
- **处置**：4 个提交留本地待推送——29e4496（开工前 F01 存档）/7283531（G13 任务文档·另一会话·其留言托 G12 收尾 push 带走）/d9aa5b1（G12 主体）/573e400（G12 回写）。代理恢复后 `git -c http.proxy=socks5://127.0.0.1:7890 push origin main` 一键带走。

## 结论行

**失败 0 项 / 偏差 6 项（全部留档处置完毕，无阻塞）/ 七道验证门全 PASS（audit 死链0·JS0·diff新增0 ｜ grep 9=9 ｜ PW 4/4 ｜ 白名单交集∅ ｜ verify 三批 100/88/43 失败0 ｜ 本清单存在 ｜ git log ≥1 提交=d9aa5b1+573e400｜推送=待推送[§6 注记]）——不影响完成判定（推送按默认决策表第 10 条口径豁免）。**
