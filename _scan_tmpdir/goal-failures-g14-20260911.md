# G14 失败清单（2026-09-11）

> 任务：20260911-G14-移动端H5与登录 ｜ 失败处理策略：跳过+留档+继续（禁卡等/禁猜测改写/禁提问）

## 失败项：0 项

全部任务组（T1 骨架/T2 五页/T3 PC 隔离/T4 文档）与六道验证门均通过，无跳过项。

## 偏差与设计注记（6 条，非失败）

1. **users 实体无「王琳」**：任务书 T2.5 措辞「王琳·商务主管·users/roles 实体取」——users 实体 12 行无王琳（王琳=todoItems 审核人·users 对应商务主管为「王强」）；按演示语义（移动端登录人=审核人王琳）取 roles 实体「商务主管」为角色名，未改 demo-data（T3 只读承诺）。账号映射：wanglin/wangqiang/chenjin/lijing 有对应名，未知账号默认王琳进入（登录页注明演示口径）。
2. **审计受护页 nav_anomaly=True×4**（待办审批/审批详情/库存查询/我的）：audit 每页独立浏览器上下文无 m-auth，守卫按设计跳 登录.html——harness 实际清扫的是登录页（0 问题），5 页各自死链 0/JS 0 达标；守卫行为本身由 PW 门①独立验证。登录页 nav_anomaly=False（未登录态本无跳转）。
3. **登录页 m-auth 字面量首轮 grep MISS**：初版经 _m.js mLogin() 封装写标记，登录页本体无「m-auth」字面量；已改为登录页内联 `localStorage.setItem('m-auth', …)`（语义更贴任务书「登录成功→写 m-auth 标记」），复检命中。_m.js 的 mLogin() 保留为共享 API（无调用方，说明性保留）。
4. **F01 外链断网噪音本次未出现**：基线与 post 两次全量审计 JS 错均为 0，G13 预注的 ERR_CONNECTION_CLOSED 豁免口径未用到（网络环境正常），豁免逻辑保留在 g14_audit_diff.py 备断网复跑。
5. **stockFlows 16 键中 3 个 ZH-\* 组合件行无 fields.status**：mobile 库存卡片只渲染 13 有效行（与任务书「stockFlows（13 行）」口径一致），组合件行不计入（PC 端按 cells 渲染不受影响）。
6. **审批详情明细为静态演示行**（任务书默认决策表 4 允许）：首行尝试从 summary 正则解析品目/数量，另 2 行为固定演示行，页面注明「演示数据」；通过/驳回结果存 localStorage `m-done` 并在待办卡片回显（刷新后仍保留，退出登录时清除）。

## 结论

**G14 六门全过（audit 113 页 PASS · grep 13/13 · PW 5/5 · 回归 verify 103/88/43 失败 0+菜单 14/14 · 失败清单本文件 0 失败 · git 提交见任务文档头部哈希）——0 失败 · 6 偏差注记，处置合理。**
