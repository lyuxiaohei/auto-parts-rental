# G11 标注污染分类定案（2026-09-10）

> 扫描源=`g11-meta-scan.json`（38 业务页 22 条命中）+补扫（`g11_supp_scan.py`·日期缺位元词）+弹窗登记 `g11-modal-scan.md`（3 条·仅登记不改）。
> 规则=A 直删/B 迁 A03 后删/C 白名单保留；疑似一律按 B（下文注「默认决策命中」）。

## A 类：直删（纯元注释·无业务信息）——5 条

| # | 页面 | 内容 | 理由 |
|---|---|---|---|
| A1 | 仓储作业/库存查询.html | `<!-- 客户在租明细（原在租台账并入·2026-09-10 台账合并）+ 资产轨迹（assetTracks 数据渲染） -->` | 开发溯源注释；溯源信息由 B 类 pin 承载（FP4-02 便签含「原在租台账 2026-09-10 并入」），assetTracks 渲染细节属开发注释不入需求标注层 |
| A2 | 仓储作业/库存查询.html | `/* 客户在租下钻·资产轨迹（原在租台账并入·2026-09-10）：按 assetTracks 键渲染四段式详情 */` | 同上（JS 开发注释） |
| A3 | 系统管理/角色管理.html | `/* G01 角色管理数据驱动（2026-09-09）：roles 实体渲染 + 权限配置矩阵（矩阵口径=G01 文档 C 默认决策表） */` | 任务号+实现溯源，无业务口径 |
| A4 | 租赁管理/组合出库录单.html | `/* ===== 添加明细：克隆明细表末行示例数据并重排序号（详情落地页补齐 2026-09-04） ===== */` | 开发注释+历史日期 |
| A5 | 采购管理/采购入库录单.html | 同 A4 串 | 同上 |

## B 类：迁标注层后删（含业务口径）——8 条（并注：B1+B2 合并为单 pin，同锚一次承载双段全文）

| # | 页面 | 原文要点 | 落点 |
|---|---|---|---|
| B1 | 库存查询 | pn-hint 客户虚拟仓＝…（on-hire）；客户转租为其子状态（2026-09-08 会议 T1 方向） | A03 库存查询 #3（锚=st-label 客户端（租出在外）·fp FP4-02·req REQ-03） |
| B2 | 库存查询 | pn-hint 客户在租明细（原在租台账·2026-09-10 并入）＝行内「客户在租」下钻；退回进度在租赁单列表跟踪 | 并入 A03 库存查询 #3 同一便签（note 第二段） |
| B3 | 基础数据/产品档案.html | pn-hint 税率口径（默认 13%/6%/9%·可覆盖）+产品档案=器具+零部件合并（2026-09-08 会议 N4） | A03 产品档案新键 #1（锚=card-title 供应商税率维护） |
| B4 | 租赁管理/租入单列表.html | div 计费＝月租金+按套数单价（无日租金，2026-09-08 会议）；应付生成=静态租入/背靠背 | A04 追加 #3（锚=+ 添加明细行 button） |
| B5 | 租赁管理/退租入库列表.html | pn-hint 退租无申请单·按拆后零件/单一产品·与财务结算解耦——租金只要发出去就要收（2026-09-08 会议拍板） | A04 追加 #4（锚=card-title 退租入库单） |
| B6 | 财务协同/应付账单.html | pn-hint 分期互算·末期补差·超账单金额拦截（2026-09-08 会议 M2/C-C） | A04 追加 #4（锚=th 期次） |
| B7 | 租赁管理/租赁单列表.html | div 押金为设计预留字段·两次会议均未涉及·待客户确认（F01 注记同步） | A04 追加 #6（锚=form-label 押金(元)） |
| B8 | 财务协同/盈亏报表.html | div 对齐说明（演示链数据）：PRJ-2601/PRJ-2604 勾稽关系 | A03 盈亏报表 #2（锚=div.pager·fp FP6-05·req REQ-02） |

> **默认决策命中（B4-B7 落 A04 而非 A03）**：G11 文档 B4 指定迁入 A03；但 annotate.py 为单数据文件整注替换（每页仅一套注入，A03∩A04=6 页现状=仅 A04 pins，A04 后跑覆盖）。租入单列表/退租入库列表/应付账单/租赁单列表为 A04 独占页——若 pin 落 A03 将被 A04 全量重注抹掉。故此 4 条按同一「迁入标注层」目的落 A04（id 续尾），A03 不加死键。B1-B3/B8 所在页为 A03 独占/新建键，按文面落 A03。

## C 类：白名单保留（不动）

- **clean pn-hint 10 条**：盘点录入×2（自动生成不可编辑/已盘进度·差异说明必填）、BOM维护（版本号只增不改）、组合出库录单×2（自动生成/订单带出）、库存调拨列表（第一期仅同仓群调拨）、应付账单 L585（自动生成+路凯周期）、应收账单 L625（可选类型三源）、采购入库录单（自动生成不可编辑）
- **业务状态词**：待确认 tag（我的待办/付款登记等 tag-orange 与筛选 option）——状态非元引用
- **演示数据 option**：项目档案「其他（演示）」、付款登记「待定选项（演示数据）」——demo 占位选项属数据非元注释
- **弹窗 3 条**：器具出租履历（G07 挂靠注释）/租入单新建（计费口径·与 B4 同文）/应收账单生成（类型三源）——仅登记不改（本任务边界）

## 附带修复（B4 告警归零所需·既有债）

1. **A03 清退 15 个已删页键（29 条）**：小组装列表/小组装录单/退租验收列表/退租验收录单/包材档案/子母件BOM/往来单位/零部件档案/客户端×2/在租资产跟踪/缺损赔偿/订单协同×3——meeting0908 四模块移除·档案合并·G07 台账合并遗留，annotate 报「页面不存在」
2. **A03 修复 12 条 selector 失配**（G07 改版后未重注）：库存查询#1 双视角→四态、#2 th 组装占用→st-foot 其中组装占用、BOM维护#1 子件→子项、组合出库列表#1→租赁出库单、#2 th 关联客户订单→关联销售订单（title 同步）、回款登记#1→收款登记、#2 th 银行水单→银行回单、应收账单#2 补 onclick 消歧、盈亏报表#1 项目盈亏报表→项目损益、银行水单核销#1 ① 银行水单→① 银行回单、项目看板#3 按钮改「损益报表」现行文本；**项目档案#2 退役**（编码规则按钮随弹窗提取消失·ruleModal 不在页面）
3. A04 34 条全绿零改；29 个弹窗模板的部分注入残块（style+data-note 无 pins/fab/script）属预存状态，本任务不触碰（弹窗边界）

## 处置统计

A=5（删）｜B=8（迁：A03 4 条目含合并 pin+A04 4 追加）｜C=白名单若干｜页面计数/菜单/文件零变更
