# -*- coding: utf-8 -*-
"""G40 登记：并行写者事件（附录到 goal-failures-g40.md）"""
import io, os

BASE = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
P = os.path.join(BASE, 'agent-handoff', 'goal-failures-g40.md')

ADD = '''
## 五、⚠️ 并行写者事件（重大登记 · 非本任务失败）

**事实**：本任务执行期间，**另一会话（G39 收尾）在同一仓库并行提交**——开工时（12:01）的前置校验未检出，属**事后发现**。

| 时点 | 事件 | 证据 |
|---|---|---|
| 12:02 | 本任务存档提交 | `90cc79a`（本 agent·只提交精确路径） |
| 12:04 | 本任务 T1 术语替换（129 文件 mtime） | 本任务会话 |
| 12:05 | 本任务 T2 `git mv` 5 处 | 本任务会话 |
| 12:06 | 对方产出 `_scan_tmpdir/g39b_after.png` | 非本 agent 产物（本任务未生成） |
| **12:08** | **对方提交 `2762601`**「G39 修复：转移出库新建页三处下拉组件叠框」 | `git log` 显示该提交非本 agent 所为（本 agent 未提交过任何原型修复） |
| **12:10** | **对方提交 `dc9bce0`**「G39 重打包：20260916.zip 覆盖 11:50 过期包（**该包早于并行会话 12:04 文件改名**…）」——**该提交把我的 5 处 `git mv` 重命名一并纳入**（`{回款登记.html => 收款登记.html}` 等 5 行·0/0），并重打 zip | `git show --numstat dc9bce0` |

**影响评估（已核）**：

1. **对方已显式识别本会话**——`dc9bce0` 提交信息原文写「该包早于并行会话 12:04 文件改名」并据此重打包，**对本任务是配合性动作**，非冲突。
2. **重命名已被对方提交**（HEAD 内已是新文件名）；**本任务的 T1／T2 内容改动仍全部未提交**（`git diff HEAD -- P3-R01-包装租赁管理后台原型/ = 128 files changed`，逐条 `git diff` 核对为 T1/T3/T4/T5 的真实内容变更，非行尾噪音）→ 本任务提交仍能完整落地。
3. **本任务未发现被对方覆盖的改动**：对方两次提交只涉及「租赁管理/转移出库新建.html」与 zip 两个对象（前者当前工作区**干净**、后者**未修改**），与本任务改动面（术语/改名/F01/A 类文档）**零重叠**。
4. **本任务的验证结论仍然有效**：审计／PW／截图均在**对方最后一次提交（12:10）之后**、以当前工作区实际内容跑出（见 `_scan_tmpdir/audit-g40.log`、`g40_pw.json`、`g40_f01_*.png`），反映的是**对方提交＋本任务改动**的合并态。

**处置**：
- 本任务提交**只提交精确路径**（本任务改动面＋本任务证据件），**排除**对方产物（`_scan_tmpdir/backup-g39b-select/`、`g39b_repack.py`、`g39b_after.png`、zip）。
- **不 push**（沿任务约定）。
- **重申纪律**：后续任务开工前应把 mtime 判读**同时**用于 `.git` 目录与 `git log --since`（本次仅查了原型目录 mtime，未查 git log，故未能在开工时发现对方写者）。此条已回写基线「口径纪律铁律」。

**结论**：**不构成任务失败**（对方提交与本任务零内容重叠、且为配合动作），但**前置校验漏检一次**，按登记项记录并回写纪律。
'''

s = io.open(P, encoding='utf-8', newline='').read()
assert '## 五、' not in s, '已写入（幂等保护）'
marker = '## 四、失败清单结论行'
assert marker in s
s = s.replace(marker, ADD.strip('\n') + '\n\n' + marker)
s = s.replace('**无失败项**（登记项 9 条，均为白名单／范围排除／假阳性／口径归一／超范围转派，非执行失败）。',
              '**无失败项**（登记项 9 条＋并行写者事件 1 条，均为白名单／范围排除／假阳性／口径归一／超范围转派／并行写者登记，非执行失败）。')
io.open(P, 'w', encoding='utf-8', newline='').write(s)
print('OK 并行写者事件已登记')
