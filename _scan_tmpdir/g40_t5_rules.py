# -*- coding: utf-8 -*-
"""G40 T5 F01 开发规则同步（现停 2026-09-10）
- 头部版本行 v3.5/2026-09-10 → v3.7/2026-09-16
- §1 viewBox 更正为现状（G15 收窄后 880；原文仍写 1280）
- §4 补「转移出库」节点规格（170 宽档·分支连接线·箭头标签·跨线注记四点）
- §11 验证项 3→4（补 viewBox/渲染几何核对）
- 新增 §12 计费与状态口径（v3.7 补）+ 命名规则（术语三条已落地）；原 §12 顺延为 §13
"""
import io, os, sys

BASE = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
P = os.path.join(BASE, 'P3-R01-包装租赁管理后台原型', 'P3-R01-F01-业务流程导航图-开发规则.md')

R = [
    # 头部版本行
    ('> 适用文件：`P3-R01-包装租赁管理后台原型/P3-R01-F01-业务流程导航图.html`（v3.5 起生效，2026-09-10 定稿）',
     '> 适用文件：`P3-R01-包装租赁管理后台原型/P3-R01-F01-业务流程导航图.html`（**v3.7** 起生效，**2026-09-16 修订**；沿革：v3.5 定稿 2026-09-10）', 1),
    # §1 viewBox 更正（G15 宽度收窄 1280→880 后文档未同步）
    ('2. 主线 SVG（`viewBox 0 0 1280 ××××`）：B1 / L1 / L2 / L3 / L4 / T1 / T2 / F1·F2',
     '2. 主线 SVG（`viewBox 0 0 880 2184`·G15 收窄后）：B1 / L1 / L2 / L3 / L4 / T1 / T2 / F1·F2', 1),
    ('3. 支线 SVG（`viewBox 0 0 1280 724`）：S1–S6',
     '3. 支线 SVG（`viewBox 0 0 880 846`·G15 收窄后）：S1–S6', 1),
    # §4 补转移出库节点规格
    ('- 「第一期实现」徽章：四条租赁线统一，64×16，accent 色，紧跟标题右侧（拍板日期不写进徽章，写进决策框）。',
     '''- 「第一期实现」徽章：四条租赁线统一，64×16，accent 色，紧跟标题右侧（拍板日期不写进徽章，写进决策框）。
- **转移出库（v3.7 新增 · 全图唯一结构改动 · D-146）**：租赁线「租赁出库」之后的分支节点，**170×56**（长副标档），挂在**业务线行右侧空白区**；**只在 L1 与 L3 各放一个**，L2/L4 不单独画——由 L1 的跨线决策备注框统一覆盖（框标题写「L1–L4 通用」）。
  - **副标固定文案**：`客户间转移 · 直接客户 → 终端客户`（不可改写）。
  - **连接线**：从「租赁出库」**顶边**上引 → 横走 → 下入新节点顶边（正交折线·实线·`marker-end="url(#arr)"`），线中点挂纸色遮罩箭头标签 **「分批 · 非必经」**——语义＝**非必经分支**（与蓝色虚线「库存缓冲衔接」严格区分，不得改用虚线）。
  - **决策注记（四点·固定文案）**：①不勾稽原租赁单（D-106）· 只记转出方 → 接收方；②按租出结算＝默认 · 转移单不进财务链路；③按终端结算＝特例 · 账单主体切终端（历史不回改）；④终止转移＝回「在客户（租出）」· 待转移 → 已转移 → 已终止。注记框沿用规则 §5「决策备注只用虚线框·位置＝补给线右侧空白区」。''', 1),
    # §11 验证 3→4 件
    ('3. 验证三件事：', '3. 验证四件事：', 1),
    ('''   - 📎 弹窗开合正常、Mermaid 折叠区开合正常。''',
     '''   - 📎 弹窗开合正常、Mermaid 折叠区开合正常；
   - **viewBox 与渲染几何**：新增节点/连接线后，主图与支线两张 SVG 的 `viewBox` **分别**核对（副标宽度用 `getComputedTextLength` 对照节点框宽），确认既有节点无裁切、无位移丢失。''', 1),
    # 原 §12 顺延为 §13
    ('## 12. 当前版本遗留事项', '## 13. 当前版本遗留事项', 1),
    # 原 §12 条目——第一项已由 v3.6 修正
    ('- S5 行有两个完全相同的「租赁出库录单」节点（均指向 `租赁出库录单.html`），疑似其一应为其他录单页——待业务侧确认归属后修正。',
     '- ~~S5 行有两个完全相同的「租赁出库录单」节点~~ **v3.6 已修正**（改为「退租入库新建」＋补「租入归还新建」chip）。', 1),
]

NEW_S12 = '''## 12. 计费与状态口径（v3.7 补 · D-145／D-146／D-148）

> 本节是「图上文案＝口径」的施工依据——凡涉及计费、库存状态、客户虚拟仓的节点副标与注记，一律按本节写。

### 12.1 计租与账单（D-145 定案 · D-148 落地）

- **计租＝按持有量计租**：`Σ(每客户 × 每物料 每日在租数量 × 日租金)`，**不勾稽原单**（D-106 口径原封不动）；单价相同合并总量，不做批次核销。
- **账单＝期段账单**：明细列固定 `起租日｜止租日｜天数｜日单价｜小计`；天数 = `max(2, 止租日 − 起租日 + 1)`（首尾双端计入）。
- **单据侧不换算**：按时间周期**直录日租金**／按次录次单价；三段式仅在物料档案录参考价时体现。
- **图上写法**：租赁线注记统一写 `按持有量计租 · 期段账单`；F1 首现处写全 `期段账单＝起租日｜止租日｜天数｜日单价｜小计`。**禁用**「按月生成」「按租赁出库自动汇总」「按实际使用量」等旧口径写法。
- **历史不回改**：`SRC_DATA` 中的历史拍板记录（如 2026-09-04 陆鸣拍板「租金按月生成」）**保留原文**，不改写——沿革可回溯优先于口径统一。

### 12.2 库存状态与客户转租（D-146）

- **五态**：`在库 / 在客户（租出）/ 客户转租出 / 退租在途 / 租入在库`。
- **状态驱动方（替代原「转租登记弹窗 / 拉表改状态」）**：行内「转移出库」跳转录单页；**「客户转租出」由转移出库单审核驱动**；单据「终止转移」后回「在客户（租出）」。
- **转移出库结算两模式**（D-132）：项目档案「转租结算方式」定默认（按租出／按终端），转移单可按单覆盖；按租出＝默认（转移单不进财务链路）／按终端＝特例（账单主体切终端·历史不回改）。
- 图上若出现「客户端（租出）」，一律以本节五态名 **「在客户（租出）」** 为准。

### 12.3 客户虚拟仓（D-145）

- 客户在租以**库存状态「在客户（租出）」维度**表达；**客户虚拟仓不维护**（D-006 关闭）。图上不再出现「按客户归集（on-hire）」类旧写法。

### 12.4 命名规则（术语三条 · D-142 补做已落地）

- **银行回单**（原「水单」）｜**损益**（原「盈亏」）｜**收款**（原「回款」）——新增文案一律用现行词，**禁用旧词**。
- 页面文件名同步：`收款登记`／`收款详情`／`损益报表`／`银行回单核销`／`银行回单核销详情`（旧名已 `git mv`，不得回写）。
- 字段口径随 A05 概念表：物料列 `物料/物料名称/物料编码`（「货品」退场）· 库房层级「库区」退场（→库房）。

'''

OLD_S12_HEAD = '## 13. 当前版本遗留事项'


def main():
    s = io.open(P, encoding='utf-8', newline='').read()
    for old, new, want in R:
        c = s.count(old)
        assert c == want, '锚点计数 %d != %d : %r' % (c, want, old[:70])
        s = s.replace(old, new)
        print('OK x%d  %s' % (want, old[:60]))
    assert s.count(OLD_S12_HEAD) == 1
    s = s.replace(OLD_S12_HEAD, NEW_S12 + OLD_S12_HEAD)
    print('OK 插入 §12 计费与状态口径（原 §12 → §13）')
    io.open(P, 'w', encoding='utf-8', newline='').write(s)

    print('--- 复核 ---')
    s2 = io.open(P, encoding='utf-8', newline='').read()
    for k in ['880 2184', '880 846', '转移出库（v3.7 新增', '分批 · 非必经', '## 12. 计费与状态口径',
              '## 13. 当前版本遗留事项', '验证四件事', 'v3.7', '2026-09-16', '按持有量计租', '期段账单',
              '客户虚拟仓不维护', '银行回单', '在客户（租出）']:
        print('   %-24s = %d' % (k, s2.count(k)))
    print('   旧值残留 1280 =', s2.count('1280'), ' 2026-09-10 =', s2.count('2026-09-10'), ' v3.5 起生效 =', s2.count('v3.5 起生效'))
    import re
    print('   章节编号:', re.findall(r'^## (\d+)\.', s2, re.M))
    return 0


if __name__ == '__main__':
    sys.exit(main())
