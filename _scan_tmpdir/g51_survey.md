# G51 T0 取证（落后清单·2026-09-18 执行）

## 前置校验

- `git status --porcelain | wc -l` = 111（>0 → 需 T-1）
- 并行会话检测：`git log -3` 最新 82016f8（G43 回填·非近 30 分钟）；`.git/index` mtime=09-17 16:20 无变动；工作区最后写入 09-18 11:46（距今 31 分钟）→ 判静默，无并行会话活动
- `_索引.md` 尾部 G51 行 = ⏳（未执行）
- 路径 exists 实测：任务书引用 16 路径 15 OK；`_AGENT基线.md` 实际位于 `agent-handoff/`（任务书未给绝对路径·确认存在）
- node --check demo-data.js / mat-search.js / select-source.js 退出码均 0

## T-1 前置批次提交

- 提交哈希 `e27ed9e`，89 files changed（+58461/−2691）
- 归属核对法：mtime 分窗（0917 批次=09-17 10:24~18:21）＋逐文件 diff 内容抽查
- 清单外但内容相符并入（0917 施工主题=库房区名化/库位刷齐/值域收口/筛选折叠）：库存查询·付款登记·收款登记·租入入库列表·采购入库列表·开票登记·应收账单·用户权限·项目档案·转移出库列表·我的待办 共 11 页＋P2-R01/A02/基线 三文档的 0917 最小同步
- 库位详情.html DEL（133→132 口径来源）随批提交；A02 已被 0917 会话刷至 v6.7
- 提交后残留 22 项=全部登记组（见 g51_failures）

## 附录 A 逐条取证（落后证据）

| # | 命令 | 结果 | 判定 |
|---|---|---|---|
| A.1a | grep -c mat-search A02 | 0 | 落后 ✓ |
| A.1b | grep -o "_data[^。]*七件" A02 | 命中「_data/` 七件」 | 落后 ✓ |
| A.2 | grep -c mat-search A05 | 0 | 落后 ✓ |
| A.3 | grep -n "父项\|BOM物料" P1-R04 | 2 处（行 50·72 均旧词） | 落后 ✓ |
| A.4a | grep -c mat-search P1-R08 | 0 | 落后 ✓ |
| A.4b | grep -o "_data 七件" 基线 | 命中 | 落后 ✓ |
| A.5a | grep -c "2026-09-17" 拍板登记 | 19 | 预查一致 ✓ |
| A.5b | grep -o "\| D-15[0-9] " P2-R01 \| tail | D-151/D-152/D-153 | 预查一致（台账至 D-153）✓ |
| A.7 | find P3-R01 -name "*.html" \| wc -l | 132 | 预期一致 ✓ |
| — | P2-R01 版本行 | V1.7（2026-09-16） | 待刷 V1.8 |

## A.6 A03 失配复核（T7 前置取证）

- token 清单（维护/查看/父项/正品仓/返回列表/value="86"/暂 存）遍历 88 pin 的 selector 字段：**命中 0**（与任务书预查一致）
- **新发现（不修·登记）**：A03 存在 5 个键指向已删除的弹窗页面文件（G36 弹窗页面化退场后键未清退）：项目管理/弹窗/上下游绑定.html、系统管理/弹窗/权限配置.html、租赁管理/弹窗/租赁单新建.html、租入管理/弹窗/租入单新建.html、租入管理/弹窗/租入归还新建.html——标注层运行时这些键不注入（页面不存在），属陈旧键残留，留待下一原型任务处理

## T8 查证性复核（结论）

- **A06**：父项⇄子项为概念行文 + 1 处「父项编码=ZH-2601-A」字段示例行文 → 按任务书 T8 拍板**保留不修**（grep 门按 T8 拍板豁免·命中数单列：html ×1 + md ×1）
- **P3-R04**：`grep -c 库位详情` = 0（已删页零引用）✓
- **P1-R01 附录 10.2**：P3-R05 挂行已在（0917 批次·T-1 已提交）→ D 号行不新增 ✓
- **菜单 v6**：0917 拍板 19 条无菜单条目；A02 v6.7 范围行「菜单内 37」与 132 口径一致 → 未动 ✓

## T9.3 术语 grep 门（范围=原型目录+根级 md+agent-handoff·排除 .prompts/99-归档/backup-*）

| 组 | 全量 | 白名单内 | 白名单外 | 判定 |
|---|---|---|---|---|
| 组1 父项编码\|父项名称 | 20 | 18 | **0** | PASS |
| 组2 正品仓\|次品仓 | 32 | 31 | **0** | PASS |
| 组3 (入库\|出库\|调出\|调入)库房 | 56 | 56 | **0** | PASS |

- 白名单口径扩展（相对任务书 T9.3 原表·逐项依据）：①agent-handoff 历史 G 号任务档（G17/G31/G34/G38 等·留档性质同拍板登记历史行）；②G51 任务书自身 token/白名单定义性引用；③P1-R01 附录 10.2＝P3-R05 排查记录转述（同源）；④A06 概念行文（任务书 T8 拍板「保留」）。
- **新发现登记（不修）**：
  1. `P2-R02-决策落实与场景闭环检查报告.md` ×2（组1·0917 改称未刷·活文档落后·默认决策表 #9 登记）
  2. `P3-R01-A03-标注数据.json` ×1（组2·BOM维护 pin3 note「实际损耗走库位（次品仓）区分」漏刷·**标注层零写入红线·登记失败清单留待下一原型任务**）

## T9 其余项

- T9.1 node --check demo-data.js 退出码 0 ✓
- T9.4 文档断言：P2-R01 D-154/D-155/D-156 各 1 行＋V1.8 ✓；A02 mat-search ×4 ✓；P1-R04 BOM物料编码 ×4 ✓；P1-R08 mat-search ×1 ✓；基线「九件」×2＋「132」×17 ✓（注：任务书写「八件」·实算 _data=9 文件·按默认决策表 #3 实算口径改验「九件」·见下「任务书口径出入」）
- T9.5 台账 D 号：153→156 连续无重 ✓；全表 157 行 max=156，唯一重复 D-132（G41 在案历史撞号注记·非本次引入）；无缺号

## 任务书口径出入（按默认决策表 #3/#9 处置·收尾报告注明）

1. **_data 件数**：任务书 T3/T6/T9.4 写「八件」；实测 `_data/` 目录 9 文件（G43 前 7→G43 +select-source.js=8〔A02 彼时写「七件」系漏更〕→0917 +mat-search.js=9）。按「数字一律实算」原则统一写**九件**并在 A02 v6.8/基线注记勘误沿革。T9.4 断言「基线含八件」相应按实算改验「九件」。
2. **库位刷齐族数**：任务书 T2 写「9 族」；实测新词覆盖 8 族 15 页（旧词页面域 0）。A05/D-154 台账按实算写 8 族 15 页。
3. **mat-search 处数**：任务书 T6 写「37+ 处」；决策表 #1 与拍板登记逐条加总=43 处（10+4+1+12+15+1）。统一写 43 处（含样板页）。


## 新发现（只登记不扩项修）

1. A03 陈旧键 5 个（上文）
2. 根目录 g51_shot_detail/mid/top.png 三张（09-18 11:22·非本会话产物）位置不当，未动
3. P1-R03-工期评估表-20260915.xlsx 与不合理清单双版有 09-17 未提交改动，归属不明，未动（登记 g51_failures）
