# G17 任务书：命名统一执行（11 项口径合并）

> **状态**：待执行（道远拍板下一轮投喂）｜ **日期**：2026-09-11 ｜ **commit**：（收尾回填）
> **执行环境**：Zcode agent 整段执行（B 形态）
> **上游**：G16（字段字典/术语表 V0.5 含待拍板区）｜ **前置**：G16 必须 ✅（查 `_索引.md`）

## 【第 0 步·上下文获取】

1. 读 `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\_AGENT基线.md`（铁律第四节）
2. 读 `P1-R04-术语表.md` **第五节待拍板区**（G16 产出的 11 项表——本任务的执行清单来源）
3. 读 `P3-R01-包装租赁管理后台原型/P3-R01-A05-字段字典.md`（G16 产出——替换时核对字段出处）
4. 项目根：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\`

## 【任务总览】

按第五节待拍板区执行 8 项合并 + 3 项审定保留；全部为**用户可见文案层**替换（列头/筛选标签/弹窗表单标签/文案），**不动**：文件名、URL/go() 路径、demo-data 实体键名、cfg 的 field 键值（仅改 label 值）、mobile 页（本轮默认不动，见 D9）。

**⚠️ 最高纪律：URL 防污染——G15 后事故教训**：2026-09-11 物料改名曾把 41 处 `go('../基础数据/产品档案.html')` URL 内文件名一并替换致全站菜单 404。本轮所有替换脚本必须**排除 URL 语境**：凡替换词出现在 `go('...')`/`href="..."`/`location.href`/`openDetail('...')` 参数内一律不动。替换后必跑全站死链校验（T3.4）。

## 【T0 前置校验与清场】

1. `_索引.md` 中 G16 行状态 = ✅，否则停止写失败清单
2. 并发检查（`_data` mtime ≥30 分钟）
3. 存量若 >0 文件：`git add -A && git commit -m "G17前置：G16后存量存档"`，不 push
4. 备份到 `_scan_tmpdir/backup-g17-20260911/`（按原相对路径）：所有将被改动的页面（T1 执行前先跑 dry-run 统计输出涉及文件清单再备份）+ `P1-R04-术语表.md` + `_索引.md` + `_AGENT基线.md` + `_scan_tmpdir/verify_listfull.py`（断言含列头文本需同步）

## 【T1 八项合并执行】

统一方法：Python 脚本 `_scan_tmpdir/g17_t1_apply.py`——逐页读取、**语境过滤**（URL 语境跳过）、替换、assert 每项替换计数与 dry-run 一致、写回。每项替换后 grep 残留=0（白名单除外）。

| # | 替换（用户可见层） | 范围 | 白名单/保留 |
|---|---|---|---|
| 1 | 列头/筛选用法统一→**物料类型**：库存查询列头「物料类别」→「物料类型」；BOM维护列头「类型」→「物料类型」；采购订单列表 createModal 表单标签「类别」→「物料类型」 | 3 页 | 采购订单筛选/列头已是「物料类型」不动；库存查询列**值**「租赁器具」保留（视角语义） |
| 2 | 物料档案「分类」→**物料分类**：筛选标签「分类：」、列头 `<th>分类</th>`、createModal 表单标签「分类」、详情弹窗「分类」字段名、税率维护区表头 | 基础数据/产品档案.html + 弹窗/产品详情.html + 弹窗/新建产品.html | F01 图内「物料分类」若有已是对的；A03 标注 JSON 同步该 3 页相关 pin 文本（grep A03 中「分类」出现在产品档案页锚点） |
| 3 | 指代性「器具」→**物料**：租入单列表/租入入库列表/租入归还列表 列头+筛选「器具」「器具：」→「物料」；退租入库列表「退租器具名称」→「退租物料名称」；采购入库录单「零件号 / 器具编码」→「零件号 / 物料编码」；BOM维护 列值「零件」→「零部件」（th/td 都换）；产品档案停用弹窗「确认停用该器具」→「该物料」 | 7 页 | **保留**：库存查询 tab「租赁器具」、盈亏报表「器具摊销(元)」、库位档案「器具区 JC」（库区名）、F01 全部图内文字、mobile 页 |
| 4 | 客商列头短名：`<th>客户名称</th>`→`<th>客户</th>`；`供应商名称`→`供应商`；`客商名称`→`客商`——**仅 th 列头与 renderListPage cfg label**；表单标签（如「客户名称：」筛选/弹窗）不动 | 涉及页按 A05 字段字典「客户名称/供应商名称/客商名称」出处反查（普查值 9/5/3 处） | 筛选标签/弹窗表单/详情 dlabel 不动 |
| 5 | `<th>单号</th>`→`<th>单据编号</th>`、`<th>单据号</th>`→`<th>单据编号</th>`——仅裸「单号」「单据号」列头 | 其他入库/其他出库列表、我的待办（4+1 处） | 「入库单号」「出库单号」「租赁单号」等带前缀**保留** |
| 6 | `<th>当前状态</th>`→`<th>状态</th>`（BOM.html/产品档案/库位档案 共 6 处，含详情弹窗 dlabel「当前状态」） | 3 页 | 页签 stab 文本不动（若与列头同词需逐处判断：stab=「当前状态」页签改不改→改，与列头同口径） |
| 7 | `<th>说明</th>`→`<th>备注</th>`（用户权限/角色管理 4 处） | 2 页 | 权限配置弹窗内组说明文案不动（非列头） |
| 8 | 「仓库」→**库房**：`入库仓库/出库仓库` 列头与筛选→`入库库房/出库库房`；库位档案页「仓库」单用→「库房」（面包屑/说明句「仓库→库区→库位」三级架构释义句**保留**——它是 WMS 层级术语） | 其他出入库/调拨/盘点列表、库位档案（约 12 处） | 三级架构释义句；A02 页面名；URL/文件名 |

**3 项审定保留（不执行）**：#5 项目三形态（所属项目/项目名称/项目——语义有别）、#9 制单人/经办人/操作人（语境有别）、#11 单位/计量单位（层级不同）。

## 【T2 术语表 V0.5 → V0.6】

1. 版本行更新：V0.6（2026-09-11 G17：第五节 11 项拍板执行——8 合并 3 保留）
2. 第五节待拍板区**整节改写为「已拍板执行记录」**：8 项标 ✅（统一词+替换计数+白名单），3 项标「审定保留」
3. 第八节追加 V0.6 词条（合并产生的规范词若不在表中：物料类型已 V0.5 有；补「单据编号」「库房」如缺）
4. 第九节废弃词按需补：「物料类别/单号/单据号/当前状态/入库仓库/出库仓库/客户名称(列头)」等

## 【T3 验证门（全部机器判定）】

1. **grep 残留断言**（Python 循环输出逐项 PASS）：被替换词在指定语境残留=0——「物料类别」「当前状态」「入库仓库」「出库仓库」「退租器具」「零件号 / 器具编码」全站（排除 backup/_scan_tmpdir/.prompts/agent-handoff/99-归档/mobile/F01）=0；「>单号<」「>单据号<」「>客户名称</th>」「>供应商名称</th>」「>客商名称</th>」=0
2. **保留词白名单断言**：「租赁器具」「器具摊销」「器具区」「仓库→库区→库位」仍各 ≥1（没被误杀）
3. **URL 防污染**：全站死链校验（G15 修复后同款）：提取所有 `go('...')`/`href` 本地 .html 引用逐一 exists——**死链=0**
4. `node --check` demo-data.js 退出码 0（本轮应零改动，跑一遍兜底）
5. Playwright 抽验（`_scan_tmpdir/g17_t3_verify.py` 留档）：库存查询列头「物料类型」渲染+值含「租赁器具」；租入单列表列头「物料」；物料档案弹窗「物料分类」标签在；BOM维护列头「物料类型」+列值「零部件」；JS 错误 0
6. **verify_listfull 三批断言**：跑前先 grep 脚本内被替换旧词（「客户名称」「单号」「入库仓库」等）并同步为新词，然后全跑——断言全过
7. **菜单门**：`python _scan_tmpdir/v32_menu_verify.py` 14 项全过（侧边栏零改动应稳过）
8. **audit 回归**：`audit_interaction.py` 死链 0/JS 0/相对 g14baseline 逐键 diff **新增 0**（列头文案变化会改变问题键？——audit 探测的是死链/JS 错误/坏按钮，列头文本不影响；若 diff 有新增逐条核对属文案变化误报则记白名单注记）
9. 失败清单 `_scan_tmpdir/g17-failures.md` 结论行

## 【T3b 收尾三件回写（硬性）】

同 G16 模式：本文档 ✅+commit 哈希+执行记录（逐项替换计数表+验证门输出+偏差）；`_索引.md` G17 行；`_AGENT基线.md` 快照滚动（术语表 V0.6+命名统一完成+audit 基线注记）；A05 字段字典中被改字段的中文标签列同步更新（「分类」→「物料分类」等 8 处对应行）。`git add -A && git commit`——**不 push**。

## 【默认决策表】

| # | 决策点 | 预定值 |
|---|---|---|
| D1 | A 类数据驱动页 cfg label 在不在替换范围 | 在（label 值改，field 键名不动）；改后该页 PW 抽验渲染 |
| D2 | stab 页签「当前状态」 | 改「状态」（与列头同口径） |
| D3 | 「仓库→库区→库位」释义句出现在非库位档案页 | 同样保留（架构术语全局保留） |
| D4 | A03 标注 JSON 含旧词 | 同步替换 pin 的 note 文本（键/锚点不动），改后 JSON load 校验 |
| D5 | 某项替换 dry-run 计数与普查值不符 | 以 dry-run 实测为准，差异记偏差清单，不阻断 |
| D6 | mobile 页含「器具」等旧词 | 本轮不动（mobile 5 页独立口径，留待道远单独拍板） |
| D7 | F01 图内「器具」字样 | 不动（图内文字=G08 定版口径，改图须专项） |
| D8 | 术语表/A02/A05 等文档内旧词 | 文档描述层同步新词；历史定版记录节（第二/三节）不回改 |
| D9 | verify_listfull 断言同步后仍失败 | 逐条核对失败归因（G13 后演示行漂移=本任务外），记失败清单不修 |
| D10 | audit diff 出现新增 | 逐条核对：文案变化所致误报→记注记；真新增→修复后重跑 |

## 【失败处理策略】

- 单项替换 assert 不符→该项整体回滚（从备份还原该项涉及页）+记失败清单+继续下一项
- 验证门 3/6/7 任一失败→修复一轮（仅限本任务引入）；再败→记失败清单+继续收尾
- 禁止卡住等待提问；拿不准按默认决策表；表没有的按最简合理默认执行并在收尾报告注明

## 【纪律】

同 G16 全部纪律 + 本任务特有：**URL 语境过滤是第一纪律**（G15 后 41 处 404 事故）；替换脚本先 dry-run 后 apply；每项替换计数与 dry-run 一致才写回。
