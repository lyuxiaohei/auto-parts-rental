# -*- coding: utf-8 -*-
"""G51 T4: P1-R04 V0.8 —— BOM 新词 2 条＋改称沿革注记＋库位字段语义"""
import io

p = 'P1-R04-术语表.md'
t = io.open(p, encoding='utf-8', newline='').read()
CRLF = '\r\n'

def sub1(old, new):
    global t
    n = t.count(old)
    assert n == 1, 'ANCHOR FAIL count=%d: %s' % (n, old[:60])
    t = t.replace(old, new)

# 1. 版本行 V0.7 -> V0.8
sub1('> **版本**：V0.7（2026-09-16 G41·D-150：',
     '> **版本**：V0.8（2026-09-17 G51·D-154：新词 2 条〔BOM物料编码/BOM物料名称〕＋旧称沿革注记〔0917 改称〕＋「库位」词条补单据字段语义；V0.7=2026-09-16 G41·D-150：')

# 2. L50 BOM 词条：字段口径更新为新词（旧称入沿革注记·白名单）
sub1('| **BOM** | 行业标准（2026-09-03 定版） | =物料清单；字段用 父项/子项；定义 A×配比+B×配比=C |',
     '| **BOM** | 行业标准（2026-09-03 定版） | =物料清单；组合件字段 2026-09-17 起定名 BOM物料编码/BOM物料名称（D-154·原称父项编码/父项名称）；父子配比关系语义保留（父项⇄子项·A06 概念行文）；定义 A×配比+B×配比=C |\r\n| **BOM物料编码** | 道远定名（2026-09-17·D-154） | BOM 组合件的物料编码；BOM 维护页为输入框录入；改称沿革=原称父项编码（2026-09-17 改称） |\r\n| **BOM物料名称** | 道远定名（2026-09-17·D-154） | BOM 组合件的物料名称；改称沿革=原称父项名称（2026-09-17 改称） |')

# 3. L72 组合件/父项/子项词条加注记
sub1('| 组合件/父项/子项 | BOM 语义 | 父项由子项按配比组成；组合件=组装后的父项实物（原"母件"改叫"父项"） |',
     '| 组合件/父项/子项 | BOM 语义 | 父项由子项按配比组成；组合件=组装后的父项实物（原"母件"改叫"父项"）；注：父项/子项为配比关系概念，字段层已定名 BOM物料编码/BOM物料名称（0917·D-154·见上条沿革） |')

# 4. L59 库位档案词条补字段语义
sub1('| 库位档案 | WMS 标准 | 仓库→库区→库位三级 |',
     '| 库位档案 | WMS 标准 | 仓库→库区→库位三级；单据中「XX库位」字段值显库区名（如 原料区 RA·D-154），库位编码为独立字段 |')

for trial in range(3):
    try:
        with io.open(p, 'w', encoding='utf-8', newline='') as f:
            f.write(t)
        break
    except OSError:
        if trial == 2: raise
chk = io.open(p, encoding='utf-8', newline='').read()
for kw in ['V0.8（2026-09-17 G51·D-154', '| **BOM物料编码** |', '| **BOM物料名称** |', '单据中「XX库位」字段值显库区名', '原称父项编码/父项名称']:
    assert kw in chk, 'VERIFY FAIL: ' + kw
print('T4 OK: P1-R04 V0.8, 2 new terms, rename-once notes, loc semantics')
