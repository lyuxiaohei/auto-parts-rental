# G13 失败清单（goal-failures-g13-20260910）

> 任务：20260910-G13-会议功能补全（13 项）｜执行会话自跑验证，未引用历史数字

## 失败项：0（13 项全部落地，无跳过）

## 偏差注记（6 条，均按默认决策表处置）

1. **「demo-data stocks」实为 stockFlows 落点**：任务书 T1① 措辞「demo-data stocks 加该状态演示行」——G12 终态无 `stocks` 实体，库存查询主表由 `stockFlows` 实体驱动（静态行为骨架）。处置：转租演示行 3 条 + 既有 10 行补 `fields.status` 均落 `stockFlows`，语义等价（库存状态+筛选+演示行三要素齐）。
2. **⑥⑩ 详情侧以 info 行呈现**：租赁单详情/销售订单详情为四段式通用骨架（detailBody 由 detail-generic 渲染，无表单区），「计费方式」单选与「订单附件」交互区无法以控件形态置入。处置：demo-data 每单 info 行动态呈现（leaseOrders×9 计费方式 / salesOrders×8 订单附件）+ 模板注释锚（grep 门⑥⑩命中来自注释+宿主页真控件）。
3. **verify batch1=103 而非 100**：库存查询新增筛选卡激活该页 3 项可选断言（select 过滤/input 过滤/重置恢复），全部通过（正偏差）。batch2=88、batch3=43 与 G12 口径一致；batch2 租入单/应收账单 stab 期望值随新演示行同步（6/14，G12 修工具先例）。
4. **⑪ 用户权限页矩阵形态**：用户权限页 roleModal 为「角色列表」形态（G12 白名单附注既有形态，非权限矩阵本体）。处置：可审单据矩阵本体落 权限配置模板+角色管理页（真双层），用户权限页 roleModal 内加同源静态矩阵段注记「双层同步」；「审核人（按角色匹配）」注记落 租赁单审核/付款确认/销售订单审核 三个代表弹窗（模板+宿主），其余审核弹窗未逐一改（最简演示形态）。
5. **⑧ 草稿单号前缀**：toast 与演示行用 `RZD-20260910-009`（租入单既有编码前缀 RZD），任务书示例「RI-xxx」为占位写法，按实物编码口径落。
6. **audit F01 外链豁免未触发**：本轮复跑网络正常，F01 页 0 外链错误（G13 v2 预注豁免口径保留备用，diff 脚本已内置剔除逻辑）。

## 结论

**G13 失败 0 项·偏差 6 条留档；13 项功能全部落地，六道验证门全过**（audit 108 页死链 0/JS 0/diff 新增 0｜grep 13/13｜PW 4/4｜verify 103·88·43 失败 0+菜单 14/14｜失败清单本文件｜提交见 git log）。
