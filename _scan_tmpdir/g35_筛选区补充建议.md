# G35 列表筛选区补充建议（勘察清单）

> 生成：2026-09-15 G35 T2（勘察脚本 g35_t1_survey.py ＋ 分级脚本 g35_t2_grade.py）
> 范围：33 个标准列表页（任务书第二节）；判据八条（任务书第三节）；分级口径=任务书第七节默认决策表
> FIX 项已程序化复核：HTML label 与 cfg label 确实不一致（assert 通过 8 处）

## 一、概览

- 勘察页数：33/33（每页有结论行）
- 实施项：**P0 12 项 / P1 12 项**（T3 落地）
- 登记项：P2 10 项（只登记不补）
- 无需补充页：见明细（「无需补充」也含理由）

| # | 页面 | 现有筛选数 | 结论 |
|---|---|---|---|
| 1 | 其他入库列表 | 6 | 无需补充 |
| 2 | 其他出库列表 | 6 | 无需补充 |
| 3 | 库存查询 | 6 | P1[补] 物料类型 |
| 4 | 库存调拨列表 | 6 | 无需补充 |
| 5 | 盘点列表 | 6 | P2 登记：盈亏合计金额区间 |
| 6 | BOM | 5 | P1[补] 更新人 |
| 7 | 产品档案 | 5 | 无需补充 |
| 8 | 客商管理 | 6 | P0[修] cfg label「客商」→「客商名称」 |
| 9 | 库位档案 | 4 | 无需补充 |
| 10 | 租入入库列表 | 6 | P1[补] 制单人；P1[补] 入库库房 |
| 11 | 租入单列表 | 6 | P2 登记：租金/押金/累计租金应付 金额区间 |
| 12 | 租入归还列表 | 6 | P1[补] 制单人 |
| 13 | 租赁出库列表 | 6 | 无需补充 |
| 14 | 租赁单列表 | 5 | P0[修] cfg label「客户」→「客户名称」 |
| 15 | 退租入库列表 | 5 | P0[修] cfg label「客户」→「客户名称」；P1[补] 入库库房 |
| 16 | 操作日志 | 5 | P0[补] 操作结果 |
| 17 | 用户权限 | 4 | P0[接线] cfg filters 补 {label:'状态', field:'status'} |
| 18 | 角色管理 | 2 | 无需补充 |
| 19 | 付款登记 | 5 | P0[修] cfg label「供应商」→「供应商名称」；P2 登记：付款金额区间 |
| 20 | 回款登记 | 5 | P2 登记：收款金额区间 |
| 21 | 应付账单 | 6 | P0[修] cfg label「供应商」→「供应商名称」；P0[补] 所属项目；P1[补] 账期 |
| 22 | 应收账单 | 7 | P2 登记：账单金额区间 |
| 23 | 开票登记 | 6 | P2 登记：开票金额区间 |
| 24 | 盈亏报表 | 3 | 无需补充 |
| 25 | 退款登记 | 6 | P2 登记：退款金额区间 |
| 26 | 采购入库列表 | 6 | P1[补] 制单人；P1[补] 入库库房 |
| 27 | 采购订单列表 | 6 | 无需补充 |
| 28 | 采购退货单列表 | 6 | P2 登记：金额区间 |
| 29 | 销售出库列表 | 6 | P0[修] cfg label「客户」→「客户名称」；P1[补] 出库库房 |
| 30 | 销售订单列表 | 5 | P0[修] cfg label「客户」→「客户名称」；P1[补] 下单人；P2 登记：金额区间 |
| 31 | 销售退货单列表 | 6 | P2 登记：金额区间 |
| 32 | 项目档案 | 7 | P0[修] cfg label「客户」→「客户名称」 |
| 33 | 我的待办 | 3 | P0[补] 所属项目；P1[补] 提交人 |

## 二、逐页明细表

### 其他入库列表（仓储作业\其他入库列表.html）

- **页面类型**：数据驱动 renderListPage（entity=otherInbounds）；状态页签 3 个
- **现有筛选（HTML）**：单据编号(input)；入库类型(select)；物料名称(input)；状态(select·extra)；入库日期(range·extra)；入库库房(select·extra)
- **cfg filters**：单据编号；入库类型；物料名称；状态；入库日期；入库库房
- **表格列（th）**：单据编号 / 入库类型 / 物料名称 / 数量 / 入库库房 / 入库日期 / 状态 / 操作
- **判定**：
  - [结论·-] 无需补充 —— 理由：判据1/2/3/4/6/7 全覆盖：单据编号/入库类型/物料名称/状态/入库日期(range)/入库库房 六项齐备且 cfg 一一接线；无人员/金额列

### 其他出库列表（仓储作业\其他出库列表.html）

- **页面类型**：数据驱动 renderListPage（entity=otherOutbounds）；状态页签 3 个
- **现有筛选（HTML）**：单据编号(input)；出库类型(select)；物料名称(input)；状态(select·extra)；出库日期(range·extra)；出库库房(select·extra)
- **cfg filters**：单据编号；出库类型；物料名称；状态；出库日期；出库库房
- **表格列（th）**：单据编号 / 出库类型 / 物料名称 / 数量 / 出库库房 / 出库日期 / 状态 / 操作
- **判定**：
  - [结论·-] 无需补充 —— 理由：判据1/2/3/4/6/7 全覆盖：单据编号/出库类型/物料名称/状态/出库日期(range)/出库库房 六项齐备且接线；无人员/金额列

### 库存查询（仓储作业\库存查询.html）

- **页面类型**：数据驱动 renderListPage（entity=stockFlows）
- **现有筛选（HTML）**：物料编码(input)；库存状态(select)；仓库(select)；库位(select)；物料名称(input)；项目(select)
- **cfg filters**：物料编码；库存状态；仓库；库位；物料名称；项目
- **表格列（th）**：编码 / 名称 / 物料类型 / 适用项目 / 在库 / 退租在途 / 客户端(租出) / 退租待入库 / 总量 / 成本单价 / 单位 / 仓库 / 操作
- **判定**：
  - [P1·新增] 物料类型（select·值域=dictItems WL 6 值） —— 理由：判据2：表格有「物料类型」列（th 第3列），筛选区无类型筛选；stockFlows 行带 fields.cls

### 库存调拨列表（仓储作业\库存调拨列表.html）

- **页面类型**：数据驱动 renderListPage（entity=transfers）；状态页签 3 个
- **现有筛选（HTML）**：调拨单号(input)；物料名称(input)；调出库房(select)；调入库房(select·extra)；调拨日期(range·extra)；状态(select·extra)
- **cfg filters**：调拨单号；物料名称；调出库房；调入库房；调拨日期；状态
- **表格列（th）**：调拨单号 / 物料名称 / 数量 / 调出库房 / 调入库房 / 调拨日期 / 状态 / 操作
- **判定**：
  - [结论·-] 无需补充 —— 理由：判据1/3/4/6/7 全覆盖：调拨单号/物料名称/调出库房/调入库房/调拨日期(range)/状态 六项齐备；无类型/人员/金额列

### 盘点列表（仓储作业\盘点列表.html）

- **页面类型**：数据驱动 renderListPage（entity=stocktakes）；状态页签 4 个
- **现有筛选（HTML）**：盘点单号(input)；盘点范围(select)；盘点口径(select)；盘点状态(select·extra)；盘点日期(range·extra)；盘点人(select·extra)
- **cfg filters**：盘点单号；盘点范围；盘点口径；盘点状态；盘点日期；盘点人
- **表格列（th）**：盘点单号 / 盘点范围 / 盘点口径 / 盘点项数 / 盈亏合计 / 关联单据 / 状态 / 盘点人 / 盘点日期 / 操作
- **页级备注**：盘点单号/范围/口径/状态/日期/盘点人 六项齐备
- **判定**：
  - [P2·登记] 盈亏合计金额区间 —— 理由：判据8：表格有「盈亏合计」金额列——按任务书 P2 只登记不补

### BOM（基础数据\BOM.html）

- **页面类型**：数据驱动 renderListPage（entity=bomList）
- **现有筛选（HTML）**：父项编码(input)；父项名称(input)；版本(select)；状态(select·extra)；更新时间(range·extra)
- **cfg filters**：父项编码；父项名称；版本；状态；更新时间
- **表格列（th）**：父项编码 / 父项名称（组合件） / 状态 / 子项数 / 组合配比摘要 / 更新人 / 更新时间 / 操作
- **判定**：
  - [P1·新增] 更新人（select·值域=bomList fields.updater 去重） —— 理由：判据5：表格有「更新人」列，筛选区无人员筛选

### 产品档案（基础数据\产品档案.html）

- **页面类型**：数据驱动 renderListPage（entity=products）
- **现有筛选（HTML）**：物料编码(input)；物料名称(input)；物料类型(select)；状态(select·extra)；建档时间(range·extra)
- **cfg filters**：物料编码；物料名称；物料类型；状态；建档时间
- **表格列（th）**：物料编码 / 物料名称 / 物料类型 / 规格 / 单位 / 参考未税采购价(元) / 参考未税销售价(元) / 参考未税租入价 / 参考未税租赁价 / 状态 / 更新时间 / 操作
- **判定**：
  - [结论·-] 无需补充 —— 理由：判据1/2/6 全覆盖：物料编码/物料名称/物料类型(select)/状态/建档时间(range) 五项齐备；四参考价列属档案属性非单据金额（见不建议项）

### 客商管理（基础数据\客商管理.html）

- **页面类型**：数据驱动 renderListPage（entity=partners）；状态页签 3 个
- **现有筛选（HTML）**：客商名称(input)；客商类型(select)；联系人(input)；合作状态(select·extra)；创建时间(range·extra)；企业编码(input·extra)
- **cfg filters**：客商；客商类型；联系人；合作状态；创建时间；企业编码
- **表格列（th）**：客商编码 / 客商 / 客商类型 / 联系人 / 联系电话 / 开票资料 / 合作状态 / 更新时间 / 操作
- **判定**：
  - [P0·修复] cfg label「客商」→「客商名称」（与 HTML 控件 label 对齐） —— 理由：判据3 主体：HTML 控件 label=客商名称、cfg label=客商，readFilters 按 label 全等匹配→主体筛选真过滤失效（控制台报「筛控件未找到」）

### 库位档案（基础数据\库位档案.html）

- **页面类型**：数据驱动 renderListPage（entity=locations）
- **现有筛选（HTML）**：仓库(select)；库位编码(input)；库位类型(select·extra)；状态(select·extra)
- **cfg filters**：仓库；库位编码；库位类型；状态
- **表格列（th）**：库位编码 / 仓库 / 库位类型 / 规格 / 承载 / 占用情况 / 状态 / 操作
- **判定**：
  - [结论·-] 无需补充 —— 理由：判据1/2/7 全覆盖：仓库/库位编码/库位类型(select)/状态 四项齐备（G34 已字典化 KW）；无日期/人员列

### 租入入库列表（租入管理\租入入库列表.html）

- **页面类型**：数据驱动 renderListPage（entity=rentInbounds）；状态页签 3 个
- **现有筛选（HTML）**：入库单号(input)；关联租入单(input)；供应商(select)；物料(select·extra)；状态(select·extra)；入库时间(range·extra)
- **cfg filters**：入库单号；关联租入单；供应商；物料；状态；入库时间
- **表格列（th）**：入库单号 / 关联租入单 / 供应商 / 物料 / 数量 / 入库库区 / 状态 / 制单人 / 入库时间 / 操作
- **判定**：
  - [P1·新增] 制单人（select·值域=rentInbounds fields.maker 去重） —— 理由：判据5：表格有「制单人」列（李国栋/陈金），筛选区无人员筛选
  - [P1·新增] 入库库房（select·值域=rentInbounds fields.area 去重） —— 理由：判据7：表格有「入库库区」列，筛选区无库房筛选

### 租入单列表（租入管理\租入单列表.html）

- **页面类型**：数据驱动 renderListPage（entity=rentInOrders）；状态页签 7 个
- **现有筛选（HTML）**：租入单号(input)；供应商(select)；物料(select)；状态(select·extra)；租期开始(range·extra)；经办人(input·extra)
- **cfg filters**：租入单号；供应商；物料；状态；租期开始；经办人
- **表格列（th）**：租入单号 / 供应商 / 物料 / 数量 / 租金 / 租期起止 / 押金(元) / 累计租金应付(元) / 状态 / 经办人 / 申请时间 / 操作
- **页级备注**：租入单号/供应商/物料/状态/租期开始(range)/经办人 六项齐备
- **判定**：
  - [P2·登记] 租金/押金/累计租金应付 金额区间 —— 理由：判据8：表格有三个金额列——P2 只登记不补

### 租入归还列表（租入管理\租入归还列表.html）

- **页面类型**：数据驱动 renderListPage（entity=rentInReturns）；状态页签 3 个
- **现有筛选（HTML）**：归还单号(input)；关联租入单(input)；供应商(select)；归还类型(select·extra)；状态(select·extra)；归还时间(range·extra)
- **cfg filters**：归还单号；关联租入单；供应商；归还类型；状态；归还时间
- **表格列（th）**：归还单号 / 关联租入单 / 供应商 / 归还类型 / 物料 / 归还数量 / 状态 / 制单人 / 归还时间 / 操作
- **判定**：
  - [P1·新增] 制单人（select·值域=rentInReturns fields.maker 去重） —— 理由：判据5：表格有「制单人」列，筛选区无人员筛选

### 租赁出库列表（租赁管理\租赁出库列表.html）

- **页面类型**：数据驱动 renderListPage（entity=comboOutbounds）；状态页签 4 个
- **现有筛选（HTML）**：出库单号(input)；所属项目(select)；客户(select)；出库状态(select·extra)；出库时间(range·extra)；组合件编码(input·extra)
- **cfg filters**：出库单号；所属项目；客户；出库状态；出库时间；组合件编码
- **表格列（th）**：出库单号 / 所属项目 / 客户 / 关联租赁单号 / 出库组合件 × 数量 / 关联销售订单 / 收货地点 / 状态 / 出库时间 / 操作
- **判定**：
  - [结论·-] 无需补充 —— 理由：判据1/3/4/6 全覆盖：出库单号/所属项目/客户/出库状态/出库时间(range)/组合件编码 六项齐备；无类型/库房列（出库走组合件）

### 租赁单列表（租赁管理\租赁单列表.html）

- **页面类型**：数据驱动 renderListPage（entity=leaseOrders）；状态页签 6 个
- **现有筛选（HTML）**：租赁单号(input)；客户名称(select)；所属项目(select)；状态(select·extra)；建单日期(range·extra)
- **cfg filters**：租赁单号；客户；所属项目；状态；建单日期
- **表格列（th）**：租赁单号 / 客户 / 所属项目 / 租赁器具 / 资产来源 / 数量 / 建单日期 / 退回进度 / 状态 / 操作
- **页级备注**：其余判据全覆盖（状态/项目/日期/单号）
- **判定**：
  - [P0·修复] cfg label「客户」→「客户名称」 —— 理由：判据3 主体：HTML label=客户名称、cfg label=客户→客户筛选真过滤失效

### 退租入库列表（租赁管理\退租入库列表.html）

- **页面类型**：数据驱动 renderListPage（entity=returnInbounds）；状态页签 3 个
- **现有筛选（HTML）**：退租入库单号(input)；客户名称(select)；验收结果(select·extra)；入库日期(range·extra)；所属项目(select·extra)
- **cfg filters**：退租入库单号；客户；验收结果；入库日期；所属项目
- **表格列（th）**：退租入库单号 / 客户 / 所属项目 / 退租物料名称 / 退租数量 / 按BOM拆解后散件明细摘要 / 拆散去向 / 入库库房 / 入库日期 / 验收结果 / 状态 / 操作
- **判定**：
  - [P0·修复] cfg label「客户」→「客户名称」 —— 理由：判据3 主体：label 不匹配→客户筛选失效
  - [P1·新增] 入库库房（select·值域=returnInbounds fields.warehouse 去重） —— 理由：判据7：表格有「入库库房」列，筛选区无库房筛选

### 操作日志（系统管理\操作日志.html）

- **页面类型**：数据驱动 renderListPage（entity=opLogs）
- **现有筛选（HTML）**：操作人(select)；所属模块(select)；操作类型(select)；操作时间(range·extra)；内容关键字(input·extra)
- **cfg filters**：操作人；所属模块；操作类型；操作时间；内容关键字
- **表格列（th）**：日志编号 / 操作时间 / 操作人 / 所属模块 / 操作类型 / 操作内容摘要 / IP 地址 / 操作结果
- **判定**：
  - [P0·新增] 操作结果（select·值域=opLogs cells 第7列值域 成功/失败）＋DATA 补 fields.result×10 行 —— 理由：判据1 状态类：表格有「操作结果」列（成功/失败），属日志常见状态维度；行数据缺 fields.result 需随筛选补齐（值取自本行 cells，不改口径）

### 用户权限（系统管理\用户权限.html）

- **页面类型**：数据驱动 renderListPage（entity=users）；状态页签 4 个
- **现有筛选（HTML）**：用户账号 / 姓名(input)；角色(select)；状态(select·extra)；数据权限范围(input·extra)
- **cfg filters**：用户账号 / 姓名；角色；数据权限范围
- **表格列（th）**：用户账号 / 姓名 / 角色 / 数据权限范围 / 手机号 / 状态 / 最近登录 / 操作
- **判定**：
  - [P0·接线] cfg filters 补 {label:'状态', field:'status'}（HTML 已有 状态 select） —— 理由：判据1 状态：HTML 状态筛选控件存在但 cfg 未配置→点查询不生效

### 角色管理（系统管理\角色管理.html）

- **页面类型**：数据驱动 renderListPage（entity=roles）
- **现有筛选（HTML）**：角色名称(input)；数据权限(select)
- **cfg filters**：角色名称；数据权限
- **表格列（th）**：角色 / 备注 / 数据权限 / 账号数 / 操作
- **页级备注**：仅 2 项但表无可筛余量
- **判定**：
  - [结论·-] 无需补充 —— 理由：表列仅 角色/备注/数据权限/账号数：现有 角色名称(input)+数据权限(select) 两项已覆盖可筛维度；备注/账号数非筛选维度（判据八条无对应列）

### 付款登记（财务协同\付款登记.html）

- **页面类型**：数据驱动 renderListPage（entity=payments）；状态页签 3 个
- **现有筛选（HTML）**：付款编号(input)；供应商名称(select)；关联应付账单号(input)；状态(select·extra)；付款日期(range·extra)
- **cfg filters**：付款编号；供应商；关联应付账单号；状态；付款日期
- **表格列（th）**：付款编号 / 供应商 / 关联应付账单号 / 付款金额(元) / 付款日期 / 付款银行 / 状态 / 操作
- **判定**：
  - [P0·修复] cfg label「供应商」→「供应商名称」 —— 理由：判据3 主体：label 不匹配→供应商筛选失效
  - [P2·登记] 付款金额区间 —— 理由：判据8：表格有「付款金额(元)」列——P2 只登记

### 回款登记（财务协同\回款登记.html）

- **页面类型**：数据驱动 renderListPage（entity=receipts）；状态页签 4 个
- **现有筛选（HTML）**：收款编号(input)；客户(select)；收款日期(range)；核销状态(select·extra)；银行账户(select·extra)
- **cfg filters**：收款编号；客户；收款日期；核销状态；银行账户
- **表格列（th）**：收款编号 / 客户 / 关联账单 / 收款金额(元) / 收款日期 / 银行账户 / 银行回单 / 核销状态 / 操作
- **页级备注**：收款编号/客户/收款日期(range)/核销状态/银行账户 五项齐备且接线
- **判定**：
  - [P2·登记] 收款金额区间 —— 理由：判据8：表格有「收款金额(元)」列——P2 只登记

### 应付账单（财务协同\应付账单.html）

- **页面类型**：数据驱动 renderListPage（entity=payableBills）；状态页签 4 个
- **现有筛选（HTML）**：应付账单号(input)；供应商名称(select)；关联采购订单号(input)；账单类型(select·extra)；状态(select·extra)；账单日期(range·extra)
- **cfg filters**：账单类型；应付账单号；供应商；关联采购订单号；状态；账单日期
- **表格列（th）**：应付账单号 / 供应商 / 账单类型 / 所属项目 / 账期 / 关联订单/租入单号 / 关联采购入库单号 / 账单金额(元) / 已付金额(元) / 未付金额(元) / 账单日期 / 到期日 / 状态 / 操作
- **页级备注**：补后 8 项
- **判定**：
  - [P0·修复] cfg label「供应商」→「供应商名称」 —— 理由：判据3 主体：label 不匹配→供应商筛选失效
  - [P0·新增] 所属项目（select·值域=projects 实体） —— 理由：判据3 主体：表格有「所属项目」列（th 第4列），筛选区无项目筛选；payableBills 行带 fields.project
  - [P1·新增] 账期（select·值域=payableBills fields.period 去重·ZQ 字典对齐） —— 理由：判据2 分类变体：表格有「账期」列且同域应收账单已有账期筛选——跨页一致性

### 应收账单（财务协同\应收账单.html）

- **页面类型**：数据驱动 renderListPage（entity=receivableBills）；状态页签 5 个
- **现有筛选（HTML）**：账单编号(input)；客户(select)；所属项目(select)；账单类型(select·extra)；账单状态(select·extra)；账期(select·extra)；生成日期(range·extra)
- **cfg filters**：账单类型；账单编号；客户；所属项目；账单状态；账期；生成日期
- **表格列（th）**：账单编号 / 账期 / 所属项目 / 客户 / 账单类型 / 包含单据 / 账单金额(元) / 已核销(元) / 状态 / 生成方式 / 生成时间 / 操作
- **页级备注**：账单编号/客户/所属项目/账单类型/账单状态/账期/生成日期 七项齐备且接线（已达查漏线：状态/主体/日期三项齐）
- **判定**：
  - [P2·登记] 账单金额区间 —— 理由：判据8——P2 只登记

### 开票登记（财务协同\开票登记.html）

- **页面类型**：数据驱动 renderListPage（entity=invoices）；状态页签 3 个
- **现有筛选（HTML）**：发票登记号(input)；发票号码(input)；购方名称(input)；发票类型(select·extra)；开票日期(range·extra)；开票状态(select·extra)
- **cfg filters**：发票登记号；发票号码；购方名称；发票类型；开票日期；开票状态
- **表格列（th）**：发票登记号 / 发票号码 / 类型 / 购方名称 / 关联应收账单 / 开票金额(元) / 税率 / 开票日期 / 状态 / 操作
- **页级备注**：发票登记号/发票号码/购方名称/发票类型/开票日期(range)/开票状态 六项齐备
- **判定**：
  - [P2·登记] 开票金额区间 —— 理由：判据8：表格有「开票金额(元)」列——P2 只登记

### 盈亏报表（财务协同\盈亏报表.html）

- **页面类型**：数据驱动 renderListPage（entity=profitRows）；状态页签 3 个
- **现有筛选（HTML）**：账期(select)；所属项目(select)；统计口径(select)
- **cfg filters**：账期；所属项目
- **表格列（th）**：项目编码 / 项目名称 / 租赁收入(元) / 组装服务收入(元) / 赔偿收入(元) / 收入合计(元) / 器具摊销(元) / 仓储+组装成本(元) / 成本合计(元) / 毛利(元) / 毛利率
- **页级备注**：仅 3 项但属报表页
- **判定**：
  - [结论·-] 无需补充 —— 理由：账期/所属项目(cfg field=code·行键即项目编码) 已接线可用；金额列全为数值列→判据8 全部 P2 登记（见不建议项·报表金额列不入筛选）；「统计口径」不建议接线（见不建议项）

### 退款登记（财务协同\退款登记.html）

- **页面类型**：数据驱动 renderListPage（entity=refunds）；状态页签 3 个
- **现有筛选（HTML）**：退款编号(input)；退款类型(select)；关联退货单号(input)；往来单位(input·extra)；状态(select·extra)；退款日期(range·extra)
- **cfg filters**：退款编号；退款类型；关联退货单号；往来单位；状态；退款日期
- **表格列（th）**：退款编号 / 退款类型 / 关联退货单号 / 往来单位 / 退款金额(元) / 资金方向 / 状态 / 退款日期 / 操作
- **页级备注**：退款编号/退款类型/关联退货单号/往来单位/状态/退款日期 六项齐备且接线
- **判定**：
  - [P2·登记] 退款金额区间 —— 理由：判据8——P2 只登记

### 采购入库列表（采购管理\采购入库列表.html）

- **页面类型**：数据驱动 renderListPage（entity=purchaseInbounds）；状态页签 3 个
- **现有筛选（HTML）**：入库单号(input)；供应商(select)；所属项目(select)；入库状态(select·extra)；入库时间(range·extra)；业务类型(select·extra)
- **cfg filters**：入库单号；供应商；所属项目；入库状态；入库时间；业务类型
- **表格列（th）**：入库单号 / 供应商 / 关联采购订单号 / 所属项目 / 业务类型 / 到货托数 / 入库库区 / 状态 / 制单人 / 入库时间 / 操作
- **判定**：
  - [P1·新增] 制单人（select·值域=purchaseInbounds cells 制单人列）＋DATA 补 fields.maker×8 行 —— 理由：判据5：表格有「制单人」列，筛选区无人员筛选；行数据缺 fields.maker 需补（值取自本行 cells）
  - [P1·新增] 入库库房（select·值域=cells 入库库区列）＋DATA 补 fields.area×8 行 —— 理由：判据7：表格有「入库库区」列，筛选区无库房筛选；行数据缺 fields.area 需补

### 采购订单列表（采购管理\采购订单列表.html）

- **页面类型**：数据驱动 renderListPage（entity=purchaseOrders）；状态页签 5 个
- **现有筛选（HTML）**：采购订单号(input)；供应商(select)；物料类型(select)；订单状态(select·extra)；下单日期(range·extra)；所属项目(select·extra)
- **cfg filters**：采购订单号；供应商；物料类型；订单状态；下单日期
- **表格列（th）**：采购订单号 / 供应商 / 物料类型 / 采购明细摘要 / 数量 / 金额(元) / 币种 / 预计到货日期 / 关联销售订单号 / 订单状态 / 操作
- **判定**：
  - [结论·-] 无需补充 —— 理由：判据1/2/3/4/6 全覆盖（订单状态/物料类型/供应商/下单日期(range)/采购订单号）；HTML 既有「所属项目」select 无表列支撑且行数据无 project 字段——不建议接线（见不建议项）

### 采购退货单列表（采购管理\采购退货单列表.html）

- **页面类型**：数据驱动 renderListPage（entity=purchaseReturns）；状态页签 4 个
- **现有筛选（HTML）**：退货单号(input)；供应商(select)；退货类型(select)；关联原单号(input·extra)；退货状态(select·extra)；退货日期(range·extra)
- **cfg filters**：退货单号；供应商；退货类型；关联原单号；退货状态；退货日期
- **表格列（th）**：退货单号 / 退货类型 / 关联原单号 / 供应商 / 物料 / 数量 / 金额(元) / 状态 / 退货日期 / 操作
- **页级备注**：退货单号/供应商/退货类型/关联原单号/退货状态/退货日期 六项齐备且接线
- **判定**：
  - [P2·登记] 金额区间 —— 理由：判据8：表格有「金额(元)」列——P2 只登记

### 销售出库列表（销售管理\销售出库列表.html）

- **页面类型**：数据驱动 renderListPage（entity=salesOutbounds）；状态页签 3 个
- **现有筛选（HTML）**：出库单号(input)；关联销售订单号(input)；客户名称(select)；状态(select·extra)；出库日期(range·extra)；所属项目(select·extra)
- **cfg filters**：出库单号；关联销售订单号；客户；状态；出库日期；所属项目
- **表格列（th）**：出库单号 / 关联销售订单号 / 客户 / 所属项目 / 出库明细摘要 / 数量 / 出库库房 / 出库日期 / 状态 / 操作
- **判定**：
  - [P0·修复] cfg label「客户」→「客户名称」 —— 理由：判据3 主体：label 不匹配→客户筛选失效
  - [P1·新增] 出库库房（select·值域=salesOutbounds fields.warehouse 去重） —— 理由：判据7：表格有「出库库房」列，筛选区无库房筛选

### 销售订单列表（销售管理\销售订单列表.html）

- **页面类型**：数据驱动 renderListPage（entity=salesOrders）；状态页签 6 个
- **现有筛选（HTML）**：销售订单号(input)；客户名称(select)；所属项目(select)；订单状态(select·extra)；下单日期(range·extra)
- **cfg filters**：销售订单号；客户；所属项目；订单状态；下单日期
- **表格列（th）**：销售订单号 / 客户 / 所属项目 / 订单明细摘要 / 数量 / 金额(元) / 订单状态 / 下单人 / 下单时间 / 操作
- **判定**：
  - [P0·修复] cfg label「客户」→「客户名称」 —— 理由：判据3 主体：label 不匹配→客户筛选失效
  - [P1·新增] 下单人（select·值域=salesOrders fields.agent 去重） —— 理由：判据5：表格有「下单人」列（D-19 拍板唯一来源=项目经理代下），筛选区无人员筛选
  - [P2·登记] 金额区间 —— 理由：判据8：表格有「金额(元)」列——P2 只登记

### 销售退货单列表（销售管理\销售退货单列表.html）

- **页面类型**：数据驱动 renderListPage（entity=salesReturns）；状态页签 4 个
- **现有筛选（HTML）**：退货单号(input)；客户名称(select)；退货类型(select)；关联出库单号(input·extra)；退货状态(select·extra)；退货日期(range·extra)
- **cfg filters**：退货单号；客户名称；退货类型；关联出库单号；退货状态；退货日期
- **表格列（th）**：退货单号 / 退货类型 / 关联出库单号 / 客户 / 物料 / 数量 / 金额(元) / 状态 / 退货日期 / 操作
- **页级备注**：退货单号/客户名称/退货类型/关联出库单号/退货状态/退货日期 六项齐备且接线（该页 label 本就一致）
- **判定**：
  - [P2·登记] 金额区间 —— 理由：判据8——P2 只登记

### 项目档案（项目管理\项目档案.html）

- **页面类型**：数据驱动 renderListPage（entity=projects）
- **现有筛选（HTML）**：项目编码 <span style="font-size:11px;color:#8c8c8c">（自动生成：前缀+日期+序号）(input)；项目名称(input)；客户名称(input)；供应商(select)；项目状态(select·extra)；立项时间(range·extra)；项目负责人(select·extra)
- **cfg filters**：项目编码 （自动生成：前缀+日期+序号）；项目名称；客户；供应商；项目状态；立项时间；项目负责人
- **表格列（th）**：项目编码 / 项目名称 / 客户 / 供应商 / 项目状态 / 项目起止 / 项目负责人 / 操作
- **页级备注**：七项已达查漏线（默认决策表：≥7 项只核对状态/主体/日期——三项均已齐，仅修 P0）
- **判定**：
  - [P0·修复] cfg label「客户」→「客户名称」 —— 理由：判据3 主体：label 不匹配→客户筛选失效

### 我的待办（我的待办.html）

- **页面类型**：静态自定义（filterTodo）（entity=-）
- **现有筛选（HTML）**：单据类型(select)；审核人(select)；关键词(input)
- **cfg filters**：（无 renderListPage cfg）
- **表格列（th）**：单据类型 / 单据编号 / 摘要 / 所属项目 / 提交人 / 提交时间 / 待办动作 / 操作
- **页级备注**：静态自定义页（filterTodo），非 renderListPage
- **判定**：
  - [P0·新增] 所属项目（select·值域=todoItems fields.project 去重·自定义 filterTodo 页加 data-project 属性） —— 理由：判据3 主体：表格有「所属项目」列，筛选区无项目筛选；单号检索已由「关键词」input 覆盖（innerText 含单号）
  - [P1·新增] 提交人（select·值域=fields.submitter 去重·data-submitter 属性） —— 理由：判据5 人员：表格有「提交人」列，筛选区无人员筛选

## 三、共性问题

1. **label 脱钩族（8 页·P0）**：HTML 控件 label 与 renderListPage cfg label 不一致（客商名称≠客商、客户名称≠客户、供应商名称≠供应商），readFilters 按 label 全等匹配→这些主体筛选控件点「查询」不生效（控制台 warn 筛控件未找到）。涉及：客商管理/租赁单列表/退租入库列表/付款登记/应付账单/销售出库列表/销售订单列表/项目档案。修复=cfg label 对齐 HTML（改脚本侧一处字符串，不动控件）。
2. **cfg 漏配族（1 页·P0）**：用户权限 HTML 有「状态」select 但 cfg filters 未列该项。
3. **行数据缺 fields 族（2 实体）**：操作日志缺 fields.result（操作结果）、采购入库缺 fields.maker/fields.area——新增对应筛选须同步补 fields（值取自本行 cells，不改口径）。
4. **主体/库房/人员覆盖缺口**：应付账单缺所属项目（判据3）；退租入库/销售出库缺库房（判据7）；BOM/租入入库/租入归还/采购入库/销售订单/我的待办缺人员（判据5）；库存查询缺物料类型（判据2）。
5. **静态 option 未动态化（既有·不在本任务范围）**：各页既有 select 的 option 多为静态写死（如客户名单）——本任务只要求**新增项**值域动态取（任务书第七节），既有项不动（真名由 B 组统一替换）。

## 四、不建议补充项（登记·不实施）

- **采购订单列表·所属项目 select 接线**：表格无「所属项目」列、purchaseOrders 行数据无 project 字段（采购为全局行为不挂项目）——接线需造行级数据，属改业务口径风险，不接线（HTML 控件保留现状）
- **盈亏报表·统计口径 select 接线**：统计口径属报表层维度（切换口径=换数据版本），profitRows 行级无口径字段，接线会把整表滤空——不接线（控件保留现状）
- **产品档案·四参考价区间**：参考价为档案属性非交易单据金额（判据8 门槛建立在单据金额上），数值区间筛选用例少——不入 P2 清单
- **各页·银行账户/币种/IP地址/关联单号类追加**：非判据八条维度：银行账户已有页面（回款登记）保留现状不推广；币种单一（人民币）无筛选价值；IP 属日志明细非筛选项；关联单号检索已由单号 input 或关键词覆盖
- **角色管理·补项**：表列无可筛余量（见逐页行），强加筛选=为凑数而加
- **页签(stabs)与筛选区的关系**：状态页签已承担状态快捷筛选（点击切换视觉态），与筛选区状态 select 并存为既有设计——本任务不动页签

## 五、P2 登记汇总（判据8 金额区间·只登记不补）

- 盘点列表：盈亏合计金额区间
- 租入单列表：租金/押金/累计租金应付 金额区间
- 付款登记：付款金额区间
- 回款登记：收款金额区间
- 应收账单：账单金额区间
- 开票登记：开票金额区间
- 退款登记：退款金额区间
- 采购退货单列表：金额区间
- 销售订单列表：金额区间
- 销售退货单列表：金额区间
