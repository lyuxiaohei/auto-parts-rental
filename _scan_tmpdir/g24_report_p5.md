## 附：检查过程偏差登记与复跑指引

> 本附录不属七节正文；登记执行口径偏差与复跑口径，供后续修复 goal 对照。

**偏差登记（3 项·均已按实际口径执行）**：

1. demo-data 实体数实测 **37**（任务书/基线预期 38）——注释剥离后顶层键计数（含 assetTracks·其定义行紧贴块注释尾部 `*/assetTracks: {` 致朴素缩进正则漏计）；38 为 09-11 productTaxes 增量时点记账口径，与本文件实测差 1，登记不改账。
2. P3-R04 主表实测 **77 行**（预期 76±2 带内；节标题计数 4 处滞后——见 3.3）；验证门主表下界按实际取 ≥74 不变。
3. A02 支撑材料实际文件名为 `P3-R01-A02-页面类型与入口对照表.md`（任务书预检路径写「页面清单」未命中，按实际文件执行·存在性 PASS）。

**复跑指引**：验证门命令为 `python _scan_tmpdir/g24_verify.py`（结构/计数/覆盖率/术语纪律断言）；原型零改动双门=原型全目录 mtime ≤ `_scan_tmpdir/g24_starttime.txt` 所记时刻（2026-09-12T15:38:29）+ `git status --porcelain` 输出中无原型目录前缀行。证据自采脚本四件（见上文「报告完」块），重跑顺序 check0→check3。

**执行纪律声明**：本检查全程只读——原型目录零写入零改名零删除；判定不引用 P3-R04 既有覆盖结论与历史 audit/listfull 读数；计数全部 python str.count/regex 自采重跑；术语叙述层按 P1-R04 V0.6 现行词（引原文处同行注「原文」）。
