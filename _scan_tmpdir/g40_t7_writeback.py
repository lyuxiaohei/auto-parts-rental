# -*- coding: utf-8 -*-
"""G40 T7 回写（第一步）：任务档执行记录 + _AGENT基线快照滚动
索引行（第二步·需 commit 哈希）由 g40_t7_index.py <hash> 执行。
"""
import io, os, sys

BASE = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
TD = os.path.join(BASE, 'agent-handoff', '20260915-G40-原型侧变更包.md')
BL = os.path.join(BASE, 'agent-handoff', '_AGENT基线.md')

EXEC = '''
## 执行记录（2026-09-16 执行）

> 会话：ZCode 无人值守 · **一个 goal 会话内完成·不拆批**｜主机 Win｜工作目录根＝`D:\\工作台-吕道远\\5-【ACTIVE】汽车物流包装租赁\\`
> 前置校验：5 项通过（第 1 项 mtime 字面判为**非阻塞**——最近文件＝G39 打包 zip 且已提交 `c5958ad`，源文件最新 mtime 距开工 104 分钟，详见 `goal-failures-g40.md` 一、）
> 备份：`_scan_tmpdir/backup-g40-20260915/`（159/159 文件·按原相对路径）
> 存档提交：`90cc79a`（G39 收尾遗留＋G40/G41 立项·**只提交精确路径**·不 push）

### T0 落账 ✅ — **D-149**

P2-R01 附录 A 决策台账新增 **D-149「页面内术语三条统一·补做（G40 执行·D-142 承接）」**；
**D-142 状态列更正**：`✅（并入 G36 执行）` → `✅（**补做**·2026-09-16 由 G40 落地；原标「并入 G36 执行」系**状态虚报**——G36 未执行本项，见 D-149）`。

### T1 术语三条文本替换 ✅

范围＝原型目录 `.html/.js/.json/.md/.css` 共 **147 文件**（排除 `.prompts/`）；读取-精确替换＋`assert` 计数＋幂等（二跑 0 改动）。

| 替换串 | 命中 |
|---|---|
| `银行水单核销.html` → `银行回单核销.html` | 181 |
| `水单核销详情.html` → `银行回单核销详情.html` | 9 |
| `回款登记.html` → `收款登记.html` | 149 |
| `回款详情.html` → `收款详情.html` | 11 |
| `盈亏报表.html` → `损益报表.html` | 132 |
| `回款` → `收款`（自由文本 1 处·L4 流程注记「→ 回款登记 → 收款核销」） | 1 |

**顺序敏感**：先长串路径（否则 `银行水单` → `银行银行回单`）→ 裸串 → 自由文本；**白名单先掩码后还原**。
**终检：白名单外 `水单／盈亏／回款` 残留 ＝ 0**（白名单 4 项计数保持不变）。

### T2 文件改名＋引用同步 ✅

`git mv` 5 处（保 git 历史）；引用同步四处：
① 全站侧边栏菜单项（href 随 T1 同步；菜单文本本即现行词「收款登记／收款核销／财务看板」）
② `demo-data.js` 的 `ops[].act`／`chain`／`url` 与说明文案
③ A02／A05／**A06** 路径行
④ A03／A04 标注层 `selector` 与 map key

**旧名残留 0**；静态引用解析「改前 vs 改后 missing 集合」＝**仅 rename 镜像差异（2 增 2 减·同源）→ 净新增死链 0**。

### T3 残留清理与复核 ✅

- **`货品`**：任务书背景所述「残留 1 处」实测为 **`供货品类` 子串假阳性**（`demo-data.js:13727`·**不改**）——页面域真术语本就 0；真术语清理＝A02 备注列「多货品明细」→「多物料明细」（G38 口径）。
- **`库龄／在库时长／流转次数` ＝ 0**（全域）。
- **`零件号`**：页面域 **0**（A03 标注层 `desc` 1 处按 G38 概念表 `material.code` 禁用写法口径改「物料编码」·登记 R-04）。
- **`入库库区`**：页面域 **0**（仅 A05 G35 沿革注记 2 处·白名单）。
- 全部剩余命中落在白名单：A05 禁用写法列／沿革注记 ＋ F01 `SRC_DATA` 会议原文。

### T4 F01 五类改法 ✅

① **计费文案** 7 条不同串／8 行 → **按持有量计租＋期段账单**（L1 首现处写全 `起租日｜止租日｜天数｜日单价｜小计`；B1 销售线属销售口径未动）；
② **新增「转移出库」节点＝全图唯一结构改动**：**L1（y=444）／L3（y=900）各一**（170×56·副标固定「客户间转移 · 直接客户 → 终端客户」）＋ **L1 补给线右侧空白区跨线决策备注框**（标题「决策备注 · 转移出库（L1–L4 通用 · D-146）」＋注记四点）；连接线自「租赁出库」顶边上引 → 横走 → 下入新节点，挂箭头标签「**分批 · 非必经**」；L3 不重复画注记（短注记指向 L1 框）；
③ **状态驱动方 2 处** → 转移单审核驱动（T2 注记行 ＋ S1 五态行）；
④ **虚拟仓注记 1 处** → 「客户虚拟仓不维护（D-145）」（用词按原型五态口径归一为「在客户（租出）」·登记 R-06）；
⑤ **版本与日期** v3.6/09-11 → **v3.7/09-16**（eyebrow／`.sub`／footer ＋ 沿革条目）。

**Mermaid 源同步**（规则 §10）：L1／L3 补 `-. 分批·非必经 .->` 分支节点、F1／F2／T2c 补计租口径、S1 补状态驱动方。
**渲染核对**：主图 `viewBox 0 0 880 2184`／支线 `0 0 880 846` **均未调整**（内容最大 y=2164 < 2184）；**158 个节点内文本溢出 0 处**；主图链接 51→53、文本 191→204、rect 80→85，**支图零变动**（既有节点无位移丢失）；JS 报错 0。截图＝`_scan_tmpdir/g40_f01_full.png`／`g40_f01_L1_row.png`／`g40_f01_L3_row.png`／`g40_f01_L1_box.png`／`g40_f01_L3_box.png`／`g40_f01_S1_row.png`／`g40_f01_F1F2.png`／`g40_f01_T2_note.png`。

### T5 F01 开发规则同步 ✅

`P3-R01-F01-业务流程导航图-开发规则.md`：头部版本行 v3.5/2026-09-10 → **v3.7/2026-09-16**；**§1 viewBox 更正为现状**（G15 收窄后 880——原文仍写 1280）；**§4 补「转移出库」节点规格**（170 宽档·连接线约定·箭头标签·跨线注记四点）；**§11 验证 3 件→4 件**（补 viewBox／渲染几何核对）；**新增 §12「计费与状态口径（v3.7 补 · D-145／D-146／D-148）」**（计租与账单／库存状态与客户转租／客户虚拟仓／命名规则）＋ 原 §12 顺延 §13；§13 首条 S5 重复节点标记「v3.6 已修正」。**章节编号 1–13 连续**。

### T6 验证 ✅

- 术语残留（白名单外）**0**；5 个新文件名在位·5 个旧名不存在；旧名引用 **0**；净新增死链 **0**；
- **全站审计 132 页 0/0/0**（`audit-g40.log` 汇总行）；
- **PW 抽验 16/16 PASS**：F01「转移出库」节点 ×2 真实导航落地 `租赁管理/转移出库列表.html`；F1 收款登记／收款核销、S2 财务看板导航落地；📎 弹窗 l1／l3／fin 三处开合正常；Mermaid 折叠区含新节点与计租口径；5 个改名页独立加载 JS 错 0·旧名 0；
- **`node --check` 退出 0**：`_data/*.js` 7 件 ＋ `mobile/_m.js` 1 件。

### T7 回写 ✅

任务档执行记录（本节）｜`_索引.md` G40 行状态 ⏳→✅ ＋挂哈希｜`_AGENT基线.md` 快照滚动（标题「G39 后」→「G40 后」＋G40 追记行）。

### 失败清单

**无失败项**（登记项 9 条＝白名单／范围排除／假阳性／口径归一／超范围转派，见 `agent-handoff/goal-failures-g40.md` 三、）。**后续＝G41**（文档全量收口·串行依赖本任务产物）。
'''

SNAP = ("- **追记（09-16 G40·D-149）**：原型侧变更包（术语三条补做＋F01 同步）——"
        "**①术语三条补做落地（D-142 原标 ✅ 系状态虚报·G36 未执行）**：水单→银行回单／盈亏→损益／回款→收款 全站替换（**147 文件**·白名单＝A05 禁用写法列与沿革注记＋F01 `SRC_DATA` 会议原文＋`.prompts/` 工具日志排除）"
        "＋**5 处 `git mv` 改名**（回款登记→收款登记／回款详情→收款详情／盈亏报表→损益报表／银行水单核销→银行回单核销／水单核销详情→银行回单核销详情）"
        "＋**引用同步四处**（侧边栏／demo-data／A02·A05·A06 路径行／A03·A04 标注层）·白名单外残留 **0**·净新增死链 **0**；"
        "**②G38 清零复核**：「货品」实测系「供货品类」子串假阳性（页面域真术语本就 0）·库龄／在库时长／流转次数 **0**；"
        "**③F01 v3.6→v3.7**（自 09-10 未同步）：**L1／L3 各补「转移出库」节点**（170×56·副标「客户间转移 · 直接客户 → 终端客户」）＋L1 补给线右侧**跨线决策备注框「L1–L4 通用」**注记四点（不勾稽 D-106／按租出结算默认·不进财务链路／按终端结算特例·账单主体切终端／终止转移回在客户（租出））＋连接线箭头标签「分批 · 非必经」·**计费文案 7 条串改「按持有量计租＋期段账单」**·**状态驱动方 2 处改「转移单审核驱动」（D-146）**·虚拟仓注记改「不维护」（D-145）·Mermaid 源同步·开发规则补 §4 节点规格＋新 §12 计费与状态口径；"
        "**④验证**：audit **132 页 0/0/0**＋PW **16/16**＋溢出 **0** 处（双 viewBox 均未调整）＋node **0**；失败清单**无失败项**（登记 9 条）；"
        "**⑤后续＝G41**（文档全量收口·串行依赖本任务产物）")


def main():
    s = io.open(TD, encoding='utf-8', newline='').read()
    assert '## 执行记录（开工时填）' in s, '任务档执行记录锚点缺失'
    assert '## 执行记录（2026-09-16 执行）' not in s, '任务档执行记录已写（幂等保护）'
    s = s.replace('## 执行记录（开工时填）', EXEC.strip('\n'))
    io.open(TD, 'w', encoding='utf-8', newline='').write(s)
    print('OK  任务档执行记录写入（%d 字符）' % len(EXEC))

    b = io.open(BL, encoding='utf-8', newline='').read()
    H_OLD = '### 当前基线快照（滚动更新 · 2026-09-16 G39 后）'
    H_NEW = '### 当前基线快照（滚动更新 · 2026-09-16 G40 后）'
    assert b.count(H_OLD) == 1, '基线标题锚点计数 %d' % b.count(H_OLD)
    b = b.replace(H_OLD, H_NEW)
    A = '- **上一任务**：✅ **G27 需求真值源收敛（2026-09-14'
    assert b.count(A) == 1, '基线插入锚点计数 %d' % b.count(A)
    b = b.replace(A, SNAP + '\n' + A)
    # 口径纪律铁律：补 G40 两条实证（原型目录 mtime 判读 / _prompts 排除）
    io.open(BL, 'w', encoding='utf-8', newline='').write(b)
    print('OK  基线快照滚动（标题 → G40 后；追记行已插入）')
    b2 = io.open(BL, encoding='utf-8', newline='').read()
    for k in ['2026-09-16 G40 后', '追记（09-16 G40·D-149）', 'L1／L3 各补「转移出库」节点']:
        print('   %-34s = %d' % (k, b2.count(k)))
    return 0


if __name__ == '__main__':
    sys.exit(main())
