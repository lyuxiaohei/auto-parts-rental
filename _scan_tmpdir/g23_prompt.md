/goal 执行 G23 PRD 文档生成（新建 P2-R01-产品需求文档.md，记录最新版需求——基线=G22 后原型现状，章节格式按 prd-auto-generator 技能标准模式模板；硬约束：原型目录只读，只生成文档）。工作目录（命令与全部相对路径之根，不依赖会话 cwd）：`D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\`

【交接文档 · 双层路由，先读全文再动手】
- `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\_AGENT基线.md`——项目铁律：第四节（历史事故与真相）+ 第七节（上手清单）逐条适用
- `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\20260912-G23-PRD文档生成.md`——本 goal 全量细则：需求来源优先级 7 源 / 模式与章节结构 / 模块代号表 10 模块 / 前置校验 7 条 / 任务四组 / 默认决策表 12 条 / 验证门 6 条；一切写死口径与计数预期以该文档为准

【前置校验 · 先于一切】
1. 单写者互斥：原型目录与 `agent-handoff/` 最近文件 mtime ≥30 分钟（任务书自身豁免）
2. `_索引.md` 无 `| G23 |` 行；项目根无 `P2-R01-产品需求文档.md`
3. prd-auto-generator 技能 7 文件在位（路径清单见任务书「模式与章节结构」节）；缺失按任务书内嵌结构执行并记偏差
4. `git status` 有改动先存档提交（不 push）；开工时间戳落盘 `_scan_tmpdir/g23_starttime.txt`
——任一不符 → 停止并写失败清单（路径见下），逐条处理口径见任务文档前置校验节

【任务】
- T1 材料收集与版本级：按任务书需求来源优先级通读 7 源（含 G22 任务文档执行记录）→ 「一、范围边界」（In 10 模块/Out 四移除模块等）+「二.1 角色列表」
- T2 模块 3 段 ×10（代号表 PRJ/SAL/LEA/PUR/WHS/BAS/FIN/SYS/WF/MOB 写死于任务书）：UC+AC 强绑定（Given-When-Then，对应用例列）｜每模块 ≥1 Mermaid 主流程+状态流转含守卫条件+异常与边界｜CRUD 矩阵+字段说明表（全量字段引 A05）；用例必须原型有对应，禁臆测；计费与单据口径按 G22 后现状（三段式租价·无日租金·退租两事件·台账合并·出库称「租赁出库」[组合出库禁用]·待办 16 类含租赁出库审核）
- T3 可选章节与质量检查：五、单据流转（三条单据链+预收预付冲抵）｜六、接口时序（路凯/吉客云·对接细节待客户答复口径）｜七、实时通信判定不产出记录｜质量检查 3 项（编号/追溯/范围）
- T4 收尾回写四件（硬性）：任务文档标 ✅+执行记录｜`_索引.md` 挂 G23 行｜`_AGENT基线.md` 滚动（含文档地图 P1-R01 版本行 V0.6→V0.7 更正）｜`P1-R01` 附录 10.1 P2 行启用声明+10.2 加 P2-R01 行（仅此两处）
——全部改动点、写死字符串与不动项见任务文档任务节

【完成判定 · 证据全部贴进对话】
1. `python _scan_tmpdir/g23_verify.py`（新建留档）→ 逐行 PASS（结构/编号/追溯/废弃词/计数预期写死于任务文档验证门 1）
2. 原型零改动双门：`P3-R01-包装租赁管理后台原型/` 全目录（含 `_data/`）mtime ≤ 开工时间戳 → 违例=空行；`git status --porcelain` 中 P3-R01 前缀=0 行
3. 质量检查 3 项结论行逐项 PASS
4. 回写四件 grep 证据 + git 提交完成（信息前缀 G23，禁 push），哈希回填任务文档与索引行

【约束】
- 无人值守：全程不需要也不得向用户提问；默认决策表 12 条内事项按表执行，表外按最简合理默认执行并在收尾报告注明
- assert 失败/材料缺失/结构不符 → 跳过该项 → 记入 `D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\goal-failures-g23.md`（任务号/文件/原因）→ 继续下一项；禁止卡死等待、禁止臆测编造原型没有的流程、禁止静默吞掉；收尾必须含失败清单结论行（空清单给「无失败项」）
- 原型只读铁律：`P3-R01-包装租赁管理后台原型/` 全目录（含 `_data/`）零写入零改名零删除；新建/修改文件仅限任务书纪律节白名单
- 分模块追加写（每模块落盘保存，中断可续）；术语一律用术语表 V0.6 现行词（废弃词禁用）；中文路径 python 内嵌处理；验证数字自重跑；多股改动入库 `git add -A`；禁 push
