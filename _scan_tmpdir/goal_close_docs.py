# -*- coding: utf-8 -*-
"""收尾文档更新：P1-R05 §5/§6+最近完成+更新行；P1-R01 附录 10.2 P3-R04 行；goal-failures.md 留档"""
from pathlib import Path
import shutil

BASE = Path(r"D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁")
BK = BASE / "_scan_tmpdir" / "backup-goal-docs-20260905"
BK.mkdir(parents=True, exist_ok=True)

def edit(path, pairs):
    if not (BK / path.name).exists():
        shutil.copy2(path, BK / path.name)
    t = path.read_text(encoding="utf-8")
    for old, new in pairs:
        n = t.count(old)
        assert n == 1, f"{path.name} 锚点计数 {n} != 1: {old[:60]}..."
        t = t.replace(old, new)
    path.write_text(t, encoding="utf-8", newline="")
    print(f"{path.name}: {len(pairs)} 处更新完成")

# ---------- P1-R05 ----------
r05 = BASE / "P1-R05-agent交接文档.md"
edit(r05, [
 # 更新行
 ("> **更新**：2026-09-04（v2.8 原型 · 全站交互修复完成 842→0；",
  "> **更新**：2026-09-05（评审前全量处理完成：操作栏冻结/弹窗宽度/P3-R04 场景修复/菜单重组，见「最近完成」尾部；此前 v2.8 原型 · 全站交互修复完成 842→0；"),
 # 最近完成 追加
 ("**操作栏冻结样板（采购入库列表）**：发现全站 124 页 CSS 已预埋 .sticky-op 三件套（position:sticky+th底色+hover底色）但零使用——样板=挂类接线（th 操作 ×1 + td.ops ×8），Playwright 窄视口验证横滚 200px 后 8 ops+th 全部贴右固定；**待道远确认样板后全站推广**（推广=全站同款挂类脚本）",
  "**操作栏冻结样板（采购入库列表）**：发现全站 124 页 CSS 已预埋 .sticky-op 三件套（position:sticky+th底色+hover底色）但零使用——样板=挂类接线（th 操作 ×1 + td.ops ×8），Playwright 窄视口验证横滚 200px 后 8 ops+th 全部贴右固定。**评审前全量处理（09-05 晚，道远 /goal 四组任务，无人值守执行）**：①**操作栏冻结全站推广**——41 页挂类（th 操作 ×44＋td.ops ×263＋`<td class=\"ops\">` 编辑表形态 ×10，录单/BOM维护默认决策一并接线），幂等脚本＋assert；②**弹窗宽度全站推广**——`.modal-lg` 680px→780px＋max-width:92vw，123 页×246 处（空格/紧凑两形态，样板=采购订单列表）；③**P3-R04 演示场景修复 14⚠️＋1❌ 全部转 ✅**——SO-→PO- 复核=现状已合规无改动（单号列全 PO-，SO- 仅在关联销售订单号列，系评审误读）；销售出库新建补不足禁用态（预置 5,000＞1,520：橙字提示＋提交按钮 disabled，双层同步，顺手修明细备注 placeholder 误植「跳转页码」）；库存查询五态卡下加口径灰字注记（09-05 拍板原文）＋F01 S1 支线同款注记＋库存流水加 L1 回库联动行（09-02 TZRK-20260902-008 +60，结存 5,260 与卡头在库对齐）；水单核销弹窗加「本次核销后余额 ¥199,700.00」（486,200−286,500，双层）；加行 5 条（租出台账 ZL-20260903-034 近月在租·退租回库件再出租／组合出库 CK-20260903-016 同链／组装 ZZ-20260904-007 再组装承接 CX-20260902-006／退租申请 TZSQ-20260904-009 部分退租·退200留294／开票 INV-20260902-013 已红冲·负数）；盈亏报表加对齐说明注记（2601＝AR 486,200、2604 L4 链构成）；租赁单新建押金字段下加口径注记（双层）；打印死链改 window.print() 触发（组合出库/采购入库 16 处，附 class 切换自反馈防审计误报）；租赁单编辑表单 Playwright 逐字段走查 18 字段全过（_scan_tmpdir/goal-t3g-walkthrough.md）；④**菜单重组方案 A**——「租赁单」移至「销售订单」后（45 页侧边栏，44 标准＋1 selected 态；文件路径零改动；一致性检查 36 项序列全站一致）。**验证**：全量审计 125 页 实际问题 0／死链 0／JS 错 0（10 处 cls=auto readonly 预填豁免）；Playwright 抽验：冻结 10 页横滚贴右 ✅／弹窗 10 页 780px 无溢出 ✅／菜单顺序 ✅／F01 死链 0＋S1 注记 ✅；P3-R04 复评 76/76 全 ✅（P0×31）。改前备份 _scan_tmpdir/backup-goal-{t1,t2,t3,t4,docs}-20260905/（原相对路径）；执行脚本 _scan_tmpdir/goal_*.py 留档；失败清单 goal-failures.md（无 assert 跳过项，仅 1 条已合规无改动记录）"),
 # §5 追加行
 ("| **详情落地页补齐（第十节 A/B/C/D）** | ✅ 2026-09-04 完成：31 详情弹窗（四段式）+40 停用/9 重置密码/9 报废确认+权限配置补绑 5+退租跳转+添加明细克隆+回款登记对称性；弹窗 44→79；全量复测 126 页死链 0/JS 错 0/菜单一致（10 条 readonly 预填为既有豁免）；D 组专项 Playwright 通过 |",
  "| **详情落地页补齐（第十节 A/B/C/D）** | ✅ 2026-09-04 完成：31 详情弹窗（四段式）+40 停用/9 重置密码/9 报废确认+权限配置补绑 5+退租跳转+添加明细克隆+回款登记对称性；弹窗 44→79；全量复测 126 页死链 0/JS 错 0/菜单一致（10 条 readonly 预填为既有豁免）；D 组专项 Playwright 通过 |\n| **评审前全量处理（/goal 四组任务）** | ✅ 2026-09-05 完成：操作栏冻结全站 41 页／弹窗宽度 780px 123 页×246 处／P3-R04 14⚠️+1❌ 全转 ✅（禁用态、口径注记、联动流水行、余额行、5 加行、对齐/押金注记、window.print、走查）／菜单重组（租赁单→销售订单后，45 页）；全量审计 0/0/0＋抽验全过＋P3-R04 复评 76/76 ✅ |"),
 # §6 已拍板追加菜单顺序
 ("- 权属第一期不区分（A1）——但 **2026-09-04 道远升级：L3/L4 第一期即闭环**（推翻\"L3 暂缓\"）",
  "- 菜单顺序方案 A：租赁单紧跟销售订单之后（2026-09-05 /goal 拍板，文件路径不动）\n- 权属第一期不区分（A1）——但 **2026-09-04 道远升级：L3/L4 第一期即闭环**（推翻\"L3 暂缓\"）"),
])

# ---------- P1-R01 附录 10.2 ----------
r01 = BASE / "P1-R01-需求梳理与功能框架.md"
edit(r01, [
 ("| P3-R04 | ./P3-R04-演示场景覆盖梳理.md | 演示场景覆盖梳理（2026-09-05：72 场景全量清单——业务口吻场景×演示动线×数据覆盖判定，51✅/17⚠️/1❌、P0×24 周一评审必演；含赔偿减库存 A1-A5 与库存口径 B 修复任务；修复后复评归绿作为演示验收口径） |",
  "| P3-R04 | ./P3-R04-演示场景覆盖梳理.md | 演示场景覆盖梳理（2026-09-05：76 场景全量清单——业务口吻场景×演示动线×数据覆盖判定；同晚修复复评 **76/76 全 ✅**（含赔偿减库存 A1-A5、库存口径 B、14⚠️ 逐条），P0×31 周一评审必演，作为演示验收口径；初版汇总 72/51/17/P0×24 为笔误以实测为准） |"),
])

# ---------- goal-failures.md ----------
GF = BASE / "_scan_tmpdir" / "goal-failures.md"
GF.write_text("""# /goal 四组全量处理 · 失败与例外清单（2026-09-05）

> 失败处理策略执行记录：任何 assert 失败/锚点不匹配/预期外结构 → 跳过并记入本文件。

## 断言跳过项

**（空）**——四组任务全部一次通过，无 assert 失败、无锚点不匹配导致的跳过。

## 预期外结构处置（非失败，无改动落地）

1. **采购单号 SO-→PO-（P3-R04 一-6 / 共性 2）**：预期结构=页面表格存在 SO- 前缀采购单号与筛选默认值；实测现状已合规——采购订单列表单号列 7 行全 PO-（PO-20260902-018 … PO-20260815-012）、筛选栏无预填默认值（仅 placeholder）、A04 pin selector=`<td><span class="lk">PO-20260902-018</span></td>`。全站 SO- 仅出现于「关联销售订单号」列及销售出库/组合出库关联列（语义正确）。判定：原 ⚠️ 系评审时误读首行第 10 列的关联销售单号；按「不匹配则保留原样并注明」处理，页面与 A04 均无改动，P3-R04 判定转 ✅（注明"现状已合规"）。

## 顺手修复（计划外，均在改动页内）

1. 销售出库新建弹窗明细行备注 input 的 placeholder 误为「跳转页码」（历史批量修复误伤分页跳转框所致）→ 改为「选填」（列表页+独立模板双层）。
""", encoding="utf-8")
print("goal-failures.md 已留档")
