/goal 标注污染清查：38 业务页本体中的元注释（会议日期/拍板/原XX并入等溯源说明）全部退出页面、回归标注层（/原型标注技能·原型与标注分离）。工作目录（命令与交接文档全部相对路径之根，不依赖会话 cwd）：D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁

【交接文档 · 双层路由，先读全文再动手】
D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\_AGENT基线.md（项目上下文与铁律，常驻滚动）
D:\工作台-吕道远\5-【ACTIVE】汽车物流包装租赁\agent-handoff\20260910-G11-标注污染清查.md（本 goal 全量细则：污染模式/白名单/处置规则=C 默认决策表）
技能口径：skills\原型标注\SKILL.md + references\annotation-format.md（数据格式唯一口径，动手前必读）

【前置校验 · 先于一切】
1. 库存查询页面本体含「原在租台账·2026-09-10 并入」字样（=未清理态）；不含（疑似已执行）→写失败清单 _scan_tmpdir\goal-failures-g11-20260910.md 后收尾，禁止重做
2. 原型目录 _data\ mtime ≥30 分钟无变化（资源互斥：同一工作区同一时刻仅本 agent 一个写）；记录 git HEAD 并自跑 audit 基线快照；工作区非 clean 先 git add -A 留档

【任务】
T1 全量扫描（只读脚本）：38 业务页命中污染模式清单→_scan_tmpdir\g11-meta-scan.json；弹窗模板 69 页仅登记 g11-modal-scan.md 不改动；F01 豁免
T2 分类定案：A 直删/B 迁入 A03 后删/C 白名单保留→_scan_tmpdir\g11-disposition.md；疑似一律按 B 并注「默认决策命中」
T3 页面清除（精确替换+逐条 assert+备份 backup-g11-20260910 原相对路径；标题括号尾巴与 ★ 标记同步清）
T4 迁入 A03-标注数据.json（页有键加 pin/无键新建；便签承载全文含出处）+ annotate.py 对 A03/A04 各全量重注一次，三类告警逐条处理至 0
T5 验证门全跑+A03 _meta 注记+P1-R01 ⑲+git 一提交（含「G11-标注污染清查·执行」）+回写三件

【完成判定 · 证据全部贴进对话】
1. annotate.py A03+A04 两次重注输出：未命中 0/多处匹配 0/替换元素 0（原样贴出）
2. 残留扫描复跑：非白名单命中=0（计数行+白名单命中清单）
3. Playwright 抽 3 页（库存查询/租赁单列表/销售订单列表）：默认态无便签裸露+?notes=1 角标渲染>0+右下角「标注 N」入口在，逐页一行 PASS
4. audit 108 页 死链 0/JS 0/相对执行时 HEAD 基线逐键 diff 新增 0
5. 示例闭环：库存查询页面 grep「原在租台账」=0 + A03 库存查询键含「客户在租」pin（grep 证据行）
6. 提交号+G11 文档头部 ✅ 哈希+_索引 G11 行 ✅+基线终态句+失败清单结论行

【约束】
- 保留白名单（不动）：纯交互/状态提示（自动生成不可编辑/已盘 N/M 项类）、业务状态词（待确认/待审核等 tag）、税率/校验等业务交互说明——勿误删
- 拿不准→按迁入 A03 处理+决策清单注明（保守不丢信息）；A04 流程链标注不动；页面计数/菜单/文件零变更；不重打 zip
- 无人值守：不需要也不得向用户提问；按 G11 文档 C 默认决策表执行并在收尾注明
- 纪律=基线「纪律铁律」节：精确替换+assert 计数；备份按原相对路径；Python 二进制读写保行尾+raw 字符串防转义；HTML 改后标签配平自检；selector 必须在干净（未注入）页面上生成校验
- 单项失败→修复一次→仍败恢复备份对应文件+记失败清单+续下一项；禁止卡住等待、禁止对不确定结构猜测性改写
- 禁 git push；99-归档不碰；P1-R05 冻结版只读；_build\modals_build.py 禁用；原型目录同一时刻仅本 agent 写
- 回写三件（硬性）：G11 文档标 ✅+执行记录+commit 哈希回填头部；_索引.md 挂 G11 行与哈希；_AGENT基线.md 同步新现状

or stop after 40 turns
